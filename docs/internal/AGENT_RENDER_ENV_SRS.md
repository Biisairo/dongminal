# 에이전트가 화면을 망가뜨리지 않게 띄운다 — SRS

> **문서 상태**: 승인·구현완료

- 접수: 사용자 (2026-09-19) — 렌더 잔재(`M11-B1`) 추적의 결론
- 관련: [`M11_SRS`](./M11_SRS.md) §2.4e·2.4f (재현과 원인) ·
  [`TERMINAL_RESUME_SRS`](./TERMINAL_RESUME_SRS.md) `FR-TRS-18a` (넛지를 alt screen 에만)

---

## 1. 개요

### 1.1 목적

Claude Code 는 터미널 폭이 바뀌면 **옛 프레임을 스크롤백에 남긴다.** 기기를 오가며
쓰면 그것이 계속 쌓여 위쪽 대화를 읽을 수 없게 된다.

터미널 쪽에서 되살릴 수 없다는 것은 조사로 확정됐다 (`M11_SRS` §2.4e·2.4f) —
스크롤백에 남은 것은 이미 배치가 끝난 글자이고, 원본을 가진 것은 앱뿐이다.

**앱에 해결책이 이미 있다.** dongminal 은 그것을 켜 주기만 하면 된다.

### 1.2 왜 이것이 dongminal 의 일인가

사용자가 매번 `/tui fullscreen` 을 치게 할 수도 있다. 그러나 이 결함은 **dongminal
에서 쓸 때 특히 심하다** — 여러 기기와 분할을 오가는 것이 이 제품의 사용 방식이고,
그때마다 폭이 바뀐다. 제품이 만든 조건이면 제품이 갖추는 것이 맞다.

### 1.3 조사 — 세 에이전트의 처지가 다르다 (2026-09-19)

| 에이전트 | 리사이즈 때 위쪽 | 제어 수단 | 조치 |
|---|---|---|---|
| **Claude Code** | **옛 프레임이 남는다** ([#49086](https://github.com/anthropics/claude-code/issues/49086)·[#54732](https://github.com/anthropics/claude-code/issues/54732) 외 다수) | 환경변수 `CLAUDE_CODE_NO_FLICKER` | **주입한다** |
| **Codex** | source-backed history 에서 스크롤백 재구성 ([PR #18575](https://github.com/openai/codex/pull/18575)) | `config.toml` 의 `[tui]` — **환경변수 없음** | 하지 않는다 |
| **oh-my-pi** | ED3 후 transcript 재구성 | `PI_TUI_RESIZE_MODE` 등 | 하지 않는다 |

Codex·omp 를 건드리지 않는 근거는 둘이다. ① **이미 스스로 처리한다** — 기본값이
알맞고 손댈 이유가 없다. ② Codex 는 TUI 설정이 TOML 전용이라 **사용자의 설정
파일을 고쳐야** 하는데, 남의 파일을 대신 쓰는 것은 얻는 것에 비해 침습적이다.

### 1.4 범위

**포함** — 새로 만드는 도구의 환경에 값을 넣는 일과 그 스위치.

**비포함** — 이미 떠 있는 도구(환경은 프로세스가 시작할 때 정해진다) · 사용자의
`~/.claude/settings.json`·`~/.codex/config.toml` 수정 · 터미널 쪽에서 잔재를
되살리는 일(불가능함이 확정됐다).

---

## 2. 요구사항

**FR-ARE-1** 새 도구의 환경에 `CLAUDE_CODE_NO_FLICKER=1` 을 넣는다. 그 도구에서
띄운 Claude Code 는 **fullscreen 렌더링**으로 서고, 대화가 alt screen 에 살아
스크롤백을 건드리지 않는다.

**FR-ARE-2** **사용자의 뜻이 이긴다.** `/tui default` 를 치면 Claude Code 가
재시작하며 이 변수를 지우므로(공식 동작), 주입은 *기본값 제안*이지 강제가 아니다.
이 성질이 있기에 기본값을 켬으로 둘 수 있다.

**FR-ARE-3** 설정으로 끌 수 있다. 자리는 **Settings ▸ Terminal**, 기본값은 **켬**
(사용자 결정 2026-09-19).

**FR-ARE-4** 이미 떠 있는 도구는 바뀌지 않는다. 환경은 프로세스 시작 시점에
정해진다 — 다음에 여는 터미널부터 적용된다. 화면에 그 사실을 적는다.

**FR-ARE-5** **toolhub 는 어느 에이전트도 알지 않는다.** 무엇을 넣을지는 호출자가
정하고, toolhub 는 받은 것을 환경에 얹는다 — `Placement` 가 컨테이너·프로파일을
모르는 것과 같은 경계다 (`manager_create.go` 의 규약).

**FR-ARE-6** daemon 모드에서도 같다. 도구를 만드는 것은 데몬이므로 값은 `create`
RPC 를 타고 건너간다. 필드를 모르는 옛 데몬에서는 **주입이 없을 뿐** 깨지지 않는다.

**FR-ARE-7** 서버가 설정 blob 에서 **이 키 하나만** 읽는다.

> `handlers_settings.go` 가 *"브라우저 설정은 서버가 해석하지 않는 JSON blob"* 이라
> 적어 두었고, 이 요구는 거기에 예외를 낸다. 예외인 이유는 분명하다 — 값을 쓰는
> 주체가 **서버**(도구를 띄우는 쪽)인데 값을 정하는 자리는 **브라우저**(설정 화면)
> 이기 때문이다. 대안이던 "서버 전용 설정"은 화면에서 못 바꾸게 되어 `FR-ARE-3` 과
> 어긋난다. 읽는 키는 하나이며 해석은 불리언 하나에서 멎는다.

### 2.1 스크롤 속도 (2026-09-20 접수)

접수한 말은 *"`CLAUDE_CODE_SCROLL_SPEED` 옵션 세팅 — 터미널에서 설정가능. 클로드
전용설정으로 tui fullscreen mode 와 함께 작동"* 이다.

**변수의 계약은 바이너리에서 확인했다** (로컬 `2.1.278`). 추측이 아니다:

```js
function vtn(r,n,o){ return !n && (r || !1 || o) ? 3 : 1 }   // 기본값
function re(r,n,o){                                          // r=xtermJs n=wheelFlood o=wtSession
  let u = vtn(r,n,o);
  let s = a.CLAUDE_CODE_SCROLL_SPEED;
  if(!s) return u;                          // 없으면 기본값
  let p = parseFloat(s);
  if(Number.isNaN(p) || p <= 0) return u;   // 숫자 아님·0 이하 → 조용히 기본값
  return Math.min(p, 20);                   // 상한 20
}
```

| 사실 | 값 |
|---|---|
| 읽는 방법 | `parseFloat` — 실수를 받는다 |
| 유효 범위 | **0 초과**, 상한 **20** (넘으면 20 으로 잘린다) |
| 무효값 | 조용히 기본값. 오류를 내지 않는다 |
| 기본값 | `!wheelFlood && (xtermJs \|\| wtSession)` 이면 **3**, 아니면 1 |

**`CLAUDE_CODE_NO_FLICKER` 와 같은 묶음에 있으나 서로 의존하지 않는다.** 둘은
바이너리의 같은 터미널 렌더 변수 목록에 나란히 있지만, `re()` 는 fullscreen 여부를
보지 않는다. 그래서 이 스펙은 **둘을 얽지 않는다** — 없는 규칙을 발명하면 Claude
Code 가 규칙을 바꿀 때 우리 쪽이 먼저 틀린다.

**FR-ARE-8** 새 도구의 환경에 `CLAUDE_CODE_SCROLL_SPEED=<값>` 을 넣는다. 값은 설정
`claudeScrollSpeed`(int, 1~20, 기본 **3**)이다.

**FR-ARE-9** **항상 주입한다** (사용자 결정 2026-09-20). 값을 비워 Claude Code 의
판단에 맡기는 길도 있었으나 고르지 않았다.

> 그 선택의 값은 이렇다. dongminal 은 xterm.js 기반이므로 `xtermJs` 가 참이고,
> Claude Code 가 스스로 고르는 값도 **3** 이다 — 기본값에서 화면은 **바뀌지 않는다.**
> 다른 것은 `wheelFlood`(휠 이벤트 폭주)가 감지된 경우뿐이며, 그때 Claude Code 는
> 1 을 고르지만 우리는 3 을 넣는다. **그 판단을 덮는다는 사실을 여기 적어 둔다** —
> 나중에 스크롤이 과하다는 접수가 오면 이 문단이 첫 번째로 볼 자리다.

**FR-ARE-10** 자리는 **Settings ▸ Terminal**, fullscreen 스위치 **아래**다. 컨트롤은
숫자 입력(`#ds-scrollspeed`)이다.

**FR-ARE-11** `FR-ARE-4` 와 같다 — 이미 떠 있는 도구는 바뀌지 않는다. 화면에 그
사실을 적는다.

**FR-ARE-12** `FR-ARE-7` 의 예외를 **넓히지 않는다.** 서버가 blob 에서 읽는 키는
이제 둘이고, 해석은 여전히 그 둘에서 멎는다. 읽는 자리도 `agentRenderEnv` 하나다.

---

## 3. 검증

| ID | 확인 | 층 |
|---|---|---|
| V-ARE-1 | 켜져 있으면 새 도구의 환경에 `CLAUDE_CODE_NO_FLICKER=1` 이 있다 | Go |
| V-ARE-2 | 꺼져 있으면 그 변수가 **없다** | Go |
| V-ARE-3 | 설정을 읽지 못해도(blob 이 비었거나 깨졌어도) 기본값 **켬**으로 동작한다 | Go |
| V-ARE-4 | 필드를 싣지 않는 옛 요청도 그대로 도구를 만든다 (관대함) | Go |
| V-ARE-5 | Settings ▸ Terminal 에 스위치가 있고, 끄면 저장된다 | e2e |
| V-ARE-6 | 새 도구의 환경에 `CLAUDE_CODE_SCROLL_SPEED=<값>` 이 있다. 설정한 값이 그대로 간다 | Go |
| V-ARE-7 | blob 이 비었거나 깨졌거나 값이 범위 밖이면 **3** 이 간다 — 없는 채로 두지 않는다 (FR-ARE-9) | Go |
| V-ARE-8 | 두 변수가 **서로 독립**이다: fullscreen 을 꺼도 스크롤 속도는 간다 (§2.1) | Go |
| V-ARE-9 | Settings ▸ Terminal 에 입력란이 있고, 바꾸면 저장되고 새로고침 뒤에도 남는다 | e2e |

---

## 4. 비목표

1. **Codex·omp 의 설정 주입** (§1.3). 이미 스스로 처리하며, 건드리면 위험만 는다.
2. **사용자 설정 파일 수정.** 우리는 환경만 정한다.
3. **이미 떠 있는 도구에 소급 적용.** 환경은 시작 시점의 것이다.

---

## 5. 변경 기록

| 날짜 | 내용 |
|---|---|
| 2026-09-20 | **스크롤 속도 구현완료.** Go 검사 열넷(`TestAgentRenderEnvScrollSpeed` — 기본·범위 밖·타입 밖·독립) · e2e 하나(V-ARE-9). 기본값과 범위는 **서술자 표에서 읽는다**(`sync.OnceValue` 로 한 번) — Go 에 `3` 을 적으면 값이 두 벌이 된다 (FR-CFG-10). **기존 검사 하나를 고쳤다**: `TestAgentRenderEnv` 가 *"끈 경우 `len(got)==0`"* 으로 쟀는데 스크롤 속도가 항상 실리므로 성립하지 않는다 — 재려던 것은 배열 길이가 아니라 **그 변수가 빈 값으로 남는가** 였으므로 접두사 검사로 좁혔다 **e2e 안정화 1건**: 값을 연달아 두 번 바꾸면 앞 저장의 방송 왕복(`_settingsRestore` — *"방송은 다시 받으라는 신호"*)이 뒤 입력보다 늦게 닿아 입력란을 옛 값으로 덮었다. 기존 설정 전반의 성질이고(`tabWidthPx` 도 같다) 사람의 입력 속도에서는 드러나지 않으므로 — 연달아 치는 것은 검사뿐이다 — 새 규약을 만들지 않고 검사가 `waitSettled` 로 왕복을 기다린다. |
| 2026-09-20 | **스크롤 속도 (FR-ARE-8~12).** 접수는 *"`CLAUDE_CODE_SCROLL_SPEED` 옵션 세팅"* 이다. 변수의 계약(`parseFloat` · 0 초과 · 상한 20 · 무효값은 조용히 기본값 · 기본 3 또는 1)은 **로컬 바이너리 `2.1.278` 에서 직접 확인했다** — 기억으로 적지 않았다. 형식은 *"정수 1~20, 기본 3"* 으로 확정됐고(사용자 결정), 그 선택이 `wheelFlood` 일 때 Claude Code 의 판단을 덮는다는 사실을 §2.1 에 적었다. fullscreen 과는 **얽지 않는다** — 바이너리가 둘을 얽지 않는다 |
| 2026-09-19 | 구현완료. Go 하나(`agent_render_env_test.go`, 여덟 갈래)와 e2e 하나가 선다. `Placement.ExtraEnv` 로 toolhub 가 에이전트를 모르게 두었고, daemon 모드는 create RPC 로 건너간다. |
| 2026-09-19 | 초안·승인. `M11-B1` 추적의 결론으로 선다 — 터미널이 못 고치는 것을 앱이 고치게 한다. 세 에이전트를 조사해 주입 대상을 Claude Code 하나로 좁혔다. |

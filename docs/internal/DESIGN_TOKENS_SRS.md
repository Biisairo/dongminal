# SRS: 디자인 토큰 — 색·크기·글자·z-index 의 목록과 파생 규칙 (IEEE 29148)

> **문서 상태**: 승인·구현중

- 접수: 2026-09-12 (로드맵 M7 선행 SRS ② · `style-kit.css` 머리말 D-5 가 종료
  조건 없이 열어 둔 것)
- 마일스톤: M7. 짝은 [ACCESSIBILITY_BASELINE_SRS](./ACCESSIBILITY_BASELINE_SRS.md)
  (선행 SRS ①) — **무엇을 만족해야 하는가**는 그쪽이, **어떻게 보장하는가**는
  이 문서가 소유한다.
- 딛는 것: [UI_KIT_SRS](./UI_KIT_SRS.md). 그 문서가 **크기** 토큰을 세웠고
  **색은 소유하지 않는다**(FR-UIK-2)고 적었다. 이 문서가 그 빈자리를 채운다.

---

## 1. 개요

### 1.1 목적

`UI_KIT_SRS` 가 크기 토큰(`--ui-btn-h`·`--ui-radius`·`--ui-icon-*`)을 세울 때
색은 명시적으로 비켜 갔다.

> 색은 소유하지 않는다 (FR-UIK-2). `--accent`·`--border`·`--text-*`·`--danger`
> 는 style.css 의 :root 와 테마가 준다.

그 판단은 그때 옳았다 — 키트의 일이 아니었다. 그러나 **아무도 소유하지 않게
됐다.** 결과가 §2 다: 글자 크기 12종이 토큰 없이 흩어졌고, z-index 값 28종이
서로를 모른 채 커졌으며, Tokyo Night 의 색 값이 CSS 안에 160곳 박혔고,
정의되지 않은 `--bg-alt` 를 네 곳이 읽는다. 그리고 **248개 글자 선언이 어떤
테마에서도 AA 를 넘지 못하는 토큰 위에 서 있다**(§2.1).

이 문서는 넷을 정한다: **토큰 목록 · 파생 규칙 · 하드코딩 금지 게이트 ·
과도기 클래스의 종료 조건.**

### 1.2 범위

**포함**
- 색 토큰의 **목록과 뜻**, 대비 보장을 위한 **파생 규칙** (§3.1·§3.2)
- 글자 크기 토큰 5종 · z-index 계층 토큰 5종 · 백드롭·그림자 토큰 (§3.3·§3.4)
- 하드코딩 금지 게이트 · 미정의 커스텀 프로퍼티 게이트 · 포커스 짝 게이트 (§3.6)
- `style-kit.css` D-5 **과도기 클래스의 종료 조건** (§3.7)
- 테마 54종 팔레트의 **원본 값 보존** — 파생은 팔레트를 고치지 않는다

**비포함**
- 접근성 기준·검사 방법 → `ACCESSIBILITY_BASELINE_SRS`
- 테마 팔레트의 **미적 선택** (어떤 색이 예쁜가). 이 문서는 **계산 가능한 제약**만
- 터미널(xterm) ANSI 팔레트 — `t.terminal` 은 터미널 텍스트의 몫이고 UI 토큰이
  아니다 (`helpers.js` 의 SLOT_EDGE_MIX 주석이 같은 이유를 적었다)
- Monaco 편집기 테마 — `FileEditor.applyTheme()` 가 별도로 파생한다
- 레이아웃 규약(사이드바 폭·분할 비율) — `UI_KIT_SRS` 비포함과 같다

### 1.3 정의

| 용어 | 정의 |
|------|------|
| **팔레트** | `themes.js` 의 `ui` 객체. 테마의 **정본 값**이다 — 이 문서는 고치지 않는다 |
| **토큰** | `--xxx` CSS 커스텀 프로퍼티. 쓰는 자리가 보는 유일한 이름 |
| **파생 토큰** | 팔레트 값에서 **계산해서** 세우는 토큰. `applyThemeObj` 가 만든다 |
| **바닥(floor)** | 그 토큰이 배경에 대해 넘어야 하는 최소 대비 |
| **앵커** | 파생이 섞어 가는 방향의 색. 대비를 올리는 쪽 끝 |
| **참조 배경** | 대비를 재는 상대. `--bg` 와 `--sidebar-bg` **둘 다** (§3.2) |
| **과도기 클래스** | 키트 도입 때 지우지 않고 남긴 옛 클래스 (`.tbtn` 등, D-5) |

### 1.4 참조

- [`UI_KIT_SRS.md`](./UI_KIT_SRS.md) FR-UIK-2·10·11, D-5·D-7 — 크기 토큰과 과도기
- [`ACCESSIBILITY_BASELINE_SRS.md`](./ACCESSIBILITY_BASELINE_SRS.md) FR-A11Y-6~10
  — 바닥의 **근거**. 이 문서는 그 바닥을 **달성하는 방법**이다
- [`BOOT_SCREEN_SRS.md`](./BOOT_SCREEN_SRS.md) FR-BTS-1·2 / D-4 — 첫 페인트 선주입이
  **최종 변수 맵을 캐시한다**. 파생 비용이 부팅에 실리지 않는 이유
- [`production/03-uiux.md`](./production/03-uiux.md) §2.2 — 결함 6건의 실측
- [`SLOT_TITLE_BOUNDARY_SRS.md`](./SLOT_TITLE_BOUNDARY_SRS.md) FR-STB-21~23 —
  **이미 있는 파생의 선례**(`--slot-edge` = border×accent). 이 문서는 그 방법을
  대비 보장으로 확장한다

---

## 2. 현재 상태 (조사로 확정한 사실)

### 2.1 색 — 글자가 읽히지 않는다

테마는 **54종**이다 (dark 43 · light 11). 로드맵이 적은 "44개" 는 낡았다.

`--bg` 대비 (WCAG 2.1 상대휘도):

| 토큰 | 최소 | 중앙값 | 최대 | ≥ 4.5:1 |
|---|---|---|---|---|
| `--text-bright` | 4.99 | 13.24 | 19.03 | 54/54 |
| `--text` | 4.13 | 9.84 | 19.03 | 53/54 |
| `--accent` | 2.69 | 5.90 | 15.89 | 45/54 |
| `--danger` | 2.80 | 4.77 | 7.84 | 33/54 |
| `--text-muted` | 1.69 | 3.21 | 6.15 | **10/54** |
| `--text-dim` | 1.09 | 1.28 | 1.91 | **0/54** |

쓰이는 방식:

| 토큰 | `color:` | `background:` | `border*:` | 합 |
|---|---|---|---|---|
| `--border` | 2 | 26 | 188 | 226 |
| `--accent` | 96 | 20 | 71 | 215 |
| `--text-muted` | **149** | 2 | 0 | 155 |
| `--text-bright` | 103 | 3 | 0 | 110 |
| `--text-dim` | **99** | 3 | 2 | 106 |
| `--danger` | 57 | 1 | 24 | 86 |
| `--text` | 73 | 0 | 0 | 80 |
| `--attn` | 49 | 4 | 13 | 78 |

**`--text-dim` 은 99번 글자로 쓰이고 54종 어디서도 3:1 조차 넘지 않는다.**

### 2.2 `--bg-alt` 는 정의된 적이 없다

7곳이 읽는데 `:root` 에도 `applyThemeObj` 의 맵에도 없다.

| 자리 | 꼴 | 결과 |
|---|---|---|
| `style.css:159` · `:564` | `var(--bg-alt)` | **투명으로 떨어진다** |
| `style-git.css:587` | `var(--bg-alt)` | 〃 |
| `style-git-views.css:94` | `var(--bg-alt)` | 〃 |
| `style-docrender.css:16` · `:63` · `:150` | `var(--bg-alt,var(--bg))` | 폴백이 받는다 |

**폴백을 적은 셋과 적지 않은 넷이 같은 파일 묶음에 있다** — 이것이 토큰에
목록이 없을 때 생기는 일이다. 로드맵이 센 "4곳" 은 폴백 없는 넷이고 정확하다.

### 2.3 하드코딩된 색 160곳

| 파일 | 색 리터럴 |
|---|---|
| `style.css` | **119** |
| `style-editor.css` | 17 |
| `style-git-views.css` | 10 |
| `style-kit.css` | 8 |
| `style-git.css` | 5 |
| `style-docrender.css` | 1 |

가장 많은 것은 `rgba(0,0,0,…)` **67회**(그림자·백드롭)다. 그다음이 **Tokyo Night
팔레트 값**이다 — `#16161e`(sidebarBg) 7회, `#7aa2f7`(accent) 3회,
`rgba(247,118,142,…)`(danger) 10회, `#1a1b26`(bg) 2회. 기본 테마에서만 맞고
나머지 53종에서 어긋난다 (`UX-14`).

### 2.4 글자 크기 12종

실측: `7px 8px 9px 10px 11px 11.5px 12px 13px 14px 15px 18px 20px`.

`11px` 과 `11.5px` 이 함께 있고 `13·14·15` 가 함께 있다 — 어느 것도 뜻으로
갈리지 않는다. `UI_KIT_SRS` 가 `--ui-font:11px` 하나를 세웠으나 키트 밖은 그것을
모른다.

### 2.5 z-index — 44 선언 · 28 값 · 1~9999

의미의 층이 값에서 읽히지 않는다. 실측으로 드러난 어긋남:

| 무엇 | 값 | 문제 |
|---|---|---|
| `#modal-overlay` (설정 모달) | 100 | 같은 뜻인데 |
| `.ui-modal` · `.confirm-overlay` · `.bg-modal` | 200 | 값이 다르다 |
| `.toast-host` 420 · `.git-undo-toast` 400 | | 같은 토스트가 둘 |
| `.ui-menu` · `.git-menu` | 3000 | 모달보다 훨씬 위 — 뜻은 맞으나 간격이 근거 없다 |
| `.ed-find` 9997 · `.tc-copy` 9999 | | **메뉴보다 위**. 편집기 찾기 줄이 컨텍스트 메뉴를 덮는다 |
| `#boot` 1000 · `#focus-edge` 500 · `#attn-edge` 501 | | 각자 정한 값 |

`9997`·`9999` 가 요점이다 — **위에 있어야 한다는 뜻을 값으로 말할 방법이 없어서
큰 수를 골랐다.** 층이 이름을 갖지 않으면 매번 그렇게 된다.

### 2.6 포커스 — `outline:none` 17 vs `:focus-visible` 9

차이 8 이 `UX-13` 이다. 지운 자리와 되살린 자리가 짝이 맞지 않는다.

### 2.7 과도기 클래스 — D-5 가 종료 조건 없이 열려 있다

`style-kit.css` 머리말:

> **기존 클래스를 지우지 않는다** (D-5 / FR-UIK-10). `.ui-btn` 은 `.tbtn` 과
> 함께 붙는다 — e2e 131개와 기존 캐스케이드가 옛 이름 위에 서 있다.

그 판단의 근거 중 **e2e 쪽 절반은 M6 이 없앴다.** `TEST-18` 판정이 확인한 것은
e2e 가 짚는 것이 **도메인 클래스**(`.sbl-item`·`.pn-tab`)이고 CSS 정리가 바꾸는
것은 **선언**이라는 사실이다. 키트 클래스를 단독으로 짚던 자리는 두 곳뿐이었고
둘 다 닫혔다 (`M6_PROGRESS.md` §4).

남은 절반은 **캐스케이드**다. 그것은 여전히 사실이고, 그래서 D-5 를 철회하는
것이 아니라 **끝나는 조건을 준다** (§3.7).

---

## 3. 요구사항

### 3.1 색 토큰의 목록과 뜻

| ID | 요구 | 등급 |
|---|---|---|
| FR-TOK-1 | 색 토큰은 **목록이 있다.** 목록 밖의 색 토큰을 새로 만들려면 이 문서를 고친다. 목록은 **뜻**으로 갈리고 값으로 갈리지 않는다. | 필수 |
| FR-TOK-2 | 글자 토큰은 **넷**이다 — `--text-bright`(가장 또렷) · `--text`(본문) · `--text-muted`(보조) · `--text-hint`(가장 흐린 것). **넷 다 파생 토큰**이다 (§3.2). | 필수 |
| FR-TOK-2a | `--text-bright` 의 바닥은 **고정값이 아니라 그 테마에서 파생된 `--text` 가 실제로 닿은 대비**다(하한 7.0). 고정 바닥만 두면 둘 다 7.0 을 넘으면서 순서가 뒤집힌다 — 실측 6/54 가 그랬다 (D-TOK-9). | 필수 |
| FR-TOK-3 | `--text-dim` 은 **글자 토큰이 아니다.** 팔레트의 원시값으로 남아 **경계·채움·비활성 배경**에만 쓰인다. 현재의 `color:var(--text-dim)` 99곳은 `--text-hint` 로 간다 (D-TOK-2). | 필수 |
| FR-TOK-4 | 글자로 쓰이는 강조색은 별도 파생 토큰을 갖는다 — `--accent-text` · `--danger-text` · `--attn-text`. 원본 `--accent`·`--danger`·`--attn` 은 **경계·배경·틴트**용으로 남는다. 대비 요구가 용도마다 다르기 때문이다 (글자 4.5:1 vs UI 경계 3:1). | 필수 |
| FR-TOK-5 | `--bg-alt` 를 **정의한다** — 표 머리·그룹 머리의 표면. `mixHex(--bg, --text, .06)` 로 파생한다. `--border` 쪽으로 섞지 않는 것은 실측 때문이다: 테마에 따라 `border` 가 `bg` 와 거의 같아 표면이 사라진다(최소 1.001 — 없는 것과 같다). `text` 쪽은 54종 전부에서 1.067~1.202 로 **고르게** 벌어지고, 밝은/어두운 테마의 방향이 자동으로 따라온다. `var(--bg-alt,…)` 의 폴백 세 자리는 폴백을 **지운다**: 토큰이 정의되면 폴백은 거짓 안전망이다. | 필수 |
| FR-TOK-5a | 구문 강조색 여섯(`--term-red`·`green`·`yellow`·`blue`·`magenta`·`cyan`)을 **터미널 팔레트에서 파생한다.** `style-docrender.css` 가 *"터미널 팔레트에서 파생하므로 편집기·터미널·렌더가 같은 색 세계를 쓴다"*(`DOC_RENDER_VIEW_SRS` FR-DRV-13d)고 적어 두고 **그 파생을 두지 않아** 여섯 다 폴백으로 떨어지고 있었다. 구문 강조도 글자이므로 같은 바닥(4.5)을 받는다. | 필수 |
| FR-TOK-5b | 고정폭 글꼴 토큰 `--mono` 를 정의한다 — `style-docrender.css` 가 스택을 따로 적고 `style-git.css` 는 없는 `--mono` 를 읽어 `monospace` 로 떨어지고 있었다. | 필수 |
| FR-TOK-6 | 백드롭·그림자가 토큰을 갖는다 — `--backdrop`(모달 뒤) · `--shadow-1`·`--shadow-2`. `rgba(0,0,0,…)` 67곳이 여기로 수렴한다. | 필수 |
| FR-TOK-7 | 위험색·상태색·구분선이 토큰화된다 — Tokyo Night 리터럴(`#16161e`·`#7aa2f7`·`#1a1b26`·`rgba(247,118,142,…)`)이 CSS 에서 사라진다 (`UX-14`). 라이트 테마에서 사라지는 구분선은 `--border` 가 아니라 `--border-strong` 을 쓰거나 파생 대비 바닥 3:1 을 받는다 (`UX-15`). | 필수 |

### 3.2 파생 규칙 — 대비 보장

> **사용자 결정 (2026-09-12)**: 팔레트 54벌을 손으로 고치지 않고 **파생**으로
> 보장한다. Nord 의 `#4c566a` 는 Nord 그 자체이므로 팔레트는 정본으로 남는다.

| ID | 요구 | 등급 |
|---|---|---|
| FR-TOK-8 | 글자 토큰은 **원시 팔레트 값을 앵커 쪽으로 섞어** 바닥을 넘는 **최초의** 값으로 세운다. 넘지 않는 동안만 섞는다 — 필요한 최소량만 움직인다. | 필수 |
| FR-TOK-9 | **참조 배경은 셋이다** — `--bg` · `--sidebar-bg` · `--bg-alt`. 같은 토큰이 본문·사이드바·표 머리 전부에 쓰이므로 **전부**에서 바닥을 넘어야 한다. 하나를 빠뜨리면 그 표면 위에서만 조용히 미달이 된다 — `--bg-alt` 를 넣기 전 실측에서 `--text-hint` 가 그 위에서 **4.33** 이었다. 그래서 `--bg-alt` 는 파생이 **소유한다**(다른 자리에서 따로 계산하면 두 값이 갈린다). | 필수 |
| FR-TOK-10 | **앵커는 `--text-bright`**, 그것이 `--text` 의 바닥을 못 넘으면 흰색/검은색 중 **배경 대비가 더 큰 쪽**(동률이면 모드가 가른다). 실측 54종 중 53종이 `--text-bright`, 1종이 흑/백이다 (D-TOK-3). | 필수 |
| FR-TOK-11 | **바닥은 사다리다** — `--text-hint` 4.5 · `--text-muted` 5.5 · `--text` 7.0. 단일 바닥 4.5 를 쓰지 않는 이유는 §4 D-TOK-4 에 있다: 54종 중 **34종에서 세 단계가 무너진다.** 강조 글자(`--accent-text`·`--danger-text`·`--attn-text`)의 바닥은 4.5 다. | 필수 |
| FR-TOK-12 | 섞는 비율은 **고정 스텝 0.05**(최대 20회)다. 이분 탐색을 쓰지 않는다 — 결정론과 재현이 정밀도보다 중요하고, 최대 오차는 한 스텝이다. | 필수 |
| FR-TOK-13 | 파생은 **`applyThemeObj` 한 자리**에서만 일어난다. `BOOT_SCREEN_SRS` FR-BTS-1 / D-4 가 이미 세운 규약이다 — 선주입은 **최종 변수 맵**을 캐시하므로 파생을 다시 계산하지 않는다. 파생을 다른 자리에 복사하면 그 규약이 깨진다. | 필수 |
| FR-TOK-14 | 파생 계산은 **순수 함수**로 분리되어 `web/js/test/` 의 단위 테스트가 브라우저 없이 부른다. 대비 게이트(§3.6)가 **같은 함수**를 쓴다 — 게이트와 런타임이 다른 계산을 하면 게이트가 거짓말을 한다 (D-TOK-5). | 필수 |
| FR-TOK-15 | **세 단계가 보존된다**: 파생 후에도 모든 테마에서 `cr(--text-hint) < cr(--text-muted) < cr(--text)`. 실측으로 FR-TOK-11 의 사다리가 54/54 에서 이것을 만족한다(최소 간격 0.56 · 1.04). | 필수 |
| FR-TOK-16 | 팔레트(`themes.js` 의 `ui` 값)는 **한 바이트도 바뀌지 않는다.** 바뀌면 그것은 이 문서가 아니라 테마 변경이다. | 필수 |
| FR-TOK-17 | 사용자 지정 테마(`customTheme`)도 같은 파생을 지난다 — 사용자가 고른 색이 읽히지 않을 수 있고, 그때 보호가 없으면 AA 선언이 거짓이 된다. | 필수 |

### 3.3 글자 크기 토큰

| ID | 요구 | 등급 |
|---|---|---|
| FR-TOK-18 | 글자 크기는 **다섯**으로 수렴한다: `--fs-xs` · `--fs-sm` · `--fs-md` · `--fs-lg` · `--fs-xl`. 12종의 사상표가 구현에서 산출되고 이 문서에 기록된다. | 필수 |
| FR-TOK-19 | 본문은 `--fs-sm` 이고 값은 **11px** 이다 — 현재 `10px`·`11px`·`11.5px` 이 여기로 모인다. 10px 에서 11px 로 오르는 것은 의도된 변경이며 §5 회귀 확인 대상이다. | 필수 |
| FR-TOK-20 | `UI_KIT_SRS` 의 `--ui-font` 는 `--fs-sm` 을 가리킨다. 두 이름이 각자 값을 갖지 않는다 — 키우면 한쪽만 커진다. | 필수 |

**사상표 (구현이 산출, 2026-09-13)**

착수 시 `web/*.css` 의 `font-size` 선언은 **385개**였고 크기는 **12종**이었다.
값은 **사용자 결정 2026-09-13** 이다 — `13px` 을 내리지 않고 `14px` 로 올렸다.
M7 이 `10px`→`11px` 을 올린 것과 같은 방향이고, `13px` 17곳에는 `.confirm-msg`·
`.git-dialog-head` 처럼 **읽어야 하는 대화상자 본문**이 들어 있다.

| 토큰 | 값 | 흡수한 것 (선언 수) | 계 |
|---|---|---|---|
| `--fs-xs` | 9px | `9px`(7) · `8px`(1, 레일 배지) | 8 |
| `--fs-sm` | **11px** | `11px`(183) · `10px`(53) · `11.5px`(1) | 237 |
| `--fs-md` | 12px | `12px`(93) | 93 |
| `--fs-lg` | 14px | `13px`(17) · `14px`(15) · `15px`(1) | 33 |
| `--fs-xl` | 18px | `18px`(2) · `20px`(1) | 3 |
| — | — | `var(--ui-font)`(2) · `var(--sb-rail-fs)`(1) · `em` 상대값(4) | 7 |
| `--pv-fs-xs/sm/md` | 7·8·9px | `.theme-preview-panel` 지역 (4) | 4 |
| | | | **385** |

**커지는 곳** `10px`→11 (53) · `13px`→14 (17) · `8px`→9 (1, 레일 배지).
**작아지는 곳** `11.5px`→11 (`#boot-step`) · `15px`→14 (`.boot-name`) ·
`20px`→18 (탭 닫기 글리프). 셋 다 한 자리씩이다.

**`--sb-rail-fs` 는 `var(--fs-xs)` 가 된다.** 값 `9px` 을 따로 들고 있을 이유가
없다 — `PANEL_SURFACE_SRS` FR-RAL-2 가 요구한 것은 "레일이 토큰 하나를 쓴다"
이고, 그 토큰이 눈금에서 오면 요구는 더 강해진다.

**테마 미리보기는 `--fs-*` 를 쓰지 않는다.** `.pv-status`(7px)는 높이 14px,
`.pv-topbar`(8px)는 높이 16px 안에 든다 — 눈금의 바닥 `9px` 로는 넘친다.
미리보기는 **앱을 축소한 그림**이고(`app-settings-theme.js:53` 이 만든다) 거기
크기는 글자가 아니라 **기하**다. 그래서 예외로 등록하지 않고 `--pv-fs-*` 지역
토큰을 갖는다 — FR-TOK-28 이 "선택자에 매인 정의" 를 정의 자리로 인정하므로
게이트는 그대로 통과하고, **게이트를 느슨하게 만들지 않는다**(FR-TOK-34).
xterm 캔버스(E-1)를 예외로 둔 것과 같은 논리다. 미리보기가 읽히는 글자를 갖게
되면 이 셋은 `--fs-*` 로 돌아온다.

> 미리보기의 **JS 인라인 스타일**에 `font-size:6px` 이 한 자리 더 있다
> (`app-settings-theme.js:61`). FR-TOK-32 는 CSS 를 재므로 게이트 밖이다 —
> 인라인 스타일을 재는 것은 이 문서의 범위가 아니고(§8), 같은 그림의 같은
> 부류이므로 남겨 둔다.

### 3.4 z-index 계층

| ID | 요구 | 등급 |
|---|---|---|
| FR-TOK-21 | z-index 는 **여섯 층**의 토큰으로 수렴한다. 층은 **뜻**이고 값은 그 뜻의 부산물이다. (초안은 다섯이었다 — 사상표를 만들다 여섯째가 필요해졌다. 아래 "왜 여섯인가" 를 본다. 사용자 결정 2026-09-13) | 필수 |
| FR-TOK-22 | 층 안의 순서는 **DOM 순서**가 정한다. 같은 층에서 값을 ±1 하지 않는다 — `#focus-edge` 500 / `#attn-edge` 501 이 그 부류다. | 필수 |
| FR-TOK-23 | 44개 선언 전부가 여섯 토큰 중 하나로 사상되고, **사상표가 이 문서에 남는다.** 사상할 수 없는 선언이 있으면 층이 부족한 것이고, 그때 층을 더하는 것은 이 문서를 고치는 일이다. | 필수 |

**층 (§2.5 의 실측 + 44선언 전수 사상에서 도출)**

| 토큰 | 뜻 | 여기 있는 것 |
|---|---|---|
| `--z-raised` | 같은 뷰 안에서 한 겹 위 | 분할 손잡이 · 드래그 표식 · 편집기 안의 알림 줄 |
| `--z-sticky` | 뷰 안의 고정 요소 | 탭 줄 · 목록 머리 · 검색 줄 · 모바일 키바 |
| `--z-overlay` | 뷰를 덮는 것 | 백드롭 · 드로어 · 부팅 화면 · 칸을 덮는 것 |
| `--z-modal` | 모달·확인창 상자 | `.ui-modal` · `#modal-overlay` · `.confirm-overlay` · `.bg-modal` · **`.ed-find`** |
| `--z-popover` | 모달 **위**에 뜨는 것 | 메뉴 · 토스트 · HUD · `.tc-copy` · 드롭다운 |
| `--z-edge` | 창 수준의 **비상호작용** 표식 | `#focus-edge` · `#attn-edge` |

**왜 여섯인가.** D-TOK-8 이 답을 미리 적어 뒀다 — *"같은 층이면 DOM 순서가
답이고, DOM 순서로 답할 수 없으면 **층이 다른 것**이다."* `#focus-edge`(500)·
`#attn-edge`(501)는 `position:fixed;inset:0;pointer-events:none` 인 **창 전체
테두리 표식**이고, 지금 모달(200·300)·토스트(420)·HUD(450) 위에 있다. 다섯 층에
넣으면 `--z-overlay` 가 되어 **설정 모달이 포커스 테두리를 덮는다** — 어느 창이
포커스를 가졌는지가 모달을 열면 안 보인다. `--z-popover` 로 올리면 뜻이 어긋난다:
popover 는 사용자가 **띄우는** 것이고 이 둘은 사용자가 띄우지 않는다. 그래서
층이 다르다. 두 표식 사이의 순서(500/501)는 같은 층 안에서 DOM 이 정한다 —
FR-TOK-22 가 지목한 바로 그 ±1 이 사라진다.

**`.ed-find` 는 편집기 찾기 줄이 아니다** (초안 정정). §2.5 가 `.ed-find`(9997)를
"편집기 찾기 줄" 로 읽고 `--z-popover` 에 넣었으나 실측은 다르다 —
`position:fixed;inset:0;background:rgba(0,0,0,.35)` 로, **배경을 깐 전체 화면
팔레트**다(빠른 열기·전체 검색이 같은 껍데기를 쓴다). 편집기 안의 찾기 줄은
`.fe-find`(12) 쪽이고 그것은 `.file-editor` 안에 사는 `--z-raised` 다. 그래서
`.ed-find` 는 `--z-modal` 로 간다. `.tc-copy`(9999)는 주석이 *"모달 배경을 깔지
않는다"* 고 적은 대로 비모달 부유 창이므로 `--z-popover` 가 맞다.

**사상표 — 44선언 전수 (구현이 산출, 2026-09-13)**

착수 시 실측은 **44선언 · 28값 · 1~9999** 다 (`web/vendor/` 제외). 값 옆의
숫자가 종전 값이다.

| 층 | 값 | 선언 (종전 값) | 계 |
|---|---|---|---|
| `--z-raised` | 100 | `.sh`1 · `.git-head`1 · `.slot-handle`5 · `.fe-render`6 · `.fe-dd-peek`6 · `.fe-offer`11 · `.fe-find`12 · `.git-hunk-bar`12 · `.fe-note`13 · `.pn-drop-indicator`15 | 10 |
| `--z-sticky` | 200 | `.git-files-bar`1 · `.tl-section`1 · `body.mobile #sb-tabs`1 · `.search-bar`10 | 4 |
| `--z-overlay` | 300 | `.tp.dragover::after`20 · `.tp-overlay`25 · `.pn-dimmed .pn-body::after`30 · `.pn.attn::after`35 · `#drawer-backdrop`40 · `body.mobile #sidebar`50 · `#mobile-keybar`60 | 7 |
| `--z-modal` | 400 | `#modal-overlay`100 · `.ui-modal`200 · `.confirm-overlay`200 · `.bg-modal`200 · `.runs-modal`200 · `.gc-modal`300 · `.git-dialog`300 · `.ed-find`9997 · `#boot`1000 | 9 |
| `--z-popover` | 500 | `.git-commit-menu`20 · `.sbx-progress`60 · `.ver-held`60 · `#mkb-tip`70 · `#attn-center`200 · `.git-undo-toast`400 · `.toast-host`420 · `#ui-size-hud`450 · `.ui-menu`3000 · `.git-menu`3000 · `.tc-copy`9999 | 11 |
| `--z-edge` | 600 | `#focus-edge`500 · `#attn-edge`501 | 2 |
| — | — | `.git-commit`5 — **선언이 사라졌다** (아래) | −1 |
| | | | **43** |

간격 100 은 여유지 의미가 아니다 — 그 사이에 값을 적을 일이 없고, 게이트가
리터럴을 막는다.

**구현이 초안의 다섯을 고쳤다.** 사상표를 종이에서 만든 뒤 **쌓임 문맥과 DOM
순서를 실제로 따라가자** 다섯 자리가 틀렸다. 셋은 "같은 층으로 접으면 DOM
순서가 답한다" 를 확인하는 과정에서, 둘은 **전량 e2e 가** 드러냈다.

| 선언 | 초안 | 실제 | 왜 |
|---|---|---|---|
| `.pn.attn::after` 35 | `--z-raised` | **`--z-overlay`** | 같은 `.pn` 문맥의 `.pn-dimmed .pn-body::after`(30)가 overlay 로 가므로, 알림 링이 raised(100)면 **흐림막이 링을 덮는다**. 둘을 같은 층에 두면 `.pn::after` 가 `.pn-body` 보다 뒤에 와서 DOM 순서가 오늘과 같은 답을 준다 |
| `#mobile-keybar` 60 | `--z-sticky` | **`--z-overlay`** | 키바는 "뷰 안의" 고정 요소가 아니라 **뷰포트에 붙어 뷰를 덮는** chrome 이다. sticky(200)로 내리면 드로어(`#drawer-backdrop`·`body.mobile #sidebar`, overlay)가 키바를 덮는다. overlay 로 두면 `index.html` 에서 키바가 그 둘보다 **뒤에** 오므로(119·120 < 225) DOM 순서가 오늘(60 > 50 > 40)과 같다 |
| `#boot` 1000 | `--z-overlay` | **`--z-modal`** | `#boot` 은 한 뷰를 덮는 것이 아니라 **앱 전체를 덮는 차단 화면**이다. overlay 로 내리면 `body.mobile #sidebar`(overlay)가 `index.html` 상 `#boot`(98)보다 뒤(120)라서 **모바일에서 드로어가 부팅 화면 위로 그려진다** — NFR-TOK-2 가 지키는 첫 페인트가 깨진다 |
| `.git-head` 1 | `--z-sticky` | **`--z-raised`** | 이름만 보고 골랐다. 그 규칙은 `position:relative` 이고 **스크롤에 붙지 않는다** — 뷰 안에서 한 겹 위일 뿐이다. sticky(200)로 올리자 형제 `.git-commit`(raised 100) 위가 되어 **머리가 커밋 메뉴의 클릭을 가로챘다**(종전 `commit 5 > head 1`). 전량 e2e 가 `git-commit.spec.ts` E12 에서 `<div class="git-head"> intercepts pointer events` 로 잡았다 |
| `.git-commit` 5 | `--z-raised` | **선언을 지웠다** | 아래 |

**`.search-bar` 의 `z-index` 는 무력하다.** 그 규칙에 `position` 이 없어서
(`style.css`) 종전 `10` 도 지금 `--z-sticky` 도 계산에 들어가지 않는다. 사상은
한다 — 값이 층에서 와야 한다는 요구는 그 선언이 효력이 있는지와 무관하고,
언젠가 `position` 이 붙는 날 뜻이 이미 적혀 있다.

**`--z-raised` 는 1 이 될 수 없다.** `.fe-dd-peek` 의 `6` 은 임의값이 아니다 —
그 규칙의 주석이 근거를 적고 있다: Monaco 의 `.view-lines` 가 **클릭을
가로챘고**(playwright 가 `intercepts pointer events` 로 거부했다) 그래서 올린
값이다. `.git-hunk-bar`(12)도 같은 부류다 — Monaco 의 **content widget**
(`panel-diff.js:447` 이 `addContentWidget` 으로 붙인다)이라 Monaco 가 인라인으로
`position:absolute` 를 주고, 그 위젯 무리 안에서 순서를 다툰다. 이 층은
**벤더(Monaco, 예외 E-2)의 내부 스택 위**여야 하므로 값은 그 위에서 고른다.

**`.file-editor` 의 세 겹은 DOM 순서가 답하지 못했다** (사용자 결정 2026-09-13).
`.fe-offer`(11) · `.fe-find`(12) · `.fe-note`(13) 은 셋 다 **첫 사용 시점에**
`appendChild` 된다 (`file-editor.js:944`·`966`, `file-editor-find.js:76`) — 사용자가
무엇을 먼저 건드렸느냐로 DOM 순서가 갈린다. 그러면서 **알림과 찾기는 좌표가
같고**(`top:6px;right:18px`) 찾기는 제안 띠 위에 얹힌다. FR-TOK-22 의 "DOM
순서가 정한다" 가 여기서는 답이 없다.

그래서 **보일 때 맨 뒤로 옮긴다** — `findOpen` 과 `note()` 가 `appendChild` 로
자신을 끝에 다시 붙인다. "마지막에 보인 것이 위" 이고, 그것이 사용자가 방금 한
일을 보여 준다. **동작 1건이 바뀐다**: 종전 사다리는 알림이 **항상** 위였고,
좌표가 같으므로 알림이 뜬 동안 찾기를 열면 **방금 열어 놓은 입력 칸이
가려졌다.** 이제 찾기가 보인다 — 변경이면서 거의 수정이다.

**`#focus-edge` / `#attn-edge` 의 ±1 은 DOM 순서로 옮겼다.** 종전 500/501 은
D-TOK-8 이 지목한 "누가 위인가를 값으로 적은 자리" 다. 같은 층(`--z-edge`)에
두고 `index.html` 에서 **`#attn-edge` 를 뒤로** 옮겼다 — 알림 테두리가 포커스
테두리 위여야 하기 때문이다(종전 501 > 500): `#focus-edge` 의
`backdrop-filter:invert` 가 알림색을 뒤집으면 알람이 알람으로 보이지 않는다.
동작은 같고 ±1 은 사라졌다.

**페인트 순서가 실제로 바뀌는 자리 (FR-TOK-22 의 대가)**

| 자리 | 이전 | 이후 | 판단 |
|---|---|---|---|
| `.tp.dragover::after`20 ↔ `.tp-overlay`25 | 오버레이가 위 | **드롭 힌트가 위** | 힌트는 `.tp` 의 `::after` 라 DOM 에서 `.tp-overlay`(`appendChild`, `term-pane.js:754`) 뒤에 온다. 드래그 중이라면 힌트가 보이는 쪽이 맞다 |
| `.sbx-progress`60 · `.ver-held`60 · `#attn-center`200 | 모달 아래(또는 같은 층) | **모달 위** | 진행·알림·드롭다운이다. 모달이 떠 있어도 보여야 하는 부류이고 `--z-popover` 의 뜻이 그것이다 |
| `.ed-find`9997 ↔ 메뉴 3000 | 팔레트가 메뉴 위 | **메뉴가 팔레트 위** | 팔레트는 배경을 깐 모달이고 메뉴는 그 위에 뜬다. 팔레트 위에서 우클릭한 메뉴가 팔레트에 가리지 않는다 |
| `#boot`1000 ↔ 모달 | 부팅 화면이 모달 위 | **모달이 부팅 화면 위** | 부팅 중에는 모달이 없다. 무해하지만 기록한다 |
| `#focus-edge`·`#attn-edge` | 메뉴·팔레트 아래 | **최상위** | `--z-edge` 가 그 뜻이다. 둘 다 `pointer-events:none` 이므로 조작을 막지 않는다 |
| `.fe-note` ↔ `.fe-find` | 알림이 항상 위 | **나중에 보인 것이 위** | 위의 세 겹 항목 |

**`.git-commit` 의 선언이 사라졌다 — 층 체계가 그것을 불필요하게 만들었다.**
종전 `.git-commit{position:relative;z-index:5}` 는 **메뉴를 형제들 위로
들어올리려고 상자에 걸린 값**이었다(그 규칙의 주석이 그렇게 적고 있다 — 뒤따르는
`.git-changes-body` 가 메뉴를 가려 클릭이 먹지 않았다).

그 방식의 대가가 실측으로 드러났다: **메뉴의 층이 상자의 층에 갇힌다.**
`.git-files-bar` 가 `--z-sticky`(200, 그 규칙은 진짜 `position:sticky` 다)로 가자
상자(`--z-raised` 100)보다 위가 되어 메뉴의 클릭이 다시 먹지 않았다.

그래서 `position:relative` 만 남기고 **층은 메뉴가 자기 것을 쓴다**
(`.git-commit-menu` → `--z-popover` 500). 상자는 쌓임 문맥을 만들지 않으므로 그
500 이 `.git-view` 문맥에서 실제로 효력을 갖고, `.git-files-bar`(200)·
`.git-head`(100)·목록 행(0)을 전부 넘는다.

**이것이 층 체계가 있는 이유다**: 팝오버는 팝오버 층에 있으면 되고, 그것을 담은
상자가 같이 올라갈 필요가 없다. 상자를 올리는 방식은 그 상자와 형제들의 값을
**영원히 서로 매어 둔다** — 44선언을 값으로 다투던 시절의 문법이다. 선언 하나가
줄어 **43** 이 됐고, 그것이 이 마일스톤이 무엇을 없앴는지를 말한다.

### 3.5 포커스

| ID | 요구 | 등급 |
|---|---|---|
| FR-TOK-24 | `outline:none` 을 쓰는 모든 규칙에 **같은 요소를 덮는 `:focus-visible` 대체**가 있다. 현재 17 vs 9. | 필수 |
| FR-TOK-25 | 포커스 링은 토큰에서 온다 — `--focus-ring`. 대비 바닥은 **3:1**(UI 컴포넌트, WCAG 1.4.11)이고 파생으로 보장한다. | 필수 |

### 3.6 게이트

| ID | 요구 | 등급 |
|---|---|---|
| FR-TOK-26 | **대비 게이트**: 테마 **54종 전부**에 대해 파생 토큰의 대비를 계산하고 바닥 미달 0 을 확인한다. 브라우저 없이 돈다. `themes.js` 를 고치는 커밋에서 즉시 빨개진다. | 필수 |
| FR-TOK-27 | 대비 게이트는 **런타임과 같은 파생 함수**를 부른다 (FR-TOK-14). 값을 다시 적지 않는다. | 필수 |
| FR-TOK-28 | **미정의 커스텀 프로퍼티 게이트**: CSS 가 읽는 모든 `var(--x)` 중 어디에서도 세워지지 않는 이름이 **0** 이다. 폴백(`var(--x,y)`)이 있어도 미정의는 미정의로 잡는다 — 폴백은 의도를 숨긴다. 세우는 자리는 넷이다: `:root` · 선택자에 매인 정의(`.run-view{--run-ok:…}`) · `applyThemeObj` 의 맵 · JS 의 `setProperty`. **범위까지 보지는 않는다** — "이 이름이 세워지기는 하는가" 가 이 게이트의 물음이고, "그 요소에서 보이는가" 는 브라우저가 답한다. | 필수 |
| FR-TOK-28a | 게이트는 **주석을 걷어낸 뒤** 읽는다. 첫 판이 `style.css` 의 *"`var(--fg)` 는 어디에도 정의된 적이 없어"* 라는 **주석을 사용으로 읽어** 이미 고쳐진 과거를 결함으로 올렸다. | 필수 |
| FR-TOK-29 | **하드코딩 금지 게이트**: CSS 의 색 리터럴이 `:root` 블록 밖에 **0** 이다. 예외는 이 문서의 §6 예외표에서 파생한다 (`currentColor`·`transparent`·`inherit` 는 리터럴이 아니다). | 필수 |
| FR-TOK-30 | **포커스 짝 게이트**: `outline:none` 을 가진 **선택자마다** 대응이 있다 — 상태 의사클래스(`:focus`·`:hover`·`:active`·`:focus-visible`·`:focus-within`)를 걷어낸 **바탕 선택자**가 같은 `:focus-visible` 규칙이 `outline` 을 그린다. 개수 조건(`outline:none` ≤ `:focus-visible`)은 그 위의 덤이다 — **개수만 보면 아무 데나 링을 더해 통과할 수 있고, 지워진 그 요소는 고쳐지지 않는다.** 게이트는 링의 색이 `var(--focus-ring)` 인 것도 함께 본다 (FR-TOK-25). | 필수 |
| FR-TOK-31 | **z-index 게이트**: `z-index:` 의 값이 숫자 리터럴인 자리가 0 이다 — 전부 `var(--z-*)`. 예외는 `0`·`auto`. | 필수 |
| FR-TOK-32 | **글자 크기 게이트**: `font-size:` 의 값이 px 리터럴인 자리가 0 이다 — 전부 `var(--fs-*)`. | 필수 |
| FR-TOK-33 | 이 문서가 세우는 **모든 게이트는 탐침으로 검출을 확인하고 지운다** — 일부러 위반을 넣어 빨개지는 것을 보고 되돌린다. `ACCESSIBILITY_BASELINE_SRS` FR-A11Y-25 와 같은 규약이고, M6 "비싸게 배운 것 9" 가 요구한다. | 필수 |
| FR-TOK-34 | 게이트를 느슨하게 만들어 통과시키지 않는다. 통과할 수 없으면 §6 에 예외로 등록하거나 이 문서를 고친다. | 필수 |

### 3.7 과도기 클래스의 종료 조건 — D-5 를 닫는다

| ID | 요구 | 등급 |
|---|---|---|
| FR-TOK-35 | 과도기 클래스(`.tbtn`·`.mtbtn`·`.preset-btn` 등)는 **CSS 선언이 0개가 되는 시점**에 과도기를 끝낸다. 그 시점 판정은 사람이 아니라 **파일이 말한다**: 규칙 본문이 비었는가. | 필수 |
| FR-TOK-36 | 선언이 빈 클래스는 셋 중 하나로 처리한다 — ① e2e·JS 가 그 이름을 짚지 않으면 **HTML 에서도 제거** ② 짚으면 **빈 규칙을 남기지 않고** HTML 의 클래스만 남긴다(CSS 규칙 자체를 지운다) ③ 캐스케이드 순서 때문에 규칙이 필요하면 **그 이유를 주석으로 적고** 남긴다. ③ 은 근거를 적을 수 있을 때만이다. | 필수 |
| FR-TOK-37 | **①/② 를 가르는 판정은 grep 이 아니라 검사다.** 이름을 지우고 전량 e2e 를 돌린다 — M6 §4-A-3 이 정확히 이 부류에서 무너졌다(격리 하네스가 `git-*` 표적 검증을 통과하고 전량에서 죽었다). | 필수 |
| FR-TOK-38 | 한 표면씩 끝낸다. 상단바 → 설정 모달 → 사이드바 → git 순이며, **표면마다 전량 e2e 를 한 번 돈다.** 겹쳐 돌리지 않고, 도는 동안 `web/js`·CSS 를 고치지 않는다 (M6 실행 규약). | 필수 |
| FR-TOK-39 | 과도기가 끝난 클래스 목록이 `style-kit.css` 머리말에서 **줄어든다.** D-5 의 문장은 남되 "남은 것" 이 이 문서 §7 의 표로 대체되고, 표가 비면 D-5 가 닫힌다. | 필수 |

### 3.8 비기능

| ID | 요구 |
|---|---|
| NFR-TOK-1 | 파생이 테마 전환에 더하는 시간이 **프레임 하나(16ms) 안**. 실측 기준 토큰 8개 × 최대 20 스텝 × 대비 계산 2회 = 320회의 부동소수 연산이다. |
| NFR-TOK-2 | **부팅 첫 페인트에 파생이 실리지 않는다** — 선주입이 최종 맵을 캐시한다 (FR-TOK-13). `BOOT_SCREEN_SRS` 의 FOUC 제거(`03 §4-4` 양호 판정)가 깨지지 않는다. |
| NFR-TOK-3 | 게이트 54종 계산이 **1초 이내**. `make gates` 의 체감을 바꾸지 않는다. |
| NFR-TOK-4 | 파생 함수는 의존이 0 이다 — `web/js/test/` 가 브라우저 없이 부를 수 있어야 한다 (`drop-entries.js` 의 선례). |

---

## 4. 설계 결정

**D-TOK-1: 팔레트는 정본이고 파생이 보장한다.**

  (사용자 결정 2026-09-12)
  54벌의 `textMuted`·`textDim` 을 손으로 올리는 대안은 두 가지를 잃는다: ① 정본
  팔레트와의 대조 가능성 — Nord·Solarized·Gruvbox 는 상류에 원본이 있고 그것과
  다르면 "우리가 왜 다른가" 를 매번 설명해야 한다. ② 새 테마의 안전 — 사람이
  손으로 맞춰야 하고, 맞추지 않으면 조용히 미달로 들어온다. 파생은 **새 테마도
  자동으로 받는다.**

**D-TOK-2: `--text-dim` 의 뜻을 가른다.**

  한 토큰이 글자(99곳)와 경계·채움(5곳)
  양쪽에 쓰이고 있었다. 글자의 바닥은 4.5, UI 경계의 바닥은 3:1 이므로 **같은
  값일 수 없다.** `--text-dim` 을 비글자용 원시값으로 남기고 `--text-hint` 를
  새로 세운다. 이름을 바꾸지 않고 값만 올리면 경계까지 밝아져 테마가 뭉개진다.

**D-TOK-3: 앵커는 `--text-bright`.**

  배경 반대편 극단(흰색/검은색)으로 바로
  섞지 않는 이유는 **색상(hue)을 잃기 때문**이다 — Gruvbox 의 흐린 글자가 회색이
  되면 Gruvbox 가 아니다. `--text-bright` 는 그 테마가 스스로 고른 "가장 또렷한
  글자" 이므로 그 쪽으로 섞으면 테마 안에 머문다. 실측 53/54 가 이 앵커로 닿고,
  닿지 않는 1종만 흑/백으로 내려간다.

  **내려갈 때 모드를 보지 않는다.** 초판 초안은 `mode` 로 흑/백을 갈랐는데,
  게이트 탐침이 그것이 틀릴 수 있음을 드러냈다: 배경이 `#808080` 이면 검은색이
  5.32 인데 흰색은 3.95 다 — `mode:'dark'` 를 따르면 **더 나쁜 쪽**을 고른다.
  `mode` 는 배경의 밝기를 **짐작한** 값이고, 여기서는 짐작할 필요 없이 **재면
  된다**. 동률일 때만 `mode` 가 가른다.

  **그리고 닿지 못하는 팔레트가 있다.** 중간 밝기 배경에서는 흰색도 검은색도
  7.0 에 미치지 못한다 — 그 배경 위에 그 대비는 **존재하지 않는다.** 내장 54종에는
  없고 게이트가 새로 들어오는 것을 막지만, 사용자 지정 테마는 게이트를 지나지
  않는다(FR-TOK-17). 그때 파생은 **닿을 수 있는 가장 먼 곳**을 준다 — 바닥을 못
  넘는 것과 원래 색 그대로 두는 것 중에는 앞이 낫다.

**D-TOK-4: 바닥은 사다리다 (4.5 / 5.5 / 7.0).**

  이것이 이 문서에서 가장 비싼
  결정이고, **실측이 결정했다.** 세 토큰에 같은 바닥 4.5 를 주면:

  > 미달 0 · **계층깨짐 34/54** · `--text-muted` − `--text-hint` 간격 평균
  > **−0.03** (최소 −0.57)

  가장 흐리던 것(`--text-dim`, 시작 1.28)이 가장 많이 올라가 **중간 단계를 추월
  한다.** Dracula 에서 hint 4.88 > muted 4.73 이 된다 — 흐리라고 만든 색이 더
  진해진다. 사다리(4.5 / 5.5 / 7.0)를 주면:

  > 미달 0 · **계층깨짐 0/54** · 간격 muted−hint 평균 0.95(최소 **0.56**) ·
  > text−muted 평균 4.28(최소 **1.04**)

  `--text` 의 7.0 은 **AAA 수준이지만 부담이 아니다** — 중앙값이 이미 9.84 이고
  손대는 테마가 13/54 뿐이다. 접근성을 위해 고른 값이 아니라 **아래 두 단계가
  숨 쉴 공간**을 만들려고 고른 값이다.

**D-TOK-5: 게이트와 런타임이 같은 함수를 쓴다.**

  게이트가 값을 다시 계산하면
  둘이 갈라지고, 갈라진 순간 **게이트는 초록인데 화면은 미달**이 된다. M6
  "비싸게 배운 것 9"(초록인 게이트와 재고 있는 게이트는 다르다)의 같은 부류다.
  그래서 파생은 의존 0 의 순수 모듈이어야 한다 (NFR-TOK-4).

**D-TOK-6: 미정의 프로퍼티는 폴백이 있어도 잡는다.**

  `style-docrender.css` 의
  `var(--bg-alt,var(--bg))` 는 지금 **동작한다** — 그러나 그 자리는 `--bg-alt` 가
  있다고 믿고 쓴 자리이고, 폴백은 그 믿음이 틀렸다는 사실을 숨긴다. 같은 묶음의
  네 자리가 폴백 없이 투명으로 떨어진 것이 그 증거다.

**D-TOK-7: 과도기의 끝은 "선언이 비었는가" 다.**

  날짜나 마일스톤이 아니라
  **파일이 말하는 조건**이어야 한다 — D-5 가 종료 조건 없이 열린 이유가 그것을
  사람의 판단에 맡겼기 때문이다. 선언이 0개면 그 클래스는 더 이상 외형을 주지
  않고, 그때부터 그것은 **선택자**이지 스타일이 아니다.

**D-TOK-9: `--text-bright` 의 바닥은 `--text` 가 정한다.**

  초안은 글자 토큰을 셋으로 적었다. 구현 중 실측이 넷째를 드러냈다 —
  `--text-bright` 는 `color:` 로 **103번** 쓰이는데, `--text` 를 7.0 까지 올리고
  나면 **6/54 에서 bright 가 본문보다 흐려진다.** Solarized Light 의 `#586e75` 는
  4.39 로 AA 자체를 못 넘었다. "가장 또렷한 글자" 가 본문보다 흐린 것은 이름이
  거짓말을 하는 상태다.

  고정 바닥(가령 8.0)으로는 풀리지 않는다 — 테마마다 `--text` 가 닿는 곳이
  달라(7.0~19.03) 어떤 고정값도 어떤 테마에서는 뒤집힌다. 그래서 바닥을
  **그 테마의 파생된 `--text`** 로 잡는다. 앵커가 이미 그 대비를 넘으므로 항상
  닿는다.

  **한 자리만 예외다**: 앵커가 `textBright` 자신이면 그리로 섞는 것이 제자리
  걸음이다. Monokai 가 그렇다 — 팔레트의 `#f8f8f0`(13.93)이 `#f8f8f2`(13.94)보다
  0.01 흐리다. **팔레트 자신의 모순**이지 파생이 만든 것이 아니다. 그때는 둘을
  합친다: 본문이 곧 가장 또렷한 글자다.

**D-TOK-8: z-index 층 안에서 값을 다투지 않는다.**

  `#focus-edge` 500 /
  `#attn-edge` 501 은 "이 둘 중 누가 위인가" 를 값으로 적은 자리다. 같은 층이면
  DOM 순서가 답이고, DOM 순서로 답할 수 없으면 **층이 다른 것**이다.

---

## 5. 검증

### 5.1 단위 (브라우저 없이)

| ID | 무엇 |
|---|---|
| TC-TOK-1 | 파생 함수가 테마 54종 전부에서 **바닥을 넘는다** (미달 0) |
| TC-TOK-2 | 파생 함수가 54종 전부에서 **계층을 보존한다** — `hint < muted < text ≤ bright` · `hint` 와 `muted` 가 같은 색으로 뭉개지지 않는다 |
| TC-TOK-3 | 이미 바닥을 넘는 값은 **손대지 않는다** (t=0) |
| TC-TOK-4 | 참조 배경 둘 중 **나쁜 쪽**을 기준으로 판정한다 (사이드바에서만 미달하는 합성 팔레트로 확인) |
| TC-TOK-5 | 앵커가 바닥을 못 넘는 팔레트에서 **흑/백으로 내려간다** · 그 선택이 `mode` 가 아니라 **대비**를 따른다 · 흑백으로도 닿지 못하면 앵커까지 간다 |
| TC-TOK-6 | 사용자 지정 테마도 같은 파생을 지난다 (FR-TOK-17) |
| TC-TOK-7 | 스텝이 고정 0.05 다 — 같은 입력이 같은 출력 (결정론) |

### 5.2 게이트

| ID | 무엇 |
|---|---|
| TC-TOK-8 | 대비 게이트 통과 · **탐침**: 팔레트 하나의 `textMuted` 를 배경에 가깝게 바꾸면 빨개진다 |
| TC-TOK-9 | 미정의 프로퍼티 게이트 0 · **탐침**: `var(--nope)` 를 넣으면 빨개진다 (폴백 있는 꼴도) |
| TC-TOK-10 | 하드코딩 금지 게이트 0 · **탐침**: `color:#fff` 를 넣으면 빨개진다 |
| TC-TOK-11 | 포커스 짝 게이트 · **탐침**: `:focus-visible` 없이 `outline:none` 을 넣으면 빨개진다 |
| TC-TOK-12 | z-index 게이트 · **탐침**: `z-index:42` 를 넣으면 빨개진다 |
| TC-TOK-13 | 글자 크기 게이트 · **탐침**: `font-size:13px` 를 넣으면 빨개진다 |
| TC-TOK-14 | 게이트가 런타임과 **같은 함수**를 부른다 — 파생 함수를 고치면 게이트 결과가 따라 바뀐다 |

### 5.3 e2e·회귀

| ID | 무엇 |
|---|---|
| TC-TOK-15 | `--bg-alt` 를 쓰는 네 자리의 배경이 **투명이 아니다** |
| TC-TOK-16 | 테마를 전환하면 파생 토큰이 함께 바뀐다 (Tokyo Night → Gruvbox Light) |
| TC-TOK-17 | 부팅 첫 페인트에 FOUC 가 없다 — `03 §4-4` 양호 판정 보존 |
| TC-TOK-18 | `.tc-copy` 가 컨텍스트 메뉴와 **같은 층**(`--z-popover`)이고 DOM 순서대로 겹친다 · `.ed-find` 는 모달 층이므로 메뉴가 그 **위**에 뜬다 (초안은 `.ed-find` 를 찾기 줄로 오독했다 — §3.4) |
| TC-TOK-19 | e2e 전량 unexpected 0 — 과도기 표면마다 1회 (FR-TOK-38) |
| TC-TOK-20 | `themes.js` 의 `ui` 값이 착수 전과 **바이트 단위로 같다** (FR-TOK-16) |

### 5.4 사람이 보는 것 (기록으로 남긴다)

- 테마별 **전후 대비표**를 산출물로 남긴다 — 54행 × 토큰 8열.
- 대표 넷(Tokyo Night · Gruvbox Dark · Solarized Light · Catppuccin Latte)을 눈으로
  확인. 계산이 통과해도 **시각 계층이 읽히는지**는 다른 물음이다.
- 본문 10px → 11px 이 레이아웃을 밀지 않는지 (좁은 사이드바·모바일 키바).

---

## 6. 예외 등록부

> FR-TOK-29·34. 하드코딩 금지 게이트의 예외는 **여기서 파생한다.**

| # | 자리 | 무엇 | 근거 | 재검토 조건 |
|---|---|---|---|---|
| — | (없음) | | | |

착수 시점에는 비어 있다. §2.3 의 160곳은 **전부 토큰으로 간다**는 것이 기본
가정이고, 갈 수 없는 자리가 나오면 근거와 함께 여기 등록한다. 등록이 늘면 그
커밋이 "토큰의 범위가 줄었다" 를 말한다.

---

## 7. 과도기 클래스 잔여표

> FR-TOK-35~39. **이 표가 비면 `style-kit.css` 의 D-5 가 닫힌다.**

| 클래스 | 표면 | 선언 수 | 상태 |
|---|---|---|---|
| (착수 시 채운다 — `style.css`·`style-git.css` 의 옛 버튼·탭·상자 규칙) | | | |

채우는 방법: 키트 클래스와 **함께 붙는** 옛 클래스를 열거하고, 각각의 CSS 선언
수를 센다. 선언 수가 0 이 되면 FR-TOK-36 의 ①/②/③ 중 하나로 처리하고 행을
지운다.

---

## 8. 비목표

1. **테마를 예쁘게 만드는 것.** 이 문서는 계산 가능한 제약만 다룬다.
2. **팔레트 값 변경.** FR-TOK-16 이 금지한다.
3. **테마 추가·삭제.** 54종은 그대로다.
4. **다크/라이트 시스템 추종**(`UX-19`) — 토큰 위에 서지만 별개 항목이다.
5. **터미널 ANSI 팔레트·Monaco 테마.** 각자 파생을 갖는다.
6. **CSS 파일 분할·재배치.** 선언을 옮기지 않고 **값만** 토큰으로 바꾼다.
7. **`!important` 도입.** `UI_KIT_SRS` FR-UIK-11 이 키트에 금지했고 그대로다.

---

## 9. 리스크

| 리스크 | 등급 | 완화 |
|---|---|---|
| 파생이 **248곳의 글자를 또렷하게** 만든다 — 시각 계층이 무너지거나 화면이 시끄러워질 수 있다 | **HIGH** | D-TOK-4 의 사다리가 세 단계를 지킨다(실측 0/54 깨짐). FR-TOK-8 이 **최소량만** 섞는다. §5.4 의 전후 표와 대표 테마 넷 육안 확인 |
| 하드코딩 160곳을 토큰으로 바꾸는 동안 **어느 자리가 어느 토큰인지 판단이 틀린다** — `rgba(0,0,0,.3)` 이 그림자인지 백드롭인지는 문맥이다 | **HIGH** | 한 표면씩(FR-TOK-38). 그림자·백드롭은 토큰을 먼저 세우고(FR-TOK-6) 자리마다 **무엇을 덮는지**로 가른다 |
| 과도기 클래스를 지우다 **e2e 가 무너진다** | MEDIUM | FR-TOK-37 — grep 이 아니라 검사로 판정. M6 §4-A-3 이 같은 부류에서 무너졌다. 표면마다 전량 1회 |
| `font-size` 10px → 11px 이 **레이아웃을 민다** | MEDIUM | TC-TOK-19·§5.4. 좁은 사이드바·모바일 키바·상태바가 먼저 드러난다 |
| z-index 를 다섯 층으로 접다가 **겹침 순서가 뒤집힌다** — 특히 `.ed-find` 9997 이 내려올 때 | MEDIUM | FR-TOK-23 의 사상표를 **먼저** 만들고 검토한다. TC-TOK-18 이 그 한 자리를 고정 |
| 파생 함수가 런타임과 게이트에서 갈라진다 | MEDIUM | D-TOK-5 · FR-TOK-14 · TC-TOK-14. 의존 0 모듈 하나가 진실 |
| `--text` 바닥 7.0 이 **13개 테마의 본문 색을 바꾼다** | LOW | 중앙값이 이미 9.84 다. 바뀌는 13종의 전후를 표로 남기고 대표 둘을 육안 확인 |

---

## 10. 변경 기록

| 날짜 | 내용 |
|---|---|
| 2026-09-13 | FR-TOK-21·22·23·31 구현 (`UX-16`). z-index **44선언 28값 → 여섯 층 · 43선언** (100/200/300/400/500/600). 사상표는 §3.4. 구현이 초안의 **다섯**을 고쳤다 — `.pn.attn::after`·`#mobile-keybar`·`#boot` 는 쌓임 문맥·DOM 순서를 따라가 보니 층이 달랐고, `.git-head`(실은 `position:relative`)·`.git-commit` 은 **전량 e2e 가** 잡았다. `.git-commit` 의 선언은 지웠다 — 메뉴를 들어올리려 상자에 걸린 값이었고, 메뉴가 `--z-popover` 를 직접 쓰면 필요 없다. `.file-editor` 의 세 겹은 DOM 순서가 답하지 못해 **보일 때 맨 뒤로 옮기는** 규약을 세웠다(사용자 결정). `#focus-edge`/`#attn-edge` 의 ±1 은 `index.html` 의 순서로 옮겼다. 게이트는 `scripts/check-z-index.mjs` — 리터럴 0 과 **층에 대한 산술 0**(이름을 붙인 ±1). 탐침 `z-index:42` · `calc(var(--z-modal) + 1)` 둘로 검출 확인. Makefile·`verify.yml` 둘 다 |
| 2026-09-13 | FR-TOK-18·19·20·32 구현 (`UX-18`). 글자 크기 **12종 → 다섯**. 사상표는 §3.3. 값은 사용자 결정(`13px`→`14px`, 내리지 않고 올린다). `--sb-rail-fs` 가 `var(--fs-xs)` 가 됐고 `--ui-font` 가 `var(--fs-sm)` 을 가리킨다. 게이트는 `scripts/check-font-size.mjs` — **종수를 세지 않고 px 리터럴을 0 으로** 잰다(종수 조건은 한 줄 고쳐 통과할 수 있다). 탐침 `font-size:13px` 로 검출 확인. Makefile·`verify.yml` 둘 다에 넣었다. **회귀 1건**: `UI_LAYOUT_DEFAULTS_SRS` 의 계산값 기준선에서 `#add-sandbox-window` 의 `inset` 이 1.73px 움직였다 — `10px`→11px 의 직접 귀결이고 그 문서 §9 에 이전/새/이유를 적었다 |
| 2026-09-13 | FR-TOK-25·30 구현 (`UX-13`). `--focus-ring` 을 파생으로 세웠다 — 원시 `--accent` 는 54종 중 **2종**(Ayu Light 2.62 · Everforest Light 2.50)에서 3:1 을 못 넘었고 그 테마에서 포커스가 보이지 않았다. FR-TOK-30 의 문장을 **개수에서 선택자로** 고쳤다: 개수 조건만으로는 링을 아무 데나 더해 통과할 수 있다. 게이트는 `scripts/check-focus.mjs` |
| 2026-09-12 | 초안. 사용자 결정 = 파생 토큰(팔레트 불변). §4 D-TOK-4 의 사다리는 **단일 바닥 4.5 가 54종 중 34종에서 계층을 무너뜨린다**는 실측에서 나왔다 |

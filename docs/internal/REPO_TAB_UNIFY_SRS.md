# SRS: Git·Editor 통합 — 저장소 하나에 창 하나 — IEEE 29148

| 항목 | 값 |
|---|---|
| 문서 | REPO_TAB_UNIFY_SRS |
| 상태 | **완료** (2026-09-05, `d79fae0`) — M1~M7 · 묶음 B · 묶음 N. §4 에 미검증 항목이 없다 (수동인 V-RTU-93·98 제외) |
| 선행 | EDITOR_TAB_SRS · GIT_SRS(FR-GIT-25~31·179~186) · GIT_SIDEBAR_TABS_SRS · SLOT_VIEW_STATE_SRS · GIT_DIR_ENTRY_SRS |
| 마무리 커밋 | `d79fae0` — 좁은 사이드에서도 누를 자리가 남는다 |

## 1. 개요 (Introduction)

### 1.1 목적 (Purpose)

접수한 말은 넷이다.

> **① "git, editor 통합 작업이야. 둘을 하나의 탭으로 만들고 탭 내에서는 왼쪽을
> git, 오른쪽을 editor 로 pane 을 나눠줘. add 를 하면 무조건 git, editor 가 같이
> 나오는거고, git 작업소가 없으면 빈 pane 에 git init 버튼을 만들어서 사용자로
> 하여금 git init 을 할 수 있게 만들어줘야해."**
>
> **② "창 좌측에 git, 탐색기를 두고 진짜 git 처럼 editor 창에 diff 를 띄우고
> history 를 띄우고 하는거야. changes 만 좌측 고정이고. 오른쪽에 뜨는 diff,
> history 등은 tab 으로 그냥 editor 와 동일한 취급으로 1개의 탭으로 취급하는거지."**
>
> **③ "git changes 와 파일탐색기도 탭으로 교체가능하도록 하면 오른쪽 영역이 더
> 넓어지고 탐색기와 git changes 를 보는게 더 편해지겠지?"**
>
> **④ "diff 창도 vsc 처럼 되었으면 좋겠어. 새 문서면 그냥 보여주는거. 그리고 diff
> 편집 가능한거."**

넷을 합치면 하나의 그림이 된다.

```
┌──────────────────┬─────────────────────────────────────┐
│ [Explorer][Changes]  [a.ts][b.ts ⇄][History][×]        │
├──────────────────┼─────────────────────────────────────┤
│ ◉ ⏲ ⎇ ≣ › ⧉     │                                     │
│ [커밋 메시지…]    │            monaco                   │
│ [Commit] □ amend │                                     │
│ main ↑2 ↓0       │                                     │
│ ▾ Staged (1)     │                                     │
│    M src/a.ts    │                                     │
│ ▾ Changes (2)    │                                     │
│    M src/b.ts    │                                     │
└──────────────────┴─────────────────────────────────────┘
```

**저장소 하나가 창 하나다.** 좌측은 `Explorer` 와 `Changes` 를 탭으로 갈아 끼우고,
diff·history·branches 는 **오른쪽에 편집기 탭과 똑같은 자격으로** 뜬다.

### 1.2 범위 (Scope)

**포함**

| 묶음 | 내용 |
|---|---|
| **T** 사이드바 | `Git`·`Editor` 두 탭을 `Repo` 하나로. 목록도 하나 |
| **W** 창 | 좌측 사이드(Explorer↔Changes 탭) + 우측 편집기 영역. 폭·상태의 영속 |
| **C** Changes 사이드 | 진입점 아이콘 줄 · 커밋 입력 · 브랜치 줄 · 변경 목록의 순서. `git init` |
| **V** git 뷰 탭 | Diff·History·Branches·Stash·Console·Worktrees 가 우측 탭이 된다 |
| **P** 미리보기 탭 | 한 번 클릭은 재사용, 더블클릭은 고정 (VSCode 규약) |
| **D** diff 편집 | 워킹 트리 쪽 편집·저장. untracked 는 단일 편집기 |
| **S** 패널 수명 | 창마다 `GitPanel` 하나. **관측기도 저장소마다 하나** (D-RTU-16) |
| **M** 마이그레이션 | 기존 `WINDOW_TYPE_GIT` 창의 소멸과 라우팅 이관 |
| **B** 모바일 | 좌측 사이드와 우측 pane 을 한 줄로 순회 |

**미포함:** §6 비목표.

### 1.3 정의 (Definitions)

| 용어 | 정의 |
|---|---|
| **Repo 창** | 경로 하나에 대응하는 창. 코드상 타입은 `editor` 그대로다 (D-RTU-1) |
| **사이드 (side)** | Repo 창 좌측의 고정 영역. 분할 트리 **밖**이며 `Explorer`·`Changes` 두 탭을 갖는다 |
| **본문 (main)** | Repo 창 우측. 기존 pane·탭·분할 트리가 그대로 산다 |
| **git 뷰 탭** | 본문에 뜨는 `type==='git'` 탭. `gitView` 로 어느 뷰인지 갈린다 |
| **미리보기 탭** | 다음 미리보기가 **대체하는** 탭. 고정되기 전까지 하나만 존재한다 |
| **워킹 트리 축** | 오른쪽이 **디스크의 파일**인 축 — unstaged·untracked. index·커밋 축과 다르다 |

### 1.4 참조 (References)

- [`./EDITOR_TAB_SRS.md`](./EDITOR_TAB_SRS.md) — Editor 창·행·재조정·탐색기 전량. 본 SRS 가 그 위에 선다
- [`./GIT_SRS.md`](./GIT_SRS.md) — FR-GIT-25~31(Git 창), 179~186(닫힌 창), 278·279(hunk 스테이징)
- [`./GIT_SIDEBAR_TABS_SRS.md`](./GIT_SIDEBAR_TABS_SRS.md) — 탭 서술자 계약, 탭↔창 연동
- [`./SLOT_VIEW_STATE_SRS.md`](./SLOT_VIEW_STATE_SRS.md) — FR-SVS-30(관측 하나)·40~46(칸마다 패널)
- [`./GIT_DIR_ENTRY_SRS.md`](./GIT_DIR_ENTRY_SRS.md) — 색과 루트 판정. `git init` 뒤 복구(FR-DIR-31)가 본 SRS 의 전제다
- `web/js/ui/renderer.js` · `web/js/core/app-layout.js` · `web/js/core/app-git.js` · `web/js/git/panel-*.js`

---

## 2. 현재 상태 (조사로 확정한 사실)

**파일:줄로만 적는다.**

### 2.1 창 타입이 셋이고 그중 둘이 같은 경로 집합을 본다

| 타입 | 개수 | 좌측 | 우측 |
|---|---|---|---|
| `terminal`(기본) | N | — | pane·탭·분할 |
| `git` | **워크스페이스에 1개** (FR-GIT-26) | — | 고정 탭 7개 |
| `editor` | 루트마다 1개 | 탐색기 | pane·탭·분할 |

그리고 `git.pinned[]` 와 `editors.list[]` 는 **이미 같은 집합이다** — 연동
(FR-EDT-31~34)이 한 번의 저장 안에서 둘을 함께 바꾼다. 실측한 워크스페이스에서도
다섯 경로가 두 목록에 똑같이 들어 있다.

> **따라서 사이드바 목록 둘을 하나로 합치는 데 새 서버 표면이 필요하지 않다.**
> 화면이 둘을 따로 그리고 있을 뿐이다.

### 2.2 git 뷰의 DOM 은 이미 탭과 독립이다

```js
web/js/git/panel-life.js:76-84   elFor(view) { … 뷰별 DOM 을 만들어 캐시하고 돌려준다 }
web/js/ui/renderer.js:425-433    if(at.type===TAB_TYPE_GIT){ … _gitPanel(slot).elFor(at.gitView) … }
```

`_mountTabBody` 는 **탭 타입별 분기**이고 git 분기가 이미 있다. 탭이 어느 창에
있는지는 보지 않는다.

> **그러므로 git 뷰를 Repo 창 본문의 탭으로 올리는 데 렌더러의 새 분기가 필요
> 없다.** 막고 있는 것은 창 게이트 하나뿐이다 (§2.3).

### 2.3 Editor 창은 편집기 탭만 받도록 막혀 있다

```js
web/js/core/app-layout.js:285-292
  if (this._isGitWin(s)) return;                          // Git 창엔 탭 추가 금지
  if (this._isEditorWin(s) && type !== 'editor') return;  // Editor 창엔 editor 만
```

FR-EDT-54 의 자리다. **이 한 줄이 요구 ②를 막는 전부다.**

드래그드롭 게이트도 창 타입을 본다 (`app-dnd.js:16`·`35`·`51`·`54`·`99`·`123`).
탭 타입 자리에는 조건이 없다 (D-25) — 그래서 git 뷰 탭이 Repo 창 **안에서**
움직이는 것은 지금 규약으로도 성립한다.

### 2.4 `GitPanel` 은 칸마다 하나이고 창을 모른다

```js
web/js/core/app-git.js:24-31    _gitPanel(slot){ … key=this._slotKey('git',slot||0) … }
web/js/core/app-git.js:551-552  get gitPanel(){ return this._gitPanel(this._slotFocused()) }
```

키에 창 id 가 없는 이유가 주석에 있다 — "Git 창은 워크스페이스에 하나뿐이므로
(FR-GIT-26) 키에 창 id 를 넣을 이유가 없다".

> **그 전제가 이번에 깨진다.** 창이 경로마다 생기므로 키가 (루트, 칸)이 되어야
> 하고, `app.gitPanel` 이 무엇을 돌려주는지도 다시 정해야 한다 — 지금은 **활성
> 칸**만 보고 활성 **창**은 보지 않는다.

관측은 앱에 하나다 (`_gitObs`, FR-SVS-30).

> **착수 시점에는 "이쪽은 바뀌지 않는다" 고 적었고, 구현이 그것을 뒤집었다**
> (D-RTU-16). `GitObserver` 는 status·signature·소실·실패 누적을 **통째로** 들고
> 있고 주기 타이머가 `any()` 로 아무 패널이나 골라 수집한다 — 관측기가 하나면
> 마지막에 수집한 저장소의 상태를 모든 Repo 창이 함께 본다.

### 2.5 뷰 전환이 Git 창의 탭 바를 딛는다

```js
web/js/git/panel-diff.js:56-62
  openView(view){
    const w=this.app._gitWindow(); if(!w||!w.layout) return;
    …그 창의 pane 에서 gitView 가 맞는 탭을 찾아 switchTab…
  }
```

파일을 클릭해 Diff 를 여는 경로가 여기다. **Git 창이 사라지면 이 함수가 갈 곳을
잃는다.**

### 2.6 diff 는 완전히 읽기 전용이다

```js
web/js/core/constants-git.js:331-332   readOnly:true, originalEditable:false
web/js/core/constants-git.js:358       GIT_DIFF_DRAWABLE=new Set(['text','absent'])
```

새 파일은 서버가 `original.kind='absent'` 를 주고(`query/diff.go:138`), 클라이언트가
그것도 그릴 수 있는 것으로 보아 **빈 왼쪽과 비교하는 화면**을 그린다.

VSCode 의 실동작은 다르다 — untracked 를 고르면 diff 가 아니라 **편집기**가 열리고,
Working Tree diff 의 오른쪽은 **편집·저장된다.** Index diff 의 오른쪽은 읽기
전용이다.

### 2.7 편집기의 저장 경로는 이미 하나다

`FileEditor.save()`(`file-editor.js:454`)가 `Cmd+S`(`:375-378`)에 걸려 있고
`/api/file/write` 로 간다. dirty 표시도 그 안에 있다 (`:520`·`:531`).

> **diff 편집의 저장은 이 경로를 그대로 써야 한다** — 새 쓰기 표면을 만들면 같은
> 파일을 두 길로 쓰게 되고, dirty·충돌 규약이 둘로 갈린다.

### 2.8 미리보기 탭이라는 개념이 없다

`addTab` 은 언제나 새 탭을 만들고, 편집기 탭은 **이미 열려 있으면 그 탭으로
이동한다**(`_findEditorTab`, FR-EDT-101). 즉 중복은 막지만 "훑어보다 버리는 탭" 은
없다. 변경 20개를 훑으면 탭이 20개 남는다.

---

## 3. 요구사항 (Requirements)

### 3.1 묶음 T — 사이드바 `Repo` 탭

**FR-RTU-1.** 사이드바 탭은 **둘**이 된다 — `Windows` · `Repo`. `Git` 과 `Editor`
서술자를 하나로 합친다.

**FR-RTU-2.** `Repo` 목록의 항목은 **경로 하나**다. 표시 이름은 마지막 조각, 툴팁은
절대경로 전체다.

**FR-RTU-3.** 목록의 원천은 `editors.list[]` 다. `git.pinned[]` 는 연동으로 같은
값을 갖지만 **화면이 읽는 자리는 하나**여야 한다 (D-RTU-2).

**FR-RTU-4.** root 행(`~`)은 최하단 고정이며 제거·재정렬 대상이 아니다 (FR-EDT-14·15
그대로).

**FR-RTU-5.** `+ Add` 는 종전대로 경로를 받아 `POST /api/editors/add` 를 부른다.
연동이 Git 핀을 함께 만든다 — **요구 ①의 "add 하면 git·editor 가 같이 나온다" 는
이미 서버가 하는 일이며, 화면이 둘로 보이던 것이 문제였다.**

**FR-RTU-6.** 행에는 변경 개수 배지가 붙는다. 근거는 `/api/git/repos` 의 마지막
관측값이며, 저장소가 아닌 행에는 배지가 없다 (FR-GOB-14 규약 유지).

**FR-RTU-7.** 직행 키는 배열에서 파생된다 — `Ctrl+Shift+2` 가 `Repo` 다.
`sidebarTab3` 은 **사라진다.** 설정 화면의 단축키 목록도 배열에서 파생되므로 손으로
지우지 않는다 (FR-EDT-5 의 규약).

**FR-RTU-8.** 순회 키는 `Repo` 행을 돈다. root 행이 마지막 자리로 포함된다.

**FR-RTU-9.** git 이 없는 환경(`_gitOff`)에서도 `Repo` 탭은 **사라지지 않는다.**
탐색기와 편집기는 git 없이도 성립한다 — 그 경우 Changes 사이드가 사유를 보인다
(FR-RTU-25). 옛 FR-SBT-8(git 없으면 탭 자체를 감춘다)은 여기서 폐기된다.

### 3.2 묶음 W — Repo 창

**FR-RTU-10.** 창 타입 문자열은 **`editor` 그대로**다 (D-RTU-1). 기존
`WINDOW_TYPE_EDITOR` 판정과 워크스페이스 값이 그대로 산다.

**FR-RTU-11.** 창은 **사이드**와 **본문**으로 나뉜다. 사이드는 분할 트리 밖의 고정
영역이며 어떤 드롭으로도 쪼개지지 않는다 (FR-EDT-46 계승).

**FR-RTU-12.** 사이드는 탭 **둘**을 갖는다 — `Explorer` · `Changes`. **한 번에
하나만 보인다.**

> 요구 ③의 근거를 그대로 따른다 — 세로로 쌓으면 파일 목록도 변경 목록도 절반
> 높이가 된다. 탭이면 보이는 쪽이 사이드 전체 높이를 쓴다.

**FR-RTU-13.** 사이드의 활성 탭은 **창마다** 기억되고 워크스페이스에 저장된다
(`window.editor.side`). 폭도 종전대로 저장된다 (`explorerWidth`, FR-EDT-47).

**FR-RTU-14.** 파일을 열면 사이드가 `Explorer` 로 바뀌지 **않는다.** 사용자가 고른
탭은 사용자의 것이다.

**FR-RTU-15.** 본문은 기존 pane·탭·분할 모델을 그대로 쓴다. 분할은 드래그드롭만이며
(FR-EDT-51) 분할 단축키·버튼은 감춰진다 (FR-EDT-50).

**FR-RTU-16.** 본문에는 **편집기 탭과 git 뷰 탭**이 산다. 터미널·run 탭은 여전히
만들 수 없다 — `app-layout.js:288` 의 게이트를 `type!=='editor' && type!==TAB_TYPE_GIT`
로 좁힌다.

**FR-RTU-17.** 탭 이동은 **같은 창 안에서만** 가능하다 (FR-EDT-53 계승). git 뷰
탭도 같은 규칙을 받는다.

**FR-RTU-18.** 창에 pane 이 하나도 없을 수 있다 (FR-EDT-55). 그때 본문은 안내문이며
사이드는 그대로 동작한다.

### 3.3 묶음 C — Changes 사이드

**FR-RTU-20.** `Changes` 사이드의 세로 순서는 **위에서부터** 다음과 같다.

| 순서 | 내용 |
|---|---|
| 1 | **진입점 아이콘 줄** — History · Branches · Stash · Console · Worktrees |
| 2 | **커밋 입력** — 메시지 · `Commit` · `amend` |
| 3 | **브랜치 줄** — 브랜치명 · ahead/behind · 원격 버튼 |
| 4 | **변경 목록** — Staged · Changes · Untracked · Conflicts (스크롤) |

커밋 입력이 브랜치 줄보다 **위**인 것은 사용자 지시다 (D-RTU-4). 1~3은 고정이고
4만 스크롤한다 — 목록이 길어도 커밋 입력이 자리를 떠나지 않는다.

**네 줄이 전부다** (D-RTU-22). 종전 Changes 는 `목록 | 손잡이 | 인라인 diff 미리보기`
로 가로 두 칸이었고, 그 손잡이가 EDITOR_GIT_UX_SRS 묶음 D(FR-CSZ-1~8)였다. 사이드는
260px 이고 그 안을 둘로 나누면 목록이 ~90px 이 되어 **행 이름이 0 으로 눌려 사라지고
호버 동작 버튼이 행 가운데를 덮는다** — 선택하려는 클릭이 `stage` 를 실행했다(실측).
diff 를 본문 탭으로 옮긴 것이 이번 통합의 요구 ②이므로 같은 것을 두 자리에 둘 이유도
없다. 그 삭제로 묶음 D 는 폐기되고, 미리보기가 들고 있던 **축 라벨**(`worktree ↔
index`)은 Diff 탭의 바로 옮겼다.

**FR-RTU-21.** 아이콘 줄의 다섯 진입점은 각각 **본문에 그 뷰의 탭을 연다.** 이미
열려 있으면 그 탭으로 이동한다 (FR-RTU-31).

**FR-RTU-22.** 아이콘 줄은 `Explorer` 탭에서는 보이지 않는다 — 그쪽은 git 의 자리가
아니다.

**FR-RTU-23.** 변경 목록의 행 동작(선택·스테이지·discard·컨텍스트 메뉴)은 **한 줄도
바뀌지 않는다.** 바뀌는 것은 그 목록이 놓인 자리뿐이다.

**FR-RTU-24. 루트가 저장소 안이면 그 저장소를 보인다** (GIT_DIR_ENTRY_SRS FR-DIR-40
과 같은 기준). 사이드 머리에 그 저장소의 이름을 적어, 지금 보는 것이 상위의
것임을 알린다.

**FR-RTU-25. 저장소가 없으면 `Changes` 사이드가 그 사실과 `git init` 을 보인다.**

```
┌──────────────────┐
│ [Explorer][Changes]
├──────────────────┤
│                  │
│  이 폴더는 git   │
│  저장소가 아닙니다│
│                  │
│  [git init]      │
│  ~/notes         │
└──────────────────┘
```

**FR-RTU-26.** `git init` 은 **확인을 거친다.** 확인창은 대상 절대경로를 밝힌다 —
홈처럼 넓은 경로에서 실수로 누르면 되돌리기가 사용자의 몫이 된다.

**FR-RTU-27.** `git init` 이 성공하면 그 경로가 **Git 핀에도 더해진다.** 이미 Repo
목록에 있으므로 목록은 변하지 않는다.

**FR-RTU-28.** init 직후 색과 목록이 곧바로 선다. 서버는 `RepoRoot` 실패를 2초
캐시하므로(`store.go:198-215`) init 을 수행한 종단이 **그 캐시를 무효화한다** —
그러지 않으면 사용자가 누른 뒤 2초 동안 아무 일도 일어나지 않은 것처럼 보인다.

**FR-RTU-29.** `git init` 종단을 신설한다.

```
POST /api/git/init  {"path":"<abs>"}  → {"repo":"<abs>"}
```

절대경로만 받고, 이미 저장소면 `exists`(409), 디렉터리가 아니면 `bad_request`(400)다.
초기 브랜치 이름은 **git 의 기본값을 그대로 쓴다** — 우리가 정하면 사용자의
`init.defaultBranch` 설정과 어긋난다.

### 3.4 묶음 V — git 뷰 탭

**FR-RTU-30.** `Diff` · `History` · `Branches` · `Stash` · `Console` · `Worktrees`
여섯은 **본문의 탭**이 된다. 탭 타입은 기존 `TAB_TYPE_GIT` 이고 `gitView` 가 어느
뷰인지 말한다.

**FR-RTU-31.** `Changes` 를 뺀 다섯은 창에 **하나씩만** 열린다. 이미 있으면 그
탭으로 이동한다.

**FR-RTU-32.** `Changes` 는 본문 탭이 **되지 않는다.** 사이드에만 있다 (요구 ②).

**FR-RTU-33.** git 뷰 탭은 편집기 탭과 **같은 자격**이다 — 이름·닫기·드래그·분할이
모두 같다. 탭 이름은 뷰 이름이며, Diff 만 대상 파일명을 쓴다 (FR-RTU-41).

**FR-RTU-34.** git 뷰 탭을 닫는 것은 자유다. 다시 열면 새로 만들어지고, 그 뷰의
상태(스크롤·선택)는 패널이 들고 있으므로 보존된다.

**FR-RTU-35.** 창을 닫거나 행을 지우면 그 창의 git 뷰 탭도 함께 사라진다 — 창의
수명이 곧 탭의 수명이다.

### 3.5 묶음 P — 미리보기 탭

**FR-RTU-40. 변경 목록의 한 번 클릭이 본문을 연다** — 추적되는 파일은 **Diff 탭**,
untracked 는 **편집기 미리보기 탭**이다 (FR-RTU-51 / D-RTU-8).

> **개정 (사용자 지시 2026-09-04, D-RTU-23).** 초판은 "한 번 클릭은 미리보기 탭을
> 재사용한다" 였고 구현은 그것을 **사이드의 인라인 미리보기**로 읽었다 — 한 번
> 클릭은 고르기만 하고 더블클릭이 본문을 열었다. 그 인라인 칸을 걷어내면서
> (FR-RTU-20 / D-RTU-22) 클릭의 결과를 보일 자리가 본문뿐이 되었고, 사용자는
> "한 번 클릭을 diff, 두 번 클릭은 제거 — VSCode 와 동치" 로 확정했다.

Diff 탭은 창에 하나이고(FR-RTU-31) 내용이 선택을 따르므로 탭이 쌓이지 않는다 —
§2.8 이 걱정한 "변경 20개를 훑으면 탭 20개" 가 그것으로 해소된다. 그래서 Diff 탭에
`preview` 를 붙이지 않는다: 미리보기 탭이 되는 것은 **편집기 탭**뿐이다.

**보조키 클릭은 열지 않는다.** `Cmd`·`Ctrl`·`Shift` 는 여러 개를 고르는 손짓이고
(FR-GIT-69), 고를 때마다 본문이 갈리면 무엇을 고르는 중인지 알 수 없다.

**FR-RTU-41.** 미리보기 탭의 이름은 대상 파일명이며 **기울임**으로 그린다 — 이 탭이
곧 대체될 것임을 보이는 표시다 (VSCode 규약).

**FR-RTU-42. 고정되는 계기는 넷이다** — ① 탭 더블클릭 ② 그 탭에서 편집을 시작
③ 탭 이름 더블클릭 ④ **탐색기 행의 더블클릭**. 고정되면 기울임이 풀리고 다음
미리보기는 **새 탭**을 만든다.

> **④에서 변경 목록이 빠졌다** (D-RTU-23). 그쪽의 더블클릭 계기 자체가 사라졌으므로
> (한 번 클릭이 연다, FR-RTU-40) 고정할 손짓이 없다. 탐색기 행의 ④는 남는다 —
> VSCode 가 그렇고, 탐색기는 한 번 클릭이 **미리보기 탭**을 여는 자리라 "이것을
> 남긴다" 를 말할 손짓이 필요하다.

**FR-RTU-43.** 미리보기 탭은 창에 **하나뿐**이다. 그 자리를 갖는 것은 편집기 탭이며
(탐색기 클릭 · untracked 클릭) 다음 미리보기가 그 탭의 대상을 갈아 끼운다.

**FR-RTU-44.** 미리보기 상태는 워크스페이스에 저장한다 (`tab.preview`). 저장하지
않으면 새로고침 뒤 모든 탭이 고정으로 되살아나 사용자가 정리해야 한다.

**FR-RTU-45.** 이미 **고정된** 탭이 있는 대상을 다시 고르면 미리보기를 만들지 않고
그 탭으로 이동한다 (FR-EDT-101 의 규약을 미리보기까지 넓힌 것이다).

### 3.6 묶음 D — diff 편집 (VSCode 규약)

**FR-RTU-50. 축마다 다르다.** 판정 기준은 **오른쪽이 디스크의 파일인가** 하나다.

| 고른 것 | 여는 것 | 오른쪽 | 편집 |
|---|---|---|---|
| Changes (unstaged) | diff | 워킹 트리 파일 | **가능** |
| Conflicts | diff | 워킹 트리 파일 | **가능** |
| Staged | diff | index | 읽기 전용 |
| Untracked (새 파일) | **단일 편집기** | 워킹 트리 파일 | **가능** |
| 삭제된 파일 | diff | 없음(`absent`) | 읽기 전용 |
| History 의 커밋 | diff | 커밋 스냅샷 | 읽기 전용 |

**FR-RTU-51.** untracked 는 diff 가 아니라 **편집기 탭**으로 연다. 비교할 왼쪽이
없으므로 diff 는 빈 쪽과의 비교가 되고, 그 화면은 자리만 절반 쓰면서 알려 주는
것이 없다.

**FR-RTU-52.** 편집 가능한 diff 의 저장은 **`FileEditor` 와 같은 경로**를 쓴다
(`/api/file/write`, §2.7). 새 쓰기 표면을 만들지 않는다.

**FR-RTU-53.** `Cmd+S`(`Ctrl+S`)가 저장하고, 저장되지 않은 변경은 탭 이름에 `●`
로 표시된다 — 편집기 탭과 같은 표시다.

**FR-RTU-54.** 읽기 전용 쪽을 고치려 하면 **그 사실을 알린다.** 조용히 무시하면
"타이핑이 먹지 않는다" 가 된다. 문구는 그 축이 왜 읽기 전용인지 말한다 (index 는
파일이 아니다 / 커밋은 지나간 것이다).

**FR-RTU-55.** 저장 뒤에는 git 관측을 즉시 갱신한다 (`_gitSignal('write')`). 방금
고친 것이 목록과 색에 곧바로 선다.

**FR-RTU-56.** 편집 중인 diff 의 대상이 폴링으로 바뀌어도 **사용자의 편집을 덮지
않는다.** dirty 인 동안 그 뷰는 다시 읽지 않는다 (기존 `isStale` 규약 위에 dirty
게이트를 더한다).

**FR-RTU-57.** hunk 단위 스테이징(FR-GIT-278·279)은 그대로 둔다. index 를 고치는
일은 여전히 그쪽의 몫이다 (D-RTU-7).

### 3.7 묶음 S — 패널 수명과 관측

**FR-RTU-60. `GitPanel` 은 (루트, 칸)마다 하나다.** 키에 창의 루트가 들어간다.

**FR-RTU-61.** 패널은 **창마다 유지한다** (사용자 지시, D-RTU-6). 창을 떠나도
파괴하지 않으므로 돌아왔을 때 스크롤·선택·diff 대상이 그대로다.

**FR-RTU-62.** 대신 **관측은 활성 창만** 한다 (FR-GIT-24·FR-EDT-76 계승). 비활성
창의 패널은 살아 있어도 git 을 부르지 않는다.

**FR-RTU-63.** 목록에서 사라진 루트의 패널은 재조정이 회수한다 (FR-EDT-42 가 창을
지우는 그 자리다).

**FR-RTU-64. 관측기(`GitObserver`)는 저장소마다 하나다** — FR-SVS-30 을 개정한다.

그 규칙이 "앱에 하나" 였던 근거는 Git 창이 워크스페이스에 하나여서 **활성 저장소도
하나**였다는 것이다 (FR-GIT-26·29). 관측기가 관측 상태를 통째로 들고 있으므로,
저장소마다 창이 서는 지금 그것을 하나로 두면 서로의 상태를 덮어쓴다 (D-RTU-16).

요청 수는 늘지 않는다 — 관측이 도는 조건은 그 표면이 화면에 있을 때이고
(FR-RTU-62), 화면에 있는 저장소만큼만 폴링이 돈다.

**FR-RTU-65.** `app.gitPanel` 은 **활성 창의 루트 + 포커스 칸**의 패널을 돌려준다.
활성 창이 Repo 창이 아니면 `null` 이며, 그것을 딛는 호출자는 없을 때를 견딘다.

### 3.8 묶음 M — 마이그레이션

**FR-RTU-70. `WINDOW_TYPE_GIT` 창은 사라진다.** 워크스페이스를 읽을 때 제거하며,
확인창은 띄우지 않는다 (D-9 와 같은 근거 — 로드 시점이고 잃는 데이터가 없다).

**FR-RTU-71.** 제거는 창을 순회하는 자리에서 한다 (`app.js` 로드 · `app-cmd.js`
SSE 동기화) — `_migrateGitWindow` 가 이미 그 자리에 있다.

**FR-RTU-72.** `openGitWindow(repo)` 는 **그 경로의 Repo 창으로 전환**한다. 그
경로가 목록에 없으면 먼저 더한다 (`/api/editors/add`). 이 함수를 부르는 자리가
사이드바 행·`dmctl`·e2e 셋이므로 이름과 계약을 유지한다.

**FR-RTU-73.** `openView(view)` 는 **활성 Repo 창의 본문**에서 탭을 찾거나 만든다.
`_gitWindow()` 의존을 걷어낸다 (§2.5).

**FR-RTU-74.** `_gitBackTarget`·`_gitCloseWindow` 는 폐기한다 — 돌아갈 자리를
계산하던 이유가 Git 창이라는 특수 창이었고, 그것이 사라진다.

**FR-RTU-75.** 옛 워크스페이스의 Git 창 안에 있던 탭은 **버린다.** 그 탭들은 전부
고정 뷰 탭이라 재현 가능하며, 옮길 자리도 이미 없다.

### 3.9 묶음 B — 모바일

**FR-RTU-80.** 모바일에서 Repo 창은 **사이드와 pane 들을 한 줄로 순회**한다.
기존 pane 순회(`‹ 1/3 ›`)의 첫 자리에 사이드가 들어간다.

**한 번에 하나만 그린다.** 사이드 자리에서는 본문(`.ed-area`)을 만들지 않고, 본문
자리에서는 사이드를 만들지 않는다 — 종전에는 사이드를 `max-width:40%` 로 양보시켜
둘을 나란히 두었고, 390px 에서 **둘 다 쓸 수 없었다**: 커밋 버튼·원격 버튼·일괄
버튼이 경계를 넘었다 (실측, V10).

**포커스가 옮겨 가면 그 자리로 따라간다.** 사이드는 분할 트리 밖이라 포커스의
대상이 아니므로 렌더는 사이드에 서 있는 동안 포커스를 따라가지 않는다 — 그러지
않으면 사이드에 설 수 없다. 그런데 파일이나 뷰 탭을 여는 일은 포커스를 옮기는
일이고, 그때는 그 자리를 보여야 한다 (열었는데 보이지 않으면 실패로 읽힌다,
FR-EDT-102). 그 구분은 `_setFocus` 가 **포커스가 실제로 바뀐 부름**만 사이드를
떠나게 하는 것으로 낸다.

**FR-RTU-81.** 사이드 자리에서는 `Explorer`·`Changes` 탭이 그대로 동작한다.

**FR-RTU-82.** 순회 표시는 사이드를 포함해 센다 — pane 이 둘이면 `1/3` 이다.

### 3.10 묶음 N — 좁은 폭의 손짓

사이드의 폭은 사용자가 정하며 **기본 220px · 하한 100px** 이다
(`EDITOR_EXPLORER_W_DEFAULT`·`_MIN`). 그 폭 안에서 한 줄에 "줄 자체를 누르는 뜻"과
"버튼을 누르는 뜻"이 함께 살면, 폭이 줄 때 **버튼이 줄을 다 먹는다** — 글자만
`min-width:0` 이고 버튼은 `flex-shrink:0` 이기 때문이다. 그 결과 누르려던 자리에
없던 버튼이 서고, 사용자가 뜻하지 않은 일이 실행된다.

같은 것을 두 번 실측했다. C4b 에서는 인라인 미리보기가 목록을 ~90px 로 눌러
**선택하려는 클릭이 `stage` 를 실행했고**(D-RTU-22 에서 그 칸을 걷어 해소됐다),
`.git-job-bar` 에서는 **로그를 펴려는 클릭이 `copy`·`close` 에 떨어진다.** 앞의
것은 원인(두 칸으로 나눈 것)을 걷어 220px 에서 증상이 사라졌을 뿐이고, **구조는
그대로라 하한 100px 에서 되살아난다.** 그래서 자리마다 고치지 않고 규칙으로 세운다.

**FR-RTU-100. `.git-job-bar` 의 접기·펴기는 전용 토글이 갖는다.** 바 맨 앞에 접기
토글이 서고 그것이 로그를 접고 편다. 토글은 히트 영역 하한(FR-GIT-195~198, 30px)을
지키며 **폭에 관계없이 자리와 크기가 고정**이다. 바의 나머지 자리를 누르는 것도
같은 일을 한다 — 넓을 때의 손짓을 잃지 않는다.

GIT_UI_REVISION_SRS FR-GIT-221 의 "접힌 로그는 **바를 눌러** 다시 편다" 를 개정한다.
그 문구가 쓰였을 때 바는 Git 창의 전체 폭(수백 px)에 있었고, 지금은 사이드에 있다.
220px 에서 바의 내용은 `kind(~40) · argv · state(한글 ~78) · spacer · cancel(~45) ·
copy(`Copy Output` ~72) · close(~42)` 이고 gap 8px 이 여섯이라, **argv 만 `min-width:0`
이므로 argv 가 0 으로 눌린다.** 즉 바에 버튼 아닌 자리가 남지 않는다.

**FR-RTU-101. 줄의 글자 자리는 60px 아래로 눌리지 않고, 그것을 지키느라 동작
버튼을 감추지 않는다.** 한 줄에 다 들어가지 않으면 **줄을 늘린다.**

| 줄 | 글자 | 버튼 |
|---|---|---|
| `.git-job-bar` | `.git-job-argv` | `cancel` · `copy` · `close` |
| `.git-file` · `.git-dir` | `.git-file-path` | `.git-file-acts` (그룹마다 2~4개) |

감추는 쪽을 택하지 않는 이유는 이미 적혀 있다 — 행 인라인 동작은 **항상 보인다**,
hover 로만 드러나면 있는 줄 모르고 터치에는 hover 가 없다(`style-git.css:511`).
좁은 폭이라고 그 근거가 달라지지 않는다.

`conflicts` 그룹의 행은 버튼이 넷(`↗`·`Ours`·`Theirs`·`+`)이라 **기본 220px 에서도**
글자 60px 을 남기지 못한다 — 그 폭에서 이미 두 줄이 된다. 그것이 지금의 "이름이
0 으로 눌린 한 줄" 보다 나은 상태다.

**FR-RTU-102.** 위 둘은 모바일에서도 같다. 모바일 사이드는 폭을 전부 쓰지만
(FR-RTU-80) 390px 은 `conflicts` 행이 한 줄에 드는 폭이 아니다.

### 3.11 비기능 요구 (NFR)

**NFR-RTU-1.** git 실행 횟수가 저장소 수에 비례해 늘지 않는다. 활성 창만 관측하므로
(FR-RTU-62) 창이 10개여도 폴링은 한 벌이다.

**NFR-RTU-2.** 창 전환이 250ms 안에 화면에 닿는다. 패널을 유지하므로(FR-RTU-61)
재구성이 없어야 한다.

**재는 것은 시간이 아니라 그 근거다** (D-RTU-35). 창을 오가고 돌아왔을 때 그
저장소의 패널과 뷰 DOM 이 **같은 요소인가**를 표식으로 잰다 — 250ms 는 이 문서가
말하는 의도로 남고, 밀리초는 기계가 정하므로 시험의 계약이 되지 않는다. 체감
속도는 사람이 본다 (GIT_MANUAL_CHECKLIST G2.12).

**NFR-RTU-3.** Monaco 인스턴스 수는 **열린 diff/편집기 탭 수 + 창당 미리보기 1**을
넘지 않는다. 창마다 패널을 유지하되 **뷰 DOM 은 탭이 있을 때만 만든다**.

**NFR-RTU-4.** 사이드 폭 최소값에서 `Explorer`·`Changes` 두 탭 이름이 잘리지 않는다.

**NFR-RTU-5.** 다시 그리기 보존 규약(FR-RPT-*)을 깨지 않는다 — 사이드 탭 전환이
목록의 DOM 을 파괴하면 hover·드래그가 죽는다.

**NFR-RTU-6. 줄 안에서 "그 줄 자체를 누르는 뜻"은 폭이 줄어도 닿을 수 있어야 한다.**
둘 중 하나로 지킨다.

1. 그 뜻을 갖는 **전용 컨트롤**을 두고, 그것이 폭과 무관하게 하한(30px)을 지킨다
   — `.git-job-bar` 의 접기 토글 (FR-RTU-100).
2. 줄 본문(글자)의 최소 폭을 **60px** 로 두고, 동작 버튼이 그 자리를 침범하면
   버튼을 감추지 않고 **줄을 늘린다** (FR-RTU-101).

측정은 두 폭에서 한다 — **기본 220px 과 하한 100px**. 하한에서 무너지는 구조는
사용자가 손잡이를 끄는 순간 무너지는 구조다.

---

## 4. 검증 (Verification)

| id | 종류 | 항목 | FR |
|---|---|---|---|
| V-RTU-1 | T | 사이드바 탭이 `Windows`·`Repo` 둘이다 | 1 |
| V-RTU-2 | T | 목록이 `editors.list` 하나에서 나오고 핀과 어긋나지 않는다 | 3 |
| V-RTU-3 | T | `+ Add` 한 번에 목록·핀·창이 함께 선다 | 5 |
| V-RTU-4 | T | `Ctrl+Shift+2` 가 Repo 탭이고 `sidebarTab3` 이 없다 | 7 |
| V-RTU-5 | T | git 이 없어도 Repo 탭이 보인다 | 9 |
| V-RTU-6 | T | 행에 변경 개수 배지가 서고, 저장소가 아니면 없다 | 6 |
| V-RTU-10 | T | 창이 사이드+본문으로 그려지고 사이드는 드롭으로 쪼개지지 않는다 | 11 |
| V-RTU-11 | T | 사이드 탭 전환이 되고 한 번에 하나만 보인다 | 12 |
| V-RTU-12 | T | 사이드 활성 탭이 창마다 저장되고 새로고침을 넘는다 | 13 |
| V-RTU-13 | T | 파일을 열어도 사이드 탭이 바뀌지 않는다 | 14 |
| V-RTU-14 | T | 본문에 git 뷰 탭을 만들 수 있고 터미널 탭은 여전히 안 된다 | 16 |
| V-RTU-15 | T | git 뷰 탭이 다른 창으로 끌리지 않는다 | 17 |
| V-RTU-20 | T | Changes 사이드의 세로 순서가 아이콘 줄·커밋·브랜치·목록이다 | 20 |
| V-RTU-21 | T | 목록을 스크롤해도 커밋 입력이 자리를 지킨다 | 20 |
| V-RTU-22 | T | 아이콘 다섯이 각 뷰 탭을 열고, 두 번 눌러도 탭이 하나다 | 21·31 |
| V-RTU-23 | T | Explorer 탭에서는 아이콘 줄이 보이지 않는다 | 22 |
| V-RTU-24 | T | 저장소가 아닌 루트에 `git init` 버튼이 나온다 | 25 |
| V-RTU-25 | T | `git init` 이 확인창을 거치고 경로를 밝힌다 | 26 |
| V-RTU-26 | T | init 뒤 핀이 생기고 **곧바로** 색과 목록이 선다 (캐시 무효화) | 27·28 |
| V-RTU-27 | T | 이미 저장소면 409, 상대경로면 400 | 29 |
| V-RTU-28 | T | 루트가 저장소 안이면 상위 저장소를 보이고 그 이름을 적는다 | 24 |
| V-RTU-30 | T | Changes 는 본문 탭이 되지 않고 사이드는 목록 하나다 (인라인 미리보기 없음) | 20·32 |
| V-RTU-31 | T | git 뷰 탭이 편집기 탭과 같은 자격이다 (닫기·드래그·분할) — `repo-tab` X1·X2·X3 | 33 |
| V-RTU-40 | T | 변경 목록 한 번 클릭이 본문을 연다 (tracked→Diff 탭 · untracked→편집기 미리보기) | 40 |
| V-RTU-41 | T | 미리보기 탭 이름이 기울임이다 | 41 |
| V-RTU-42 | T | 탐색기 행·탭의 더블클릭과 편집이 탭을 고정하고 다음 미리보기가 새 탭이 된다 | 42 |
| V-RTU-43 | T | 미리보기 탭은 창에 하나뿐이다 | 43 |
| V-RTU-44 | T | 미리보기 상태가 새로고침을 넘는다 | 44 |
| V-RTU-45 | T | 고정 탭이 있는 대상은 미리보기를 만들지 않는다 — `repo-tab` X5 | 45 |
| V-RTU-50 | T | unstaged diff 의 오른쪽을 고쳐 저장하면 파일이 바뀐다 | 50·52 |
| V-RTU-51 | T | staged diff 는 읽기 전용이고 사유를 알린다 | 50·54 |
| V-RTU-52 | T | untracked 는 diff 가 아니라 편집기로 열린다 | 51 |
| V-RTU-53 | T | 삭제된 파일은 오른쪽이 빈 diff 다 | 50 |
| V-RTU-54 | T | 저장이 `/api/file/write` 를 지나고 dirty 표시가 탭에 선다 | 52·53 |
| V-RTU-55 | T | 저장 뒤 목록·색이 즉시 갱신된다 | 55 |
| V-RTU-56 | T | dirty 인 동안 폴링이 편집 내용을 덮지 않는다 | 56 |
| V-RTU-60 | T | 창마다 패널이 서고 창을 떠났다 와도 상태가 남는다 | 60·61 |
| V-RTU-61 | T | 비활성 창은 git 을 부르지 않는다 (요청 수, 3주기) | 62 |
| V-RTU-62 | T | 행을 지우면 그 패널이 회수된다 (인스턴스 수) | 63 |
| V-RTU-70 | T | 옛 워크스페이스의 Git 창이 로드에 사라진다 | 70 |
| V-RTU-71 | T | `openGitWindow(repo)` 가 그 경로의 Repo 창으로 간다 | 72 |
| V-RTU-72 | T | 목록에 없는 경로로 부르면 먼저 더해진다 | 72 |
| V-RTU-73 | T | Changes 에서 파일 클릭이 본문 Diff 탭을 연다 | 73 |
| V-RTU-80 | T | 모바일 순회에 사이드가 첫 자리로 들어가고 계수가 맞는다 — `repo-tab` M1·M2 | 80·82 |
| V-RTU-90 | T | 보이지 않는 저장소는 폴링되지 않는다 — `repo-tab` X6 | NFR-1 |
| V-RTU-91 | T | Diff 탭을 닫으면 Monaco 인스턴스도 놓는다 — `repo-tab` X4 | NFR-3 |
| V-RTU-92 | T | 사이드 탭 전환이 목록 DOM 을 파괴하지 않는다 — `git-repaint` P12 | NFR-5 |
| V-RTU-93 | M | 사이드 최소 폭에서 두 탭 이름이 읽힌다 | NFR-4 |
| V-RTU-94 | T | 220px·100px 에서 접기 토글이 로그를 펴고 접는다 — 그 자리에 다른 버튼이 서지 않는다 | 100 / NFR-6 |
| V-RTU-95 | T | 220px·100px 에서 `.git-job-argv`·`.git-file-path` 가 60px 이상이다 | 101 / NFR-6 |
| V-RTU-96 | T | 좁아져도 행의 동작 버튼이 전부 닿는다 (`conflicts` 행 넷 포함) | 101 |
| V-RTU-97 | T | 창을 오가도 그 저장소의 패널을 다시 만들지 않는다 — `git-repaint` P13 | NFR-2 |
| V-RTU-98 | M | 창 전환이 멈추는 느낌 없이 즉시다 (GIT_MANUAL_CHECKLIST G2.12) | NFR-2 |

---

## 5. 구현 계획

각 단계는 **그 자체로 동작하는 상태**로 끝난다.

| M | 이름 | 내용 | FR |
|---|---|---|---|
| **M1** | 패널 재배치 | `GitPanel` 키에 루트 · `app.gitPanel` 재정의 · 관측 게이트 · 회수 | 60~65 |
| **M2** | 창 골격 | 사이드(Explorer/Changes 탭) · 본문 게이트 완화 · `openView` 이관 | 10~18, 30~35, 73 |
| **M3** | 사이드바 통합 | `Repo` 서술자 하나 · 단축키 파생 · 배지 | 1~9 |
| **M4** | Changes 사이드 | 세로 순서 · 아이콘 줄 · `git init` 종단과 확인창 · 캐시 무효화 | 20~29 |
| **M5** | 미리보기 탭 | `tab.preview` · 재사용·고정 규칙 · 영속 | 40~45 |
| **M6** | diff 편집 | 축별 편집 가능 · untracked 라우팅 · 저장·dirty·폴링 게이트 | 50~57 |
| **M7** | 마이그레이션·모바일 | Git 창 제거 · `openGitWindow` 이관 · 순회 | 70~75, 80~82 |

**M1 이 먼저인 이유.** 패널이 창을 모르는 동안에는(§2.4) 창을 여럿 띄우는 순간
두 창이 같은 패널을 다투게 되어 M2 를 검증할 수 없다.

**M7 이 마지막인 이유.** Git 창을 먼저 지우면 그때부터 M2~M6 를 손으로 확인할
길이 사라진다 — 옛 창을 남겨 둔 채 새 자리를 세우고, 다 선 뒤에 걷는다.

### 5.1 파일 단위 영향 범위

| 파일 | 변경 | M |
|---|---|---|
| `web/js/core/app-git.js` | 패널 키·`gitPanel` getter·회수·`openGitWindow`·마이그레이션 | M1·M7 |
| `web/js/git/panel-diff.js` | `openView` 의 창 의존 제거 · 미리보기 라우팅 · 편집 가능 판정 | M2·M5·M6 |
| `web/js/git/panel-life.js` | 뷰 DOM 을 탭이 있을 때만 (NFR-3) | M2 |
| `web/js/git/diff-view.js` | `readOnly` 축별 · 저장 · dirty | M6 |
| `web/js/ui/renderer.js` | 사이드 골격 · 탭 렌더(기울임) | M2·M5 |
| **신규** `web/js/ui/repo-side.js` | 사이드의 탭 전환과 두 패널의 마운트 | M2 |
| `web/js/ui/sidebar-tabs.js` | 서술자 둘 → 하나 | M3 |
| `web/js/core/app-layout.js` | 탭 게이트(`:288`) · `addTab` 의 미리보기 | M2·M5 |
| `web/js/core/app-editor.js` | 사이드 상태 · 파일 열기 라우팅 | M2·M5 |
| `web/js/core/constants*.js` | 사이드 탭 · 아이콘 · 문구 · init 확인 | 전 단계 |
| **신규** `internal/webserver/gitapi/handlers_git_init.go` | `POST /api/git/init` + 캐시 무효화 | M4 |
| `internal/webserver/domain/git/store/store.go` | 루트 캐시 무효화 진입점 | M4 |
| **신규** `e2e/repo-tab.spec.ts` · `e2e/repo-diff-edit.spec.ts` | V-RTU-* | 전 단계 |

### 5.2 구현 현황 (2026-09-04)

| M | 상태 | 비고 |
|---|---|---|
| **M1** 패널 재배치 | ✅ | 키가 (루트, 칸). **관측기도 저장소마다 하나로 갈랐다** — §7 D-RTU-16. 관측 조건은 "그 표면이 화면에" 로 좁혔다 — D-RTU-25 |
| **M2** 창 골격 | ✅ | 사이드(Explorer/Changes)·본문 git 뷰 탭·`openView` 이관. **git 뷰 탭의 닫기·드래그·분할을 뒤늦게 구현했다** — D-RTU-24 |
| **M3** 사이드바 통합 | ✅ | `Repo` 한 탭. `sidebarTab3` 파생 제거. 배지 관측 조건의 옛 탭 id 와 행의 소실 사유를 고쳤다 — D-RTU-26 |
| **M4** Changes 사이드 | ✅ | 세로 순서·아이콘 줄·`git init` 종단과 캐시 무효화. **인라인 diff 미리보기와 손잡이를 걷었다** — D-RTU-22 |
| **M5** 미리보기 탭 | ✅ | 한 번 클릭이 본문을 연다. 더블클릭 계기 없음 — D-RTU-23 |
| **M6** diff 편집 | ✅ | 축별 편집·저장·dirty·폴링 게이트·untracked 라우팅. 축 라벨을 Diff 바로 옮겼다 |
| **M7** 마이그레이션 | ✅ | **M3 직후로 앞당겼다** — §7 D-RTU-17. 헤더 리포 드롭다운도 창 전환으로 이관 — D-RTU-27 |
| **묶음 B** 모바일 | ✅ | 사이드가 순회의 첫 자리다. 종전 `max-width:40%` 를 걷었다 — 390px 에서 둘 다 쓸 수 없었다 |
| **묶음 N** 좁은 폭의 손짓 | ✅ | 인계 §5 의 관찰을 규칙으로 세웠다 — D-RTU-33·34. `.git-job-bar` 의 접기 토글과 줄의 글자 폭 하한. `repo-tab` N1·N2 · `git-remote` R20b |

**§3.1 의 미해결은 해소됐다.** "untracked 행에서 연 탭에 `preview` 가 붙지 않는다"
는 관측은 **결함이 아니었다** — 그 경로는 더블클릭 핸들러였고, 사용자 확정으로
더블클릭 계기 자체가 사라졌다 (D-RTU-23). 지금은 한 번 클릭이 편집기 **미리보기**
탭을 열고 그 단언이 선다.

**e2e 는 통합의 여파를 뒤늦게 따라갔다.** 앞 세션이 `.git-view.git-changes` 만
치환했고, `.pn-body` 안의 Changes 요소(45곳)·사이드바 행 클래스(`.git-repo` →
`.ed-entry`)·옛 `Git` 창을 전제한 시험들이 남아 있었다. 그 정리에서 드러난 **구현
결함 다섯**을 함께 고쳤다 — D-RTU-25(폴링 게이트 둘) · D-RTU-26(배지 관측의 옛 탭
id, 행의 소실 사유) · D-RTU-27(드롭다운) · D-RTU-24(뷰 탭의 닫기·드래그).

**검증을 채웠다.** `repo-tab.spec.ts` 에 여섯을 더했다 — X1·X2·X3(V-RTU-31:
닫기·드래그·창 경계) · X4(V-RTU-91 / NFR-RTU-3: 탭을 닫으면 Monaco 도 놓는다) ·
X5(V-RTU-45) · X6(V-RTU-90 / NFR-RTU-1) · M1·M2(V-RTU-80·82).

**미검증으로 남아 있던 둘을 채웠다** (2026-09-04). V-RTU-92 는 `git-repaint`
P12 가 표식 기법으로 잰다. NFR-RTU-2 는 **시간 대신 그 근거를 잰다** — 창을 오가도
패널이 같은 요소인가를 `git-repaint` P13(V-RTU-97)이 재고, 체감 속도는
GIT_MANUAL_CHECKLIST G2.12 로 옮겼다 (D-RTU-35).

---

## 6. 비목표 (Non-goals)

1. **Git 뷰 내부의 개선.** History·Branches·Stash·Console·Worktrees 의 동작은 한
   줄도 바뀌지 않는다. 바뀌는 것은 어디에 뜨느냐뿐이다.
2. **index 편집.** 스테이지된 내용을 고치는 일은 hunk 단위 스테이징의 몫이다
   (D-RTU-7).
3. **탐색기의 새 기능.** EDITOR_TAB_SRS 의 다섯 조작 그대로다.
4. **사이드를 좌우로 옮기는 설정.** 좌측 고정이다.
5. **여러 저장소를 한 창에서 보기.** 창 하나에 저장소 하나다.
6. **미리보기 탭의 설정화.** VSCode 에는 끄는 설정이 있으나 여기서는 두지 않는다 —
   설정이 늘면 두 동작을 다 검증해야 한다.
7. **diff 에서의 되돌리기(revert hunk).** 지금의 discard 와 hunk 스테이징이 그
   자리를 이미 갖는다.

---

## 7. 확정 결정 (Decisions)

인터뷰(2026-09-04)로 확정했다. **본문과 어긋나면 이 표가 이긴다.**

| id | 결정 | 근거 |
|---|---|---|
| **D-RTU-1** | 창 타입 문자열은 `editor` 를 유지하고 개념만 넓힌다 | 워크스페이스 값과 e2e 가 그 문자열 위에 서 있다. 이름을 바꾸면 얻는 것 없이 마이그레이션만 는다. 사용자에게 보이는 이름은 `Repo` 다 |
| **D-RTU-2** | 사이드바 목록의 원천은 `editors.list` 하나 | 두 목록은 서버 연동으로 이미 같은 집합이다(§2.1). 화면이 둘을 읽으면 어긋난 순간을 그린다 |
| **D-RTU-3** | 사이드는 **탭 교체**이고 세로 스택이 아니다 | 사용자 지시(요구 ③). 스택은 목록 둘을 반씩 잘라 어느 쪽도 한눈에 들어오지 않는다 |
| **D-RTU-4** | 커밋 입력은 브랜치 줄 **위** | 사용자 지시. 가장 자주 쓰는 입력이 목록의 스크롤에 밀리지 않는다 |
| **D-RTU-5** | 나머지 다섯 뷰의 진입점은 **Changes 사이드 머리의 아이콘 줄** | 사용자 지시. git 진입점이 한 자리에 모이고 Explorer 탭에서는 보이지 않아 파일 작업을 방해하지 않는다 |
| **D-RTU-6** | `GitPanel` 은 **창마다 유지** | 사용자 지시. 전환이 즉시이고 스크롤·선택이 남는다. 대가(메모리)는 뷰 DOM 을 탭이 있을 때만 만들어 줄인다 (NFR-3) |
| **D-RTU-7** | diff 편집은 **워킹 트리 쪽만** | 사용자 지시("VSCode 와 동일하게"). index 편집은 VSCode 에 없고, 우리에겐 이미 hunk 단위 스테이징이 그 일을 한다 — 규칙이 둘이 되면 무엇이 스테이지를 정하는지 말할 수 없다 |
| **D-RTU-8** | untracked 는 **단일 편집기**로 연다 | 사용자 지시. 빈 왼쪽과의 비교는 자리를 절반 쓰면서 알려 주는 것이 없다 |
| **D-RTU-9** | 미리보기 탭을 도입한다 | 사용자 지시. 변경 20개를 훑는 것이 탭 20개가 되면 목록을 보는 일 자체가 정리를 부른다 |
| **D-RTU-10** | 루트가 저장소 **안**이면 상위 저장소를 보인다 | 사용자 지시. 색의 기준(FR-DIR-40)과 패널의 기준이 같아야 한다 — 다르면 같은 화면이 두 저장소를 말한다 |
| **D-RTU-11** | 모바일은 **영역 순회** | 사용자 지시. 기존 pane 순회에 자리 하나를 더하는 것이라 새 조작을 배우지 않는다 |
| **D-RTU-12** | git 이 없어도 `Repo` 탭은 남는다 | 탐색기와 편집기는 git 없이 성립한다. 옛 규칙(FR-SBT-8)은 그 탭이 git 전용일 때의 것이었다 |
| **D-RTU-13** | `git init` 은 확인을 거치고 캐시를 무효화한다 | 홈처럼 넓은 경로에서 되돌리기는 사용자의 몫이 된다. 그리고 무효화하지 않으면 누른 뒤 2초 동안 아무 일도 없는 것처럼 보인다 (§FR-RTU-28) |
| **D-RTU-14** | 옛 Git 창의 탭은 **버린다** | 전부 고정 뷰 탭이라 재현 가능하고 옮길 자리도 없다 |
| **D-RTU-15** | UI 라벨은 **영어** | 사용자 지시. 기존 사이드바 탭(`Windows`)과 git 뷰 이름(`Changes`·`History`)이 이미 영어다 — 한 화면에서 두 언어가 섞이지 않는다 |
| **D-RTU-16** | **관측기도 저장소마다 하나다** — FR-SVS-30 개정 | 구현 중 실측으로 뒤집혔다. `GitObserver` 는 status·signature·소실·실패 누적을 **통째로** 들고 있고, 주기 타이머가 `any()` 로 아무 패널이나 골라 수집한다. 관측기가 하나면 마지막에 수집한 저장소의 상태를 모든 Repo 창이 함께 본다 — 다른 창에서 파일을 만들어도 이 창의 목록이 갱신되지 않았다. FR-SVS-30 의 근거("앱에 하나")는 **Git 창이 하나여서 활성 저장소도 하나**라는 것이었고, 그 전제가 이번에 깨졌다 |
| **D-RTU-17** | **M7(마이그레이션)을 M3 직후로 앞당겼다** | 스펙의 순서 근거("옛 창을 남겨 둔 채 새 자리를 세운다")가 M3 에 의해 무효화됐다 — 사이드바에서 `Git` 탭이 사라진 순간 옛 Git 창으로 가는 **진입점이 이미 없어졌다.** 남겨 두면 중간 상태의 e2e 를 두 번 고쳐야 했다 |
| **D-RTU-18** | **Repo 창의 신원은 id 가 아니라 루트다** | 실측한 결함이다 — 목록에 없던 경로를 `openGitWindow` 로 열면 로컬이 창을 만들고 전환하는데, 곧이어 온 `workspace_changed` 가 서버 스냅샷으로 목록을 덮고 재조정이 같은 루트의 창을 **새 id 로** 만든다. `activeWindow` 는 옛 id 를 가리킨 채 남아 폴백이 엉뚱한 일반 창을 고른다. `_edKeepActive` 가 루트로 다시 찾는다 |
| **D-RTU-19** | `repo-side.js` 를 만들지 않았다 | 사이드는 자기 상태도 DOM 도 소유하지 않는다(탐색기·패널이 소유) — 파일을 나눌 이유가 없어 `renderer._rSide` 한 함수로 두었다. §5.1 의 표는 착수 전 예상이다 |
| **D-RTU-20** | `_gitBackTarget` 은 폐기하지 않았다 | FR-RTU-74 가 `_gitCloseWindow` 와 함께 폐기라 했으나, 그 계산은 `Windows` 탭이 돌아갈 자리로도 쓰인다 (FR-SBT-23·36). 폐기한 것은 `_gitCloseWindow` 와 `_gitLeaveIfRemoved` 의 본문뿐이다 |
| **D-RTU-21** | `git init` 은 `writeCommands` 에 들어가되 **인자를 받지 않는다** | `Exec` 은 읽기 전용이라 가드에 막힌다 (FR-GIT-95). 경로 인자를 허용하면 핸들러의 존재·디렉터리 검사를 우회해 다른 자리에 저장소가 생긴다 |
| **D-RTU-22** | **Changes 사이드의 인라인 diff 미리보기와 손잡이를 걷었다** — EDITOR_GIT_UX_SRS 묶음 D(FR-CSZ-1~8) 폐기 | 사용자 지시("스펙 문구대로 vsc 처럼 탭으로 에디터창에"). 실측이 그 판단을 받쳤다 — 사이드는 260px 이고 그 안을 `목록 \| 손잡이 \| 미리보기` 로 나누면 목록 칸이 **~90px** 이 된다. `.git-file-path` 가 0 으로 눌려 호버 시 이름이 사라지고 동작 버튼이 행 가운데를 덮어, **선택하려는 클릭이 `stage` 를 실행했다** (C4b 가 그것으로 실패했다). 그리고 diff 를 본문 탭으로 옮긴 것이 요구 ②이므로 같은 것을 두 자리에 둘 이유가 없다. §1.1 의 그림과 FR-RTU-20 의 네 줄에도 그 칸이 없었다 — 초판 스펙이 옳고 구현이 옛 자리를 들고 있었다. 미리보기가 들고 있던 축 라벨은 Diff 탭의 바로 옮겼다 (정보를 잃지 않는다) |
| **D-RTU-23** | **변경 목록의 한 번 클릭이 본문을 연다. 더블클릭 계기는 없다** — FR-RTU-40 개정, FR-RTU-42 ④에서 변경 목록 제외 | 사용자 지시("한번클릭을 diff, 두번클릭은 제거. 이것도 vsc 와 동치"). 인라인 미리보기를 걷으면 클릭의 결과를 보일 자리가 본문뿐이고, 그때 더블클릭을 남기면 "한 번 눌렀는데 아무 일도 없다" 가 된다. §2.8 이 미리보기 탭을 도입한 근거("변경 20개를 훑으면 탭 20개")는 Diff 탭이 창에 하나라는 FR-RTU-31 이 이미 만족시키므로, Diff 탭에는 `preview` 를 붙이지 않는다 |
| **D-RTU-24** | **git 뷰 탭은 닫히고 끌린다** — FR-RTU-33 을 실제로 구현했다. 이름 변경만 없다 | 초판이 요구했으나 구현되지 않은 채 M2 가 ✅ 로 적혀 있었다 (`closeTab` 이 `TAB_TYPE_GIT` 을 조기 반환하고 렌더가 `×`·draggable 을 떼고 있었다). 옛 금지의 근거는 Git 창의 탭이 **고정 일곱**이라 자리가 늘 같아야 근육 기억이 선다는 것이었고(FR-GIT-28) 그 창이 사라졌다. 이름만 예외인 이유는 탭 이름이 뷰 이름에서 파생되기 때문이다 — 고쳐도 다음 렌더가 되돌린다. 닫을 때 그 뷰의 DOM·Monaco 는 놓고 상태(스크롤·선택·diff 대상)는 패널에 남긴다 (FR-RTU-34 + NFR-RTU-3) |
| **D-RTU-25** | **관측의 조건은 "창이 보인다" 가 아니라 "그 표면이 보인다" 다** — `_gitSurfaceOn` · 탐색기도 같은 게이트 | FR-RTU-62 의 문구가 이미 "그 표면" 이었는데 구현은 창 가시성만 봤다. 그 결과 **저장소가 아닌 루트의 Repo 창에도** status 가 3초마다 나갔다 (V-EDT-47 이 1회를 기대하는데 4회). 사이드가 `Explorer` 이고 본문에 git 뷰 탭도 없으면 그 관측을 쓰는 화면이 하나도 없다. 대칭으로 탐색기 트리도 사이드가 `Changes` 면 묻지 않는다 — 그 게이트가 없어 폴링을 끈 설정에서도 status 가 왔다 (V18·V5 실측). `_gitRescheduleAll` 은 **패널을 만들지 않는다**: 만들면 그것이 곧 폴링이었다 |
| **D-RTU-26** | **`Repo` 행이 소실 사유와 `norepo` 를 되찾았다** | 옛 `Git` 목록의 행이 하던 일(FR-RMS-11·17)이 두 탭을 합칠 때 빠졌다 — 폴더가 사라진 저장소의 행이 아무 말도 하지 않았다 (V-RMS-11 실측). 배지만으로는 안 된다: 사라진 저장소는 배지가 `null` 이고 그것은 "관측이 아직 없다" 와 구분되지 않는다. 그래서 핀 항목의 `isRepo`·`reason` 을 읽는 자리(`_gitPinEntry`)를 따로 둔다. **저장소가 아닌 것과 사라진 것은 다르다** — 앞쪽은 이제 정당한 행이므로(D-RTU-12) 사유를 적지 않는다 |
| **D-RTU-27** | **헤더의 리포 드롭다운은 `openGitWindow` 다** | `setRepo` 는 Repo 창의 패널에서 조기 반환하므로(저장소가 창의 루트다) 드롭다운이 **아무 일도 하지 않았다** — 실측한 결함이다 (V207). 고른 뒤 그 창의 사이드도 `Changes` 로 돌린다: 사용자가 누른 자리가 Changes 이므로 "같은 자리의 다른 저장소" 로 가는 것이 그 손짓의 뜻이다 |
| **D-RTU-28** | **`+ Add` 가 하나가 되면서 사라진 화면이 둘 있다** | ① FR-FLW-6 의 "저장소가 아닌 경로 거부" — 종단이 `/api/editors/add` 하나이고 저장소가 아닌 경로도 정당한 행이다 (D-RTU-12). ② FR-RMS-10 의 절반("핀 없는 저장소에는 핀 제거 버튼이 없다") — Repo 창이 서는 근거가 `editors.list` 이고 그 목록에 든 저장소 루트는 `LinkEditorAdd` 가 핀까지 만든다 (FR-EDT-33). 즉 "창은 있는데 핀은 없는 저장소" 를 화면으로 만들 수 없다. 규칙 자체는 남고 e2e 만 그 절반을 놓는다 |
| **D-RTU-29** | **모바일은 사이드와 본문을 나란히 두지 않는다** — 순회의 자리 하나씩이다 | 초판 FR-RTU-80 이 "한 줄로 순회" 라고 적었으나 구현은 `max-width:40%` 로 둘을 나란히 두는 데 그쳤다. 390px 에서 그것은 **둘 다 쓸 수 없는 폭**이다 — 사이드가 156px 이 되어 커밋·원격·일괄 버튼이 경계를 넘었다 (실측 V10). 자리 하나씩 보이면 각 자리가 전체 폭을 쓴다 (D-RTU-11: 기존 pane 순회에 자리를 더하는 것이라 새 조작을 배우지 않는다). **포커스가 옮겨 가면 사이드를 떠난다** — 그 계기를 `_setFocus` 한 자리에 두었고, `addTab` 이 새 탭에도 그것을 부르게 했다 (종전에는 "이미 있으면" 분기만 불렀다) |
| **D-RTU-30** | **탭 바가 스크롤되는 것이 계약이다** — 옛 GIT_HEAD_MOBILE V13("일곱 탭이 전부 뷰포트 안") 은 폐기 | 그 요구의 근거는 탭이 **고정**이라 사용자가 줄일 수 없다는 것이었다 — 들어가지 않으면 닿을 방법이 없었다. 뷰 탭이 필요할 때 열고 닫는 보통 탭이 된 지금(FR-RTU-30·33) 남는 계약은 **닿을 수 있는가**이며 `.pn-tabs{overflow-x:auto}` 가 그것을 준다. 390px 에서 여섯 탭은 447px 을 쓴다 |
| **D-RTU-31** | **사이드의 두 컨트롤도 히트 영역 하한을 지킨다** | `.ed-side-tab`·`.ed-side-act` 가 24px·23px 로 서 있었다 — 통합이 새로 넣은 컨트롤이 기존 FR-GIT-195~198(하한 30px, 기준은 이 앱의 `.si` 행 높이)을 지나쳤다 (V80 실측). 새 표면이라고 기준이 달라질 이유가 없다 |
| **D-RTU-32** | **활성 Repo 창은 루트로 되살린다** | 새로고침 뒤 `Windows` 탭으로 돌아가는 결함이었다 (V-SBT-4 실측). Repo 창은 `editors.list` 에서 **재조정이 만들므로**(FR-EDT-42) 저장이 서버에 닿기 전이거나 다른 브라우저가 쓴 워크스페이스를 처음 읽으면 같은 루트의 창이 새 id 로 선다. `sessionStorage` 의 id 만으로는 사용자가 보던 창을 찾지 못한다 — 루트를 함께 적고(`activeEditorRoot`) id 가 없으면 루트로 다시 찾는다. `_edKeepActive` 도 그 키를 함께 옮긴다 |
| **D-RTU-33** | **`.git-job-bar` 에 전용 접기 토글을 둔다. 바 클릭 계기는 유지한다** — GIT_UI_REVISION_SRS FR-GIT-221 개정 | 사용자 지시(2026-09-04, 인계 §5 를 물어 확정). 그 요구가 "바를 눌러 편다" 라고 적었을 때 바는 Git 창의 전체 폭에 있었고, 통합이 그것을 220px 사이드로 옮겼다 — 그 폭에서 `kind·state·cancel·copy·close` 가 `flex-shrink:0` 이라 **argv 만 0 으로 눌리고 바에 버튼 아닌 자리가 남지 않는다.** 펴려는 클릭이 `copy`·`close` 에 떨어지고 핸들러는 `closest('button')` 으로 조기 반환한다. 폭이 손짓의 대상을 정하는 것을 끊는 유일한 길이 **폭과 무관한 고정 컨트롤**이다. 바 클릭을 함께 두는 것은 넓을 때의 손짓을 잃지 않기 위해서다 — 계기가 둘이어도 하는 일이 같으므로 뜻이 갈리지 않는다 |
| **D-RTU-34** | **자리마다 고치지 않고 규칙으로 세운다 — NFR-RTU-6. 행 구조(`.git-file`)도 대상이다** | 사용자 지시(범위 선택). 같은 결함을 **두 번 실측했다**: C4b 의 "선택하려는 클릭이 `stage` 를 실행" 과 이번 바의 것이다. 앞의 것은 원인(사이드를 두 칸으로 나눈 것)을 걷어 220px 에서 증상만 사라졌고 **구조는 그대로다** — `.git-file-path{min-width:0}` + `.git-file-acts{flex-shrink:0}` 이므로 사용자가 손잡이를 하한 100px 까지 끄는 순간 되살아난다. 두 번 같은 자리에 온 것을 자리마다 고치면 세 번째가 온다. 감추는 쪽을 택하지 않은 것은 기존 판단을 그대로 잇는 것이다 — 행 인라인 동작은 "항상 보인다, hover 로만 드러나면 있는 줄 모르고 터치에는 hover 가 없다"(`style-git.css:511`). 그래서 줄을 늘린다 |
| **D-RTU-35** | **NFR-RTU-2 는 시간이 아니라 구조로 잰다** — V-RTU-97(자동) + V-RTU-98(수동) | 사용자 지시(2026-09-04). 그 NFR 이 스스로 적은 근거가 "패널을 유지하므로 재구성이 없어야 한다" 이고, 250ms 는 그 근거가 지켜졌을 때 따라오는 결과다. e2e 로 밀리초를 재면 계약이 **기계 성능**이 되어 1079개 중 하나가 부하에 따라 흔들리고, 그 붉음은 결함을 뜻하지 않는다 — 회귀가 신호를 잃는다. 표식 기법(`__rptMark`)은 V-RTU-92 가 이미 쓰는 도구이므로 새 장치도 필요 없다. 체감 속도는 자동이 대신할 수 없는 자리이므로 수동 체크리스트에 한 줄로 남긴다 |

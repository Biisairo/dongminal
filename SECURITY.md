# 보안 경계

이 문서는 **dongminal 이 무엇을 보증하고 무엇을 보증하지 않는가**를 적습니다.
보증하지 않는 것을 보증하는 것처럼 적지 않습니다.

읽어야 할 한 줄이 있다면 이것입니다:

> **이 제품은 인증을 지원하지 않습니다. 앞으로도 지원하지 않습니다.**
> 접근 통제를 **오버레이 망**과 **출발지 허용 목록**에 위임합니다.

이것은 준비 중인 기능이 아니라 **확정된 경계**입니다.

---

## 1. 위협 모델

dongminal 은 **상시 노출된 원격 작업 도구**입니다. 브라우저로 붙어 셸을 쓰는
것이 이 제품이 존재하는 이유이고, 그러므로 "기본이 `127.0.0.1` 이니 노출 경로의
결함은 낮은 등급" 이라는 추론을 하지 않습니다.

이 제품에 붙는다는 것은 **여러분의 계정으로 셸을 얻는다**는 뜻입니다. 파일을
읽고 쓰고, git 자격증명을 쓰고, 프로세스를 띄웁니다. 그러므로 이 문서에서
"접속" 은 곧 "전권" 입니다.

**막는 것**

| 공격 | 무엇이 막는가 |
|---|---|
| 다른 웹페이지가 여러분의 터미널에 명령을 보내는 것 (CSRF·DNS rebinding) | `requestGate` — Origin·Host·`Sec-Fetch-Site`·Content-Type 을 mux 바깥에서 봅니다 |
| 허용하지 않은 기기에서의 접속 | `accessGate` — 출발지 IP 를 봅니다 (**켠 경우에만**) |
| 터미널 출력이 스크립트가 되는 것 | 마크업 보간이 전부 이스케이프를 지납니다 (CI 게이트) |
| 같은 호스트의 다른 사용자가 소켓에 붙는 것 | 홈 `0700` · 소켓 `0600` |

**막지 않는 것** — §4 에 그대로 적습니다.

---

## 2. 배포 형태별 보증

### ① 오버레이 망 뒤 + 허용 목록 — **유일한 권장 형태**

```
[여러분의 기기] ──(Tailscale / WireGuard)──> [dongminal + ACL]
```

- **기밀성**: 오버레이 망이 줍니다. **제품이 주지 않습니다.**
- **접근 통제**: 망이 1차, 허용 목록이 2차입니다.
- **데스크톱 알림**: Tailscale 의 HTTPS 이름으로 붙으면 동작합니다. 평문 IP 로
  붙으면 브라우저가 막습니다 (§4-3).

오버레이 망은 **권고가 아니라 전제**입니다. 이 형태를 벗어나면 아래 둘 중
하나이고, 둘 다 열등합니다.

### ② LAN + 허용 목록 — 열등

- **기밀성 없음.** 평문 HTTP 이므로 같은 네트워크에서 트래픽이 읽힙니다.
  여러분이 치는 것과 화면에 나오는 것 전부입니다.
- **데스크톱 알림이 동작하지 않습니다.** 브라우저가 secure context 가 아닌
  곳에서 `Notification` 을 막습니다. 이 경우 앱이 그 사실을 화면에 표시하고
  알림음을 기본으로 켭니다.
- 공용 Wi-Fi 에서는 **쓰지 마세요.**

### ③ loopback 전용 — 개발용

기본값입니다. 이 기계 밖에서 닿지 않습니다.

### ④ 미지원 선언

다음은 **지원하지 않습니다.** 동작할 수도 있지만 우리는 그것을 보증하지 않고,
그 형태에서 생긴 문제를 결함으로 받지 않습니다.

- **리버스 프록시** (nginx·Caddy·Cloudflare Tunnel 등). `requestGate` 의 Host·
  Origin 판정이 프록시가 고쳐 쓴 헤더 위에서 서므로, 그 조합의 안전을 우리가
  말할 수 없습니다.
- **Tailscale Funnel 등 공개 노출.** 인증이 없는 서버를 인터넷에 여는 것입니다.
- **멀티유저 호스트.** 이 제품은 **단일 사용자 전용**입니다 (결정 7).
  역할·권한 스코프가 없고, 만들 계획도 없습니다.

---

## 3. 허용 목록(ACL) — 기여와 한계

**Settings ▸ Access** 에서 접속을 허용할 출발지를 지정합니다.
IP(`100.117.248.111`) · 대역(`192.168.0.0/24`) · 이름(`macmini`).

### 기여

**켜면 무단 기기가 도달조차 못 합니다.** 요청이 mux 에 닿기 전에 거절되므로,
어느 종단의 결함이든 그 기기에서는 닿을 수 없습니다. 의미 있는 방어입니다.

노출하면서 이것이 꺼져 있으면 **서버가 뜨지 않습니다**. 되돌리는 길은
`--insecure-no-acl` 하나이고, 그 이름이 곧 경고입니다.

### 한계 — 넷

1. **기본이 꺼짐입니다.** loopback 으로만 쓰는 사람에게 목록을 강제하지 않기
   때문입니다. 노출할 때 켜는 것은 여러분의 몫이고, 기동 게이트가 그것을 묻습니다.
2. **"어느 기기" 만 가릅니다. "누구" 는 묻지 않습니다.** 허용된 기기를 쓰는
   사람이면 누구든 전권을 갖습니다.
3. **IP 기반이라 스스로 잠길 수 있습니다.** DHCP 로 주소가 바뀌면 어제 넣은
   항목이 오늘은 다른 기기를 가리킵니다. 서버가 도는 컴퓨터에서는 목록과
   무관하게 언제나 접속되므로 되돌릴 수는 있습니다.
4. **통과한 기기는 전부 동등하고, 그중 하나가 목록 자체를 끌 수 있습니다.**
   `PUT /api/access` 는 다른 종단과 같은 등급입니다.

### 방어선의 구조

```
요청 ──> 요청 ID ──> 로그 ──> accessGate ──> requestGate ──> recover ──> mux
                              (어느 기기)     (어느 출처)
```

게이트는 **둘**입니다. **"누구인가" 를 묻는 게이트는 없으며, 생길 예정도
없습니다.** 이 직렬 배치가 유일한 방어선입니다.

---

## 4. 보증하지 않는 것 (잔여 위험)

아래 넷은 **알려진 것이고 고칠 계획이 없습니다.** 결함이 아니라 경계입니다.

### 4-1. 평문입니다. 그리고 앞으로도 평문입니다

TLS 를 넣지 않습니다. `crypto/tls` 를 부르는 코드를 저장소에 넣지 않습니다.

**기밀성을 제품이 제공하지 않고 오버레이 망에 위임합니다.** 이것이 이 문서에서
가장 중요한 문장입니다 — 오버레이 망 없이 노출하면 트래픽은 읽힙니다.

### 4-2. 허용 목록을 통과한 기기 하나가 목록을 끌 수 있습니다

`PUT /api/access` 에 특별한 보호가 없습니다. 목록에 넣는 기기는 여러분이
신뢰하는 것이어야 합니다.

### 4-3. 평문 접속에서는 데스크톱 알림이 동작하지 않습니다

브라우저가 secure context 가 아닌 곳에서 `Notification` 을 막습니다.
**앱이 그 사실을 화면에 표시하고**, 그 환경에서는 알림음을 기본으로 켜며,
탭 제목의 배지가 대기 중인 도구 수를 계속 보여 줍니다.

"HTTPS 로 접속하세요" 를 안내하지 않는 이유는 §4-1 입니다 — 제품이 제공하지
않는 길을 안내하지 않습니다.

### 4-4. loopback 출발지는 허용 목록을 무조건 통과합니다

서버가 도는 기계에서 오는 요청은 목록과 무관하게 들어옵니다. 그것이 §3 의
"잘못 설정해도 되돌릴 수 있다" 를 성립시키는 장치입니다.

그 기계의 브라우저를 통한 공격은 `requestGate` 가 막습니다 — **이 배치에서
유일한 방어**입니다.

### 4-5. 파일 API 에 경로 경계가 없습니다 (2026-09-20 ~)

**`/api/file/*` 와 `/api/git/*` 은 절대경로면 어디든 읽고 씁니다.** 허용 루트
대조·홈 쓰기 금지·git `repo` 검사를 **전부 제거했습니다** (사용자 결정).

이 문서가 종전에 "막는 것" 으로 적던 항목이며, 그 줄을 지웠습니다. 지금의 사실은
이렇습니다:

- **접속한 기기는 서버를 띄운 계정의 권한으로 파일시스템 전체를 읽고 씁니다.**
- `$DONGMINAL_HOME` 아래도 예외가 아닙니다 — `settings.json` · `access.json` ·
  `ext` 매니페스트가 그 안에 있습니다.
- `ext` 매니페스트에 적힌 설치 명령은 **실행됩니다.** 즉 파일 쓰기 하나가
  **임의 명령 실행**에 닿습니다.
- `access.json` 을 덮을 수 있으므로 **허용 목록 자체를 끌 수 있습니다.** §4-2 가
  적어 둔 잔여 위험이 이제 요청 한 번입니다.

**따라서 §2 의 "① 오버레이 망 뒤 + 허용 목록" 은 권장이 아니라 필수입니다.**
그 밖의 형태로 노출하지 마십시오. §1 의 *"접속은 곧 전권"* 이 문자 그대로가
됐습니다.

폐기 경위와 무엇이 열렸는지는
[`FILE_API_BOUNDARY_SRS`](./docs/internal/FILE_API_BOUNDARY_SRS.md) 머리에 적어
두었습니다.

---

## 5. 데이터가 사는 곳

| 자리 | 무엇 | 권한 |
|---|---|---|
| `$DONGMINAL_HOME` | 홈 전체 | `0700` |
| `$DONGMINAL_HOME/paned.sock` | 데몬 IPC 소켓 | `0600` |
| `$DONGMINAL_HOME/access.json` | 허용 목록 | 홈 권한을 따릅니다 |
| `$DONGMINAL_HOME/settings.json` | 브라우저 설정 blob | 같음 |
| `$DONGMINAL_HOME/workspace.json` | 창·탭 배치 | 같음 |
| `$DONGMINAL_HOME/tool-history/` | 도구 셸의 히스토리 | 같음 |
| `/tmp/dongminal.log` (또는 `$DONGMINAL_LOG`) | 서버 로그 | OS 기본 |

**데몬 IPC 에는 인증이 없습니다.** 같은 호스트의 다른 UID 가 그 소켓에 붙으면
여러분의 PTY 전부에 입출력 접근이 가능합니다 — 홈 `0700` 과 소켓 `0600` 이
그것을 막는 전부입니다. 멀티유저 호스트를 지원하지 않는 이유가 여기 있습니다.

**로그에 무엇이 실리나요.** 요청의 메서드·경로·상태·출발지 주소가 실립니다.
터미널의 **내용**은 실리지 않습니다. `dongminal doctor --bundle` 로 만드는 신고용
번들은 자격증명을 마스킹하지만, **붙이기 전에 한 번 열어 확인**하시기를 권합니다.

---

## 6. 고쳐진 취약점

### 2026-09-16 — 작업 폴더의 스크립트가 셸 시작에 실행될 수 있었습니다 (v1.1.0 에서 고침)

**무엇이었나.** 도구 셸을 띄울 때 dongminal 은 자기 `bin/` 에 있는 훅을 걸고 그
자리를 `PATH` 에 더합니다. 그 경로를 만드는 식이
`DONGMINAL_HOME` + `/bin` 이었는데, **그 변수가 비면 결과가 `bin` 이라는 상대경로**
가 되었습니다. 셸은 상대경로를 **터미널이 열린 폴더 기준**으로 풉니다.

**무엇이 가능했나.** 그 상태에서 작업 중인 폴더에 아래 파일이 있으면, 그 폴더에서
새 터미널을 열 때마다 그것이 **셸 시작에 실행**되었습니다.

| 셸 | 실행되던 파일 |
|---|---|
| bash | `bin/bash-hook.sh` |
| zsh | `bin/zdotdir/*` |
| PowerShell | `bin\powershell-hook.ps1` |

`PATH` 에도 그 상대 조각이 얹혀, 같은 폴더의 실행 파일이 우선될 수 있었습니다.
**클론한 저장소를 열어 보는 것만으로 성립하는 길**입니다.

**해당 조건.** 서버 프로세스에 `DONGMINAL_HOME` 이 **없을 때**만입니다.

| 기동 방법 | 해당 여부 |
|---|---|
| `dongminal start` (기본) | **아니오** — 자식 프로세스에 그 변수를 심습니다 |
| `dongminal service` 로 등록한 서비스 | **아니오** — plist·unit 에 실립니다 |
| `dongminal start --foreground` 을 변수 없이 직접 띄운 경우 | **예** |

**영향 판.** v1.0.12 이하. **고친 판.** v1.1.0.

**지금 할 일.** v1.1.0 이상으로 올리세요. 올릴 수 없다면 서버를 띄울 때
`DONGMINAL_HOME` 을 명시하세요 — 그 변수가 있으면 이 경로는 성립하지 않습니다.

**어떻게 고쳤나.** 자리를 **모르면 훅을 걸지 않습니다.** 절대경로가 아닌 훅
경로는 거절하고, `PATH` 에도 더하지 않습니다. 그때는 작업 폴더 추적과 에이전트
래퍼가 동작하지 않지만 셸 자체는 정상입니다 — 기능이 빠지는 쪽을 골랐습니다.

**어떻게 드러났나.** Windows 에서 PowerShell 이 그 상대경로를 모듈 이름으로 읽어
`The module 'bin' could not be loaded` 를 찍었고, 그것을 쫓다 원인이 나왔습니다.
POSIX 에서는 bash·zsh 가 없는 파일을 말없이 넘기므로 **조용했습니다.**

---

## 7. 취약점을 찾으셨다면

이 저장소의 이슈로 알려 주세요. 단일 사용자용 도구이고 별도의 조율 창구를 두지
않습니다.

**§4 에 적힌 넷은 이미 알려진 것입니다** — 그 넷의 재신고는 필요하지 않습니다.
경계를 넓히자는 제안은 환영하지만, 인증과 TLS 는 확정된 비목표입니다.

---

## 8. 근거

| 결정 | 근거 |
|---|---|
| 인증 미지원 | 로드맵 결정 9 (2026-09-11) |
| TLS 영구 제외 | 같은 결정 — *"tls 인증은 하지 않을거야"* |
| 단일 사용자 전용 | 로드맵 결정 7 |
| 게이트 둘의 순서 | [`REQUEST_GATE_SRS`](docs/internal/REQUEST_GATE_SRS.md) FR-RQG-1 |
| 노출 판정 | 같은 문서 FR-RQG-24 — **loopback 만 로컬**입니다 |
| 허용 목록의 규약 | [`ACCESS_ALLOWLIST_SRS`](docs/internal/ACCESS_ALLOWLIST_SRS.md) |
| 알림 결손의 조치 | [`ATTENTION_FIRING_SRS`](docs/internal/ATTENTION_FIRING_SRS.md) 묶음 S |

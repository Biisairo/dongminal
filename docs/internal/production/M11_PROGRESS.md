# M11 진행 — 잔재의 홀 하나와 에이전트 GUI 여섯

> 스펙은 `docs/internal/M11_SRS.md`. 이 문서는 **어떻게 재었는가**와 **무엇을 배웠는가** 다.

## 1. 커밋

| 커밋 | 내용 |
|---|---|
| `90dc73f` | FR-M11-1 — 폭이 바뀐 사실이 소켓과 함께 사라지지 않는다 |
| `571c0c1` | FR-M11-2·3·4 — 스크롤·색·접기 |
| `e130693` | B2·B3·B7 의 실측 상태 기록 |
| `d0fc41c` | FR-M11-5 — 전환은 끝난 것을 보고 연다 |
| `70581bb` | `initialize` 응답 원문 실측 |
| `c747160` | FR-M11-6 — 계정·플랜·기동 모델 |
| `9e01b0b` | FR-M11-7 — 세션 id 도 신호다 |
| `ba2d128` | FR-M11-8 — 신원은 활동과 **같은 요청**에 온다 |
| `88b658e` | FR-M11-9 — 끝난 세션은 올릴 수 없다 |
| `007dc96` | FR-M11-10·11 — 시선을 옮기는 명령은 한 곳에서만 |
| `cf95a8a` | FR-M11-12 — 아무것도 돌지 않는 도구는 올릴 수 없다 |
| `3fe6381` | 세 번째 물결의 절반 — FR-M11-17·20~23·26·27·33~36 |
| (이 단계) | 세 번째 물결의 나머지 열 — FR-M11-13·14·24·25·27 개정·28·29·30·31·37 |
| (이 단계) | 네 번째 물결 열셋 — FR-M11-38~51 (사용자가 GUI 로 직접 쓰며 낸 접수) |

전량 e2e: 8샤드 전부 `unexpected 0` (expected 1744 · skipped 3 · flaky 6 — 여섯 전부
부하성이다: git·editor·splitview 쪽이고 단독 실행에서 재시도 없이 통과했다).

## 2. 배운 것

### 2-1. 재현하지 못한 것을 "없다" 로 접지 않는다 — **무력화로 가른다**

M11-B1 은 여섯 배치에서 한 번도 나지 않았다. 거기서 멈추면 결론이 *"못 찾았다"* 뿐이다.
대신 **고침 하나만 무력화했다** (`_refreshForWidth` → `return false`). 잔재가 났고,
되살린 대조군은 깨끗했다. 그때서야 물음이 *"무엇이 잔재를 만드나"* 에서 **"그 재생이
언제 안 걸리나"** 로 바뀌었고, 그 물음에는 코드가 답할 수 있었다.

**순서가 요점이다.** 자연 경로부터 찾았으면 `!this.ws` 갈래를 떠올리지 못했다.

### 2-2. 겉모습이 맞는 재현을 근거로 세울 뻔했다

서버만 재시작하고 그 틈에 폭을 바꾸니 **델타로 재개되고 옛 폭의 그림이 남았다** —
증상도 조건도 맞아 보였다. SRS 에 그것을 FR-M11-1 의 근거로 적었다.

그런데 `_seq` 가 `862` 그대로였다. **`_refreshForWidth` 를 부르지도 않은 경로였다** —
`ptySize()` 가 `had` 를 `doFit()` **뒤**의 값과 비교하기 때문이고, 그 자리는 D-M10-3 이
이미 경계로 둔 *소유자 자신의 리사이즈*였다. 살아 있는 claude TUI 로 다시 재니 잔재도
나지 않았다.

**교훈**: 재현의 겉모습이 아니라 **그 경로가 고치려는 코드를 지나갔는가**를 확인한다.
여기서는 `_seq` 한 칸이 그것을 말했다. `M10_PROGRESS` §2-13 과 같은 자리다.

### 2-3. 검사가 고침 없이도 통과하면, 재는 대상이 틀린 것이다

V-M11-7(색)을 처음 세웠을 때 **고침을 stash 해도 초록이었다.** 사용자·에이전트의
`border-left-color` 가 1px 테두리 색으로 이미 달랐기 때문이다. 접수한 말은 *"단조로워서
구분하기 힘들다"* 였고 실제로 같았던 것은 **에이전트 말과 도구 카드**다.

재는 대상을 ① 띠의 **굵기**(1px → 3px) ② **셋이 서로 다른 색인가** 로 바꾸니 RED 가
섰다. `M9_PROGRESS` §2-12 가 적은 *"증상을 재라"* 의 실제 사용례다.

### 2-4. 값이 없는 것과 값을 버리는 것은 다르다

*"하단에 정보가 없다"* 를 한 덩이로 고치려 하면 어디부터인지 알 수 없다. `initialize`
응답의 **원문을 찍어** 갈랐다:

- **프로토콜에 없다** — 비용·토큰·한도. 턴의 결과 프레임에만 있다
- **오는데 버린다** — 계정·플랜. 어댑터가 `ProtoStatus.Account` 로 날라 오는데 화면이 안 읽었다
- **우리가 안다** — 기동 옵션의 모델·권한 모드. `recordLocked` 가 이미 그 사다리를 썼다

셋의 고침이 전부 다르다. **원문 한 번**이 그 셋을 갈랐고, 프로브는 되돌린 뒤
`grep -c TEMP-PROBE` = 0 으로 확인했다.

### 2-5. "조용한 실패" 는 원인이 아니라 **가려진 자리**다

M11-B7 의 신원 보고는 *"어떤 실패에서도 조용하다"* 로 적혀 있었다. 그래서 훅이 도는데
서버가 모르는 상태를 아무도 설명하지 못했다.

답은 로그가 아니라 **판정 한 줄**이었다 — `reportContext` 가 나갈지를 전사본과 압축으로만
보았고, 전사본 파일은 **첫 프롬프트에야 생긴다**. 사용자의 한마디(*"첫 프롬프트를 보내면
나와"*)가 그 갈래를 즉시 갈랐다. **재현보다 싼 관측이 사용자에게 있을 때가 있다.**

### 2-6. 규약은 대개 **이미 있고, 한 자리가 안 쓴다**

- 스크롤(M11-B4): `.ui-scroll` 이 `style-kit.css` 에 있고 GUI 만 안 썼다
- 접기(M11-B6): `agp-raw` 가 이미 `details` 를 쓴다
- 모델 사다리(FR-M11-6): `recordLocked` 가 이미 `opts` 로 떨어진다

새 모양을 만들면 두 벌이 되고, 두 벌은 한쪽만 고쳐진다. **고치기 전에 같은 일을 하는
자리를 먼저 찾는다.**

### 2-7. 계약을 바꾸면 그것을 **재던 검사가 떨어진다** — 전량이 못 돌면 그 사실이 보이지 않는다

`FR-M11-10·11·12` 를 넣은 뒤 전량이 **두 번 다 세션 경계에서 죽었다**. 그 사이 회귀
다섯이 숨어 있었다 — `TC-SXE-7`(제목부터 옛 계약: *"focus 는 게이팅되지 않는다"*) 하나와
`agent-tool.spec.ts` 의 넷(훅만 흉내 내어 셸의 전경이 비었고, 새 `Tools.Busy` 게이트에
걸렸다).

**다섯 다 새 결함이 아니었다.** 직전 커밋들이 계약을 바꾸면서 Go 단위 검사는 같은
변경에서 고쳤는데, **e2e 쪽은 돌지 않아 보이지 않았다.** 커밋 메시지는 고친 검사를
정직하게 적었고 — 적히지 않은 것은 *보지 못한 것*이다.

  전량이 죽는다  →  회귀가 쌓인다  →  다음 결함의 귀속이 갈리지 않는다

**세션 경계가 세 번째로 죽였을 때** 손을 바꿨다: `nohup` 으로 프로세스를 세션에서
떼어낸다. `run_in_background` 는 세션에 묶여 있어 경계를 넘지 못한다.

    nohup env PW_WORKERS=1 sh -c 'seq 6 8 | xargs -P 3 -I{} scripts/e2e-shard-run.sh {} 8' \
      > /tmp/dm-e2e-678.log 2>&1 &

**샤드 리포트가 부분 완주를 살린다.** 세 번째 실행은 s1~s5 를 완주하고 죽었는데, 그
다섯은 `test-results/s<N>/report.json` 에 `unexpected 0` 으로 남아 있었다 — 소스가
그 뒤로 바뀌지 않았으므로 **남은 셋만 다시 돌리면 전량과 같다.**

### 2-8. 자리를 세우면 **"비지 않음" 이 더 이상 도착 신호가 아니다**

`FR-M11-16`(하단은 자리를 세우고 모름을 말한다)을 넣자 `V-M9-35` 가 떨어졌다. 그 검사는
cwd 가 도착하기를 이렇게 기다리고 있었다:

    await expect(cwd).not.toBeEmpty({ timeout: 10000 });

**빈 문자열을 "아직"의 뜻으로 쓰고 있었다.** 초기값이 생기는 순간 그 단언은 즉시
통과하고, 검사는 아직 오지 않은 값을 읽는다. 같은 자리에서 `.agp-limits` 는 생성
시점의 `hidden` 때문에 새 검사가 떨어졌다 — **자리를 세우는 코드가 한 자리에 모여
있지 않으면**(`_dashUnknown()` 밖에 `hidden=true` 가 하나 남아 있었다) 그 한 곳이 규약을
깬다.

**교훈**: 빈 값·숨김을 *"아직"* 의 신호로 쓰는 대기는, 그 자리가 초기값을 갖게 되는
변경에서 **조용히 무너진다**. 새 신호는 **값 자체가 말하는 것**이어야 한다 — 여기서는
출처를 적는 `title` 이다.

### 2-9. **원본을 재는 것이 명세를 쓰는 것보다 싸다**

세 번째 물결의 접수 열아홉을 각각 갈래로 세우고 FR 을 열일곱 개 썼다. 그 뒤 사용자가
한 줄로 정리했다 — *"실제 에이전트의 사용 패턴, 동작을 그대로 옮겨적으면 돼."*

**실제 claude 를 띄워 스무 번 남짓 재니** 그 열일곱 중 열둘의 답이 화면에 이미 적혀
있었다 (`M11_SRS` §2.10). 그리고 **내가 정한 설계 셋이 원본과 달랐다**:

| 내가 정한 것 | 원본 |
|---|---|
| 접기 = 앞 2줄·뒤 2줄 | **의미 한 문장** + `(ctrl+o to expand)` |
| 게이지를 주기마다 | 게이지는 **컨텍스트 하나**, 한도는 글로 |
| diff 를 별도 창으로 | 도구 결과 **그 자리**에 |

셋 다 인터뷰로 "확정" 했던 것이다. **인터뷰는 원본을 대신하지 못한다** — 사용자도
원본을 기억으로 답하기 때문이다. (게이지는 그 뒤 *"추가한건 좋은거같아"* 로 유지가
됐다. 원본과 다른 것이 **틀린 것은 아니다** — 다만 **알고 다른 것**이어야 한다.)

**순서가 요점이다.** 재기 전에 명세를 쓰면 그 명세를 지키는 검사까지 함께 세우게 되고,
그때 원본과의 차이는 **초록 뒤에 숨는다.**

### 2-10. `run_in_background` 는 세션 경계를 넘지 못한다 — 세 번 죽었다

전량 e2e 가 세 번 `[killed]` 됐다. 세 번째에 손을 바꿨다:

    nohup env PW_WORKERS=1 sh -c 'seq 1 8 | xargs -P 4 -I{} scripts/e2e-shard-run.sh {} 8' &

**그리고 샤드 리포트가 부분 완주를 살린다.** 세 번째 실행은 s1~s5 를 완주하고 죽었는데
그 다섯은 `test-results/s<N>/report.json` 에 `unexpected 0` 으로 남아 있었다 — 소스가
그 뒤로 바뀌지 않았으므로 **남은 셋만 다시 돌려** 전량과 같은 판정을 얻었다.

### 2-11. **한 손이 두 접수의 뿌리였다**

B22(*"출력이 한 덩이"*)와 B26(*"추론이 안 보인다"*)을 서로 다른 일로 잡고 있었다. 실측을
끝내고 보니 원인이 **같은 다섯 줄**이었다:

```js
// _message() — 스냅샷이 올 때마다
for(const c of [...d.children]) if(!c.classList.contains('agp-who')) c.remove();
```

`assistant` 프레임은 **블록마다** 온다 (§2.11 (2)). 그러니 이 줄은 턴이 도는 동안
**앞 블록을 계속 지운다** — 그래서 화면에 한 덩이만 남았고(B22), `thinking_delta` 로
세운 추론 칸도 빈 스냅샷이 올 때 함께 지워졌다(B26).

**둘을 따로 고쳤다면 하나는 고쳐지지 않았을 것이다.** 재고 나서야 한 자리로 보였고,
재기 전에는 "블록 경계를 만들자"·"추론 UI 를 만들자" 라는 **두 개의 새 장치**를 지을
뻔했다. 지우는 손을 멈추는 것으로 둘 다 닫혔다.

### 2-12. **"안 된다" 의 절반은 프로토콜이 아니라 우리 쪽 한 줄이었다**

재기 전의 예상과 실측이 갈린 자리 넷이다:

| 접수 | 예상했던 원인 | 실측한 원인 |
|---|---|---|
| B26 추론이 안 보인다 | 프로토콜이 추론을 안 준다 | **정말 안 준다** — 내용은 없고 토큰만. 원본도 시간만 말한다 |
| B22 출력이 한 덩이 | 프로토콜이 경계를 안 준다 | **주고 있었다** — 우리가 지웠다 |
| B28 이미지가 없다 | 프로토콜이 이미지를 안 받는다 | **받는다** — `content` 를 문자열로만 보내던 한 줄이 막고 있었다 |
| B11 `/model` 이 안 된다 | TUI 전용이라 프로토콜에 없다 | **정상 응답한다** — `terminal_slash_commands` 에도 없다. 없는 것은 **고를 자리** |

**넷 중 셋이 우리 쪽이었다.** "에이전트가 안 해 준다" 는 진단은 **재기 전에는 가설**이고,
그 가설은 고칠 자리를 우리 코드 밖으로 옮겨 놓는다 — 가장 비싼 오진이다.

### 2-13. **없는 데이터는 원본을 옮기는 대상이 아니다**

`D-M11-3`(원본이 진실)과 `FR-CBG-5`(모른다 ≠ 값이다)가 두 자리에서 부딪혔다 — 원본이
보이는 것을 **프로토콜이 주지 않는** 추론 내용과 diff 의 줄번호다.

**고른 것은 뒤쪽이다** (`D-M11-4`): 옮기는 것은 **모양**이지 없는 데이터가 아니다. 추론은
`추론 2.4s · 250 토큰` 으로, diff 는 줄번호 없는 `±` 로 같은 뜻을 말한다. 지어냈다면
화면은 원본과 더 닮았겠지만 **그 수는 거짓**이 된다.

### 2-14. 계측은 **되돌아선 줄**까지 가리킨다 — 원인 후보를 지우는 데 세 번이면 된다

B10 의 인계 메모에는 원인 후보가 둘 적혀 있었다(*"계기가 없다"* · *"첫 마운트에 term 이
없다"*). 세 번의 계측이 **둘 다 지웠다**:

| 잰 것 | 답 | 지운 후보 |
|---|---|---|
| 새로고침 뒤 `/api/agent/session` 요청 수 | **0건** | — (증상 확정) |
| `_mountTabBody` 의 갈래 | `moved=true · term=true` | 둘 다 |
| `refreshLift` 가 본 것 | `isConnected === false` | 남은 것이 답 |

**증상만 재고 멈췄다면** 계기를 하나 더 다는 고침을 했을 것이다 — 계기는 이미 있었고
그 안에서 되돌아섰다. 마지막 한 줄까지 재는 값이 그것이다.

### 2-15. 무력화 프로브가 **재는 대상이 틀린 검사**를 다시 잡았다

큐잉 검사 다섯 중 `V-M11-60`(*"턴이 끝나면 큐의 맨 앞이 나간다"*)이 **큐잉을 꺼도
초록**이었다. 큐 없이 곧바로 보내도 "말풍선이 생긴다" 는 참이기 때문이다.

고친 것은 구현이 아니라 **검사**다 — 보낸 직후 **큐에 서 있고 대화에는 없다**를 먼저
단언한다. §2-3 과 같은 교훈이고, 이번에는 **프로브가 먼저 알려 주었다.**

### 2-16. **"구현 끝" 은 판단이고, 감사는 그 판단을 검사한다**

열 접수를 구현하고 검사를 열다섯 세운 뒤 *"끝났다"* 고 보고했다. 사용자가 되물었다 —
*"결함이있으면 다 고치고해야지."* 전량을 멈추고 코드를 처음부터 다시 훑자 **다섯이
나왔다**:

| 결함 | 왜 부분 실행이 지나쳤나 |
|---|---|
| `/config` 가 **마지막에 만진 키 하나만** 보낸다 | 검사가 키 하나만 골랐다 |
| **고르는 화면이 큐를 건너뛴다** | 큐 검사는 입력창 경로만 밟았다 |
| **재생의 추론 시간이 `0.0초`** | 살아 있는 턴만 쟀다 |
| 큐에 쌓인 `/config` 가 **남의 답을 가로챈다** | 큐와 `/config` 를 따로 쟀다 |
| 본문 상한 1 MiB 라 **이미지가 413** | 8×8 테스트 이미지로만 쟀다 |

**공통점이 하나다 — 전부 두 기능이 만나는 자리다.** 큐×고르는 화면, 큐×첨부,
큐×`/config`, 재생×추론, 이미지×본문 상한. 기능마다 검사를 쓰면 그 교차는 아무도
재지 않는다. 그리고 **내가 쓴 검사는 내가 생각한 경로만 밟는다** — 같은 사람이 짠
구현과 검사는 같은 사각을 공유한다.

**그래서 구현 뒤의 감사는 검사와 다른 일이다.** 검사는 *생각한 것이 되는가* 를 묻고,
감사는 *생각하지 않은 것이 있는가* 를 묻는다.

### 2-17. 무력화 프로브가 **검사의 결함**을 두 번 잡았다

RED 확인은 고침이 맞는지를 보는 절차인데, 이번엔 **검사가 틀린 것**을 두 번 냈다:

| 검사 | 고침을 꺼도 초록이었던 이유 | 바꾼 것 |
|---|---|---|
| `V-M11-60` (턴 끝나면 큐가 나간다) | 큐 없이 곧바로 보내도 "말풍선이 생긴다" 는 참 | **먼저 큐에 서고 대화에는 없다**를 단언 |
| `V-M11-62b` (표식 지우면 첨부도 안 간다) | 본문 글자는 첨부가 실리든 말든 같다 | 화면 대신 **나가는 요청 본문**을 잰다 |

둘 다 §2-3 의 반복이다. 다른 점은 **이번엔 프로브가 먼저 알려 주었다는 것**이다 —
RED 를 "형식적으로" 돌리지 않고 실제로 보면, 그 한 번이 검사를 검사한다.

### 2-18. 상한은 **전제와 함께** 읽는다

프롬프트 본문이 1 MiB 에서 막혔다. 그 값의 주석이 근거를 적어 두었다 — *"이 앱의 JSON
종단이 다루는 것보다 한참 크다 — 설정·워크스페이스·명령이 전부 킬로바이트 단위다."*

**이미지가 실리는 프롬프트는 그 전제 밖이다.** 그래서 고친 것은 값이 아니라 **적용
범위**다: 이 종단만 8 MiB 를 쓰고 나머지 열일곱은 그대로 둔다. 전역으로 올렸다면
*"메모리를 요청이 정한다"* 를 막던 조항(`FR-RQG-14`)이 열일곱 자리에서 함께 느슨해진다.

상한을 만날 때 물을 것은 *"얼마가 맞나"* 가 아니라 **"그 값이 선 전제가 지금도 참인가"** 다.

### 2-19. **쓰면서 나온 접수는 종류가 다르다**

세 번째 물결까지는 사용자가 **화면을 보고** 냈다(색·자리·글꼴). 네 번째 물결은 GUI 로
**실제로 일하면서** 냈고, 그러자 **절반이 손에 관한 것**이 되었다 — 포커스·키·스크롤.

그리고 그 절반이 **앞선 결정 둘을 뒤집었다** (`D-M11-7`):

| 뒤집힌 것 | 그때의 근거 | 써 보니 |
|---|---|---|
| `FR-APS-6` *"Esc 는 답이 아니다"* | 실수로 닫은 것을 되돌릴 수 있어야 | **되돌릴 길보다 멈출 길이 급했다** |
| `D-M9-20` *"터미널 탭은 남는다"* | 올린 뒤에도 셸로 돌아갈 수 있어야 | 같은 세션이 **두 자리**에 보인다 |

둘 다 그때는 옳은 추론이었다. 바뀐 것은 근거가 아니라 **무게**이고, 그것은 **써 봐야
안다.** 그러므로 *"앞서 정했다"* 는 그 자체로 논거가 아니다 — 다만 뒤집을 때는 뒤집었다고
적는다.

### 2-20. **"안 된다" 의 원인은 동작이 아니라 상태일 수 있다**

접수는 *"뭔가 특정 동작을 하는중에 interrupt 가 안되는거같아"* 였다. 나는 *어떤 동작*을
찾기 시작했다 — 도구 실행 중인가, 승인 대기인가, 추론 중인가.

**네 경로를 계측하니 동작은 무관했다.** 갈린 것은 **포커스**였다:

| 경로 | interrupt 요청 |
|---|---|
| 입력창에 포커스 + `Esc` | 1건 — 된다 |
| **대화를 클릭한 뒤** `Esc` | **0건** |

핸들러가 `textarea` 하나에만 걸려 있었고, 도구가 도는 동안 화면을 보려 대화를 클릭하면
그때부터 키가 닿지 않는다. 사용자가 *"특정 동작을 하는중에"* 라고 느낀 것은 **그 사이에
화면을 만졌기 때문**이다.

**접수의 말을 가설로 굳히지 않는 것이 요점이다.** 사용자는 증상이 일어난 **때**를 말하지
원인을 말하지 않는다.

### 2-21. 사용자의 결정이 **그대로는 이뤄지지 않을 때**

*"모달을 닫으면 interrupt"* 를 고르게 하고 그대로 구현했더니 — 실측 ④ — **열린 요청이
남고 상태가 `waiting` 그대로**였다. claude 에는 *답 없이 닫는 프레임*이 없다
(`claudeProto.Cancel` 이 `nil` 인 그 사실이 이미 적혀 있었다).

그래서 **거절을 먼저 보내고 이어서 끊는다.** 결정을 바꾼 것이 아니라 그 결정이 이루려던
것(*무한정 기다리지 않는다*)이 성립하는 **유일한 경로**다.

**고르게 한 뒤에도 재야 한다** — 고른 것이 실제로 그 일을 하는지는 다른 물음이다.

### 2-22. 내가 만든 결함 넷을 내가 잡았다 — **전부 기존 규약과 부딪힌 자리**

| 결함 | 부딪힌 규약 |
|---|---|
| 거절이 답보다 **먼저** 나갔다 | `UIKit.modal` 이 `close()` 를 `onClick` **앞에** 부른다 |
| 질문 마법사에 **마우스 길이 없었다** | 내가 스펙에 적은 *"키는 더하는 것이지 대신하는 것이 아니다"* |
| `/model` 에 엔터를 **두 번** | 드롭다운이 엔터를 먼저 먹는다 |
| 올린 뒤 **엉뚱한 탭**이 활성 | `closeTab` 이 마지막에 활성을 다시 고른다 |

**넷 다 새 기능이 기존 손과 만나는 자리**다. 그 자리는 기능마다 쓰는 검사로는 잡히지
않는다 — §2-16 이 적은 *"두 기능이 만나는 자리"* 와 같은 말이고, 이번엔 **구현 중에**
그것을 알고 찾았다.

### 2-23. `git checkout <파일>` 로 한 시간을 잃었다

프로브를 되돌리려고 `git checkout web/style.css` 를 불렀다. 그 파일에는 **이번 물결의
CSS 가 전부 들어 있었다** — 서브에이전트·바닥 버튼·드롭다운·큐·마법사·하단 색까지
한 번에 사라졌다.

**프로브는 프로브로 되돌린다** (`cp` 백업 또는 같은 치환의 역). `git checkout` 은
*그 파일에 다른 변경이 없을 때만* 되돌리기이고, 작업 중에는 거의 언제나 있다.

## 3. 실측 방법

### 3-1. 버퍼인가 렌더인가는 **한 번에** 가른다

```js
const b = p.term.buffer.active;
const buf = [...]; // b.getLine(i).translateToString(true)
const dom = [...p.el.querySelector('.xterm-rows').children].map(r => r.textContent);
```

여덟 배치 전부에서 둘이 **완전히 일치**했다. 그것이 "렌더가 낡았다" 갈래를 지운 근거다.
증상이 없는 배치에서도 이 비교는 값을 낸다 — **일치가 쌓이는 것 자체가 증거다.**

### 3-2. 재생이 수렴하는지는 **창을 잘라** 재면 된다

같은 tail 을 ① 전체 ② 뒤 절반으로 각각 `termReset`+`termHardClear`+`alignHead` 해서
두 xterm 에 넣고 화면을 비교한다. **글자 하나까지 같았다** — 재생 창의 시작점이 달라도
전량 재생은 수렴한다. 이 한 번이 FR-M10-2 경로를 용의선상에서 지웠다.

### 3-3. TUI 가 칸을 **건너뛰는지**는 와이어가 답한다

```sh
dmctl read-output --at <탭 uuid> --bytes 400000 | cat -v | head
```

`^[[2C`(커서 전진) · `^[[19G`(절대 열)가 보이면 그 TUI 는 **공백 칸을 쓰지 않고 지나간다.**
그러면 모형과 버퍼가 어긋나는 순간 잔재가 **정확히 공백 자리에** 앉는다. 접수한 화면의
규칙이 그것이었고, 이 명령 한 줄이 그 성질을 확정했다.

`LC_ALL=C grep -c '1049'` 로 대체 화면 여부도 같은 바이트에서 답한다 — 0 이면 주 화면이다.

### 3-4. 서버만 재시작해 **소켓이 없는 구간**을 만든다

```sh
dongminal stop --port <n> --home <홈>     # --all 이 없으면 dongminald·세션은 산다
```

도구는 데몬이 들고 있으므로 살아남고, 브라우저의 WS 만 끊긴다. 재접속 대기 구간에서
일어나는 일을 재는 유일한 손이다. (§2-2 의 함정도 여기서 나왔다 — 조건은 만들어지지만
그 경로가 고치려는 코드를 지나는지는 따로 확인해야 한다.)

## 4. 접수와 남은 것

### 접수의 상태 (M11-B10~B36)

| ID | 접수 | 상태 |
|---|---|---|
| **B13** | 첫 프롬프트 전에는 하단이 안 뜬다 (**결정 번복**) | **끝** — FR-M11-16 · V-M11-32·33·34 |
| **B12·B16** | 하단 글씨가 너무 작다 → *"터미널 기본값과 같은 상수"* | **끝** — FR-M11-15 · V-M11-35 |
| **B17·B30** | 게이지는 전부이거나 전무 → 주기 옆에 그 게이지 | **끝** — FR-M11-19 · V-M11-36·37·38 |
| **B14** | 다른 곳에 다녀오면 스크롤이 맨 위로 | **끝** — FR-M11-17 · V-M11-51 |
| **B18** | 항목 구분이 안 간다 | **끝** — FR-M11-20 (원본의 `│` 를 테두리로) |
| **B19** | 입력창이 고정이고 작다 | **끝** — FR-M11-21 · V-M11-44 |
| **B20** | `Cmd+좌우` 가 안 먹힌다 | **끝** — FR-M11-22 · V-M11-45 |
| **B24** | 새 글마다 스크롤이 내려간다 | **끝** — FR-M11-26 · V-M11-48 |
| **B31·B32·B33** | 비용 제거 · 저장소 *알 수 없음* · 현재 경로 | **끝** — FR-M11-33·34·35 · V-M11-41 |
| **B34** | 에이전트 내용이 선택되지 않는다 | **끝** — FR-M11-36 · V-M11-42 |
| **B21** | 출력을 md 로 | **끝** — FR-M11-23 · V-M11-46 (원문 토글 포함) |
| **B25** | 접힌 출력에 요약 | **끝** — FR-M11-27 · V-M11-49·49b |
| **B10** | 새로고침하면 '에이전트로' 가 사라진다 | **끝** — FR-M11-13 · V-M11-30 (`isConnected` 가 *아직 붙지 않았다* 를 *사라졌다* 로 읽었다) |
| **B11** | `/model`·`/config` TUI 가 안 된다 | **끝** — FR-M11-14 · V-M11-65·66 (막힌 것은 명령이 아니라 **고를 자리**였다) |
| **B22** | 출력이 한 덩이 | **끝** — FR-M11-24 · V-M11-52 (경계는 오고 있었고 우리가 지웠다) |
| **B23** | 시스템 텍스트가 섞여 나온다 | **끝** — FR-M11-25 · V-M11-55 (전수 실측한 여섯 종을 **첫 토큰**으로) |
| **B25** | 접힌 출력의 요약 | **개정** — D-M11-5 · V-M11-56 (앞뒤 2줄 → **한 문장**) |
| **B26** | 추론 내용도 보이면 좋겠다 | **끝** — FR-M11-28 · V-M11-53·54 (claude 는 **내용을 주지 않는다** — 시간·토큰) |
| **B15·B27·B35** | 턴 중 입력 · 연속 프롬프트 · 큐 취소 | **끝** — FR-M11-29 · V-M11-57~60 |
| **B28** | 이미지 첨부 | **끝** — FR-M11-30 · V-M11-61·62 (프로토콜은 받고 있었다) |
| **B29** | 질문 모달의 자유 입력 | **끝** — FR-M11-31 · V-M11-63 (원본은 **목록의 한 항목**) |
| **B36** | 실시간 diff | **끝** — FR-M11-37 · V-M11-64 (재료는 **도구 입력**) |

**세 번째 물결의 접수 열아홉이 전부 닫혔다.**

### 네 번째 물결 (M11-B37~B52) — 사용자가 GUI 로 **직접 쓰며** 낸 접수

| ID | 접수 | 상태 |
|---|---|---|
| **B37** | diff 는 펼친 채 | **끝** — FR-M11-38 · V-M11-70 (편집 도구만) |
| **B38·B39** | 엔터로 보내면 바닥 · 바닥 버튼 | **끝** — FR-M11-39 · V-M11-74 |
| **B40** | 고르는 화면일 때 출력 감춤 | **끝** — FR-M11-40 · V-M11-75 (**연 뒤에** 감춘다) |
| **B41** | 모달 닫으면 무한 대기 | **끝** — FR-M11-41 · V-M11-77 (**거절 후 끊기**) |
| **B42·B47** | 특정 동작 중 interrupt 안 됨 | **끝** — FR-M11-42 · V-M11-76 (원인은 **포커스**였다) |
| **B43** | 올리기가 탭 이름·원래 탭 | **끝** — FR-M11-43 · V-M9-33/36 개정 |
| **B44** | `Ctrl+C` 로 나가기 | **끝** — FR-M11-44 (선택이 있으면 **복사**가 먼저) |
| **B45** | 질문 모달 TUI화 | **끝** — FR-M11-45 · V-M11-63 개정 (+ 마우스 이전/다음) |
| **B46** | 하단 글꼴·색 | **끝** — FR-M11-46 · V-M11-79 |
| **B48** | 탭 열면 입력창 포커스 | **끝** — FR-M11-47 · V-M11-78 |
| **B49** | 서브에이전트 모아보기 | **끝** — FR-M11-49 · V-M11-71 (`parent_tool_use_id`) |
| **B50** | 백그라운드 표시 | **끝** — FR-M11-50 · V-M11-72 |
| **B51** | 슬래시 드롭다운·검색 | **끝** — FR-M11-48 · V-M11-73 |
| **B52** | 도는 중 애니메이션 | **끝** — FR-M11-51 · V-M11-80·80b·80c |

**M11 의 접수 쉰둘이 전부 닫혔다** — 열려 있는 것은 `M11-B1` 본체 하나뿐이다.

### 그 밖

- **M11-B1 본체.** 접수한 화면을 재현하지 못했다. 닫은 것은 홀 하나다
- **FR-STA-4 사다리 2단계** · **M4 인증 계열** — 종전대로 의도적 보류·회피

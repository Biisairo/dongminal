# SRS: 전경 이름 복원이 새 이름을 지우는 경쟁 — IEEE 29148

## 1. 개요

### 1.1 목적

`tab-names.spec.ts` 가 흔들린다. 실행마다 다른 케이스가 같은 증상으로 실패한다 —
`pushForeground` 로 넣은 이름이 라벨에 도달하지 않고 `Shell` 로 남는다.

근본 문제는 검사가 성급하다는 것이 아니라 — **`_fgRestore` 가 자기 응답보다 나중에
일어난 갱신을 지운다는 것**이다. 그리고 이 결함은 **테스트만의 것이 아니다.**

```js
// app-cmd.js — 복원은 SSE 가 열리는 바로 그 순간에 시작된다
es.onopen = () => { … this._fgRestore() };

_fgApply(tools){
  …
  for(const id of Array.from(m.keys())){   // ← 응답이 **도착한 시점**의 Map
    if(!seen.has(id)){ m.delete(id); changed=true }
  }
}
```

요청이 떠난 뒤 응답이 오기까지 사이에 SSE 로 `tool_foreground` 가 도착하면, 그
도구는 서버 스냅숏(`seen`)에 없으므로 **태어나자마자 지워진다.**

### 1.2 이 규약은 이미 이 파일에 적혀 있다

`_fgRestore` 의 주석은 스스로 이렇게 말한다:

> 합류/재연결 시의 스냅샷 복원 (**`_attnRestore` 와 같은 규약**).

그런데 `_attnRestore` 는 그 규약을 지키고 있고, 사유까지 적어 뒀다:

```js
/**
 * **지울 후보는 요청을 떠나기 전에 확정한다.** 응답은 요청 시점의 서버 상태이고,
 * 그 사이 SSE 로 새 알람이 올라올 수 있다 — 이 함수는 SSE 가 열리는 바로 그
 * 순간에 불린다(`es.onopen`). 응답이 도착한 시점의 집합을 지우면 그 새 알람이
 * 태어나자마자 사라진다.
 */
_attnRestore(){
  const before = new Set(this._attn.keys());     // ← 요청 전에 확정
  fetch('/api/tools/attention').then(j => {
    …
    for(const pid of before){ if(!live.has(pid)) this._attnDrop(pid) }
  });
}
```

**같은 규약을 선언만 하고 지키지 않았다.** 고치는 것은 새 설계가 아니라 이미 있는
규약을 이쪽에도 적용하는 것이다.

### 1.3 범위

| 묶음 | 내용 | 리스크 |
|---|---|---|
| **A** | `_fgRestore` 가 지울 후보를 요청 전에 확정한다 (프로덕션 결함) | LOW |
| **B** | `tab-names.spec.ts` 가 SSE 뿐 아니라 **스냅숏도 흉내낸다** | LOW |

**묶음 A 만으로는 흔들림이 멎지 않는다.** 조사 순서상 A 를 먼저 찾았고 그것도
실제 결함이지만, 이 스펙이 흔들리던 사유는 B 다 (§7.1).

---

## 2. 현재 동작과 새 동작

| | 현재 | 새 동작 |
|---|---|---|
| 복원 응답이 도착했을 때 지우는 대상 | 응답 도착 시점 Map 의 **전부** 중 스냅숏에 없는 것 | **요청 전에 있던 것** 중 스냅숏에 없는 것 |
| 요청~응답 사이 SSE 로 온 이름 | **지워진다** (결함) | 남는다 |
| 죽은 도구의 이름 | 지워진다 | 지워진다 (변화 없음) |

**사용자가 보는 변화**: 재연결 직후 프로그램을 띄우면 탭 이름이 파생되지 않고
`Shell` 로 남던 것이 없어진다. 그 밖의 동작은 같다.

---

## 3. 상세 요구사항

**FR-FGR-1** `_fgRestore` 는 `fetch` 를 떠나기 전에 현재 Map 의 키를 `before` 로
확정하고, 응답에 그것을 함께 넘긴다.

**FR-FGR-2** `_fgApply(tools, before)` 는 **`before` 에 있던 id 만** 지운다.
`before` 가 주어지지 않으면 종전대로 Map 전부를 훑는다 — 다른 호출자가 생겼을 때
조용히 규약이 바뀌지 않게 한다.

**FR-FGR-3** 서버 스냅숏에 있는 이름을 반영하는 동작(`m.set`/`m.delete`)은 바뀌지
않는다. 이 SRS 가 고치는 것은 **지우는 범위**뿐이다.

### 3.2 묶음 B — 흉내는 두 경로에 일관되게

**FR-FGR-4** `tab-names.spec.ts` 는 `/api/state` 를 가로채, `pushForeground` 가 넣은
이름을 **스냅숏 응답에도 싣는다**.

이 스펙은 결정론을 위해 실제 프로세스를 띄우지 않는다(파일 머리 주석). 그런데
전경 이름의 진실은 **둘**이다 — SSE(변화)와 `/api/state`(스냅숏). SSE 만 흉내내면
복원이 도는 순간 두 진실이 어긋나고, 복원은 **서버를 진실로 삼는 자기 일을 옳게
하면서** 그 이름을 지운다.

**서버를 고치는 것이 아니라 흉내를 두 경로에 일관되게 내는 것이다.**

**FR-FGR-5** 가로채기가 실패해도(페이지가 닫히는 중이거나 요청이 취소되면
`route.fetch`·`route.fulfill` 이 던진다) 검사를 죽이지 않는다. 실패하면 손대지
않고 원래 요청으로 흘려보낸다.

---

## 4. 검증

**TC-FGR-1** `tab-names.spec.ts` 전량을 **10회 연속** 돌려 실패가 없다. 고치기 전
같은 조건에서 흔들리던 것이 근거다 (작업 시작 전 커밋에서 3회 중 1회 실패).

**TC-FGR-2** e2e 전량이 통과한다.

---

## 5. 비목표

| # | 하지 않는 것 | 사유 |
|---|---|---|
| N1 | 검사에 재시도·대기를 더해 덮기 | 이 저장소의 관행이 그것을 금한다 (`946b439 fix(e2e): 흔들리던 두 검사의 사유를 없앤다 — 재시도로 덮지 않고`). 그리고 결함은 실사용에도 있다 |
| N2 | `_activityRestore`·`_bgRefresh`·`_focusRestore` 점검 | 같은 자리(`es.onopen`)에서 불리므로 같은 경쟁이 있을 수 있다. **범위 밖이며 별도 결정이 필요하다** — §7 에 조사 결과를 남긴다 |


---

## 7. 실행 결과 (Outcome)

### 7.1 가설이 두 번 틀렸다 — 그 기록

이 흔들림의 사유를 찾는 데 가설 셋이 필요했다. **앞의 둘은 측정으로 기각됐다.**

| # | 가설 | 어떻게 기각했나 |
|---|---|---|
| 1 | `firstTab` 이 `toolId` 가 붙기 전에 불린다 | 진단을 넣고 6회 돌렸다 — `toolId` 는 언제나 채워져 있었다 |
| 2 | `resetWorkspace` 가 409(stale ETag)로 조용히 실패해 앞 테스트 상태가 남는다 | 응답 상태를 찍었다 — 409 가 한 번도 나지 않았고, reset 60ms 뒤에도 `windows` 는 비어 있었다 |
| 3 | **복원이 SSE 로 넣은 이름을 지운다** | `_fgRepaint` 안에 계측을 넣었다 (§7.2) |

조사 도중 **진단 자체가 오탐을 만든 적도 있다.** `firstTab` 이 `manual` 이면
실패시키게 했더니 V-TAN-4 가 걸렸는데, 그 검사는 **rename 뒤에 `firstTab` 을 다시
부르는 것이 본문**이라 `manual` 이 정상이었다.

### 7.2 확정 근거

`_fgRepaint` 에 계측을 넣고 실패를 포착했다:

```
miss : []      ← querySelector 는 요소를 찾았다. DOM 을 못 고친 것이 아니다
map  : []      ← _fgNames 가 비어 있다. 넣은 이름이 사라졌다
```

`tabName()` 은 `claude` 를 내는데 DOM 은 `Shell` 이던 V-TAN-15 의 실패도 같은
사실의 다른 얼굴이다 — 그 검사는 `expect(computed).toBe('claude')` 를 **통과한 뒤**
라벨에서 실패했다. 즉 규칙은 맞고 Map 이 비었다.

### 7.3 검증

| | 결과 |
|---|---|
| TC-FGR-1 (`tab-names` 10회 연속) | **10 / 10 통과** |
| 고치기 전 같은 조건 | 10회 중 3회 실패 (작업 전 커밋에서도 3회 중 1회) |
| TC-FGR-2 (e2e 전량) | §7.4 |

### 7.4 남은 것

- **비목표 N2** — `_activityRestore`·`_bgRefresh`·`_focusRestore` 도 같은
  `es.onopen` 에서 불린다. 묶음 A 와 같은 경쟁이 있는지 **확인하지 않았다.**
  `_attnRestore` 만 규약을 지키고 있음이 확인됐고, 나머지 셋은 별도 결정이 필요하다.

---

## 8. §7.4 의 미조사 항목을 닫는다 — 조사 결과

§7.4 는 `_activityRestore`·`_bgRefresh`·`_focusRestore` 에 묶음 A 와 **같은 경쟁이
있는지 확인하지 않았다**고 남겼다. 조사했다. 결론부터: **하나는 있고 둘은 없다.
그리고 묶음 A 의 수정이 한 방향만 막았다는 것이 함께 드러났다.**

### 8.1 판정 기준 — 이 결함이 성립하는 조건

묶음 A 의 결함은 "fetch 를 쓴다"가 아니라 **둘이 동시에 참일 때** 생긴다:

1. 그 상태를 **SSE 가 증분으로** 갱신한다 (`set`/`delete`)
2. restore 가 서버 스냅숏과 대조해 **로컬에만 있는 것을 지운다**

`window_focus` 핸들러의 주석이 이 구분을 이미 말하고 있다 —
"전체 소유권 맵이 온다. **증분이 아니므로** 통째로 갈아치우면 되고".

### 8.2 다섯 자리의 판정

| 함수 | 상태 | SSE 가 증분 갱신? | restore 의 방식 | 판정 |
|---|---|---|---|---|
| `_attnRestore` | `_attn` | **예** (`_onToolAttention`) | `before` 확정 후 차분 | 규약 지킴 |
| `_fgRestore` | `_fgNames` | **예** (`_onToolForeground`) | `before` 확정 후 차분 | 묶음 A 에서 고침 |
| **`_activityRestore`** | `_activity` | **예** (`_onToolActivity`) | **응답 도착 후 `clear()`** | **결함** |
| `_bgRefresh` | `_bg` | 아니오 — SSE 가 `_bgRefresh()` 재호출 | 통째 교체 | 없음 |
| `_focusRestore` | `_windowFocusOwner` | 아니오 — 맵 전체가 온다 | 통째 교체 | 없음 |

`_bg` 와 `_windowFocusOwner` 는 **쓰는 자리가 각각 하나뿐**이다(생성자 초기화 제외).
증분 갱신 경로가 없으므로 "지울 후보" 라는 개념 자체가 성립하지 않는다.

### 8.3 `_activityRestore` — 확정 재현

```js
_activityRestore(){
  fetch('/api/tools/activity').then(...).then(j=>{
    this._activity.clear();          // ← 응답이 도착한 시점에 전부 비운다
    for(const a of j.activities) this._activity.set(...);
  });
}
```

묶음 A 보다 **넓다.** `_fgApply` 는 "스냅숏에 없는 것"만 지웠는데, 이쪽은 전부
지우고 스냅숏으로 다시 채운다.

스냅숏 내용을 위조해 두 방향을 결정적으로 재현했다:

| 방향 | 시나리오 | 결과 |
|---|---|---|
| **A** | 요청 중 `tool_activity` 도착 → 스냅숏에 없음 | `넣은직후:true → 응답후:false` **지워진다** |
| **B** | 요청 중 `ended` 도착 → 낡은 스냅숏에는 살아 있음 | `지운직후:false → 응답후:true` **되살아난다** |

**노출은 재연결 순간에 그치지 않는다.** `_activityRestore` 는 `es.onopen` 말고도
`_agentsStartPoll` 이 **`agentsPollMs`(기본 5,000ms)마다** 부르고, `.ag-refresh`
클릭과 `_softReload` 도 부른다. 패널이 열려 있는 동안 창이 5초마다 열린다.

다만 증상은 영구적이지 않다 — 폴링이 돌면 다음 주기에 스스로 낫는다. 묶음 A 의
탭 이름(다시 부를 것이 없어 영구했다)과 다른 점이다.

### 8.4 함께 드러난 것 — 묶음 A 의 수정은 **한 방향만** 막았다

`before` 규약은 "지울 후보"만 좁힌다. **스냅숏에서 되살리는 쪽은 그대로다.**
`_fgApply` 는 목록의 `fgName` 을 무조건 `m.set` 하므로, 요청 중에 이름이 지워지면
낡은 스냅숏이 되살린다. 재현했다:

```
FG-B {"지운직후":null,"응답후":"vim"}
```

`_attnRestore` 도 같다 — `for(const pid of live){if(!this._attn.has(pid))this._attn.set(...)}`
가 요청 중에 해제된 알람을 되살린다.

즉 **방향 B 는 세 함수 모두에 남아 있는 계통적 잔여**이고, 이 SRS 의 §1.2 가 인용한
규약이 애초에 한 방향만 다뤘다.

### 8.5 남은 결정

고치는 것은 이 SRS 의 범위가 아니다. 방향 B 를 막으려면 "요청 중에 도착한 갱신은
스냅숏보다 새롭다" 를 판정할 근거가 필요한데, 지금 세 함수 어디에도 그 근거(도착
시각·seq)가 없다. **설계 결정이 필요하므로 별도 SRS 를 세워야 한다.**

`_activityRestore` 의 방향 A 만 먼저 막는 것은 `before` 규약의 단순 적용이라 값이
싸다 — 다만 `clear()` 를 차분으로 바꾸면 신규 id 의 삽입 순서가 달라지므로
(`_agentOrderSync` 가 `ws.agentsOrder` 에 없는 id 를 Map 순서로 덧붙인다) 그
영향까지 스펙에 적어야 한다. **묶음 A 처럼 규약을 그대로 옮기는 일이 아니다.**

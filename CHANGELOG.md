# 변경 이력

형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/) 를 따르고,
판 번호는 [유의적 버전](https://semver.org/lang/ko/) 을 따릅니다.

## [미배포]

### 추가

- **Changes 탭의 두 칸을 끌어 나눕니다.** 파일 목록과 미리보기 사이에 손잡이가
  섰습니다. 가로·세로 값을 따로 기억하므로 데스크톱에서 정한 폭이 모바일의
  높이가 되지 않습니다
- **Diff 에 개요 눈금이 섭니다.** VSCode 처럼 스크롤바에 변경 위치가 색으로
  찍힙니다. 변경 없는 구간의 접기는 **기본이 꺼짐**입니다 — 접으면 눈금이 접힌
  좌표계 위에 서서 실제 파일의 줄 위치와 어긋나기 때문입니다. 필요하면 Diff
  머리의 `Fold Unchanged` 로 켭니다
- **Editor 에서 파일을 찾습니다.** `cmd+p` 파일 이름, `cmd+shift+f` 전체 내용.
  `cmd+f`(현재 파일)는 Monaco 의 것을 그대로 씁니다. 전체 검색은 ripgrep 이
  있으면 그것을, 없으면 자체 구현으로 훑고 어느 쪽을 썼는지 함께 보입니다
- **이미지를 봅니다.** 편집기 탭에서 PNG·JPEG 등이 그대로 보입니다
- **열 수 없는 형식을 알립니다.** 이진 파일은 열지 않고 사유를 보입니다.
  종전에는 깨진 글자로 열렸고, 그것을 저장하면 원본이 파괴됐습니다. 판정은
  확장자가 아니라 내용으로 합니다

### 고침

- **원격 접속이 한 번씩 끊기던 것.** 사라진 도구를 가리키는 터미널 패널이 지연 0
  으로 무한히 재접속했습니다 — 실측 초당 95연결, TIME_WAIT 2,881개. macOS 의 임시
  포트(16,384개)가 마르면 Tailscale 경로가 먼저 죽고, 루프백을 쓰는 `health` 는
  통과해 "서버는 정상인데 붙지 않는" 모양이 됐습니다. 세 겹으로 막았습니다:
  도구 종료 통보를 **종단으로 처리**하고, 재연결 백오프를 연결이 3초 유지된 뒤에만
  되돌리고, 커맨드 SSE 가 **더 이상 포기하지 않게** 했습니다. SSE 가 20회 실패로
  영구히 포기하면 죽은 도구를 거두는 자가 치유가 영영 돌지 않는다는 것이
  마지막 고리였습니다
- **서버가 되풀이되는 미스 요청을 늦춥니다.** 배포된 클라이언트 수정은 이미 열려
  있는 탭에 닿지 않으므로, 없는 도구를 창 안에 다시 부르는 연결만 2초 늦춥니다.
  첫 요청은 면제되어 정상 경로는 그대로입니다
- **`dmctl: No such file or directory` 훅 오류.** 헬퍼 설치가 `지우기 → 다시 걸기`
  순서라 그 사이에 경로가 비었습니다(실측 300회 재설치 중 소실 5,037회). 임시
  이름에 만든 뒤 `rename` 으로 덮도록 바꿔 목적 경로가 한 순간도 비지 않습니다

## [1.0.0] — 2026-08-30

첫 정식 배포입니다. 이제 [Releases](https://github.com/Biisairo/dongminal/releases)
에서 자기 OS 바이너리 하나를 받아 바로 실행할 수 있습니다 — Go 도 다른 의존도
필요 없습니다.

### 추가

- **바이너리 배포.** 태그 `v*` 를 밀면 다섯 대상(macOS arm64·amd64, Linux
  amd64·arm64, Windows amd64)이 빌드되어 릴리스에 첨부됩니다. `SHA256SUMS` 동봉
- **`dongminal version`** (`--version`) — 판·대상·go 런타임을 찍습니다. 릴리스
  산출물이 아니면 `dev` 로 나옵니다
- **Windows 10 1809+ 네이티브 지원.** ConPTY 로 PTY 의미론을 얻습니다. WSL 은
  `linux-amd64` 를 그대로 씁니다
- **`dongminal doctor`** — 플랫폼 계층을 계층별로 실제 실행하는 진단. 터미널이
  뜨지 않을 때 어느 계층에서 막혔는지 그대로 보여 줍니다
- **`dongminal verify`** — 격리 인스턴스를 띄워 종단간 22항목을 훑는 검증.
  세 OS 가 같은 목록을 돕니다 (개발·CI 용)
- 파일 전송

### 변경

- **OS 이음매를 인터페이스 뒤로.** PTY, 프로세스 제어·그룹·조회, 셸 선택과 훅,
  로컬 IPC, 경로 규약, 브라우저 실행이 `internal/shared/platform` 안으로 들어갔고
  그 패키지 밖에는 OS 분기가 한 줄도 없습니다
- git 기록의 자격증명 마스킹이 `https://user:***@h` 에서 `https://***@h` 로
  바뀌었습니다. `user` 자리가 토큰인 형태가 흔하고 구분할 수 없기 때문입니다
- `a/b` 같은 원격 이름을 `fetch`·`tag` 에서도 실행 전에 거부합니다. 종전에는
  엄격한 검사가 `push` 에만 걸려 있었습니다
- `stop` 이 대상을 죽이지 못하면 실패로 보고하고 pidfile 을 보존합니다. 살아 있는
  데몬의 pidfile 을 지우면 고아가 되기 때문입니다

### 수정

- **Windows 에서 터미널이 비어 있던 결함.** `CreateProcess` 에
  `STARTF_USESTDHANDLES` 를 세우지 않아 자식이 부모 콘솔을 물려받았고, 의사
  콘솔이 아니라 그쪽에 글자를 그리고 있었습니다
- Windows 에서 파일 전송이 통째로 막혀 있던 결함
- 경로 구분자·줄 끝 전제에서 비롯된 결함 다수
- 셸이 스스로 끝나도 탭이 닫히지 않던 결함
- `migrate` 가 대상이 없는데도 `settings.json` 을 재작성하던 결함
- **붙여넣기 감싸기를 셸에게 물어보지 않던 결함.** API·에이전트 입력이 언제나
  bracketed paste 로 감싸여 나가, 그 모드를 모르는 셸(macOS 가 싣는 bash 3.2)에서는
  마커가 명령줄에 글자로 들어가 `bash: 200~echo: command not found` 가 됐다.
  에이전트 간 메시지와 API 입력 주입이 그 셸에서 통째로 깨져 있었다. 이제 셸이
  그 모드를 켰을 때만 감싼다

[1.0.0]: https://github.com/Biisairo/dongminal/releases/tag/v1.0.0

# 개발자 문서

Dongminal 컨트리뷰터·유지보수자 대상 문서.

디렉터리 규칙은 하나다 — **이 폴더는 지금 읽어야 하는 문서, `archive/` 는 완료된
작업의 기록**이다. 보관 문서는 당시 어휘와 당시 코드 위치를 그대로 담고 있으므로
**갱신하지 않는다.** 지금의 사실은 `architecture.md` 와 코드가 답한다.

## 현행

| 문서 | 내용 |
|------|------|
| [WORDING_COLOR_SRS.md](./WORDING_COLOR_SRS.md) | **말을 고치고 색을 테마 안으로 되돌린다** (IEEE 29148, `FR-WRD-*`). 묶음 B4 — B2·B3 이 **모양**을 한 벌로 만든 뒤 그 안에 든 **말**과, 테마 밖으로 샌 **색 25건**이 남았다. **§2.1 이 이 문서의 뿌리다** — 감사도 인계서도 표본이었고(보이는 글자 20→**18 + 선언된 영어 15** · 해라체 2→**17** · `t()` 건너뛴 자리 8→**13**) 이 저장소에서 같은 모양이 **여섯 번째**다; 그래서 수는 전부 파싱으로 세고 세는 방법을 절마다 적는다 (FR-WRD-2). **D-WRD-2·3·5 가 감사의 제안을 셋 깎는다** — 설정 탭 12는 `lang="en"` 으로 **선언된 영어**이고(FR-B-5), `title` 은 영어가 요구이며(FR-TIP-2), `Background`·`Runs`·`Split H/V` 는 **다른 SRS 가 글자로 못박았다**(FR-SBR-11 은 `Background 2` 까지 적었고 e2e 가 단정한다): 구조(`data-i18n`+키)만 세우고 ko 값은 영어로 둔다 — 낱말 결정이 오면 카탈로그 한 줄이다 (FR-B-9 가 설계한 사용법). **색은 25건 전부**다 (사용자 결정 2026-09-20) — A등급 `--git-st-add` 는 라이트 11종에서 1.26~1.83:1 이라 실사용 화면이 깨지고, 고칠 자리는 한 줄이며 선례가 같은 저장소에 있다(`.run-view{--run-ok:var(--term-green)}` 의 주석이 이미 이 토큰을 이름으로 든다). **D-WRD-6 은 패턴 A 를 피한다** — 조사를 호출부에서 문장 두 벌로 고르면 한쪽만 고쳐진다; 마커(`{을를}`)로 문장을 하나로 두고 치환 경로 둘을 늘리지 않는다. **H 가 FR-SAF-21 의 빚을 닫는다** — 서버는 `OutputTruncated` 를 싣는데 `web/` 에서 그것을 읽는 자리가 **0**이라 배지가 하한을 정확한 수처럼 그린다 |
| [KIT_COMPONENTS_SRS.md](./KIT_COMPONENTS_SRS.md) | **킷에 없는 것을 세우고 옮긴다** (IEEE 29148, `FR-CMP-*`). 묶음 B3 — `KIT_APPLICATION_SRS`(B2)와 **성격이 정반대다**: B2 는 *"세워 놓고 안 옮겼다"* 였고 여기는 *"세운 적이 없다"* 다. §1.1 의 표가 요체 — 불리언 설정이 **세 모양**(맨 체크박스·알약 토글·체크박스+글자), `text-transform:uppercase` **9규칙**, 개수 표기 **넷**, 인라인 알림 **12규칙**, 빈 상태 **13개**이고 그중 누를 수 있는 것이 **하나도 없다**. **§2.1 이 이 문서의 뿌리다** — 감사가 적은 "빈 상태 다섯"·"머리글 여섯 자리" 는 그때 **화면에 있던 표본**이었고 실측은 13·9 다; 이 저장소에서 같은 모양이 **다섯 번째**이므로(§7.6 잔여표 · FR-A11Y-28 · FR-A11Y-16 · 버튼 "6종 98개") 대상을 열거하지 않고 **파싱으로 파생한다**(FR-CMP-2). **D-CMP-1 이 가장 비싼 결정** — `.ui-switch` 는 **껍데기만** 바꾸고 DOM 은 `<input type=checkbox>` 로 남긴다: 체크박스는 `aria-checked`·폼 의미론·키 조작을 공짜로 주고, `_settingsA11y` 가 이름을 **구조에서 파생**하므로(FR-A11Y-13) 요소 종류를 바꾸면 그 파생이 흔들린다. 대가는 `.ds-switch` 한 자리가 예외로 남는 것이다. **D-CMP-2 는 경계다** — 대문자를 걷는 것은 외형(여기)이고 낱말을 고치는 것은 카탈로그(B4)다: 섞으면 빨개졌을 때 어느 쪽인지 말할 수 없다. **D-CMP-3 은 범위를 지킨다** — 감사가 제안한 빈 상태 주동작 중 *"에이전트 안내 보기"* 는 **그런 화면이 없다**; 만들면 컴포넌트 작업이 기능 작업이 된다. **구현 완료** — 실측이 감사를 또 정정했다(빈 상태 5→**13** · 머리글 6자리→**9규칙** · 키캡이 둘이었다). **FR-CMP-85 와 빈 상태의 주동작은 이 묶음이 아니다**: 전자는 `<span>` 이라 이미 상태이고 혼동의 원인이 **낱말**(`로드`)이라 B4, 후자는 가리킬 것이 없다 — 편집기는 새 배선이고 Runs 는 공용 클립보드 헬퍼가 없어 **같은 수법의 세 번째 복사본**이 된다(그 추출은 B5) |
| [KIT_APPLICATION_SRS.md](./KIT_APPLICATION_SRS.md) | **킷이 이미 가진 것을 끝까지 적용한다** (IEEE 29148, `FR-KIT-*`). 프로덕션 승격 감사 1단계(`refactor/` 214건) 중 **패턴 A 의 UI 쪽** — *"단일 출처를 세우고 호출부 일부만 옮겼다"* 가 다섯 자리에서 같은 모양으로 반복된 묶음 B2. §1.1 의 표가 요체다: `.ui-scroll` 은 **1곳**에 닿고 스크롤 표면 **33**이 브라우저 기본이며, `UIKit.dialogOpen` 은 **2곳**에서 불리고 모달 골격 **5벌**에 트랩이 없고, `UIKit.roving` 은 `web/js/git/` **전체**에 닿지 않아 `tabindex` 가 **0곳**이다 — 계약은 전부 **이미 있고 적용이 일부다**. **묶음 A 가 첫 항목인 이유가 이 문서의 성격을 말한다**(§2.2): 킷의 `.ui-modal-body` 가 자기가 대체하는 옛 `.modal-body` 보다 **못해서**(스크롤바를 안 그린다) 모달 이주가 진행될수록 회귀가 는다 — 킷을 먼저 세우지 않으면 고치는 일이 망가뜨리는 일이 된다. §2.3 이 왜 게이트 33종 전량 통과 상태에서 버튼 98개가 살아남았는지 답한다 — `DESIGN_TOKENS_SRS` §7.6 잔여표가 *"키트 클래스와 **함께 붙는** 옛 클래스"* 를 세므로 **한 번도 병기되지 않은 것을 못 본다**(FR-KIT-23 이 그 세는 방법을 고친다). **D-KIT-1 이 B2/B3 경계를 긋는다** — `.sc-key` 36개는 `.ui-key` 를 요구하고 그것은 킷에 **없으므로** B3 이다; B2 는 포커스 링만 준다(두 번 이주하지 않는다). **D-KIT-3 은 다수를 버린 자리다** — 손잡이 색 실측이 `--border` 14 vs 킷 `--text-dim` 2 인데도 킷을 택한다: 다수 쪽으로 접으면 **hover 를 잃고**, `--border` 는 경계용 원시값이라 "집는 것" 으로 안 보인다. **D-KIT-5 는 미룸을 뒤집는 것이 아니라 닫는 것이다** — `FR-TOK-45` 의 비목표 근거는 *"골격 수렴과 계약을 섞으면 어느 쪽 회귀인지 말할 수 없다"* 였고 골격 수렴은 이미 끝났다. **감사 보고서를 두 자리에서 정정한다**(D-KIT-7 · 규약 3-4): 스크롤 표면 33/34 의 어긋남은 `.dr-body` 에 JS 가 클래스를 붙이는 한 자리 차이였고, *"`.ui-btn` 이 CSS 전체에서 유일한 버튼 포커스 규칙"* 은 **틀렸다** — 실제로는 여섯이며 그 정정이 묶음 E 의 크기를 바꾼다. 게이트 둘은 D-SAF-4 를 이어 **같은 커밋에** 선다. **구현 완료** — 실측 셋이 감사를 정정했다: 스크롤 표면 33/34 의 어긋남은 `.dr-body` 에 JS 가 클래스를 다는 한 자리 차이 · *"`.ui-btn` 이 유일한 버튼 포커스 규칙"* 은 **여섯**이었고 · *"6종 98개"* 는 한 화면의 **표본**으로 소스 자리로는 **73자리**였다. 묶음 G(키보드 도달)는 사용자 결정으로 **별도 접근성 작업**으로 뺐다 — `FR-A11Y-16` 이 도달 대상을 **손으로 열거**하므로 git 만 고치면 목록이 하나 늘 뿐 같은 구멍이 남는다 (D-KIT-8) |
| [SAFETY_CORRECTNESS_SRS.md](./SAFETY_CORRECTNESS_SRS.md) | **조용히 틀리는 일곱 자리를 소리 나게 만든다** (IEEE 29148, `FR-SAF-*`). 프로덕션 승격 감사 1단계(`refactor/` 214건) 중 **실패가 성공으로 보이는** 자리만 모은 묶음 B1. §2.1 이 요체다 — 일곱 중 넷은 **새 요구가 아니다**: `FR-ERR-7`(방언 넷이 `X-Error-Code` 를 싣는다)· `FR-RQG-14`(본문 읽기가 **전부** `httpreq` 를 경유한다)·`FR-WSL-22`(도구 삭제가 **모든 슬롯**을 판다)· `FR-GDT-22`(대형 저장소의 잘림)가 이미 못박았고 구현이 일부만 닿았다. `httpreq/body.go` 머리말이 그 일을 **과거형**으로 적은 것이 결손을 가린 직접 원인이므로 주석 수정을 요구로 올린다(FR-SAF-9 · D-SAF-5). §2.2 가 이 묶음의 성격을 정한다 — **설계가 아니라 적용이다**: 일곱 전부 같은 저장소 안에 정답이 있다(`settingsStore.Save` 는 실패를 돌려주는데 `saveAccess` 는 로그만 찍고 `return nil` · `store_messages.go` 는 `Messages` 를 복사하며 레이스를 적어 두었는데 같은 처방이 `Members`·`Worktree` 에 없다 · `EventBus.publish` 는 콜백 예외를 막는데 `TimerHub._tick` 의 `after` 만 구멍이다 · `_slotReap` 은 `SLOT_MAX` 를 도는데 `_killToolInstances` 는 손으로 적은 두 칸을 돈다). 판단이 필요한 자리는 둘뿐이고 §4 가 정한다 — **D-SAF-1** 은 접근 목록 저장 실패 시 메모리를 **되돌린다**(디스크와 메모리가 갈리면 다음 기동의 동작을 아무도 예측할 수 없고, 접근 목록은 그 불확실성을 감당할 대상이 아니다), **D-SAF-2** 는 `StatusOf` 를 오류로 끝내지 않고 `Truncated` 에 **사실을 싣는다**(다른 조회처럼 실패시키면 대형 저장소에서 배지·관측·stash·clean·rebase·push 가 **함께** 막힌다). **D-SAF-3 이 가장 비싼 순서다** — 묶음 B 의 복사는 반환 포인터로 저장소를 고치던 호출처를 **조용히** 무력화하므로 `find_referencing_symbols` 전수 확인이 **먼저**다(§7 의 유일한 HIGH). D-SAF-4(사용자 지시)로 게이트 셋을 **같은 커밋에** 세운다 — 고친 뒤에 세우면 고치는 동안 다시 새는 것을 못 막는다. §2.3 이 왜 `make gates` 33종 전량 통과 상태에서 일곱이 살아남았는지 답한다: **검사가 세는 방법** 때문이다. **구현 완료** |
| [EDITOR_REPLACE_AND_SEED_SRS.md](./EDITOR_REPLACE_AND_SEED_SRS.md) | **찾은 것을 바꾸고, 고른 것을 들고 들어간다** (IEEE 29148, `FR-ERS-*`). 접수 둘(*"editor 글자 검색 후 한번에 수정"* · *"전체 문서에서 검색 시 드래그 선택한 글자 미리 채워놓기"*)은 화면이 다르지만 **같은 결핍**이다 — 찾기가 거기서 끝난다. 묶음 R 은 `EDITOR_FIND_PANEL_SRS` 의 **비목표 1 을 연다**(그 문서가 Monaco find 위젯을 걷으며 `Mod+H`·`Mod+Alt+F` 까지 닫아 **바꾸는 길이 하나도 없었다**). 묶음 S 는 `FR-EFP-9`(편집기 찾기는 이미 한 줄 선택을 싣는다)를 전체 검색으로 옮긴다 — 접수한 말의 *"일반 검색처럼"* 이 그 대비를 가리킨다. D-3 이 `모두 바꾸기` 를 **편집 한 번**으로 둔 근거: 하나씩 부르면 `undo` 가 그 수만큼 필요해져 되돌리기가 사용자의 일이 된다. D-4 는 씨앗을 `grep` 에만 건 이유 — 그 패널을 네 모드가 공유하므로 **파일 이름 칸에 본문 조각**이 들어간다. **구현 완료** |
| [SETTINGS_ITEM_LEGIBILITY_SRS.md](./SETTINGS_ITEM_LEGIBILITY_SRS.md) | **설정 항목은 덩어리로 읽힌다** (IEEE 29148, `FR-SIL-*`). 접수는 *"setting 창에 탭 내에서 각각 항목에 대한 구분이 힘듬"* 이었고 조사가 원인을 둘로 확정했다 — **모든 경계가 같은 굵기**이고(`.ds-row` 마다 `border-bottom`, 컨테이너는 `gap:10px` 로 모두를 똑같이 벌린다) **설명이 자기 항목에 붙어 있지 않다**(`.ds-hint` 는 형제이고 그 사이에도 같은 간격이 선다 — 실측 행 38 · 설명 33). D-1 이 세로선을 고른 근거: 테두리 상자는 38개가 늘어서면 화면이 격자가 되지만 세로선은 **시작과 끝만** 말한다. D-2 는 래퍼 `<div>` 를 넣지 않은 이유 — 38행을 감싸면 `aria-labelledby` 를 구조에서 파생하는 `_settingsA11y`(FR-A11Y-13)의 전제가 흔들린다. 짝은 `SETTINGS_CONTROLS_SRS`(그쪽이 **무엇으로 조작하는가**). **구현 완료** |
| [TERM_REPLY_SEAT_SRS.md](./TERM_REPLY_SEAT_SRS.md) | **응답 좌석 — 질문에 답하는 자리는 하나다** (IEEE 29148, `FR-RPS-*`). 접수는 "`11;rgb:3f3f/3f3f/3f3f2026;0$y…` 같은 문자열이 입력되는 문제" 한 줄이었고, 그 문자열은 **터미널이 낸 답장**이다 — `3f3f/3f3f/3f3f` 는 zenburn 의 터미널 배경이므로 답한 쪽이 우리 브라우저라는 증거다. 원인이 둘이었고 하나는 `TERMINAL_RESUME_SRS` FR-TRS-5a 가 함께 닫았다(재생분에 남은 낡은 질의). 이 문서는 **남은 하나** — 라이브 질의가 방송으로 **모든** 클라이언트에 닿아 각자 답하는 자리다(§2.2: 출력은 1:N 방송, 입력은 N:1 합류). **D-2 가 요체다** — 기준은 **접속 순서이지 포커스가 아니다**: `resizeCheck` 는 `windowFocused` 가 거짓이면 무조건 거짓이라 브라우저가 백그라운드면 **아무도 답하지 않고**, 크기는 안 보내도 그만이지만 답장은 앱을 질의 대기에 걸리게 한다(§2.4). D-3 은 승계를 전체 재통보로 둔 이유 — 주인에게만 알리면 나머지가 **추론**해야 하고 추론하는 상태는 어긋난다. §6 이 폐기한 안이 값을 갖는다: 서버가 입력에서 답장 모양을 골라 버리는 길은 목록에서 **하나만 빠져도 정상 답장이 사라져** 증상이 "앱이 멈춘다" 가 된다. 판정 정규식 `TERM_REPORT_RE` 는 서버의 `snapshotQueryPattern` 과 **쌍**이다. **구현 완료** |
| [FONT_SIZE_SETTING_SRS.md](./FONT_SIZE_SETTING_SRS.md) | **글자 크기 — UI 와 터미널을 따로** (IEEE 29148, `FR-FSS-*`). 접수는 "글자크기 변경 기능" 한 줄이었고 인터뷰가 둘로 갈랐다(사용자 결정 2026-09-19: *"터미널 + UI 각각 따로"*) — `uiFontScale`(배율 %, `--fs-*` 다섯·크기 토큰·Monaco)과 `termFontSize`(px, xterm). 터미널이 `--fs-lg` 에 묶여 있었으므로 **그 결합을 끊는 것이 이 스펙의 절반**이다(§2.1) — `FR-M11-15` 가 만든 그 결합은 짝을 **이미 잃은** 것이었다(함께 묶여 있던 대시보드는 `AGENT_GUI_REMOVAL_SRS` 와 함께 사라졌고, 남은 것은 UI 를 키우면 터미널이 따라 커지는 자리뿐이었다). FR-FSS-13 이 그 정리다 — `fontSize` 의 진실은 **설정 키 하나**이며 `TOPTS` 에 같은 수를 두 번 적지 않는다. 비목표가 경계를 말한다: 단축키·`Ctrl+휠` 은 열지 않고(열면 `term-pane.js` 의 `Ctrl+` 통과 규약과 브라우저 줌을 함께 정리해야 한다), 값은 **전역 하나**이며 pane 별로 두지 않는다. **구현 완료** |
| [ACCESSIBILITY_BASELINE_SRS.md](./ACCESSIBILITY_BASELINE_SRS.md) | **접근성 기준 — 목표·범위·검사 방법·완료 조건** (IEEE 29148, `FR-A11Y-*`). **M7 선행 SRS ①.** 사용자 결정 2 = **WCAG 2.1 Level AA 전면**(2026-09-12)이고 xterm 캔버스·Monaco 위젯은 예외 등록부(§6)의 2건이다. `07 §7` 이 적은 것은 결함 목록이 아니라 **부재**였다 — 기준이 없으면 `UX-4`·`UX-6` 을 "어디까지 하면 끝인가" 로 판정할 수 없다. §2.2 가 실측: `aria-hidden` 29 로 가장 많고 그 반대편(`role=` 3 · `tabindex` 1 · `aria-live` **0**)이 비어 있다 — 숨기는 일은 이미 규약이 됐는데 **이름을 주고 역할을 말하는 쪽**이 없다. D-A11Y-1 이 핵심 화면 한정(선택지 b)을 버린 이유를 적는다: "핵심" 의 경계가 또 하나의 미결정이므로, 경계를 **화면 목록이 아니라 등록된 예외 목록**이 지게 한다. D-A11Y-3·4 는 M6 이 비싸게 배운 둘의 적용 — 검사 표면을 손으로 적지 않고 파생시키는 것, 라이브 리전을 "있다" 가 아니라 **"들어왔다"** 로 재는 것 |
| [DESIGN_TOKENS_SRS.md](./DESIGN_TOKENS_SRS.md) | **디자인 토큰 — 색·크기·글자·z-index 의 목록과 파생 규칙** (IEEE 29148, `FR-TOK-*`). **M7 선행 SRS ②.** `UI_KIT_SRS` FR-UIK-2 가 "색은 소유하지 않는다" 로 비켜 간 자리를 채운다 — 비켜 간 판단은 그때 옳았으나 **아무도 소유하지 않게 됐다.** §2 가 실측: 글자 크기 12종 · z-index 44선언 28값(`.ed-find` 9997 이 컨텍스트 메뉴를 덮는다) · 색 리터럴 160곳 · 미정의 `--bg-alt` 를 네 곳이 읽어 **투명으로 떨어진다**(폴백을 적은 셋과 안 적은 넷이 같은 파일 묶음에 있다). §2.1 이 이 문서의 뿌리다 — **`--text-dim` 은 99번 글자로 쓰이는데 테마 54종 어디서도 3:1 조차 넘지 않는다.** D-TOK-1 이 사용자 결정: 팔레트는 정본이고(Nord 의 `#4c566a` 는 Nord 그 자체다) **파생이 보장한다.** D-TOK-4 가 가장 비싼 결정이고 **실측이 정했다** — 단일 바닥 4.5 는 54종 중 **34종에서 세 단계 계층을 무너뜨린다**(가장 흐리던 것이 중간을 추월한다). 사다리(4.5/5.5/7.0)가 54/54 에서 계층을 보존한다. §7 의 잔여표가 비면 `style-kit.css` 의 **D-5 과도기가 닫힌다** |
| [WINDOW_CLOSE_UNDO_SRS.md](./WINDOW_CLOSE_UNDO_SRS.md) | **창 닫기의 되돌리기** (IEEE 29148, `FR-WCU-*`). M7 `UX-2` — 한가한 창의 `×`·`Delete` 는 묻지 않고 5초 Undo 를 준다. 유예 동안 도구는 **백그라운드**에 산다(보이지 않는 상태를 만들지 않는다, D-WCU-1)·확인을 지난 닫기에는 Undo 가 없다(안전망은 길마다 하나, D-WCU-2)·만료는 어느 창에도 없는 도구만 죽인다(FR-WCU-4). **구현 완료** |
| [UX_BATCH7_HANDOFF.md](./UX_BATCH7_HANDOFF.md) | **2026-09-08 접수 열 건의 인계 노트.** 세 스펙(UI_KIT·PANEL_SURFACE·ALERT_MOBILE_CONTEXT)의 진행 상태, 이 작업이 세운 공통 키트의 규약 셋, 그리고 **다시 밟지 말아야 할 함정 둘** — 부모의 `opacity:<1` 이 backdrop root 를 만들어 자식의 `backdrop-filter` 를 격리하는 것과, 같은 요소의 `clip-path` 가 `mask-image` 를 무효로 만드는 것(둘 다 실측). 다음 세션의 착수 순서가 §3 에 있다 |
| [UI_KIT_SRS.md](./UI_KIT_SRS.md) | **공통 UI 키트 — 버튼·탭·아이콘·크기 표시** (IEEE 29148, `FR-UIK-*`·`FR-GLY-*`·`FR-HSZ-*`). 2026-09-08 접수 열 건 중 묶음 **A**(⑤⑥⑧). §2.1 이 실측 — 버튼이 **열한 종**이고 그중 셋은 `style-git.css:79` 에서 이미 한 줄에 묶여 있다(같이 적으려는 시도가 파일 경계에서 멈춘 자리). §2.6 은 아이콘 인벤토리 — 전부 유니코드 문자이고 크기 규칙은 레일 한 자리에만 있어 28px 버튼 안에 12px 글자가 앉는다. D-5 가 이 작업의 안전선이다: **기존 클래스를 지우지 않고 공통 클래스를 함께 붙인다**(`SidebarList` 의 `.sbl-*` 와 같은 방법) — e2e 131개와 캐스케이드가 그 이름 위에 서 있다. §7 의 이전 순서에서 git 표면이 마지막인 것은 그 파일들이 가장 넓게 얽혀 있기 때문이다. **구현 중** — 키트·상단바·사이드바 탭·설정 모달까지 넷 완료(§7 표), git 아이콘 상수의 소비 지점은 §7.1 에 조사돼 있다 |
| [PANEL_SURFACE_SRS.md](./PANEL_SURFACE_SRS.md) | **패널 표면 — 에이전트 그룹 · 레일 목록 · 변경 통합 · 검색 통합** (IEEE 29148, `FR-AGG-*`·`FR-RAL-*`·`FR-CMG-*`·`FR-HSU-*`). 묶음 **B**(①④⑦⑨). D-9 가 요체다 — **그룹은 상태가 아니라 파생이다**: `ws` 에 그룹을 저장하면 창 순서·탭 이동과 두 진실이 생기고 "즉각 반영" 이 동기화 코드가 된다. §2.3 이 `changes`/`untracked` 를 견준 표로, **diff 축·행 동작·그룹 일괄이 셋 다 같고** 갈리는 것은 상태 문자와 폐기의 명령(`checkout` vs `clean -f`)뿐임을 확인한다 — 그래서 D-4 의 확인창이 유일한 방어선이다. §2.4 는 History 검색의 사실 — 서버가 아는 검색은 `--grep` 하나이며, 그것이 D-13(저장소 전체 확장은 grep 갈래만)의 근거다. **구현 미착수** — 착수를 막던 다른 세션의 git 작업은 `ed54faf` 로 끝났다 |
| [ALERT_MOBILE_CONTEXT_SRS.md](./ALERT_MOBILE_CONTEXT_SRS.md) | **알림 가장자리 · 모바일 키보드 · Run 컨텍스트** (IEEE 29148, `FR-AED-*`·`FR-MKB-*`·`FR-RCX-*`). 묶음 **C**(②③⑩). ②는 `UNFOCUSED_EDGE_SRS` 구조의 **두 번째 사례**이며 다른 것은 셋뿐이다(계기·색·맥박). §2.5 가 접수한 물음("조정자 스스로의 context 는 확인 못하나?")의 답 — **신호는 이미 도착해 있고 저장할 자리가 없다**: 훅이 `toolId` 와 실측 토큰을 보내지만 서버가 `findByTool` 로 **멤버에서만** 찾아 조정자의 것은 버린다. §2.3 은 ③의 연결 고리 — 소프트 키보드를 올리지 않기로 하면 지금 구조에서 Ctrl+C 를 낼 길이 사라지므로, 접수한 말이 그 둘을 한 문장에 담고 있다. **구현 중** — ②와 설정 전파(FR-SYN, 추가 접수) 완료, ③⑩ 미착수 |
| [UX_BATCH6_SRS.md](./UX_BATCH6_SRS.md) | **접수한 결함 16건 — IME 순서 · 실시간 갱신 · 컨텍스트 측정 · 백그라운드 수명** (IEEE 29148). 묶음 **I**(FR-IME) · **M**(FR-SBM) · **V**(FR-GLV) · **R**(FR-SCR) · **C**(FR-CTX) · **B**(FR-BGP) · **N**(FR-RUN) · **T**(FR-DSP) · **P**(FR-MMP). **§2.1 은 결함이 아니었던 한 건의 기록** — ①(docker 갈래)은 이미 `f7f8176` 에 있었고 실행 중인 바이너리가 낡았을 뿐이다. **§2.7 이 이 문서의 실측 중심** — 202,185 토큰인 세션의 transcript 가 968KB 이고 옛 공식으로는 134% 가 나온다; 지배적 오차는 바이트 추정이 아니라 **200k 로 못박은 한계**다. FR-CTX-5 는 그 한계의 근거를 **모델 문자열**로 옮긴 것이고(전사 기록 전량을 세어 `[1m]` 접미어를 확인했다), FR-CTX-6 의 자동 확장은 그 접미어 없이 기록되는 세션을 위한 그물이다. **FR-DSP-1a·1b·1c 는 ④가 일으킨 파문의 기록** — 기본 사이드를 Changes 로 옮기자 저장소가 아닌 루트가 3초마다 물리고, 연 파일이 탐색기에서 드러나지 않고, e2e 열여섯이 무너졌다. **FR-RUN-6b 는 e2e 가 잡은 데이터 손실** — 없는 자리를 지목한 `closeTab` 이 사용자의 포커스 탭을 닫고 있었다. **구현 완료** |
| [UX_BATCH5_SRS.md](./UX_BATCH5_SRS.md) | **폴더 폐기 · 샌드박스 런타임 · 전역 툴팁 · 서브모듈 · 워크트리** (IEEE 29148). 한 번에 접수한 다섯 요구를 묶음 **A**(FR-DBA) · **B**(FR-SRT) · **C**(FR-TIP) · **D**(FR-SUB) · **E**(FR-WTG) 로 가른다. **§5 D-9 가 정정의 기록** — 초안이 "`submodule` 은 읽기·쓰기가 다른 하위 명령" 이라 적었으나 허용 목록은 `argv[0]` 으로 판정하므로 둘이 같고, worktree 가 별도 Manager 인 것과 같은 이유로 별도 도메인이 됐다. **§2.5.1 은 재현되지 않은 결함의 기록** — 워크트리를 저장소로 다루는 경로에 결함이 없어 `Open` 의 낡은 문구 한 줄만 고쳤다(D-11). **§8.2 는 "모든 버튼" 이라는 요구가 미달이던 자리 전부** — 파괴적 확인창의 `Run` 이 무엇을 실행하는지 스스로 말하지 않았다. **§8.1 의 플레이키 절은 기준선을 재서 확정한 것** — 변경 전에도 전체 실행이 2건 실패한다. **구현 완료** |
| [TERM_XFER_NOTICE_SRS.md](./TERM_XFER_NOTICE_SRS.md) | **터미널 전송 알림 — 화면 밖 팝업** (IEEE 29148, `FR-TXN-*`). 접수한 말은 "탭에 드래그드롭하여 업로드하면 다른 키를 누르기 전까지 프로세스가 남아있는 것 같다" 였고, 정체는 **셸 프롬프트의 미복귀**였다 — `_say` 가 xterm 에 직접 쓰므로 그 글자는 pty 를 거치지 않고, zle 은 자기 프롬프트가 원래 자리에 있다고 믿은 채 다음 키 입력까지 화면을 고치지 않는다(§1.2). FR-FTR-10 의 엔터 금지는 옳고 **알림의 자리가 틀렸다.** D-1 이 프롬프트를 되돌리는 두 길(SIGWINCH·`ESC[L` 삽입)을 버린 근거 — 셸의 화면을 계속 만지는 설계는 여러 줄 프롬프트·입력 중인 줄을 전부 우리 문제로 만든다. D-3 은 진행 팝업에 자동 소멸을 주지 않은 이유. **FR-FTR-9·10 의 "터미널에 쓰는 줄" 을 이 문서가 대체한다.** |
| [TAB_WIDTH_SRS.md](./TAB_WIDTH_SRS.md) | **탭 너비 고정** (IEEE 29148, `FR-TBW-*`). 이름 길이에 따라 탭이 넓어졌다 좁아졌다 하면 **같은 탭이 이웃의 이름이 바뀔 때마다 다른 자리로 옮겨 간다.** §1.2 가 VSCode 의 계약(`tabSizing` 의 세 값과 두 폭)을 확인한 것이고, D-1·D-2 가 그것을 그대로 옮기지 않은 근거 — 이 앱은 넘칠 때 스크롤하므로 `shrink` 를 두면 "스크롤과 줄임 중 무엇이 언제 이기는가" 를 사용자가 알아야 한다. D-3 은 하한 40px 을 닫기(`×`) 좌표 실측으로 확정한 자리. §7.1 의 1번은 **디바운스가 확정을 미루면 안 되는 이유** — 폭을 바꾸고 곧바로 새로고침하면 값이 날아갔다. **구현 완료** |
| [NOTES_LIVE_EXPLORER_SRS.md](./NOTES_LIVE_EXPLORER_SRS.md) | **메모장 · 탐색기의 살아있는 반영 · 캐럿** (IEEE 29148). 묶음 **N**(FR-NOT) · **L**(FR-FSL) · **K**(FR-CUR). §2.1 이 인터뷰를 뒤집은 근거 — 저장 위치를 전수 조사하니 **브라우저 저장소 어디에도 "내용" 이 사는 자리가 없어**, "브라우저 세션에 저장" 요구가 서버 저장으로 확정됐다(D-1). D-2 가 이 작업의 전부다: 메모를 **파일**로 두면 메모 루트를 `Roots()` 에 한 줄 더하는 것으로 창·탐색기·편집기·조작이 전부 선다. §2.3 은 탐색기가 **바깥의 변경을 아예 반영하지 않던** 사실(폴링이 바꾸는 것은 색뿐이다)이고 D-5 가 세 후보를 견준 표, D-6 이 fsnotify 를 기각한 근거. **D-9 는 구현 중 걸린 결함의 기록** — 부분 갱신 리터럴이 셋으로 흩어져 한 자리를 빠뜨리자 메모장 창이 동기화 한 번에 사라졌다. **구현 완료** |
| [CODE_AUDIT_FIXES_SRS.md](./CODE_AUDIT_FIXES_SRS.md) | **전수검사가 찾아낸 결함의 수정** (IEEE 29148). `go vet`·`staticcheck`·`deadcode`·`go test -race`·핵심 경로 정독이 찾은 것 **전부**를 담고 고칠 순서를 근거와 함께 정한다. 핵심은 원자적 쓰기 — `domain/run/store.go` 가 "부분 쓰기가 살아 있는 파일이 되면 안 된다" 를 지키는 동안 `workspace.json`·`tools.json`·`settings.json` 과 편집기의 저장은 `os.WriteFile` 로 가고 있었다. 저장 순서 역전, HTTP 핸들러의 패닉 복구 누락도 함께. **구현 완료** |
| [WINDOW_SLOTS_SRS.md](./WINDOW_SLOTS_SRS.md) | **창 슬롯 — 두 창을 한 화면에** (IEEE 29148, `FR-WSL-*`). 접수한 요구는 "window·git·editor 를 동시에 보려고 크롬 창을 두 개 켜고 있다" 였다. 콘텐츠 영역이 `activeWindow` **하나만** 그리던 것을 칸 넷까지로 늘린다. 칸 **사이의 이동에 키를 만들지 않은 것**(D-5)이 결정의 요체 — 분할 칸 이동이 창의 끝에서 넘어간다. **구현 완료** |
| [SLOT_VIEW_STATE_SRS.md](./SLOT_VIEW_STATE_SRS.md) | **칸별 시선 — 슬롯이 창을 각자의 눈으로 본다** (IEEE 29148, `FR-SVS-*`). 같은 창을 두 칸에 놓지 못하던 것을 푼다. **관측과 시선을 가른다**(D-4·D-5) — 디렉터리 목록·git 색은 루트마다 하나(`FileTreeStore`), 펼침·선택·스크롤은 칸마다 하나(`FileTree`). 규약의 원본은 터미널이다: PTY 는 서버에 하나, xterm 은 칸마다 하나. FR-SVS-50 이 편집 문서를 **파일 경로 단위**로 못박은 근거(탭 단위면 한쪽 저장이 다른 쪽을 덮는다). **구현 완료** |
| [SLOT_RUN_VIEW_SRS.md](./SLOT_RUN_VIEW_SRS.md) | **Run 뷰의 슬롯 다중화** (IEEE 29148). 같은 창을 두 칸에 놓고 Run 탭을 활성으로 두면 **한 칸이 빈다** — 싱글턴 패널이 요소 하나를 두 자리에 붙이려 하기 때문이다. `SLOT_VIEW_STATE_SRS` 가 탐색기에 대해 세운 규약을 Run 뷰에 적용한다. **구현 완료** |
| [SLOT_TITLE_BOUNDARY_SRS.md](./SLOT_TITLE_BOUNDARY_SRS.md) | **슬롯 제목과 경계** (IEEE 29148, `FR-STB-*`). 어느 칸이 무엇을 보고 있는지(windows·git·editor)와 창 이름을 합쳐 보이고, 한 칸이 어디까지인지를 경계로 말한다. FR-STB-3 이 **창 타입의 보이는 이름을 사이드바 탭 서술자 하나에서** 가져오는 근거 — 제목이 부르는 이름과 탭이 부르는 이름이 갈리면 둘을 잇는 일이 사용자 몫이 된다. **구현 완료** |
| [TOOL_LIST_UNKNOWN_SRS.md](./TOOL_LIST_UNKNOWN_SRS.md) | **도구 목록을 모르는 순간과 재연결 폭주** (IEEE 29148, `FR-TLU-*`). `RECONNECT_STORM_SRS` 가 닫은 것은 **없는 도구**를 향한 재접속이었고, 남은 것은 그 반대다 — 살아 있는 도구를 클라이언트가 죽은 것으로 읽는 창. 요체는 **빈 목록을 사실로 받지 않는 것**(FR-TLU-5·6): 서버가 "모른다" 고 말할 수 있게 하고, 모르는 스냅숏으로는 도구도 pane 도 창도 지우지 않는다. **구현 완료** |
| [RELOAD_CONTINUITY_SRS.md](./RELOAD_CONTINUITY_SRS.md) | **새로고침의 연속성 — 자동 갱신과 잃지 않을 기억** (IEEE 29148). 새 판이 뜨면 배너를 띄우고 사용자를 기다리던 것을 **즉시 새로고침**으로 바꾸고, 그때 "이 페이지를 나가시겠습니까?" 를 묻지 않는다 — 물으면 자동이 아니게 된다. 함께 새로고침이 사이드바 선택 같은 화면 상태를 잃던 것을 고친다. **구현 완료** |
| [RESTORE_FLIGHT_SRS.md](./RESTORE_FLIGHT_SRS.md) | **복원 중에 도착한 갱신은 스냅숏보다 새롭다** (IEEE 29148, `FR-RSF-*`). `FG_RESTORE_RACE_SRS` §8 이 조사로 확정한 결함을 없앤다. 세 함수(`_attnRestore`·`_fgRestore`·`_activityRestore`)가 같은 창을 갖고 있었고, **규약을 주석으로 선언하고 손으로 옮겨 적으니 한 방향을 놓쳤다** — 그래서 규약을 함수로 옮긴다. **구현 완료** |
| [SETTINGS_PORTABILITY_SRS.md](./SETTINGS_PORTABILITY_SRS.md) | **설정 내보내기·가져오기** (IEEE 29148). 값이 세 저장소(서버 블롭·localStorage·sessionStorage)에 흩어져 새 기계에서 손으로 다시 짜야 했다. 이 창에서 설정할 수 있는 값 **전부**가 JSON 하나로 나가고 돌아온다. 가져오기를 **전체 교체**로 둔 근거와, 화면 치수·리포별 보기 취향을 담지 않은 근거가 결정에 있다. **구현 완료** |
| [ASSET_VERSION_SINGLE_SOURCE_SRS.md](./ASSET_VERSION_SINGLE_SOURCE_SRS.md) | **자산 판의 단일 출처 — `?v=` 를 손으로 적지 않는다** (IEEE 29148). `index.html` 이 캐시 무효화 번호를 **70 곳**에 적고 있었고, 자산을 고치며 올리지 않으면 브라우저가 캐시의 옛 JS 를 계속 돌린다 — 실제로 그렇게 지나간 커밋이 있다. 서버가 **자산의 내용에서 계산해** 넣는다. **구현 완료** |
| [CONFIRM_ONE_STAGE_SRS.md](./CONFIRM_ONE_STAGE_SRS.md) | **파괴적 동작의 확인을 한 걸음으로** (IEEE 29148). 확인을 두 번 누르게 하던 것을 한 번으로 줄인다. 고칠 자리가 많지 않은 이유는 **Git 의 파괴적 동작이 전부 한 자리를 지나기 때문**이다. 실수를 막는 것은 걸음 수가 아니라 기본 선택이 `취소` 이고 `Enter` 가 실행이 아닌 것이며, 그 둘은 그대로 둔다. **구현 완료** |
| [BRANCH_MENU_UNIFY_SRS.md](./BRANCH_MENU_UNIFY_SRS.md) | **브랜치 메뉴 — merge 통합과 삭제 통합** (IEEE 29148). pull/merge 가 remote 에만 되던 것, 브랜치 삭제가 local·remote 로 갈려 있던 것을 합친다 — VS Code 처럼 한 번에 둘을 지우는 선택을 준다. **구현 완료** |
| [HISTORY_BRANCH_BUTTON_SRS.md](./HISTORY_BRANCH_BUTTON_SRS.md) | **History 의 브랜치 생성 버튼** (IEEE 29148). 브랜치 생성 **기능**은 이미 있었고 닿는 길이 **우클릭뿐**이었다 — 보이는 진입점을 준다. **구현 완료** |
| [ATTENTION_FIRING_SRS.md](./ATTENTION_FIRING_SRS.md) | **알람의 발화 조건과 포커스와의 공존** (IEEE 29148). 알림 깜빡임이 포커스 링에 가려 보이지 않던 것과, 알람이 울 자리가 아닌 곳에서 울던 것. 맥박이 생기면서(`ATTENTION_PULSE_SRS`) 겹침 순서의 전제가 바뀐 것이 계기다. **구현 완료** |
| [ATTN_UTIL_RELOCATE_SRS.md](./ATTN_UTIL_RELOCATE_SRS.md) | **`app-attn.js` 의 공용 유틸을 제자리로** (IEEE 29148). `APP_STATE_EXTRACT_SRS` §7.4 가 남긴 관측의 해소 — 알림과 무관한 공용 유틸이 알림 파일에 살고 있었다. **객체 추출이 아니므로** 위임 껍데기가 없고 호출부가 한 줄도 바뀌지 않는다. **구현 완료** |
| [README_REWRITE_SRS.md](./README_REWRITE_SRS.md) | **README 재작성** (IEEE 29148). 최상위 README 가 사용자 문서가 된다 — 346줄 중 절반 이상이 빌드·패키지 트리·로드맵이었고 **그림이 하나도 없었다**(화면을 쓰는 도구인데). §2.1 이 절마다 사용자 것인지 개발자 것인지 가르고, §2.2 가 옮길 곳을 확인한다(프로세스 다이어그램만 갈 곳이 없어 `architecture.md` 로 옮겼다). §2.3 이 촬영의 제약 — 운영 화면에는 홈 경로·호스트명·실제 저장소 이름이 보이고 README 는 공개되므로 **격리 인스턴스에서만 찍는다**(D-1). D-3 은 개발자 내용을 지우지 않고 옮긴 이유, D-4 는 촬영을 스크립트로 남긴 이유. **구현 완료** |
| [LEFTOVERS_SRS.md](./LEFTOVERS_SRS.md) | **남은 둘 — 규약 위반과 헬퍼 잔재** (IEEE 29148). 묶음 **S**: `platform` 밖의 `runtime.GOOS` 셋을 걷어 `check-seams.sh` 를 되살린다 — 셋 다 "이 OS 인가" 가 아니라 **"이 능력이 있는가"** 를 묻고 있었고, 그 질문의 자리는 이미 `testpath` 에 있었다(D-1). 묶음 **R**: 설치가 **자기가 놓았던 것을 거둔다** — 폐지된 `mdview` 가 사용자 `bin/` 에 남아 있던 것이 계기다. §2.3 이 제약 — `bin/` 에는 훅·플러그인·자기 복사본이 함께 살아 **모르는 파일을 지우는 규칙은 둘 수 없다**. 그래서 기록(`.helpers`)으로 우리가 놓은 것만 거두고(D-2), 기록 이전의 이름만 폐지 목록이 맡는다(D-3). **구현 완료** |
| [PANEL_SHORTCUTS_SRS.md](./PANEL_SHORTCUTS_SRS.md) | **BG·Runs 진입점의 단축키** (IEEE 29148). 상단 툴바 다섯 중 `Agents` 만 키가 있었다. §2.1 이 착수 전 사실 — 여는 함수는 이미 있고 **버튼만이 그것을 부르는 유일한 자리**였으며, 설정 목록은 자동이 아니라 `_renderShortcutList` 의 하드코딩된 무리 배열이다(그 사실을 구현 중 e2e 가 잡아 스펙을 고쳤다). §2.2 가 쓸 수 있는 키를 고정한다 — `R` 은 `SOFT_RELOAD_SRS` D-6 이 이미 배제했으므로 `Runs` 가 `O` 를 쓴다. **구현 완료** |
| [HELPER_INSTALL_SRS.md](./HELPER_INSTALL_SRS.md) | **헬퍼 설치의 안정성** (IEEE 29148). 사용자의 Stop 훅이 `bin/dmctl: No such file or directory` 로 죽던 사건의 스펙. §2.1 이 원인 — 설치가 `os.Executable()` 을 그대로 링크하는데 `go run` 아래에서 그것은 **끝나면 지워지는 임시 산출물**이라, 설치는 성공한 채 다섯 링크가 한꺼번에 죽는다. §2.2 가 왜 이것이 개발 기계에서만 터지는지(CI 는 홈이 매번 새것), §2.3 이 왜 늦게 발견되는지(죽는 시점이 설치 뒤)를 적는다. D-1 은 `go run` 을 막는 대신 **사라지지 않을 것을 가리키게** 한 이유(릴리스 게이트가 그 명령을 쓴다), D-2 는 판정에 두 조건을 모두 요구한 이유, D-4 는 `doctor` 가 고치지 않고 알리기만 하는 이유(§2.5 — 진단이 대상을 바꾼 것이 이 사고가 퍼진 경로다). **구현 완료** |
| [WINDOW_COMMAND_SRS.md](./WINDOW_COMMAND_SRS.md) | **`dongminal window`** (IEEE 29148). 창을 여는 일과 서버를 띄우는 일을 갈라 놓는다 — `start --open` 을 폐기하고(FR-OPN-1~3) 독립 명령으로 옮겼다. §2.1 이 이유다: 이미 돌고 있는 서버에 창만 붙일 길이 없었고, `start` 를 다시 부르면 **돌던 서버를 죽이고 새로 띄운다.** D-2(죽은 서버에는 열지 않는다)·D-3(창 열기 실패는 이 명령의 실패다 — `start` 에서 경고였던 이유는 거기서 본체가 서버였기 때문)·D-5(기다리지 않는다). **구현 완료** |
| [STATUS_BAR_REFLOW_SRS.md](./STATUS_BAR_REFLOW_SRS.md) | **상태바 줄바꿈과 백그라운드 진입점 이전** (IEEE 29148). §2.1 이 실측 — 좁아진 상태바는 지표를 **잘라내고 있었다**(560px 3개·420px 5개가 표시 없이 사라졌다). `flex-shrink` 가 없어 상자가 글자보다 좁아지고, `overflow:hidden` 이 나머지를 먹었다. 고침은 `flex-wrap` 과 `flex-shrink:0` 이고, D-5 가 줄 첫 항목의 구분선을 지우는 수단(음수 마진 + 클리핑)이다. FR-SBR-7 은 고정 높이가 풀린 대가 — 상태바가 커지면 터미널을 다시 맞춰야 하는데 `window.resize` 는 그것을 알리지 않는다. 묶음 B 는 진입점을 상단바 `Runs`·`Agents` 사이로 옮기고 **FR-BGU-5(0개면 숨김)를 폐기한다**(D-3). **구현 완료** |
| [ATTENTION_PULSE_SRS.md](./ATTENTION_PULSE_SRS.md) | **주의 표식의 맥박** (IEEE 29148). 정지해 있던 `--attn` 표식이 2초 주기로 숨쉰다(1초 사라짐·1초 나타남). §2.1 이 표식이 사는 일곱 자리를 세고, D-1 이 이 작업의 전부다 — **요소가 아니라 표식을 그리는 속성을 움직인다.** `opacity` 한 줄이면 끝나지만 그 대가로 터미널 화면과 탭·창 이름이 1초마다 사라진다(§2.2). D-2 는 글자색을 고정한 이유(읽을 수 없게 되는 것은 알림이 아니다), D-4 는 `prefers-reduced-motion`. **§2.3 은 맥박이 드러낸 원래 결함의 기록** — 분할 칸의 링이 테두리와 안쪽 그림자 **두 곳**에서 나오고 안쪽 그림자를 자식이 면마다 다르게 덮어, 실측 두께가 위 2px·왼쪽 3px·아래 4px 로 어긋나 있었다. D-5 가 링을 자식 위에 뜬 겹으로 옮겨 네 변 3.00px 을 얻는다. **구현 완료** |
| [EXPLORER_ROOT_KEYS_SRS.md](./EXPLORER_ROOT_KEYS_SRS.md) | **탐색기의 루트 자리 · 생성 직후 · 키보드 길 · 탭 바 여백** (IEEE 29148, `FR-EXR-*`). 사용자 보고 여섯(`U-4`·`U-11`·`U-12`·`U-13`·`U-14`·`U-16`)을 한 묶음으로 닫는다. 요체는 **이미 있는 규약을 넓히는 일**이다 — "헤더와 빈 여백이면 루트다"(`FR-FTR-20`)가 드롭에만 있었고 클릭·더블클릭이 그 밖에 있었다(§2.1). §2.5 가 착수 전 제약을 실측으로 지웠다: 전역 `keydown`(`FR-EKB-1`·`4`)과 겹치는 키가 **하나도 없어** 그 규약을 개정하지 않는다. 최대의 함정은 §7 의 첫 줄 — 렌더가 `.ed-win` 을 떼면 포커스는 빼앗기는 것이 아니라 **잃는다**(`FR-EXR-58`, 인라인 입력의 `FR-EDT-66` 과 같은 함정). D-2 는 루트 행을 만들지 않고 머리에 표시를 얹은 근거, D-4 는 메모장의 폴더 금지를 서버에도 둔 근거다. **구현 완료** |
| [POPUP_DEFAULT_ACTION_SRS.md](./POPUP_DEFAULT_ACTION_SRS.md) | **팝업의 기본 포커스와 `Enter` — 목적의 버튼으로 통일** (IEEE 29148, `FR-PDA-*`). `U-15`. **이 저장소가 요구사항으로 못박고 있던 것을 뒤집는다** — `FR-GIT-97`·`176` · `FR-COS-6`·`7` · `UX-1`(일부)이 함께 개정됐다. 근거는 사용자의 한 문장이다: "UX 는 사용자가 하고자 했던 걸 유지하는 방향이지 안전한 방향이 아니다". §2.1 이 전수 조사 — **팝업 여덟 자리에 규약 여섯 종**이었고 그중 하나(`open-url`)는 이미 새 규칙대로 서 있었다. 요점은 D-1 이다: 요구를 그대로 구현하면 `Enter` 를 가로채게 되고 **포커스가 취소에 있어도 실행된다** — 브라우저 기본 동작에 맡겨 규약을 "기본 포커스를 어디에 두는가" 한 줄로 줄였다. D-2 는 안전이 옮겨 간 자리(확인창의 존재와 `Esc`), D-3 은 모바일을 같게 둔 근거, D-4 는 닫기 확인창의 목적이 `저장 후 닫기` 인 근거다. **구현 완료** |
| [UI_LAYOUT_DEFAULTS_SRS.md](./UI_LAYOUT_DEFAULTS_SRS.md) | **UI 레이아웃 기본값 — 켜기/끄기 · 스크롤 소유권 · 골격 배치** (IEEE 29148, `FR-LAY-*`). `U-7`(결함)·`U-8`(구조 변경). 성공 판정이 **"아무것도 달라지지 않는 것"** 인데 시각 회귀 기준선이 없어 **계산값 스냅샷**을 만들었다 — 그 수단을 스스로 검증해야 했다(§5.1: 첫 판이 4회 중 2회 흔들려 픽셀을 버리고 잘림/스크롤 여부로 바꿨다. **재기준화는 "달라진 것이 없다" 의 증명이 아니다**). §2.3 이 이 스펙의 중심 — 문서가 **가능성**으로 적은 "빠뜨린 뷰" 가 실제였다: 여덟 뷰 중 둘이 명시도가 낮은 선택자로 골격을 적어 `.git-view.vis` 에 **져서** 목록이 **870px 이상 잘려 닿을 수 없었다**. §2.3b 는 그 자매 결함(모바일 `m-add-tab` 이 `hidden` 인데 보였다), §2.1c 는 `style.css` 끝의 **닫히지 않은 주석**이다. §2.1b 는 C1 의 전제 셋이 실측에 뒤집힌 기록이고(D-7), **C3 는 어긋난 자리가 하나도 없어 선언을 바꾸지 않았다**(D-4) — 산출물은 게이트다. 게이트 셋(visibility·scroll·skeleton)은 **일곱 형태를 탐침으로 하나씩 확인**했고 그중 하나가 검출에 실패해(macOS awk) 되돌렸다. **구현 완료** |
| [PAGE_TITLE_SRS.md](./PAGE_TITLE_SRS.md) | **페이지 제목 설정** (IEEE 29148). 브라우저 탭 이름을 Settings ▸ Display 에서 정하고 서버 설정 blob 에 남긴다. §2.1 이 제약 — 주의 배지(FR-PAN-13b)가 제목 전체를 다시 쓰므로 **합성 자리가 하나여야 한다**(`_applyPageTitle`). D-1 은 값이 브라우저별이 아니라 서버의 것인 근거(FR-TAN-19·FR-KEY-6 의 선례), D-2 는 빈 문자열이 곧 기본 이름인 이유. **구현 완료** |
| [EDITOR_TAB_SRS.md](./EDITOR_TAB_SRS.md) | **Editor 탭 — 파일 탐색기와 편집기 창** (IEEE 29148). 편집기를 일반 창의 탭에서 걷어내고 경로마다 하나씩 서는 제3의 사이드바 탭으로 옮긴다. FR-EDT-1~120, V-EDT-1~96, 확정 결정 D-1~D-29. §2 는 착수 전 조사와 1차 검증으로 굳힌 사실이며 **§2.4(`layout` 없는 창이 지워진다)가 이번 작업 최대의 제약**이다. §3.7 은 VSCode 의 폴더 색이 우선순위가 아니라 **경로 사전순 우연**임을 소스로 확인하고 모사하지 않기로 한 근거를 담는다. **구현 완료** |
| [RECONNECT_STORM_SRS.md](./RECONNECT_STORM_SRS.md) | **재연결 폭주와 헬퍼 설치의 비원자성** (IEEE 29148). 원격 접속이 한 번씩 끊기던 원인 — 사라진 도구를 향한 지연 0 의 무한 재접속이 임시 포트를 말렸다. §2 는 전부 실측(초당 95연결·TIME_WAIT 2,881·포트 16,384)이고, §2.5 는 **배포된 클라이언트 수정이 이미 열려 있는 탭에 닿지 않는다**는 사실을 재측정으로 확인한 기록이다. §6.1 에 판단 오류 둘(비목표 #4 철회·D-5 철회)을 근거와 함께 남긴다. **구현 완료** |
| [EDITOR_GIT_UX_SRS.md](./EDITOR_GIT_UX_SRS.md) | **Changes 크기조정 · Diff 개요 눈금 · Editor 검색 · 열 수 있는 형식** (IEEE 29148). 묶음 **D**(FR-CSZ) · **O**(FR-DOR) · **F·G**(FR-EQO·FR-EGS) · **V**(FR-EVW) · **K**(FR-EKB). §1.3 의 인터뷰 결정 셋이 스펙보다 앞선다. §2.4 가 착수 전 제약 — 전역 keydown 은 Monaco 안에서 돌지 않으므로 키를 두 자리에 배선해야 한다. **구현 완료** |
| [EXPLORER_TRANSFER_IGNORE_SRS.md](./EXPLORER_TRANSFER_IGNORE_SRS.md) | **무시 표시 · 폴더 단위 전송 · 경로 자동채움 · 터미널 복사** (IEEE 29148). 묶음 **A**(무시 흐림, `check-ignore` 온디맨드) · **B**(폴더 zip 다운로드) · **C**(폴더 업로드, `relPath`) · **D**(실패했을 때의 선택) · **E**(`+ Add` 의 자동채움) · **F**(OSC 52). §2.4 와 §2.5 가 실측으로 굳힌 두 결함이 핵심이다 — 서버가 **자기 cwd 를 그럴듯하게** 답하고 있었고, xterm 은 OSC 52 를 **받는 사람이 없었다**. D-4 는 폴더를 폴더 그대로 내려받는 길(File System Access API)이 secure context 를 요구해 막힌 기록이며, D-12 가 복사에서 같은 벽을 만난다. **구현 완료** |
| [SOFT_RELOAD_SRS.md](./SOFT_RELOAD_SRS.md) | **내부 새로고침** (IEEE 29148). 페이지를 다시 열지 않고 서버의 사실만 다시 받는다. §2.1 이 요점 — **조각은 이미 다 있고 부르는 자리가 없었다**(복원 경로 다섯은 SSE `onopen` 만 쓰고 있었다). §2.2 는 왜 그것으로 부족한지를 적는다: 구독이 형식상 살아 있는데 브로드캐스트를 놓친 경우 회복의 계기가 없다. 터미널은 **전부** 다시 붙이되 pane 은 다시 만들지 않는다(D-2·D-3) — 그 구분이 페이지 새로고침과의 차이 전부다. **구현 완료** |
| [CONNECTIVITY_RESILIENCE_SRS.md](./CONNECTIVITY_RESILIENCE_SRS.md) | **재연결 폭주의 종식과 끊긴 순간의 기록** (IEEE 29148). 묶음 **A**(붙잡기) · **B**(진단 스냅샷). **원인을 고치지 않는다** — 아직 모른다(D-6). 대신 밝혀진 유일한 실제 결함을 없애고, 다음 발생 때 원인이 확정되도록 준비한다. §2.2 가 핵심이다: `onclose` 가 재연결의 유일한 계기이므로 **닫는 한 재연결은 반드시 온다** — 지연은 주기를 늘리고 거절은 오히려 줄인다. §2.5 는 구현 중 실측으로 FR-CNR-6 을 뒤집은 기록이다(hijack 뒤에는 `r.Context()` 가 절단을 알리지 않는다). **구현 완료** |
| [CONNECTIVITY_INVESTIGATION.md](./CONNECTIVITY_INVESTIGATION.md) | **간헐적 접속 불가 — 조사 노트** (SRS 가 아니다). SSH 까지 끊긴다는 새 사실 때문에 `RECONNECT_STORM_SRS` 와 같다고 단정할 수 없다. §1 은 확정한 사실(폭주는 17분의 1 로 줄었으나 **멎지 않았다** · 절전은 배제 · 데몬이 둘), §2 는 근거 없는 가설 셋, §3 은 **끊긴 그 순간에** 찍어야 할 명령과 그것을 가르는 표다. 원인이 서면 그때 SRS 를 연다 |
| [UX_REVISION_SRS.md](./UX_REVISION_SRS.md) | **Run 수명 · 사이드바 블루프린트 · 편의 개정** (IEEE 29148, `FR-BLP-*` 외). 세 묶음 아홉 줄의 요구를 받는다. 요체는 **사이드바 목록의 블루프린트화** — 창 목록과 Git 핀 목록은 같은 것인데 각자 reconcile·드래그·행 생성을 구현하고 있었고, 그래서 한쪽에만 있는 동작이 생겼다(핀 재배치가 폴링까지 옛 순서로 남던 것). 구현을 하나로 내리고 **탭은 타깃만** 준다(D-3). **구현 완료** |
| [GIT_VIEW_REFRESH_SRS.md](./GIT_VIEW_REFRESH_SRS.md) | **Git 창 — 쓰기 뒤 뷰 갱신의 누락** (IEEE 29148, `FR-GVR-*`). "push 를 했는데 history 가 바로 안 바뀐다" 의 실체는 **아예 갱신하지 않는 것**이었다. §2.1 이 갱신을 부르는 자리를 전수로 세고, 원격 경로에만 그 대응물이 없음을 보인다. **갱신을 부르는 자리만 고친다** — 폴링 주기도 파일 감시도 손대지 않는다. **구현 완료** |
| [REPO_TAB_UNIFY_SRS.md](./REPO_TAB_UNIFY_SRS.md) | **Git·Editor 통합 — 저장소 하나에 창 하나** (IEEE 29148, `FR-RTU-*`). `Git`·`Editor` 두 사이드바 탭이 `Repo` 하나가 되고, 창의 좌측은 `Explorer`↔`Changes` **탭 교체**이며 diff·history 는 본문에 **편집기 탭과 같은 자격으로** 뜬다. 옛 `WINDOW_TYPE_GIT` 창은 로드에 사라진다. §7 의 **결정 32개**가 본문보다 앞서며, 그중 D-RTU-16~32 는 **구현이 스펙을 뒤집은 것**이다 (관측기를 저장소마다 · 인라인 미리보기 폐기 · 한 번 클릭 = diff · 뷰 탭의 닫기·드래그 · 관측 게이트를 표면으로 · 모바일 영역 순회). **구현 완료** |
| [REPO_TAB_UNIFY_HANDOFF.md](./REPO_TAB_UNIFY_HANDOFF.md) | 위 마일스톤의 **인계 노트** (SRS 가 아니다). 스펙이 말하지 않는 것 — 왜 그렇게 됐는지, e2e 가 크게 바뀐 자리, e2e 정리에서 드러난 **구현 결함 열하나**. **닫혔다** (`d79fae0`) — 다음 작업은 아래 WORKBENCH 문서로 이어진다 |
| [WORKBENCH_REVIEW_SRS.md](./WORKBENCH_REVIEW_SRS.md) | **작업대 사용자 검토 — 결함 여섯과 신규 둘** (IEEE 29148, `FR-WBR-*`). 새 창은 **홈**에서 뜨고(UX_REVISION_SRS FR-CWD-1·4 폐기, `dmctl new-window --cwd` 신설), 재조정이 창을 지울 때 **저장하지 않은 편집을 지키며**, 탐색기 실패 메시지에 수명이 생기고, 편집기 줄바꿈이 설정으로 열린다. §7 의 결정 아홉이 본문보다 앞서며 그중 D-WBR-7·9 는 **조사가 뒤집은 것**이다. **1차 + 묶음 S 완료** |
| [WORKBENCH_REVIEW_HANDOFF.md](./WORKBENCH_REVIEW_HANDOFF.md) | 위 작업의 **인계 노트**. 메모 저장 조사에서 **헛짚은 가설 둘**(다시 파지 않도록), 아직 보지 않은 자리 둘(`_doc.saving` 이 남는 길 · 죽어 있는 소프트 리로드 트리 갱신), 메모리·신규 둘의 착수점, 회차마다 자리가 바뀌는 **e2e flake 관측** |
| [GIT_DIR_ENTRY_SRS.md](./GIT_DIR_ENTRY_SRS.md) | **디렉터리 상태 항목** (IEEE 29148, `FR-DIR-*`). 서브모듈·중첩 저장소는 **폴더가 상태의 단위**다 — `git status` 가 `dir/` 한 줄로 접는 것을 이름 있는 항목으로 펴고, 그 자리에 사유와 진입점을 보인다. 루트가 저장소 **안**이어도 색을 입힌다 (FR-DIR-40). **구현 완료** |
| [SANDBOX_PICK_COPY_SRS.md](./SANDBOX_PICK_COPY_SRS.md) | **샌드박스 선택창과 scratch 복사** (IEEE 29148, `FR-SPK-*`). 샌드박스 창을 열 때 작업 폴더를 고르고, 원본 대신 **복사본**을 컨테이너에 줄 수 있게 한다 — 그 사실은 창의 목록 배지로 남는다. **구현 완료** |
| [GIT_HEAD_MOBILE_SRS.md](./GIT_HEAD_MOBILE_SRS.md) | **Git 머리의 왼쪽 정렬 · History 이식 · 모바일 폭** (IEEE 29148). 상단 버튼의 정렬, Changes 의 버튼 무리를 History 에도 세우는 일, 좁은 폭에서의 배치. **구현 완료** |
| [GIT_REPO_MISSING_SRS.md](./GIT_REPO_MISSING_SRS.md) | **Git 리포 소실** (IEEE 29148). "저장소 폴더가 사라졌다" 를 서버가 **확정된 사유로 분류**하게 하고, 패널이 낡은 목록 대신 그 사실을 보이며 그 자리에서 핀을 제거할 수 있게 한다. **구현 완료** |
| [GIT_ACTIONS_SRS.md](./GIT_ACTIONS_SRS.md) | **Git 창의 동작 표면 완성** (IEEE 29148). "VSCode Git · Git Graph 의 GUI 기능을 다 갖춘다" 는 결정의 단일 진실 공급원. 범위의 출처는 `GIT_SURFACE_MAP.md` 의 126항목이다 |
| [GIT_FOLLOW_REMOVAL_SRS.md](./GIT_FOLLOW_REMOVAL_SRS.md) | **follow 제거와 리포 추가 개선** (IEEE 29148). `follow` 를 표면에서 걷어내고, 그것이 유일하게 하던 일(핀하지 않은 리포로 가는 한 번의 클릭)을 `+ Add` 가 대신하게 한다. **구현 완료** |
| [FILE_TRANSFER_SRS.md](./FILE_TRANSFER_SRS.md) | **파일 전송과 탐색기 이동** (IEEE 29148, `FR-FTR-*`). 업로드·다운로드를 바로잡고 그것을 **편집기의 파일 탐색기에서도** 쓰게 한다. 탐색기의 전송이 조회·조작과 **같은 루트 가드**를 받는 근거가 여기 있다. **구현 완료** |
| [BRACKETED_PASTE_SRS.md](./BRACKETED_PASTE_SRS.md) | **붙여넣기 모드를 물어보고 감싼다** (IEEE 29148). API·에이전트가 넣는 입력이 **조건 없이** bracketed paste 로 감싸여 나가던 것 — 받는 프로그램이 그 모드를 켜지 않았으면 제어열이 글자로 새어 들어간다. **구현 완료** |
| [BRIGHT_DARK_THEMES_SRS.md](./BRIGHT_DARK_THEMES_SRS.md) | **Bright Dark 테마 추가** (IEEE 29148). 다크이면서 배경 휘도·채도가 높아 "밝게 느껴지는" 테마군. 아카이브의 `THEMES_EXPANSION_SRS` 후속. **구현 완료** |
| [ICON_ASSETS_SRS.md](./ICON_ASSETS_SRS.md) | **아이콘·파비콘 자산** (IEEE 29148). 파비콘·터치 아이콘·PWA 아이콘의 크기·형식·생성 경로 |
| [RELEASE_SRS.md](./RELEASE_SRS.md) | **바이너리 배포 — GitHub Releases** (IEEE 29148). 쓰려면 클론해서 Go 로 빌드해야 하던 것을 릴리스 애셋으로 바꾼다. **바이너리를 저장소에 커밋하지 않는 근거가 실측으로 §2.1 에 있다**(5종 69MB · git 은 모든 판을 영구 보관 · 델타 압축이 먹지 않는다). darwin 을 macOS 러너에서 빌드해야 하는 이유(CGO 가 꺼지면 지표가 조용히 빠진다)도 여기다. **구현 완료** |
| [CROSS_PLATFORM_SRS.md](./CROSS_PLATFORM_SRS.md) | **OS 이음매의 인터페이스화 · Linux/WSL/Windows 지원** (IEEE 29148). darwin 전용이던 것은 설계가 아니라 **OS 의존 호출이 부르는 자리에 그대로 박혀 있었기 때문**이다. `syscall.Kill`·`Setsid`·`/proc`·`lsof`·`pgrep`·`/bin/bash`·unix 소켓을 `platform` 뒤로 모은다. §11 이 실기 기록이다 |
| [CROSS_PLATFORM_HANDOFF.md](./CROSS_PLATFORM_HANDOFF.md) | 위 작업의 인계 문서. 네 트랙 종료 시점의 상태와 CI 잡 넷이 초록이 된 근거. ConPTY 의 원인이 `STARTF_USESTDHANDLES` 하나였다는 결론이 §0 에 있다 |
| [WINDOWS_TEST_PARITY_SRS.md](./WINDOWS_TEST_PARITY_SRS.md) | **Windows 테스트 패리티** (IEEE 29148). `test (windows-latest)` 가 **도입 이래 한 번도 통과한 적이 없었다** — 한 번도 초록이 아니었으므로 아무도 읽지 않았고, 그 사이 실패 356줄 아래 **제품 결함 4건**이 묻혀 있었다. 초록이 아닌 검사는 없는 검사라는 사실의 기록. **구현 완료** |
| [E2E_UNIFICATION_SRS.md](./E2E_UNIFICATION_SRS.md) | **종단간 검증의 통일 — 세 OS 가 같은 목록을 돈다** (IEEE 29148, `FR-E2I-*`). macOS 는 셸 스크립트 21항목, Linux·Windows 는 CI 인라인으로 5단계를 돌았고 **세 벌이 서로의 목록을 몰랐다.** Go 한 벌(`dongminal verify`)로 모은다 — 두 벌이면 한쪽만 고쳐지고, 실제로 그런 적이 있다(D-3). **구현 완료** |
| [CI_E2E_MATRIX_SRS.md](./CI_E2E_MATRIX_SRS.md) | **브라우저 종단간은 세 OS 에서 돈다** (IEEE 29148, `FR-CEM-*`). 접수한 말은 "ci 는 모든 os 에 대해 e2e 를 해야한다" 였다. Playwright 스위트가 **ubuntu 하나**에서, 그것도 사람이 부를 때와 태그를 밀 때만 돌던 자리를 **세 OS · 매 푸시**로 옮긴다 — `retries: process.env.CI ? 2 : 0` 을 들고 있던 스위트가 매 푸시에 돌지 않았고, 한 OS 의 초록은 다른 OS 를 말하지 않는다. D-4·NFR-CEM-2 가 이 작업의 요체 — OS 분기를 1,257항목에 흩지 않고 `e2e/osenv.ts` 한 자리에 둔다. macOS 의 임시 디렉터리를 `os.tmpdir()` 로 통일하지 않은 근거(유닉스 소켓 경로 상한 104바이트)가 그 파일에 있다. D-5 는 통과하지 못하는 항목을 `skip` 으로 감추지 않는다는 규약 |
| [HANDOFF_WS2.md](./HANDOFF_WS2.md) | 인계 — WS-2 헤드리스 멤버 (`ORCHESTRATION_V2_SRS` §3.2, `FR-HLM-1~12`). 브리핑을 기억으로 쓴 사고의 기록이 머리말에 있다 |
| [HANDOFF_WS4.md](./HANDOFF_WS4.md) | 인계 — WS-4 패턴 카탈로그 + P2P 봉인 해제 (`ORCHESTRATION_V2_SRS` §3.4). 묶음 P 완료·승인 시점의 상태와 남은 하나 |
| [building.md](./building.md) | **빌드·검사·배포.** 받아 쓰는 사람에게는 필요 없다 — README 가 사용자 문서가 되면서 옮겨 온 개발자 절이다 (`README_REWRITE_SRS` FR-RDM-9) |
| [architecture.md](./architecture.md) | 패키지 레이아웃, **에이전트 접합면과 Run**, **에이전트 어댑터 레지스트리**, **멤버 프리앰블**, 어댑터 패턴, 커맨드 브로드캐스트, 핫패스 성능, 종료 경로 |
| [test-checklist.md](./test-checklist.md) | 백엔드·프론트엔드 동작 체크리스트 + 테스트 커버리지 현황 |
| [ENTITY_MODEL_RESTRUCTURE_SRS.md](./ENTITY_MODEL_RESTRUCTURE_SRS.md) | 엔티티 모델(Window ─ Pane ─ Tab ─ Tool)과 백그라운드 도구의 단일 진실 공급원. 요구 1·2 완료. §7 의 Run 접합면(FR-EM-17/18)은 후속 작업의 근거다 |
| [ENTITY_MODEL_HANDOFF.md](./ENTITY_MODEL_HANDOFF.md) | 위 작업의 인계 문서 — 확정된 모델과 근거, P1~P8 완료 내역, **반복하면 안 되는 함정 7개**, 검증 방법 |
| [USER_CHECKLIST_FIXES_HANDOFF.md](./USER_CHECKLIST_FIXES_HANDOFF.md) | 트랙 1(사용자 확인 피드백)의 인계 문서. 스펙·계획은 `archive/` 로 보관됐고 이 문서만 현행이다 — §4 의 **반복하면 안 되는 함정 15개**가 계속 유효한 필독 자료이기 때문 |
| [SYSTEM_STATS_SRS.md](./SYSTEM_STATS_SRS.md) | 상태바 지표 수집 재설계 (IEEE 29148). `/api/stats` 요청당 프로세스 6개·1.5초를 커널 직접 호출로 제거 + 메모리 계산식 정정. **구현 완료** (§4.1 실측) |
| [WORKSPACE_IDENTITY_SRS.md](./WORKSPACE_IDENTITY_SRS.md) | 식별자와 단일 실행자의 단일 진실 공급원 (IEEE 29148). 묶음 I(엔터티 id → uuid)·X(생성 명령 단일 실행자)·**U(모든 발급 지점을 uuid 로 통일 + 형태 판별 제거)**. **구현 완료** |
| [SKILL_INJECTION_SRS.md](./SKILL_INJECTION_SRS.md) | MCP 폐지와 세션 스코프 스킬 주입의 단일 진실 공급원 (IEEE 29148). 에이전트 접합면 = `dmctl` (액션) + `--plugin-dir`/`--settings` 주입 스킬·훅 (정책). **구현 완료** |
| [RUN_ORCHESTRATION_SRS.md](./RUN_ORCHESTRATION_SRS.md) | AI 오케스트레이터의 단일 진실 공급원 (IEEE 29148). Run 레코드·상태/대기 계약·에이전트 어댑터 레지스트리·worktree 격리·프리앰블/보고 계약·스킬 재작성. 착수 전 결정 5건 확정. **묶음 S·R·P·A·K 구현 완료, W 남음** |
| [ORCHESTRATOR_RESEARCH_NOTES.md](./ORCHESTRATOR_RESEARCH_NOTES.md) | 위 SRS 의 입력 — orca(MIT)·paseo(AGPL) **실제 소스** 조사 노트. 파일 경로 근거 포함. §9 는 기존 문서 서술을 뒤집은 5건 |
| [WAVE1_HANDOFF.md](./WAVE1_HANDOFF.md) | **3축 병렬 웨이브 1 의 완료 기록.** 워크스트림 **8개 전부 완료** — WS-1 식별자 uuid 전용화 · WS-2 헤드리스 멤버 · WS-3 컨텍스트 예산·승계 · WS-4 패턴 카탈로그+P2P · WS-5 Run 시각화 · WS-6 사이드바 탭 · WS-7 전경 프로세스 탭 이름 · WS-8 백그라운드 즉시 종료. §2 남은 것(수동 실사 M-1~M-10 · e2e 미실행) · §3 판정 기록 · **§5 반복하면 안 되는 것** — 브리핑을 기억으로 쓴 사고 3회, 대칭적 추종으로 같은 항목이 열 번 뒤집힌 사고, 게이트 전제, 플레이크 판정, "서버 완성 ≠ 기능 완성" · §7 커밋 분할 |
| [PARALLEL_DELIVERY_PLAN.md](./PARALLEL_DELIVERY_PLAN.md) | **아래 세 SRS 를 한 번에 다루는 실행 계획.** §2 확정 결정 12건(스펙보다 앞선다) → §3 파일 단위 충돌 지도(핫스팟 6개) → §4 **Step 0 골격 선행 커밋**(동작 불변 리팩터, 게이트 3종) → §5 8 워크스트림·배타적 파일 소유권·웨이브 배치 → §7 통합 게이트와 수동 실사 10항목. §6 은 dongminal 자신의 팀 Run 으로 실행하는 구성(부트스트랩 주의 포함), §8 은 후속 독립 트랙(expose 인증·cross-platform)의 착수점. **완료 (2026-08-28 웨이브 1)** |
| [ORCHESTRATION_V2_SRS.md](./ORCHESTRATION_V2_SRS.md) | **오케스트레이션 2세대** (IEEE 29148). 묶음 **I** 식별자 uuid 전용화(FR-IDU) · **H** 헤드리스 멤버(FR-HLM) · **C** 컨텍스트 예산·승계(FR-CBG) · **P** 패턴 카탈로그(FR-PAT) · **V** Run 시각화(FR-RVZ). §2.1 은 조사로 확정한 현재 상태 — **레이아웃 경로는 이미 uuid 전용이고 IO 경로가 라벨을 받는다**는 역전이 핵심 사실이다. **완료 (2026-08-28 웨이브 1)** |
| [ATTENTION_LIFECYCLE_GIT_OBSERVE_SRS.md](./ATTENTION_LIFECYCLE_GIT_OBSERVE_SRS.md) | **도구 알람의 수명과 핀 리포 전체 관측** (IEEE 29148). 묶음 **L** 도구가 죽으면 알람도 죽는다(FR-ATL — 활동만 정리하고 주의를 남기던 네 자리) · **J** 탭 없는 도구의 알람이 착지하는 자리(FR-ATJ) · **O** Git 탭 진입 시 핀 전부 관측(FR-GOB, `?observe=1`). §2.1 의 감사 A1~A14 가 근거다 — **주의와 활동은 직교하는 두 레이어인데 정리 규약이 활동에만 있었다**는 것이 알람 결함의 뿌리다. D-2 로 `GIT_SIDEBAR_TABS_SRS` FR-SBT-12(Git 탭 헤더 배지)를 **폐기**한다 — 관측을 Git 탭 안으로 가둔 이상 그 밖의 숫자는 근거가 없다. **구현 완료** |
| [GIT_SIDEBAR_TABS_SRS.md](./GIT_SIDEBAR_TABS_SRS.md) | **사이드바 상단 탭** (IEEE 29148). Windows/Git 의 세로 공존(`max-height:40%`)을 탭 전환으로 대체. FR-SBT-1~33. §3.6~3.8 은 2026-08-28 인터뷰로 추가된 것 — **탭의 서술자 인터페이스화**(새 탭 = 서술자 1개) · **탭 선택이 콘텐츠 창까지 전환** · **탭마다 직행 키 + 순회 키 재해석**. 이로써 `GIT_REMAINING` §1.2 의 보류건 **I5·I6 이 함께 닫힌다**. §7 은 철회된 결정 2건(D-4·D-6)과 그 근거. §4.1 은 착수 첫 작업 — 기존 Git e2e 의 가시성 전제 영향 범위 확정. **완료 (2026-08-28 웨이브 1)** |
| [CONVENIENCE_SRS.md](./CONVENIENCE_SRS.md) | **편의 기능** (IEEE 29148). 묶음 **N** 전경 프로세스 자동 탭 이름(FR-TAN, 수동 이름이 영구히 이긴다) · **X** 백그라운드 도구 즉시 종료(FR-BGK, 확인 필수·전체 종료 없음). §2.1(c) 가 핵심 제약을 고정한다 — **데몬 모드는 PTMX 를 노출하지 않으므로 전경 프로세스 조회는 데몬 안에서 수행하고 IPC 로 실어 나른다.** FR-TAN-23/24 는 그 조회를 단일 함수 + build tag 로 격리해 cross-platform 후속 트랙이 갈아낄 자리를 만든다. §6 비목표에 분리한 둘(expose 인증·cross-platform). **완료 (2026-08-28 웨이브 1)** |
| [GIT_SRS.md](./GIT_SRS.md) | **Git 창의 단일 진실 공급원** (IEEE 29148). FR-GIT-1~178, 검증 V1~V69, 21단계 구현 계획. §7 은 확정 결정 O1~O14, §7.1 은 요구사항 해석 I1~I8 |
| [GIT_REMAINING.md](./GIT_REMAINING.md) | **Git 트랙에서 아직 끝나지 않은 것 전부.** §1 사용자 검토 10건 — **오류 3건은 완료**(§1.3·§1.4 에 전수 조사 결과·E4·Git Graph 재구현), **개선 7건은 미착수이며 §1.2 에 착수점과 물어야 할 결정이 있다** · §2 수동 실사 잔여 · §3 문서 흡수 · §4 P1/P2 로 미뤄둔 기능(**2026-08-27 지시로 범위 안**, §4.1 의 자격증명·한글 안내문만 예외) · §5 알려진 간헐 실패 · §6 트랙 밖 별건. **목표는 여기 남은 전부를 끝내는 것이고, 이것이 다음 세션의 출발점이다** |
| [GIT_REVIEW4_SRS.md](./GIT_REVIEW4_SRS.md) | **4차 사용자 검토의 오류 3건 명세** (IEEE 29148). `GIT_SRS`·`GIT_UI_REVISION_SRS` 를 개정한다. **FR-RPT-1~8** 은 Git 밖까지 걸리는 교차 규약이다 — 바깥 계기(폴링·SSE)의 다시 그리기가 요소 상태(hover·더블클릭·드래그·선택·툴팁)를 깨뜨리지 않게 하는 규칙이고, 공통 수단은 `web/js/ui/repaint.js` 다. **§2.3.1 은 VSCode Git Graph 의 배치 규칙 R1~R4 를 근거 코드와 함께 고정한다.** FR-GIT-227~235, V104~V131. §3.6 은 개선 7건이 착수될 때 채워진다 |
| [GIT_SURFACE_MAP.md](./GIT_SURFACE_MAP.md) | 위 SRS 의 입력 — VSCode·gitMaster·Git Graph 의 기능 126개를 6개 표면(S1~S6)에 배치하고 P0/P1/P2 로 나눈 지도. **MVP = P0 38개** |
| [GIT_INTEGRATION_ANALYSIS.md](./GIT_INTEGRATION_ANALYSIS.md) | 같은 SRS 의 설계 근거 (Informative). §3.5 확정 설계(창 싱글턴·고정 탭·Monaco DiffEditor), §4.5 변경 감지 실측(fsnotify·watcher·fsmonitor 기각 근거) |
| [design/](./design/) | **21단계 구현 계약** (`GIT_M*_STEP*_CONTRACT.md`). 각 단계 착수 시 SRS 를 다시 해석하지 않고 이 문서를 단일 진실 공급원으로 삼는다. `design/README.md` 가 색인·픽스처 규약·검증 게이트 |
| [PACKAGE_RESTRUCTURE_SRS.md](./PACKAGE_RESTRUCTURE_SRS.md) | **프로세스 축 패키지 재구성의 단일 진실 공급원** (IEEE 29148). `internal/` 을 helper·daemon·webserver·ctl + `shared/` 로 재배치하고, 대형 패키지 3개(`server` 19,653줄 · `git` 10,936줄 · `app.js` 2,999줄)를 역할별로 갈랐다. §2.1 의 프로세스×패키지 실행 행렬과 §2.3 의 Go 메서드-패키지 제약 실측이 구조를 결정한 근거다. **16단계 전량 구현 완료** (§8.10·§8.11·§8.12) — 프로세스 축 밖 패키지 0개, `handlers_api.go` 701→262줄. §8 은 스펙 이탈 D-1~D-7 과 15·16단계 기록 — 특히 D-1·D-5 는 측정 방법의 결함(경계를 넘는 비공개 멤버 접근을 놓쳤다)을, §8.12 는 §5 비목표 #4 를 철회한 근거를 담는다 |
| [DEEPENING_REFACTOR_SRS.md](./DEEPENING_REFACTOR_SRS.md) | **얕은 모듈 깊이화 리팩터의 단일 진실 공급원** (IEEE 29148). 프로세스 축 구조 **안에서** 계약이 복제된 자리 여섯을 깊이화한다 — 묶음 A `apierr`(sentinel → (status, code) 등록부, 번역기 17개 흡수 + 전수성 강제), B git 쓰기 파이프라인(`gitwrite.go`, 순서 불변식을 타입이 쥔다), C 프론트 `gitFetch`(stale·echo 가드 회수), D `constants.js` 주제별 분할, E `Deps` 임베딩, F `ctl/cli` 액션 표. **전량 구현 완료** — §7 이 측정·무동작변경 증명·의도한 행위 변경 셋을 담는다. §1.1 은 복제가 이미 드리프트한 증거(`core.ErrRefName` 이 두 코드로 갈렸다)를, §3.2.2 는 묶음 B 가 드러낸 잠재 결함(nil Git 역참조 패닉, 검사가 22곳에 복제돼 세 곳이 빠져 있었다)을 적는다 |
| [SPLIT_REFACTOR_SRS.md](./SPLIT_REFACTOR_SRS.md) | **통짜 파일 넷 분할의 단일 진실 공급원** (IEEE 29148, 요구 번호 `FR-MSP-*`). 앞선 두 리팩터가 남긴 것은 깊이의 문제가 아니라 **자리**의 문제다 — 이 저장소가 이미 확립한 분할 관행(`Object.assign` 프로토타입 증강 · 타입이 곧 파일)을 네 파일만 따르지 않았다. 묶음 A `toolhub/tool.go` 1,390→5파일, B `git/panel.js` 2,984→10파일, C `ui/file-tree.js` 1,279→5파일, D `style.css` 2,673→4파일. **전량 구현 완료** — §2.2 는 `DEEPENING_REFACTOR_SRS` 비목표 N6("크지만 얕음의 증거를 못 찾았다")을 **뒤집지 않고** 판정 기준이 다름을 밝히고, §7.2 는 무동작변경을 diff·바이트 동일·멤버 집합으로 증명하며, §7.3 은 구간 경계가 메서드의 **선행 주석**을 앞 구간에 떨구는 것을 실측으로 기록한다 |
| [E2E_HELPER_RECLAIM_SRS.md](./E2E_HELPER_RECLAIM_SRS.md) | **e2e 헬퍼 회수와 죽은 코드 정리** (IEEE 29148, `FR-EHR-*`). `fixtures.ts` 가 있는데도 87개 스펙이 같은 헬퍼를 각자 정의하던 것(`waitForInit` 63 · `copyFx` 22 · `openGit` 20)을 거뒀다 — **본문이 바이트 동일한 것만**, 변종은 그대로. §2.1 이 검증 독립성 원칙(`toHaveCount(7)` 을 `GIT_VIEWS.length` 로 바꾸지 않는 이유), §7.5 가 억제 주석으로 덮으려다 지적이 가리키던 것 자체를 없앤 기록 |
| [FG_RESTORE_RACE_SRS.md](./FG_RESTORE_RACE_SRS.md) | **전경 이름 복원이 새 이름을 지우던 경쟁** (IEEE 29148, `FR-FGR-*`). `_fgRestore` 가 "`_attnRestore` 와 같은 규약" 이라 주석에 써놓고 그 규약(지울 후보를 요청 전에 확정)을 지키지 않았다. §7.1 이 **기각된 가설 둘의 기록**이고, §7.2 가 계측으로 확정한 근거다. 재시도로 덮지 않았다 |
| [APP_STATE_EXTRACT_SRS.md](./APP_STATE_EXTRACT_SRS.md) | **App 의 상태를 소유자에게 돌려주는 리팩터** (IEEE 29148, `FR-ASE-*`). 묶음 A(`RunsPanel`)만 완료 — `app-runs.js` 730→38줄. **§7.2 가 앞선 측정의 정정**(`this.<이름>` 을 전부 필드로 세어 56%→실제 46%), **§7.3 이 추출의 세 조건**, §2.3 이 최대 함정(형제 파일은 `app.` 이 아니라 `this.` 로 만진다). 남은 후보 셋은 현재 측정으로 **전부 보류** |
| [REFACTOR_HANDOFF.md](./REFACTOR_HANDOFF.md) | **`refactor` 브랜치 인수인계.** 네 단계의 결과·반드시 지킬 절차·손대지 않기로 한 것과 사유. 다음 세션은 [REFACTOR_NEXT_SESSION.md](./REFACTOR_NEXT_SESSION.md) 를 첫 메시지로 붙여넣는다 |
| [CLI_CONSOLIDATION_SRS.md](./CLI_CONSOLIDATION_SRS.md) | 운영 스크립트 8개를 바이너리 액션 4개(`start`/`stop`/`migrate`/`health`)로 통합하고 `scripts/` 에 `build.sh` 하나만 남긴 근거 (IEEE 29148). **구현 완료** |
| [MOBILE_TUI_INPUT_SCROLL_SRS.md](./MOBILE_TUI_INPUT_SCROLL_SRS.md) | **모바일 TUI 입력·스크롤 교정의 단일 진실 공급원** (IEEE 29148). FR-MTI-1~19, TC-MTI-1~14. xterm 을 포크하지 않고 앱 계층에서 교정한다 — `beforeinput` 가로채기(중복·유실 제거), 터치 감도 배율+관성, `visualViewport` fit 병합, 키바 포커스, sticky modifier. **구현 완료** |
| [MOBILE_TUI_SCROLL_INPUT_ANALYSIS.md](./MOBILE_TUI_SCROLL_INPUT_ANALYSIS.md) | 위 SRS 의 입력 — 실측 근거 (Informative). 대상 TUI 가 켜는 터미널 모드 캡처, 터치 1:1 스크롤 수치, `CompositionHelper._handleAnyTextareaChanges` 의 중복·유실 재현. §2 는 초기 판정(`_inputEvent` 게이트)을 관측 시점 오류로 정정한 기록을 남긴다 |
| [GIT_MANUAL_CHECKLIST.md](./GIT_MANUAL_CHECKLIST.md) | Git 창 수동 검증 체크리스트 (V14·V60). 자동 테스트가 잡지 못하는 것만 — 배치·색·읽힘, 모바일 실기기, 성능·보안 기준. 픽스처(`e2e/git_fixture.sh`) 기준 |
| [NEXT_SESSION_PROMPT.md](./NEXT_SESSION_PROMPT.md) | 다음 세션 첫 메시지로 붙여넣을 프롬프트(파일 전체가 그대로 첫 메시지다). **열려 있는 것은 Git 창 하나** — 재구성 트랙은 16단계로 닫혔다. `GIT_REMAINING.md` 가 출발점이고, 착수 전에 물어야 할 것(단축키 배정 · "원래 있던 윈도우"의 정의 · 자격증명 배제 유지 여부)과 반복하면 안 되는 함정(`stop` 의 포트 기반 대상 선정, BSD `sed` 의 `\b`, 보호 테스트 약화)을 담는다 |
| [ACCESS_ALLOWLIST_SRS.md](./ACCESS_ALLOWLIST_SRS.md) | **접속 허용 목록** (IEEE 29148, `FR-ACL-*`). `--expose` 는 `0.0.0.0` 에 바인드하는 것이 전부였고 **그 뒤에 어떤 접근 제어도 없었다** — 그런데 이 서버가 여는 표면은 PTY·파일 읽기/쓰기/zip·명령 실행이다. 즉 `--expose` 는 인증 없는 원격 코드 실행 종단을 네트워크에 여는 일이었다. 출발지 IP 로 가르는 목록을 세운다. 짝 문서는 `REQUEST_GATE_SRS`(그쪽이 **누가 부를 수 있는가**). **구현 완료** |
| [ACCESS_DENIED_PAGE_SRS.md](./ACCESS_DENIED_PAGE_SRS.md) | **막힌 출발지에게 보이는 화면** (IEEE 29148, `FR-ADP-*`). 종전에는 `403 forbidden` **평문 한 줄**이었고, 그 한 줄은 무엇이 막혔는지도 무엇을 해야 하는지도 말하지 않았다 — 접속하려던 사람은 **자기 IP 를 알 방법이 없고**, 소유자는 무엇을 허용해야 하는지 들을 방법이 없었다. **구현 완료** |
| [AGENT_ADAPTER_COMPLETION_SRS.md](./AGENT_ADAPTER_COMPLETION_SRS.md) | **어댑터가 에이전트의 전부를 안다 — 이름을 한 자리로** (IEEE 29148, `FR-AAC-*`). `AGENT_EVENT_ABSTRACTION_SRS` 가 **이벤트**를 어댑터로 올렸고, 그때 올리지 않은 셋(훅 설치·전사본 해석·컨텍스트 창)이 전부 claude 의 구현 세부에 묶여 있었다. **구현 완료** |
| [AGENT_EVENT_ABSTRACTION_SRS.md](./AGENT_EVENT_ABSTRACTION_SRS.md) | **에이전트 이벤트를 선언으로 추상화하고 알람을 그 위에 세운다** (IEEE 29148, `FR-AEV-*`). 증상은 "`omp` 가 활동을 보고하는데 알람이 울리지 않는다" 였고, **원인은 omp 의 결함이 아니라 배선이 에이전트마다 손으로 쓰여 있다는 것**이었다(§2.1). 그래서 고칠 것이 한 자리가 아니라 구조였다 — 이벤트를 **선언**으로 만들면 새 에이전트를 붙일 때 빠진 것이 드러난다. **구현 완료** |
| [AGENT_GUI_REMOVAL_SRS.md](./AGENT_GUI_REMOVAL_SRS.md) | **에이전트 GUI 제거** (`FR-AGR-*`). `AgentPane` 탭 · `type:'agent'` · `toolhub.KindAgent` · `/api/agent/*` · `domain/agentsess` 를 걷고 **어댑터 한 벌**(`internal/shared/agentadapter`)만 남긴다. 제거 대상은 에이전트를 GUI 대화 뷰로 **직접 구동하는 길** 전체다. **구현 중** |
| [AGENT_PROTOCOL_SURFACE_SRS.md](./AGENT_PROTOCOL_SURFACE_SRS.md) | **에이전트를 프로토콜로 붙이고 훅을 걷는다** (IEEE 29148). 에이전트를 PTY 바이트스트림이 아니라 프로토콜로 붙여 "지금 무엇을 하는가" 를 추정이 아닌 사실로 만들려던 문서다. **대체됨** — GUI 갈래가 `AGENT_GUI_REMOVAL_SRS` 로 걷혔다. |
| [AGENT_RENDER_ENV_SRS.md](./AGENT_RENDER_ENV_SRS.md) | **에이전트가 화면을 망가뜨리지 않게 띄운다** (`FR-ARE-*`). Claude Code 는 터미널 폭이 바뀌면 **옛 프레임을 스크롤백에 남기고**, 기기를 오가며 쓰면 그것이 쌓여 위쪽 대화를 읽을 수 없게 된다. 터미널 쪽에서 되살릴 수 없다는 것은 조사로 확정됐다(`M11_SRS` §2.4e·2.4f) — 스크롤백에 남은 것은 **이미 배치가 끝난 글자**이고 원본을 가진 것은 앱뿐이다. 그래서 앱을 띄우는 **환경**을 고친다. **구현 완료** |
| [API_ANSWER_NOT_ABSENCE_SRS.md](./API_ANSWER_NOT_ABSENCE_SRS.md) | **답을 부재로 적지 않는다 — 가짜 404 를 가른다** (IEEE 29148, `FR-ANA-*`). 사용자 보고 `U-22`. "저장소가 아니다" 는 **답**인데 그것을 `404` 로 적으면 없는 종단과 구별되지 않는다 — 콘솔이 붉은 줄로 차고, 진짜 부재가 그 속에 묻힌다. **구현 완료** |
| [APP_TESTING_CONTRACT_SRS.md](./APP_TESTING_CONTRACT_SRS.md) | **e2e 는 공개 계약만 본다** (IEEE 29148, `FR-ATC-*`). 실측(2026-09-12): **`app._xxx` 507회 · 86파일 · 서로 다른 이름 114개** — e2e 가 앱의 내부를 직접 짚고 있었고, 그래서 내부를 고치면 검사가 깨졌다. 로드맵이 적은 533/81 을 이 문서가 정정한다. **구현 완료** |
| [BOOT_SCREEN_REUSE_SRS.md](./BOOT_SCREEN_REUSE_SRS.md) | **다시 세우는 모든 동작에 로딩 화면** (IEEE 29148, `FR-BTR-*`). 로딩 화면이 도는 것은 **브라우저 새로고침 하나뿐**이었고, 나머지 새로고침 셋은 낡은 화면이나 빈 골격을 그대로 노출했다. **구현 완료** |
| [BOOT_SCREEN_SRS.md](./BOOT_SCREEN_SRS.md) | **부팅 화면과 테마 선주입** (IEEE 29148, `FR-BTS-*`). 페이지를 열 때 **저장된 테마가 아닌 기본 테마(Tokyo Night)가 먼저 그려지던 것**을 없애고, 앱이 쓸 수 있는 상태가 될 때까지 부팅 화면으로 덮는다. 선주입이 `head` 에 사는 이유가 요체다 — 스크립트가 서기 전에 색이 정해져야 한다. **구현 완료** |
| [CI_GATES_SRS.md](./CI_GATES_SRS.md) | **CI 게이트 — 최소 스펙**. **검사기가 있어도 돌지 않으면 없는 것과 같다.** 이 저장소가 그 값을 이미 치렀다 — `check-seams.sh`·`check-timers.sh` 는 진작 있었는데 아무도 CI 에 걸지 않았고, 그래서 `runtime.GOOS` 가 `platform` 밖으로 샌 채 **main 이 초록이었다**. §3 이 flaky 정책의 자리이며(재시도 한 번에 통과한 것까지가 flaky, 수와 트레이스는 남긴다), `DM_E2E_STRICT_FLAKY=1` 이 그것을 실패로 올린다. **구현 완료** |
| [CLIENT_API_SRS.md](./CLIENT_API_SRS.md) | **브라우저의 API 호출을 한 자리로** (IEEE 29148, `FR-CAPI-*`). 브라우저가 서버를 부르는 자리가 **29파일 82곳**이었고 그 하나하나가 같은 네 줄(`fetch`·`json`·`catch`·기본값)을 다시 썼다 — 오류 규약이 82벌이면 한 벌만 고쳐진다. **구현 완료** |
| [CONFIG_MANAGEMENT_SRS.md](./CONFIG_MANAGEMENT_SRS.md) | **설정 관리 — 서술자 단일 원천 · `server.json` · 환경변수** (IEEE 29148, `FR-CFG-*`). **설정에 원천이 없었다** — 같은 키 목록이 세 곳에 손으로 적혀 있고, 서버가 자기 기동값을 어디서 받는지의 규칙은 코드에만 있었으며, 환경변수 넷은 문서 어디에도 없었다. 셋을 한 번에 다루는 근거: **나누면 서술자 표를 세 번 고친다.** **구현 완료** |
| [CONTEXT_MENU_UNIFY_SRS.md](./CONTEXT_MENU_UNIFY_SRS.md) | **컨텍스트 메뉴 한 벌 — `UIKit.menu` 위의 `GitMenu`** (IEEE 29148, `FR-CMU-*`). 메뉴가 두 벌이었다 — `UIKit.menu` 는 키보드 이동이 없고 비활성 사유를 보이지 않았으며, `GitMenu` 는 둘 다 갖되 자기 DOM·CSS 를 따로 그렸다. 그리고 셋(분할 칸 **탭** · 탐색기의 **빈 여백** · 터미널 **본문**)에는 메뉴가 아예 없었다. **구현 완료** |
| [DAEMON_STALENESS_SRS.md](./DAEMON_STALENESS_SRS.md) | **낡은 데몬의 감지** (IEEE 29148, `FR-DFP-*`). 빌드한 직후 **지금 도는 데몬이 그 빌드로 갈아탈 필요가 있는지**가 드러나지 않았다. 그래서 사람은 둘 중 하나를 했다 — 늘 `--restart-daemon` 을 붙여 **필요 없는 터미널 세션 손실**을 감수하거나, 붙이지 않고 낡은 데몬 위에서 확인하거나. **구현 완료** |
| [DIFF_HUNK_BAR_SRS.md](./DIFF_HUNK_BAR_SRS.md) | **Diff 탭의 부분 스테이징을 커서 툴바로** (IEEE 29148, `FR-DHB-*`). Diff 탭이 같은 diff 를 **두 자리에** 그리고 있었다 — 위는 Monaco DiffEditor, 아래는 줄 단위 스테이징용 목록. 하나로 합치고 조작을 커서 자리의 툴바로 옮긴다. **구현 완료** |
| [DOC_RENDER_VIEW_SRS.md](./DOC_RENDER_VIEW_SRS.md) | **문서의 렌더 뷰 (Markdown · SVG · HTML · 표)** (IEEE 29148, `FR-DRV-*`). 편집기가 **그림으로 그리는 것이 이미지 파일 하나뿐**이었다 — `README.md` 도 `icon.svg` 도 `data.csv` 도, 사람에게 보이려고 만들어진 문서인데 전부 소스 텍스트였다. **구현 완료** |
| [DOCLANG_LSP_SRS.md](./DOCLANG_LSP_SRS.md) | **문서 언어의 코드 탐색 (Markdown · SVG · YAML · JSON · HTML · CSS)** (IEEE 29148, `FR-DLS-*`). 언어 서버가 서는 파일이 셋뿐(Go · TS/JS · Python)이라 `.md` 를 열면 세션이 서지 않던 것을 넓히려던 문서다. **폐기** |
| [DRIFT_RECLAIM_SRS.md](./DRIFT_RECLAIM_SRS.md) | **드리프트 회수 리팩터** (IEEE 29148, `FR-DRC-*`). `DEEPENING_REFACTOR_SRS` 가 세운 여섯 초크포인트는 지금도 서 있는데, 그 뒤로 들어온 기능 개발이 **그 옆으로 자랐다.** 근본 문제는 복제가 생긴 것이 아니라 — **초크포인트에 게이트가 없어서** 복제가 생긴 것이다. 회수하고 게이트를 세운다. **구현 완료** |
| [E2E_FLAKY_ISOLATION_SRS.md](./E2E_FLAKY_ISOLATION_SRS.md) | **flaky 독립 재실행 — 흔들림에 재현 수단을 준다** (IEEE 29148, `FR-EFI-*`). `retries: 1` 이 봐준 항목을 **격리된 조건에서 세 번 더** 돌려 그것이 **부하**인지 **결함**인지 그 회차 안에서 가른다. 종전 게이트는 flaky 를 세기만 했다. **구현 완료** |
| [E2E_PARALLEL_SRS.md](./E2E_PARALLEL_SRS.md) | **브라우저 종단간을 병렬로** (IEEE 29148, `FR-EPL-*`). 1,257항목이 **한 줄로** 돌아 로컬(macOS) 22분, Windows 러너는 8샤드로 갈라도 한 샤드가 한 시간을 넘겼다. **구현 완료** |
| [E2E_QUIESCENCE_SRS.md](./E2E_QUIESCENCE_SRS.md) | **e2e 가 저장의 정착을 기다린다** (IEEE 29148, `FR-EQS-*`). 전체 e2e 에서 22개가 무너졌고 **그중 열하나는 제품이 아니라 검증이 틀린 것**이었다 — 검증이 화면의 저장이 서버에 닿기 전에 서버를 읽거나 새로고침했다. **구현 완료** |
| [EDITOR_DIRTY_DIFF_SRS.md](./EDITOR_DIRTY_DIFF_SRS.md) | **Editor 편집기의 변경 표시 (dirty diff)** (IEEE 29148, `FR-EDD-*`). VS Code 가 *dirty diff* 라 부르는 것 — 줄번호 왼쪽의 좁은 세로 막대와 overview ruler 의 색 자국. 종전에 그것을 가진 화면은 Diff 탭뿐이었다. **구현 완료** |
| [EDITOR_EXTERNAL_CHANGE_SRS.md](./EDITOR_EXTERNAL_CHANGE_SRS.md) | **편집기와 외부 변경** (`FR-EXC-*`). 편집기로 연 파일이 **밖에서 바뀌었을 때** 무엇을 보일지 정한다. 대상은 `FileEditor` 가 연 텍스트 파일이다(이미지·이진은 열지 않으므로 대상이 아니다). **구현 완료** |
| [EDITOR_FIND_PANEL_SRS.md](./EDITOR_FIND_PANEL_SRS.md) | **편집기 파일 내 찾기 패널** (IEEE 29148). 접수는 결함 보고였고 **조사가 증상을 더 심각하게 확정했다**(§2.4) — 찾기 키를 누른 뒤 타이핑하면 그 글자가 **문서에 삽입된다.** 검색하려고 누른 키가 파일을 고치고 있었다. **구현 완료** |
| [EDITOR_LIVE_RELOAD_SRS.md](./EDITOR_LIVE_RELOAD_SRS.md) | **열어 둔 파일이 밖에서 바뀌면 곧바로 따라간다** (`FR-ELR-*`). 뒷문장(바뀐 사실을 안다)은 `EDITOR_EXTERNAL_CHANGE_SRS` 가 이미 참으로 만들었고, 이 문서가 더하는 것은 앞문장 하나다 — **clean 인 편집기는 묻지 않고 따라간다.** **구현 완료** |
| [EDITOR_LSP_SRS.md](./EDITOR_LSP_SRS.md) | **편집기의 코드 탐색 (LSP)** (IEEE 29148, `FR-LSP-*`). Editor 창의 편집기가 **글자만 알던** 것을 고친다 — 문법 색칠은 Monaco 의 정적 문법이었고, `needle` 이 함수인지 변수인지 어디서 정의됐는지는 아무도 몰랐다. **구현 완료** |
| [EDITOR_MINIMAP_TOGGLE_SRS.md](./EDITOR_MINIMAP_TOGGLE_SRS.md) | **편집기 미니맵 토글** (IEEE 29148, `FR-LVC-*`). `minimap.enabled` 가 코드에 `true` 로 박혀 있어 끄는 길이 없던 것을 `Settings ▸ Display` 의 스위치 하나로 연다. **구현 중** |
| [ERROR_CONTRACT_SRS.md](./ERROR_CONTRACT_SRS.md) | **오류 규약 — 코드·카탈로그·요청 ID** (IEEE 29148, `FR-ERR-*`). **오류에 이름이 없었다** — `http.Error(w, "bad request", 400)` 이 65곳이고 그 문자열은 사람이 읽는 말이지 클라이언트가 분기할 수 있는 것이 아니다. 같은 400 이 열 가지 이유로 나는데 브라우저는 그중 어느 것인지 알 길이 없었다. 사용자용 문장은 `docs/external/errors.md` 가 받는다. **구현 완료** |
| [EVENT_TIMER_HUB_SRS.md](./EVENT_TIMER_HUB_SRS.md) | **시간과 전파를 두 클래스가 소유한다** (IEEE 29148, `FR-BUS-*`). 브라우저 갱신에는 축이 둘이다 — *언제* 도는가(시간)와 *무엇이 누구에게* 가는가(전파). 그 둘이 **33개 파일과 13개 `if` 분기**에 흩어져 같은 규약이 서로를 모른 채 최대 **4벌** 재구현돼 있었다. **구현 완료** |
| [EXPLORER_MULTI_SELECT_SRS.md](./EXPLORER_MULTI_SELECT_SRS.md) | **탐색기의 다중 선택** (IEEE 29148, `FR-EMS-*`). 사용자 요구 `U-5`(`Cmd`+클릭 · `Shift`+클릭). 선택이 문자열 하나(`FileTree._sel`)였던 것을 **집합**으로 넓히되 **넓히는 것과 소비하는 것을 가른다** — 선택을 읽는 자리가 열 곳이 넘는다. **구현 완료** |
| [FE_MODULE_BOUNDARY_SRS.md](./FE_MODULE_BOUNDARY_SRS.md) | **프론트 모듈 경계 — 거대 모듈 분리와 계층 역전** (IEEE 29148, `FR-FMB-*`). 로드맵 §M6 의 마지막 단계로, 앞의 일곱이 **동작**을 고쳤다면 이 단계는 **자리와 계약**을 고친다. 두 문제를 묶음으로 가르는 이유는 섞으면 어느 쪽도 증명할 수 없기 때문이다. **구현 완료** |
| [FILE_API_BOUNDARY_SRS.md](./FILE_API_BOUNDARY_SRS.md) | **`/api/file/*` 의 경계** (IEEE 29148, `FR-FAB-*`). ⚠ **경계 조항은 폐기됐다**(2026-09-20, 사용자 결정) — 묶음 B·W·G·O 가 효력을 잃고 `/api/file/*`·`/api/git/*` 은 **절대경로면 어디든** 읽고 쓴다. 문서 머리가 **무엇이 열렸는지**를 표로 적는다: `ext` 매니페스트(임의 명령 실행) · `access.json`(ACL 무력화) · 파일시스템 전체. 살아 있는 조항은 §3.3 묶음 S(크기 상한)뿐이며 그것은 경계가 아니라 브라우저 보호다. `SECURITY.md` §4-5 가 짝이다. **폐기(부분)** |
| [FS_LIST_PAGING_SRS.md](./FS_LIST_PAGING_SRS.md) | **잘린 폴더의 나머지에 닿는 길** (IEEE 29148, `FR-FSL-*`). 감사 `FUI-10`. 종전에는 **사실만 말하고 끝났다** — *"잘림. N개 표시"* 는 사용자가 이미 짐작한 것이고, 나머지에 닿는 길이 없었다. **구현 완료** |
| [GIT_CHANGES_CONTROLS_SRS.md](./GIT_CHANGES_CONTROLS_SRS.md) | **Changes 사이드의 손잡이 정리** (IEEE 29148, `FR-GCC-*`). 손잡이가 너무 많고 생김새가 제각각이었다 — 쓰이지 않는 둘(`Sync`·`Push preview`)을 화면·클라이언트·**서버 API** 까지 걷고, 남는 것을 한 벌의 아이콘 언어로 모은다. **구현 완료** |
| [GIT_DETECT_TIER_SRS.md](./GIT_DETECT_TIER_SRS.md) | **감지는 두 단이다 — 싼 것을 자주, 비싼 것을 필요할 때** (IEEE 29148, `FR-GDT-*`). 사용자 진단이 정확했다(`GP-7`) — `hub/gitwatch.go` 가 감시 회차마다 `Store.Status()` 를 부르고, TTL 200ms 캐시를 지나도 1초 주기에서는 매번 실행이 된다. **구현 완료** |
| [GIT_EXEC_UNIFY_SRS.md](./GIT_EXEC_UNIFY_SRS.md) | **git 실행 층을 하나로 — 인가는 그대로** (IEEE 29148, `FR-GXU-*`). git 프로세스를 띄우는 자리가 **다섯**이고 다섯이 각자 환경·타임아웃·출력 상한·취소·오류 분류·기록을 가졌다. `core.Env()` 를 쓰는 자리는 그중 둘뿐 — 나머지 셋은 사람을 기다릴 수 있었다. **구현 완료** |
| [GIT_LIVE_TRIGGERS_SRS.md](./GIT_LIVE_TRIGGERS_SRS.md) | **되살리기의 계기 — 앱당 한 벌의 복귀 신호와 워치독** (IEEE 29148). git 실시간 갱신의 주 경로는 서버의 `git_changed` 방송이고(`GIT_PUSH_OBSERVE_SRS`), **그 방송의 생명선은 브라우저의 `/api/git/status` 요청**이다 — 그 요청이 곧 관심의 표명이므로 끊기면 방송도 멎는다. **구현 완료** |
| [GIT_OBSERVE_REVIVE_SRS.md](./GIT_OBSERVE_REVIVE_SRS.md) | **멈춘 git 관측은 실제로 되살아난다 — 워치독의 공회전 교정** (IEEE 29148). 접수는 브라우저 콘솔 한 뭉치였다 — `관측이 멈춰 있어 되살립니다 — age=never` 가 반복되는데 **되살아나지 않았다.** **구현 완료** |
| [GIT_PUSH_OBSERVE_SRS.md](./GIT_PUSH_OBSERVE_SRS.md) | **git 관측을 서버가 밀어 준다** (IEEE 29148, `FR-BGV-*`). 브라우저가 저장소 하나를 보고 있으면 **60초에 180번** 묻는다(signature 120 · status 60) — 변화가 한 번도 없어도. 그 물음의 답은 **누가 묻든 같고** 서버는 이미 알고 있다. **구현 완료** |
| [GIT_REFRESH_LIFECYCLE_SRS.md](./GIT_REFRESH_LIFECYCLE_SRS.md) | **git 갱신·수명 규약 — 멈춘 것은 스스로 돌아오고, 멈춘 사실은 보인다** (IEEE 29148, `FR-GLW-*`). 전수 감사 18건 중 **감지가 아니라 갱신**(화면에 오는가) 쪽 결함 아홉을 닫는다. **구현 완료** |
| [GIT_WATCH_LEASE_SRS.md](./GIT_WATCH_LEASE_SRS.md) | **감시 임대를 SSE 구독에 맡긴다** (IEEE 29148, `FR-GLW-*`). `gitStatusInterval` 을 `끔`이나 `2분`으로 두면 **서버의 자동 push 까지 함께 죽었다** — 요청을 줄이려고 고른 설정이 자동 갱신을 통째로 끄는데 화면에는 아무 표시도 없었다. **구현 완료** |
| [HOST_PARITY_SRS.md](./HOST_PARITY_SRS.md) | **훅과 알람이 세 대상에서 같게 동작한다** (IEEE 29148, `FR-HPR-*`). 접수는 "Windows 에서 알람이 모든 명령마다 뜨고 claude 훅이 걸리지 않는다" 였고, 파 내려가 **결함 9건**이 나왔다. 그중 하나는 Windows 가 아니라 **Linux 의 것**이었다 — bash 도구 셸에서 훅이 아예 로드되지 않았고, macOS 가 zsh 기본이라 가려져 있었다. **구현 완료** |
| [LEAVE_CONFIRM_TOGGLE_SRS.md](./LEAVE_CONFIRM_TOGGLE_SRS.md) | **나가기 확인 토글** (IEEE 29148). 도구가 하나라도 열려 있으면 브라우저의 떠남 확인이 반드시 떴고 **그 가드는 끌 수 없었다.** **구현 완료** |
| [LSP_PLUGIN_SRS.md](./LSP_PLUGIN_SRS.md) | **언어 서버는 플러그인으로 온다** (IEEE 29148, `FR-LSP-*`). 사용자의 말이 근거다 — *"embedded lsp 는 빼자. 대신 플러그인 형식으로 넣자. 컴퓨터에는 영향을 미치지 않고 이 dongminal 에서만 쓰이도록 격리하자."* 종전 방식은 두 자리에서 그 요구를 어겼다. **구현 완료** |
| [LSP_WINDOWS_PORTABILITY_SRS.md](./LSP_WINDOWS_PORTABILITY_SRS.md) | **언어 서버가 Windows 에서도 발견된다** (IEEE 29148). 접수한 것이 **CI 다** — LSP M1~M5 를 푸시하자 `test (windows-latest)` 가 여덟을 냈다. **구현 완료** |
| [M10_SRS.md](./M10_SRS.md) | **M10 — 돌아온 기기가 자기 폭을 되찾는다** (`FR-M10-*`). `FR-M9-3` 이 세운 것은 비소유자가 PTY 를 **따라가는** 길이었고, 없던 것은 소유를 **되찾을 때 자기 폭으로 돌아오는** 길이다. **구현 중** |
| [M11_SRS.md](./M11_SRS.md) | **M11 — 잔재를 기록으로 잡고, 에이전트 GUI 를 읽을 수 있게 만든다**. 둘이다 — **M11-B1** 은 인계 중 접수한 **렌더 잔재**(공백이어야 할 칸에 옛 글자가 남는다)로, M10 의 폭 수정 뒤에도 남았고 폭이 아니라 **칸을 지우는** 문제였다. §2.4e·2.4f 가 `AGENT_RENDER_ENV_SRS` 의 근거가 된 조사다. **구현 중** |
| [M12_SRS.md](./M12_SRS.md) | **M12 — 에이전트의 차이는 어댑터에서 끝난다**. 넷이며 **둘째가 본체이고 나머지 셋은 그 둘레다.** **구현 중** |
| [M8_UNIFIED_SRS.md](./M8_UNIFIED_SRS.md) | **M8 통합 일정 — Go 부채 · 국제화 · 에이전트 프로토콜 표면** (IEEE 29148). 셋을 따로 두면 **서로를 딛는다는 사실이 순서를 정해 버리고**, 그 순서를 지키면 각각이 반쪽으로 끝난다. §3.3 이 언어 정책의 원문이다(`FR-B-1`) — UI 는 한·영, 범위 밖은 CLI 출력·서버 로그·오류 본문 셋. **구현 완료** |
| [M9_SRS.md](./M9_SRS.md) | **M9 — M8 이 남긴 것 + 추가 이슈** (`FR-M9-*`). M8 P7 이 **사유와 함께 남긴 것**을 거두고 사용자가 더한 이슈를 같은 일정에 싣는다. 착수 첫 일이 **재감사**인 것이 이 문서의 규약이다 — 수치·위치는 실측으로 정정한다. `OpSize`(0x05) 크기 통보가 여기서 섰다(`FR-M9-3`). **구현 완료** |
| [MONACO_VENDORING_SRS.md](./MONACO_VENDORING_SRS.md) | **Monaco 벤더링과 사전압축 정적 서빙** (IEEE 29148, `FR-MVN-*`). 편집기를 **런타임에 서드파티 CDN 에서 받고 있었다**(`file-editor.js:5`) — 망이 없으면 편집기가 서지 않고, 받는 곳이 우리 것이 아니다. **구현 완료** |
| [OBSERVABILITY_SRS.md](./OBSERVABILITY_SRS.md) | **관측성 — 구조화 로그 · 요청 ID · 진단 종단 · 크래시 마커** (IEEE 29148, `FR-OBS-*`). **배포된 제품에서 들어온 문제를 받을 수 있어야 한다.** 없던 것 셋 — 로그에 **수준이 없고**, 줄끼리 **이어지지 않으며**(한 요청의 세 줄을 사람이 시각으로 맞춰야 했다), 상태를 한 번에 묻는 자리가 없었다. **구현 완료** |
| [OMP_AGENT_SUPPORT_SRS.md](./OMP_AGENT_SUPPORT_SRS.md) | **지원 에이전트에 `omp`(oh my pi)를 더한다** (IEEE 29148). 사용자 요구 `U-21`. 아는 에이전트가 둘(`claude` 검증 대상 · `codex` best-effort)이던 것에 **세 번째 선언**을 더한다. **구현 완료** |
| [OPTIMISTIC_LAYOUT_SRS.md](./OPTIMISTIC_LAYOUT_SRS.md) | **아직 저장되지 않은 로컬 레이아웃은 원격 채택에 지지 않는다** (IEEE 29148, `FR-OPL-*`). e2e 전량에서 되풀이된 흔들림 아홉 중 **다섯이 같은 증상**이었다 — **방금 연 git 뷰 탭(또는 그 탭을 담은 창)이 사라진다.** **구현 완료** |
| [PANE_DOM_RECONCILE_SRS.md](./PANE_DOM_RECONCILE_SRS.md) | **레이아웃 DOM 재조정 — 살아 있는 위젯을 움직이지 않는다** (IEEE 29148, `FR-PDR-*`). TUI 를 띄운 터미널에서 화면이 스스로 맨 위로 올라가고 아무 키나 누르면 되돌아오던 결함을 **근본 원인에서** 없앤다. 근본 원인은 스크롤 복원의 계산 실수가 아니라 `render()` 가 레이아웃 DOM 을 다시 만드는 것이었다. **구현 완료** |
| [POLL_INTERVAL_SETTINGS_SRS.md](./POLL_INTERVAL_SETTINGS_SRS.md) | **폴링 주기 설정 — 한 자리에 모으고 죽은 계층을 걷는다** (IEEE 29148). 주기의 진실이 `STATE_REGISTRY` 로 모인 것은 `EVENT_TIMER_HUB_SRS` 가 한 일이고, 참이 아니던 뒷부분(사용자가 그것을 조절할 자리)을 이 문서가 연다. **구현 완료** |
| [REFACTOR_STABILIZATION_SRS.md](./REFACTOR_STABILIZATION_SRS.md) | **중복을 접고 구조를 안정화한다** (IEEE 29148, `FR-RST-*`). 계기는 e2e 였다 — 전체 실행마다 **서로 다른** 2~3개가 무작위로 실패했고, 하나를 고치면 다음 실행에서 다른 파일이 **같은 이유로** 무너졌다. **구현 완료** |
| [REPO_SIDE_WIDTH_SRS.md](./REPO_SIDE_WIDTH_SRS.md) | **Repo 창 사이드의 폭은 하나다** (IEEE 29148, `FR-RSW-*`). 좌측 사이드의 폭이 **창마다 따로** 기억돼, 창을 옮길 때마다 같은 종류의 자리가 서로 다른 치수로 섰다. 워크스페이스 변수 하나로 모은다. **구현 완료** |
| [REQUEST_GATE_SRS.md](./REQUEST_GATE_SRS.md) | **요청 게이트 — 브라우저 매개 공격을 mux 바깥에서 닫는다** (IEEE 29148, `FR-RQG-*`). **"로컬 전용" 은 네트워크 경계일 뿐이고 실제 공격면은 브라우저 동일 출처 정책의 틈이다** — 두 경로가 기본 설정(`127.0.0.1:58146`)에서 성립했다. `ACCESS_ALLOWLIST_SRS` 의 짝으로, 그쪽이 **어느 기기**, 이쪽이 **누가 부를 수 있는가** 를 본다. 판정이 mux 밖인 것이 `Upgrader.CheckOrigin` 이 항상 참인 근거다. **구현 완료** |
| [SANDBOX_WINDOW_SRS.md](./SANDBOX_WINDOW_SRS.md) | **샌드박스 창 — 컨테이너로 격리된 Window** (IEEE 29148). Window 하나를 컨테이너 하나에 대응시킨다. 프로파일 2종의 경계가 요체다 — **`scratch` 만 보안 경계이고 `dev` 는 아니다**(`dev` 에는 `dmctl` 이 들어 있어 컨테이너 안 코드가 호스트에서 명령을 실행할 수 있다). **구현 완료** |
| [SCHEDULER_REARM_SRS.md](./SCHEDULER_REARM_SRS.md) | **돌지 않은 회차도 마감을 다시 건다 — 스케줄러의 공회전 교정** (IEEE 29148, `FR-SRA-*`). `TimerHub` 는 **가장 이른 마감 하나만 예약하는 단일 타이머**이고(FR-SCH-3), 그 설계가 성립하려면 모든 job 의 `nextAt` 이 늘 **미래**여야 한다 — 과거의 마감이 하나라도 남으면 루프가 공회전한다. **구현 완료** |
| [SETTINGS_CONTROLS_SRS.md](./SETTINGS_CONTROLS_SRS.md) | **설정 컨트롤의 공통 언어** (IEEE 29148, `FR-SCT-*`). 설정 패널의 입력 수단을 **한 벌의 시각 언어**(`.ds-switch`·`.ds-range`)로 모은다 — 새로 들어오는 값이 기존 항목과 따로 놀지 않게 하고, 이미 어긋나 있던 항목(슬롯 방향)을 그 언어로 되돌린다. **구현 완료** |
| [SIDEBAR_COLLAPSE_SRS.md](./SIDEBAR_COLLAPSE_SRS.md) | **좌측 사이드바 접기** (IEEE 29148, `FR-SBC-*`). 사이드바를 좁히는 유일한 손잡이가 `#sb-handle` 드래그뿐이었고 **100px 아래로 내려가지 않았다.** **구현 완료** |
| [STATE_FILE_DURABILITY_SRS.md](./STATE_FILE_DURABILITY_SRS.md) | **상태 파일의 내구성 — 세대·격리·빈 판 방지** (IEEE 29148). **사용자의 워크스페이스가 조용히 사라지지 않게 한다.** 세 구멍이 한 뿌리다 — 깨졌을 때 **돌아갈 곳이 없고**, 깨진 것을 **알아채는 자리가 없고**, 그 상태에서 브라우저가 **빈 판을 저장한다.** **구현 완료** |
| [SUBMODULE_DIRTY_NOTICE_SRS.md](./SUBMODULE_DIRTY_NOTICE_SRS.md) | **서브모듈 행이 왜 사라지지 않는지 말한다** (IEEE 29148). 확인 결과 **버그가 아니었다** — 재현으로 그 사실을 확정하고, 대신 **왜 남는지를 말하지 않던 것**을 고친다. **구현 완료** |
| [SYSTEM_THEME_FOLLOW_SRS.md](./SYSTEM_THEME_FOLLOW_SRS.md) | **시스템 다크/라이트 추종 — 테마 둘과 첫 페인트** (IEEE 29148). 테마 54종이 각자 `mode` 를 갖고 있으므로 부족한 것은 **"어느 쪽을 따를 것인가" 의 스위치와 모드마다 하나씩의 자리**뿐이었다. 첫 페인트가 걸리는 자리는 `BOOT_SCREEN_SRS` 의 선주입과 같다. **구현 완료** |
| [TERMINAL_FOLDER_DROP_SRS.md](./TERMINAL_FOLDER_DROP_SRS.md) | **터미널에 폴더를 놓는다** (IEEE 29148, `FR-FTR-*`). **문서가 거짓을 말하고 있었다** — `README.md` 와 `features.md`·`commands.md` 가 폴더 드롭을 적는데, `term-pane.js` 는 `dataTransfer.files` 만 보아 터미널 쪽은 사실이 아니었다. **구현 완료** |
| [TERMINAL_MODE_RESTORE_SRS.md](./TERMINAL_MODE_RESTORE_SRS.md) | **재접속이 터미널의 모드를 잃지 않는다** (`FR-TMR-*`). 다시 붙으면 **새 xterm 이 서고** 그 인스턴스는 앱이 켜 둔 모드를 하나도 모른다 — 모드는 `ESC[?…h` 를 **본** 터미널만 알기 때문이다. 화면은 스크롤백 재생으로 되살아나도 모드는 그렇지 않다(링버퍼를 넘으면 그 한 줄이 밀려난다). 접수된 `U-32`(Claude Code 의 이미지 붙여넣기가 안 되던 것)가 그 유실의 정체였다 — bracketed paste 가 꺼져 빈 붙여넣기가 감싸지지 않았다. **구현 완료** |
| [TERMINAL_RESUME_SRS.md](./TERMINAL_RESUME_SRS.md) | **재접속은 이어 붙이는 것이지 다시 뿌리는 것이 아니다** (IEEE 29148, `FR-TRS-*`). 접수는 "랜더가 자꾸 깨진다 … 이전 랜더 내용이 그대로 남아있는게 제일 문제다" 였고, 원인 셋이 **같은 자리**에서 나왔다 — WS 가 붙을 때마다 서버가 스크롤백 전량을 되뿌리는데 받는 쪽 xterm 은 그 내용을 이미 갖고 있다(실측: WS 916회에 스냅샷 898회, claude 도구는 **매번 1 MiB 전량**). `since` 오프셋으로 **델타 재개**를 만들고, 재개 불가일 때만 클리어를 앞세운 전량 재생으로 강등한다. FR-TRS-5a 가 재생분에서 지우는 질의 목록이며 그것이 `TERM_REPLY_SEAT_SRS` 와 쌍을 이룬다. **구현 완료** |
| [TOOL_HISTORY_ISOLATION_SRS.md](./TOOL_HISTORY_ISOLATION_SRS.md) | **도구별 셸 히스토리 — 재기동이 히스토리를 섞지 않는다** (IEEE 29148, `FR-THI-*`). 서버를 내렸다 올리면 모든 터미널의 `↑` 히스토리가 하나로 합쳐졌다 — 모든 도구 셸이 **한 개의 히스토리 파일**을 공유했기 때문이다. **구현 완료** |
| [UNFOCUSED_EDGE_SRS.md](./UNFOCUSED_EDGE_SRS.md) | **포커스를 잃은 창의 가장자리 표시** (IEEE 29148, `FR-UFE-*`). 여러 창·여러 기기에 같은 워크스페이스를 띄웠을 때 **지금 키를 받는 창이 어느 것인지** 화면만 보고 알 수 있게 한다. `ALERT_MOBILE_CONTEXT_SRS` 의 알림 가장자리가 이 구조의 두 번째 사례다. **구현 완료** |
| [UPDATE_NOTICE_SRS.md](./UPDATE_NOTICE_SRS.md) | **새 판이 나온 것을 사용자가 묻지 않아도 알게 한다** (IEEE 29148, `FR-UPD-*`). `dongminal update --check` 는 이미 있었다 — **문제는 그것을 아무도 치지 않는다는 것**이었다. **구현 완료** |
| [UX_BATCH8_SRS.md](./UX_BATCH8_SRS.md) | **접수한 4건 — 닫기 가드 · 아이콘 간격 · 미니맵 좌표계 · 미리보기 표면** (IEEE 29148). 접수한 넷 중 **둘은 이미 서 있는 것이 확인됐다**(§2.1·§2.4) — 그래서 이 문서가 실제로 다루는 것은 나머지와 인터뷰에서 붙은 다섯째다. **구현 완료** |
| [UX_BATCH9_SRS.md](./UX_BATCH9_SRS.md) | **저장·갱신·조합 — 손이 한 일이 화면에 닿지 않는 세 자리** (IEEE 29148, `FR-B9-*`). 셋은 기능이 다르지만 사용자에게는 한 가지로 읽힌다 — **내가 한 일이 화면에 닿지 않는다.** A 는 편집기의 `Cmd+S` 가 저장하지 않던 것(창을 닫을 때는 저장됐다). IME 조합 중 커서 이동을 다루는 `FR-IMK-*` 가 여기 있다. **구현 완료** |
| [VERSION_HEALTH_SRS.md](./VERSION_HEALTH_SRS.md) | **판 불일치 감지와 헬스 의미론** (IEEE 29148, `FR-VHL-*`). 둘을 세운다 — 데몬과 서버의 판이 다르면 **드러나고**(종전에는 조용히 공존했다), `GET /api/health` 가 "이 인스턴스가 어떤 상태인가" 를 한 번에 답한다. **구현 완료** |
| [VIEW_SCROLL_RESTORE_SRS.md](./VIEW_SCROLL_RESTORE_SRS.md) | **시선 보존 — 탭을 떠났다 돌아와도 스크롤이 남는다** (IEEE 29148). 사용자 보고 `U-20` 을 근본 원인에서 없앤다. 요구가 셋이며 첫째가 편집기 탭의 시선이다. **구현 완료** |
| [VIEWER_URL_OPEN_SRS.md](./VIEWER_URL_OPEN_SRS.md) | **보고 있는 기기에서 URL 열기** (IEEE 29148, `FR-VUO-*`). `claude` 로그인 · `gh auth login` · dev 서버 자동 오픈처럼 CLI 가 브라우저를 띄우는 자리가 대상이다 — 종전에는 그 브라우저가 **서버가 도는 컴퓨터**에서 떠서, 원격으로 보는 사람에게는 아무 일도 일어나지 않았다. **구현 완료** |
| [WINDOWS_TOOL_CWD_SRS.md](./WINDOWS_TOOL_CWD_SRS.md) | **서버도 도구의 cwd 를 듣는다** (IEEE 29148, `FR-WTC-*`). Windows CI 가 드러낸 것이 **검사의 문제가 아니라 제품의 구멍**이었다. **구현 완료** |
| [WORKSPACE_SAVE_CONFLICT_SRS.md](./WORKSPACE_SAVE_CONFLICT_SRS.md) | **워크스페이스 저장 충돌** (IEEE 29148). 접수한 것은 콘솔 로그였고 **처음 판단은 "소음" 이었다.** 재현이 그것을 틀렸다고 확정했다 — 409 는 소음이 아니라 **다른 화면이 만든 창이 사라지는 경로**다(§2.5). **구현 완료** |

## 언어 정책 (결정 — 2026-09-13, `M8_UNIFIED_SRS` FR-B-1)

| 항목 | 결정 |
|---|---|
| 지원 로케일 | `ko` · `en` — 카탈로그는 `web/js/i18n/<locale>.js`, 읽는 함수는 `t`·`tn` (`web/js/core/i18n.js`) |
| 기본 | `ko` |
| 감지 | **하지 않는다.** `navigator.language`·`Intl` 로 고르지 않고 설정 키 `locale` 만 본다 — 단일 사용자 제품이고, 감지는 e2e 전량을 브라우저 로케일에 묶는다 |
| 폴백 | 활성 로케일에 없는 키 → `ko` 문장 + `console.warn`(키마다 한 번). `ko` 에도 없으면 키 자체 |
| 전환 | 설정 저장 → `localStorage['dm.locale']` → 페이지 재로드 (D-B-1). 첫 페인트 전의 `<html lang>` 은 head 인라인 스크립트가 같은 키를 읽는다 |
| 범위 밖 | CLI 출력(`dongminal`·`dmctl`) · 서버 로그 · 서버 오류 응답 **본문**(D-ERR-2 동결, 문장은 `X-Error-Code` → 프론트 `err.<code>`) · `SETTINGS_SCHEMA.where` · 테스트 문자열 |
| 게이트 | `scripts/check-i18n.mjs` — JS 문자열 리터럴·HTML 텍스트/속성·CSS `content` 의 한글 0, ko·en 키 집합 일치. 예외 등록부는 스크립트 상단 |
| 툴팁 | `UX_BATCH5_SRS` FR-TIP-2(`title` 은 영어)는 **ko 카탈로그의 데이터**로 유지 |

## 용어

좌표계는 `W{n}.P{n}.T{n}` 이고 계층은 아래와 같다. 보관 문서의 `session`·`region`·
`paneId` 는 각각 아래의 Window·Pane·Tool 을 뜻한다.

```
공간 축:  Client ▶ Window ─ Pane ─ Tab ─ Tool
실행 축:  Run ─ Member ──1:1──▶ Tool        (직교. 접합 필드만 구현됨)
```

| 용어 | 뜻 |
|------|----|
| **Client** | 브라우저 창. 휘발성 뷰포트. Window 하나에 attach |
| **Window** | Pane 들을 담는 작업공간. 서버 영속. tmux 의 session |
| **Pane** | Window 안에서 나뉜 공간. 탭 목록 보유 |
| **Tab** | 도구를 담는 공간 |
| **Tool** | 탭에 탑재되는 실체 (`terminal` \| `editor`) |
| **Run** | 오케스트레이션 실행 인스턴스 (미구현) |

`paned`·`paned.sock`·`paned.pid` 는 **데몬 프로세스의 이름**이며 개명 대상이 아니다.
`internal/ctl/migrate` 안의 `panes.json`·`region`·`paneId` 는 **구 어휘가 입력**이라
그대로 둔다.

## 보관 (`archive/`) — 완료된 작업의 기록

### 아키텍처 · 리팩터

| 문서 | 시점 |
|------|------|
| [ARCHITECTURE_DEEPENING_RFC.md](./archive/ARCHITECTURE_DEEPENING_RFC.md) — C1–C5 모듈 심화 | 2026-04 |
| [FOLLOWUP_HOTFIX_RFC.md](./archive/FOLLOWUP_HOTFIX_RFC.md) — H1–H5·F1–F4. H5 는 workspace 비동기 쓰기 | 2026-04 |
| [DESIGN_REVIEW_FOLLOWUP.md](./archive/DESIGN_REVIEW_FOLLOWUP.md) — 설계 리뷰 후속 계획 | 2026-05 |
| [APP_DECOMPOSE_SRS.md](./archive/APP_DECOMPOSE_SRS.md) — App 클래스 3분할 | 2026-05 |
| [PANE_MANAGER_DECOMPOSE_SRS.md](./archive/PANE_MANAGER_DECOMPOSE_SRS.md) — PaneManager 분해 + mutex 정리 | 2026-05 |
| [HANDLERS_API_ROUTER_SRS.md](./archive/HANDLERS_API_ROUTER_SRS.md) — handlers_api 라우터 테이블화 | 2026-05 |
| [WORKSPACE_SNAPSHOT_SRS.md](./archive/WORKSPACE_SNAPSHOT_SRS.md) — Workspace Snapshot 단일 진입점 | 2026-05 |
| [MCP_BIND_HELPER_SRS.md](./archive/MCP_BIND_HELPER_SRS.md) — MCP typed bind helper. **전체 폐지** (SKILL_INJECTION_SRS §6) | 2026-05 |
| [RUNTIME_HELPERS_GO_SRS.md](./archive/RUNTIME_HELPERS_GO_SRS.md) — 런타임 헬퍼 Go 재작성 | 2026-05 |
| [TS_MIGRATION_SRS.md](./archive/TS_MIGRATION_SRS.md) — 프론트엔드 TypeScript 마이그레이션 | 2026-05 |
| [TODO.md](./archive/TODO.md) — 2026-04~05 작업 로그 (완료 49건) | 2026-05 |
| [NEXT_SESSION_PROMPTS.md](./archive/NEXT_SESSION_PROMPTS.md) — 당시 세션 프롬프트 | 2026-05 |

### 안정성 · 동시성

| 문서 | 시점 |
|------|------|
| [SAFETY_WARMUP_SRS.md](./archive/SAFETY_WARMUP_SRS.md) — L1/L4/L8 | 2026-05 |
| [CONCURRENCY_HARDENING_SRS.md](./archive/CONCURRENCY_HARDENING_SRS.md) — L3/L5/L7 | 2026-05 |
| [OUTBUF_BACKPRESSURE_SRS.md](./archive/OUTBUF_BACKPRESSURE_SRS.md) — PTY 출력 백프레셔 | 2026-05 |
| [HOT_RELOAD_SRS.md](./archive/HOT_RELOAD_SRS.md) — 무중단 재기동 | 2026-05 |
| [MDSCROLL_LEAK_FIX_SRS.md](./archive/MDSCROLL_LEAK_FIX_SRS.md) — 스크롤 리스너 누수 | 2026-05 |

### 레이아웃 · 포커스

| 문서 | 시점 |
|------|------|
| [SPLIT_SERIALIZATION_SRS.md](./archive/SPLIT_SERIALIZATION_SRS.md) — 분할 직렬화 | 2026-05 |
| [SHORTCUT_DISPATCH_SRS.md](./archive/SHORTCUT_DISPATCH_SRS.md) — 단축키 디스패치 | 2026-05 |
| [PANE_SCROLL_PRESERVE_SRS.md](./archive/PANE_SCROLL_PRESERVE_SRS.md) — 창 전환 시 스크롤 보존 | 2026-05 |
| [SPLIT_KEEPFOCUS_FIX_SRS.md](./archive/SPLIT_KEEPFOCUS_FIX_SRS.md) — keepFocus 시맨틱 정정 | 2026-06 |
| [THEMES_EXPANSION_SRS.md](./archive/THEMES_EXPANSION_SRS.md) — 테마 라이브러리 확장 | 2026-05 |

### 편집기 · 마크다운 (현재는 내장 Monaco 편집기로 대체됨)

`8dc0a3f`("feat: editor 임베드")에서 markdown 뷰어와 code-server 통합이 제거됐다.
아래 문서들의 대상은 더 이상 존재하지 않는다.

| 문서 | 시점 |
|------|------|
| [MULTI_TAB_TYPE_SPEC.md](./archive/MULTI_TAB_TYPE_SPEC.md) — 다중 탭 타입 인프라 + Markdown 뷰어 | 2026-04 |
| [MD_FOCUS_NEW_PANE_CWD_SRS.md](./archive/MD_FOCUS_NEW_PANE_CWD_SRS.md) — 파일 탭의 cwd 상속 (`editor` 로 이관돼 살아 있다) | 2026-05 |
| [MD_SCROLL_SYNC_SRS.md](./archive/MD_SCROLL_SYNC_SRS.md) — 마크다운 스크롤 동기화 | 2026-05 |
| [MD_VIEWER_REGRESSION_FIX_SRS.md](./archive/MD_VIEWER_REGRESSION_FIX_SRS.md) — 뷰어 도입 후 회귀 (포커스 불변식은 살아 있다) | 2026-05 |
| [CODESERVER_SHUTDOWN_SRS.md](./archive/CODESERVER_SHUTDOWN_SRS.md) — code-server graceful shutdown | 2026-05 |
| [CODESERVER_STABILITY_SRS.md](./archive/CODESERVER_STABILITY_SRS.md) — code-server 안정화 | 2026-05 |

### 모바일

| 문서 | 시점 |
|------|------|
| [MOBILE_MODE_RFC.md](./archive/MOBILE_MODE_RFC.md) — 모바일 모드 전반 | 2026-05 |
| [MOBILE_KEYBAR_ALWAYS_VISIBLE_SRS.md](./archive/MOBILE_KEYBAR_ALWAYS_VISIBLE_SRS.md) | 2026-05 |
| [MOBILE_KEYBAR_LAYOUT_ROBUSTNESS_SRS.md](./archive/MOBILE_KEYBAR_LAYOUT_ROBUSTNESS_SRS.md) | 2026-05 |
| [MOBILE_KEYBAR_TOOLTIPS_SRS.md](./archive/MOBILE_KEYBAR_TOOLTIPS_SRS.md) | 2026-05 |
| [MOBILE_VERIFICATION_AUTOMATION_SRS.md](./archive/MOBILE_VERIFICATION_AUTOMATION_SRS.md) — RFC §7.2 검증 자동화 | 2026-05 |

### 사용자 확인 피드백 (트랙 1)

| 문서 | 시점 |
|------|------|
| [USER_CHECKLIST_FIXES_SRS.md](./archive/USER_CHECKLIST_FIXES_SRS.md) — 8개 항목의 명세 (묶음 A~F) | 2026-08 |
| [USER_CHECKLIST_FIXES_PLAN.md](./archive/USER_CHECKLIST_FIXES_PLAN.md) — 묶음·순서 + 착수 전 결정 10건 | 2026-08 |

인계 문서(함정 15개)는 현행으로 남아 있다 — 위 표 참조.

### 식별자 · 원격 제어 · 에이전트 접합면

| 문서 | 시점 |
|------|------|
| [UUID_IDENTITY_SRS.md](./archive/UUID_IDENTITY_SRS.md) — UUID 기반 엔티티 정체성 | 2026-05 |
| [DMCTL_UUID_FINALIZE_SRS.md](./archive/DMCTL_UUID_FINALIZE_SRS.md) — dmctl UUID 전환 마무리 (location uuid-only 정책) | 2026-05 |
| [DMCTL_WHO_AM_I_SRS.md](./archive/DMCTL_WHO_AM_I_SRS.md) — `who-am-i` 추가 + 출력 라인 통일 (`internal/helper/toolline`) | 2026-06 |
| [LIST_PANES_NAME_FILTER_SRS.md](./archive/LIST_PANES_NAME_FILTER_SRS.md) — 이름 필터 (현 `list_workspace`) | 2026-06 |
| [REMOTE_SESSION_TAB_CREATE_SRS.md](./archive/REMOTE_SESSION_TAB_CREATE_SRS.md) — `newWindow`/`newTab` 의 keepFocus·name | 2026-06 |
| [RENAME_TAB_SESSION_SRS.md](./archive/RENAME_TAB_SESSION_SRS.md) — `renameTab`/`renameWindow` | 2026-06 |
| [REMOTE_COMMAND_RESULT_SRS.md](./archive/REMOTE_COMMAND_RESULT_SRS.md) — 생성 명령의 새 uuid 반환 (long-poll correlation) | 2026-06 |
| [DONGMINAL_WORKFLOW_SKILL_SRS.md](./archive/DONGMINAL_WORKFLOW_SKILL_SRS.md) — `dongminal-workflow` 스킬. 호출명·설치 경로는 SKILL_INJECTION_SRS 가 개정 | 2026-06 |

### 알림 · 활동

| 문서 | 시점 |
|------|------|
| [PANE_ATTENTION_NOTIFY_SRS.md](./archive/PANE_ATTENTION_NOTIFY_SRS.md) — 출력 감시 기반 주의 알림 (SSE `tool_attention`) | 2026-06 |
| [AGENT_ACTIVITY_PANEL_SRS.md](./archive/AGENT_ACTIVITY_PANEL_SRS.md) — 에이전트 활동 패널 (SSE `tool_activity`) | 2026-06 |

### 데몬 분리

| 문서 | 시점 |
|------|------|
| [DAEMON_SPLIT_SRS.md](./archive/DAEMON_SPLIT_SRS.md) — `dongminald` + `dongminal` 분리 | 2026-06 |
| [DAEMON_CWDPANE_RESOLVE_SRS.md](./archive/DAEMON_CWDPANE_RESOLVE_SRS.md) — 데몬 모드 cwd 해석 | 2026-06 |
| [DAEMON_PANE_BUSY_RESOLVE_SRS.md](./archive/DAEMON_PANE_BUSY_RESOLVE_SRS.md) — 데몬 모드 busy 해석 | 2026-06 |

## 남은 작업

| 항목 | 상태 |
|------|------|
| 요구 3 — AI 오케스트레이터 (`RUN_ORCHESTRATION_SRS`) | **완료.** 묶음 **S**(상태·대기 계약)·**R**(Run 레코드)·**P**(멤버 프리앰블)·**A**(어댑터 레지스트리)·**K**(스킬 재작성)·**W**(worktree 격리) 전부. FR-STA-4 준비완료 사다리 2단계(화면 패턴)는 스펙에 남기고 구현을 보류했다. **남은 별건**: 실제 격리 팀으로 한 바퀴 — 첫 격리 Run 은 새 worktree 경로가 신뢰 목록에 없어 폴더 신뢰 모달에 걸릴 수 있다 |
| 요구 4 — Git 창 (`GIT_SRS`) | **P0 전량 구현 완료.** 그 뒤 사용자 검토 4회를 반영했다 — 1~3차는 [GIT_UI_REVISION_SRS.md](./GIT_UI_REVISION_SRS.md)(FR-GIT-179~226), 4차 오류 3건은 [GIT_REVIEW4_SRS.md](./GIT_REVIEW4_SRS.md)(FR-RPT-1~8, FR-GIT-227~235). `go test ./...` 통과, Playwright 431. **남은 것은 4차 검토의 개선 7건·수동 실사·P1/P2 기능**이며 전부 [GIT_REMAINING.md](./GIT_REMAINING.md) 에 있다 |
| ~~`TC-BGU-9b` 기존 실패~~ | **해소** (트랙 4 0-A). 제품 결함이 아니라 테스트가 서버 관측을 클라이언트 단정의 배리어로 쓴 것이었다. 별개로 `location` 미지정 복귀의 조용한 무효는 실재했고 FR-BGR-7 로 닫았다 |
| ~~프론트엔드 id 가 UUID 가 아니다~~ | **해소** — 엔터티 id 는 `crypto.randomUUID()` 로 만든다. 생성 명령의 다중 실행도 함께 닫았다 ([WORKSPACE_IDENTITY_SRS.md](./WORKSPACE_IDENTITY_SRS.md)) |
| 워크스페이스 PUT 의 last-write-wins | 미해소. 사람 둘이 각자 브라우저에서 동시에 편집하면 한쪽이 유실된다. 오케스트레이터 경로는 FR-SXE-\* 가 덮는다 (WORKSPACE_IDENTITY_SRS §2.4·§5) |
| ~~사용자 인스턴스 v1 → v2 마이그레이션~~ | **완료** (2026-08-24 12:24). `~/.dongminal` 에 `.v1.bak` 3개, `panes.json`→`tools.json` 전환 확인 |
| `~/.dongminal/runs.json` | 커밋된 코드에 소비자가 없는 산출물. 실행 중 바이너리에 문자열조차 없다 — 출처 불명의 Run 레코드 프로토타입 |
| ~~`internal/shared/uuid`(Go v7) 가 죽은 패키지~~ | **해소** — 묶음 U 가 `toolId`·`reqId` 의 단일 생성기로 삼았다 (FR-UNI-6) |
| ~~`toolId` 가 서버 카운터~~ | **해소** — uuid. 카운터가 영속되지 않아 모든 도구가 닫힌 상태로 재기동하면 `"1"` 부터 재사용됐다 (WORKSPACE_IDENTITY_SRS §2.7) |
| ~~LAN 노출 시 엔터티 생성 실패~~ | **해소** — `crypto.randomUUID` 는 보안 컨텍스트 전용이라 `--expose` 접속에서 undefined 였고 폴백이 없었다. `newUUID()` 가 `getRandomValues` 로 폴백한다 (FR-UNI-3) |
| `~/.dongminal/panels.json` | v1 시절 도구 기록. 소비자 없음. 삭제 여부 미정 |
| `CLIENT_ATTACH_SRS` — Client↔Window attach 서버 등록, visibility 파생 | 미착수 (ENTITY_MODEL SRS §7 후속) |
| 사용자 대상 기능 TODO | 저장소 루트 [README.md](../../README.md) |

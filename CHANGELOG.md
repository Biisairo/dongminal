# 변경 이력

형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/) 를 따르고,
판 번호는 [유의적 버전](https://semver.org/lang/ko/) 을 따릅니다.

## [Unreleased]

### 수정

- **다른 웹페이지가 여러분의 터미널을 조종할 수 없게 했습니다.** dongminal 을 띄워
  둔 채 아무 웹사이트나 열면, 그 페이지의 스크립트가 여러분의 셸에 명령을 실행시킬
  수 있었습니다. 기본 설정(로컬 전용)에서도 그랬습니다
  > "로컬 전용" 은 네트워크의 경계일 뿐이고, 브라우저는 그 경계를 그냥 건너갑니다.
  > 여러분이 연 페이지는 여러분의 컴퓨터에서 도는 프로그램이니까요. 포트가 고정값
  > 이라 찾을 필요도 없었습니다
  >
  > 두 문이 열려 있었습니다. 하나는 터미널 연결(WebSocket)로, 페이지가 그것을 열면
  > 서버가 새 셸을 하나 만들어 주고 이후 타이핑을 그대로 받았습니다. 다른 하나는
  > 명령 실행 API 로, 요청 한 줄이 곧 셸 명령이었고 **응답을 읽지 못해도 실행은
  > 이미 일어났습니다.** 접속 허용 목록은 이것을 막지 못합니다 — 출발지가
  > 여러분 자신의 컴퓨터이기 때문입니다
  >
  > 이제 요청이 **어디에서 왔는지**를 문 앞에서 봅니다. 남의 페이지에서 온 것,
  > 낯선 이름으로 들어온 것, 브라우저가 몰래 보낼 수 있는 형식으로 온 것을
  > 거절합니다. `dmctl` 같은 터미널 도구는 종전대로 통과합니다
  >
  > 파일 API 도 함께 좁혔습니다. 종전에는 절대경로면 **어디든** 읽고 쓸 수
  > 있었고, `~/.ssh` 같은 자리가 요청 한 번에 열렸습니다. 이제 여러분이 연
  > 편집기 폴더·터미널의 작업 폴더·메모·설정 아래로 제한됩니다. 그 밖을 열어야
  > 하면 설정의 `fileApiUnrestricted` 로 켤 수 있습니다(노출 상태에서는 무시됩니다)

- **터미널이 뱉은 글자가 화면의 코드가 되지 않습니다.** 셸에서 도는 어떤
  프로그램이든 특정 바이트열 한 줄로 하단 상태바에 임의의 HTML 을 심을 수
  있었습니다. `cat` 한 파일, `curl` 응답, 원격 호스트의 프롬프트, 에이전트 출력이
  전부 그 통로였습니다
  > 이 화면은 터미널·파일·git 쓰기·설정에 닿으므로, 화면에서 코드가 도는 것은
  > 명령 실행과 다르지 않습니다. 이제 값은 언제나 글자로만 들어가고, 그 규약이
  > 지켜지는지를 검사기가 매번 확인합니다

- **`--expose` 로 열 때 허용 목록을 반드시 켜게 했습니다.** 켜지 않으면 서버가 뜨지
  않고, 무엇을 고쳐야 하는지 알려 줍니다. 그대로 진행하려면 `--insecure-no-acl`
  이 필요합니다 — 이름이 곧 경고입니다
  > 인증이 아직 없는 동안 목록마저 꺼진 채 노출하면 같은 Wi-Fi 의 누구나 셸을
  > 얻습니다. 노출은 이 제품이 존재하는 이유이므로, 그 경로가 기본으로 위험한
  > 상태를 남겨 둘 수 없습니다

- **탭을 닫을 때 Enter 가 실수로 프로세스를 죽이지 않습니다.** 확인창의 기본 선택이
  취소가 되고, `Enter` 는 실행이 아니라 취소입니다
  > git 의 파괴적 확인창은 이미 그 규약이었는데 탭 닫기 확인창만 반대였습니다.
  > 사용자는 어느 창이 떠 있는지로 손가락을 바꾸지 않습니다

### 그 밖

- 요청 본문에 크기 상한이 생겼습니다(1MB, 워크스페이스는 8MB). 설정 저장은 JSON
  인지 확인한 뒤에 씁니다 — 종전에는 받은 바이트를 그대로 파일에 썼습니다
- 동시에 열 수 있는 터미널 수(256)와 화면 연결 수(64)에 상한이 생겼습니다
- 서버에 연결 타임아웃이 생겼습니다. 종료할 때 진행 중인 저장을 잠깐 기다립니다
- 설정·소켓·로그 파일의 권한을 좁혔습니다(홈 0700 · 소켓 0600 · 로그 0600).
  기본 로그 위치가 `/tmp/dongminal.log` 에서 `$DONGMINAL_HOME/server.log` 로
  옮겨졌습니다 — 공유 폴더의 예측 가능한 이름에 접속 주소와 실행 명령이 남고
  있었습니다

- **검사기를 CI 에 겁니다.** 프론트엔드 35,000줄에 린터·타입 검사·단위 테스트가
  하나도 없었고, Go 쪽에도 린터와 취약점 스캐너가 없었습니다. 그리고 단위 테스트
  경로에서 `web` 패키지가 빠져 있어 그 두 파일이 CI 에서 한 번도 돌지 않았습니다
  > 사용자가 쓰는 동작이 바뀌지는 않습니다. 다만 이제 고칠 때 회귀를 사람이
  > 아니라 CI 가 잡습니다 — 이 저장소는 이미 그 값을 치른 적이 있습니다. 이음매
  > 검사기가 진작 있었는데 아무도 CI 에 걸지 않아서, OS 의존 호출이 추상화 밖으로
  > 샌 채로 main 이 초록이었습니다
  >
  > 붙인 것: `golangci-lint`(정확성 검사만) · `gofmt` · `govulncheck` · 커버리지
  > 기준선 · e2e 타입 검사 · 프론트 `eslint`(없는 이름·죽은 변수·빈 블록) ·
  > `node:test` 로 순수 로직 46건 · 제3자 자산의 판·해시 기록과 그것을 지키는
  > 검사기 · 로컬 원커맨드 `make gates`
  >
  > 붙이면서 실제로 나온 것도 있습니다. 죽은 선언 넷(`gitFetch` 로 옮기며 남은
  > 것들), 아무것도 재지 않던 검사 하나(조건만 세우고 빈 블록), 그리고 `git` 의
  > `fetch`·`pull`·`push` 가 CI 에서 한 번도 실행되지 않고 있었다는 사실입니다 —
  > "네트워크와 자격증명이 필요하다" 로 미뤄 뒀는데, 로컬 저장소를 원격으로 세우면
  > 둘 다 필요 없었습니다. 이제 다섯 경로가 실제로 돕니다
  >
  > 공급망 쪽도 함께 조였습니다. GitHub 액션을 움직일 수 있는 태그 대신 커밋 해시로
  > 고정하고(대신 dependabot 이 갱신을 제안합니다), 빌드에서 개발자의 파일 경로를
  > 지워 **같은 소스가 어느 기계에서든 같은 해시**를 내게 했습니다. 그리고 브라우저
  > 종단간이 실패한 판은 이제 릴리스로 나가지 못합니다

- **`git 상태 안전망` 을 줄이거나 꺼도 실시간 갱신이 멎지 않습니다.** 설정 ▸ Polling
  에서 그 주기를 `끔` 으로 두면 git 화면이 90초 뒤부터 영영 따라오지 않던 것을
  고쳤습니다. 같은 화면에서 `1분`·`2분` 선택지는 뺐습니다
  > git 화면이 실시간으로 따라오는 것은 서버가 변화를 알려 주기 때문입니다. 그런데
  > **서버가 어느 저장소를 지켜볼지 정하는 근거가 브라우저의 그 물음 하나뿐**이었고,
  > 90초 동안 묻지 않으면 지켜보기를 그만두었습니다. 그래서 "요청을 줄이려고" 고른
  > 설정이 알림 자체를 껐습니다 — 화면에는 아무 표시도 없이
  >
  > 이제 서버는 **화면이 붙어 있는 동안** 지켜봅니다. 물음의 빈도와 무관하고,
  > 오래 묻지 않았다고 놓지 않습니다 — Repo 창을 띄워 두고 터미널에서 작업하다
  > 이따금 보는 동안에도 갱신이 살아 있습니다. 놓는 것은 그 화면이 닫혔을 때입니다
  >
  > `1분`·`2분` 을 뺀 것은 그 뒤의 정리입니다. 이 주기는 알림이 놓친 것을 줍는
  > 그물인데, 1~2분에 한 번이면 그물이라기보다 없는 것에 가깝고 그 사이는 알림이
  > 이미 덮습니다. 짧게 두거나(10초·30초) 끄거나 둘 중 하나입니다. 이미 `1분`·`2분`
  > 으로 두셨다면 다음 실행에 30초로 돌아옵니다
  >
  > 서버 쪽에는 로그 한 줄을 더했습니다. 지켜보는 저장소가 상한(16)을 넘어 밀려날
  > 때 종전에는 조용했습니다

- **한참 두었다 돌아온 탭의 git 화면이 스스로 따라옵니다.** 탭을 숨겼다 돌아오거나
  다른 창을 보다 돌아왔을 때, 아무것도 누르지 않으면 Changes·History 가 낡은 채로
  서 있던 것을 고쳤습니다
  > git 화면이 실시간으로 따라오는 것은 서버가 변화를 알려 주기 때문이고, 그
  > 알림은 **브라우저가 상태를 물어보는 것**을 "지금 이것을 보고 있다" 로 읽어
  > 유지됩니다. 90초 동안 아무도 묻지 않으면 서버는 그 저장소를 놓습니다
  >
  > 그 물음을 다시 시작하게 하는 계기가 둘 있었고 둘 다 성겼습니다. 하나는 탭이
  > 다시 보이거나 창이 포커스를 받을 때인데, 그 신호가 **부팅 순간에 열려 있던
  > 저장소 하나에만** 연결돼 있었습니다 — 사용자가 그 뒤에 연 저장소는 이 계기를
  > 한 번도 갖지 못했습니다. 나머지 하나는 "멎었으면 되살린다" 는 검사인데, 그것이
  > **화면을 다시 그릴 때만** 돌았습니다. 화면은 사용자가 무언가 할 때 그려집니다
  >
  > 그래서 돌아와서 가만히 있으면 두 안전망이 함께 비어 있었습니다. 이제 복귀
  > 신호는 앱에 한 벌이고 열려 있는 저장소 전부에 닿으며, 되살리기 검사는 1초마다
  > 스스로 돕니다(탭이 숨어 있는 동안에는 쉽니다). 정상일 때 늘어나는 요청은
  > 없습니다 — 검사는 산술 몇 번으로 끝납니다
  >
  > 서버 쪽에는 로그 두 줄을 더했습니다. 감시가 사라지는 두 경우 — 브라우저가
  > 말을 멈춰 만료된 것과, 저장소가 읽히지 않아 빠진 것 — 이 로그에서 구분됩니다

- **돌지 않는 주기 작업이 CPU 를 갉지 않습니다.** 조건이 꺼져 있거나 앞 회차가
  아직 끝나지 않은 작업이, 그 동안 스케줄러를 4밀리초마다 깨우던 것을 고쳤습니다
  > 앱의 모든 주기는 **가장 이른 마감 하나만** 예약하는 타이머 하나를 지납니다.
  > 그런데 마감이 왔는데 돌지 않은 회차(조건이 거짓이거나 앞 회차가 비행 중)가
  > 자기 마감을 **과거에 남긴 채** 물러났습니다. 과거의 마감은 언제나 "가장 이른
  > 마감" 이므로, 스케줄러는 즉시 다시 깨어나 같은 판단을 하고 또 즉시 깨어났습니다
  >
  > 실측하면 주기 50밀리초짜리 작업 하나가 조건이 꺼진 400밀리초 동안 **85번**
  > 깨어났습니다 (제자리 값은 8번). 하는 일은 없고 깨어나기만 합니다
  >
  > 눈에 띄지 않았던 것은 콜백이 정확히 옳게 돌기 때문입니다 — 도는 것은
  > 스케줄러이지 작업이 아니라서, 횟수를 세는 검사는 전부 통과했습니다. 가장 흔한
  > 조건인 "탭이 숨었을 때" 는 브라우저가 배경 탭의 타이머를 1초로 조여 눌러 놓고
  > 있었고, 보이는 탭에서 남는 자리가 **응답이 주기보다 오래 걸릴 때**였습니다
  >
  > 이제 돌지 않은 회차도 다음 마감을 겁니다. 무엇이 도는지는 바뀌지 않습니다

- **멎은 git 화면이 실제로 되살아납니다.** 새로고침 직후 Changes·History 가 최대
  30초 동안 비어 있던 것과, 칸을 오갈 때 자동 갱신이 꺼진 채 남던 것을 고쳤습니다
  > 멈춘 관측을 스스로 되살리는 장치는 있었지만, **폴링 타이머는 도는데 답이 오지
  > 않는** 회차에서 그것은 경고만 찍고 물러났습니다 — 되살리기가 "주기를 다시
  > 걸었는가" 에 매달려 있었고, 주기가 그대로면 다시 받아 보는 일 자체가 빠졌기
  > 때문입니다. 실측한 자리가 그것입니다: 새로고침 뒤 첫 관측이 유실됐고, 사용자가
  > 창을 클릭해 다른 경로가 열릴 때까지 5.4초 동안 요청이 한 건도 나가지 않았습니다.
  > 클릭이 없었으면 30초였습니다
  >
  > 이제 관측이 낡았으면 그 자리에서 다시 받아 봅니다. 대신 **묻는 일에 주기를
  > 걸어** 두었으므로, 서버가 답하지 않는 동안에도 요청은 멎은 저장소당 주기마다
  > 한 번을 넘지 않습니다 — 정상일 때는 종전대로 한 건도 늘지 않습니다
  >
  > 함께 고친 것이 하나 더 있습니다. 같은 저장소를 보는 칸이 둘일 때, **보이지
  > 않는 칸 하나가 보이는 칸의 자동 갱신까지 꺼버렸습니다.** 폴링을 켤지 정하는
  > 판정이 칸마다였고 타이머는 저장소마다 하나여서, 나중에 판정한 칸이 이겼기
  > 때문입니다. 이제 그 저장소를 보고 있는 칸이 하나라도 있으면 갱신이 돕니다

- **다른 칸을 누르면 왼쪽 패널이 그 자리에서 따라옵니다.** 터미널이 있는 칸을
  눌렀을 때 사이드바 탭·활성 탭 표시·창 이름이 뒤늦게 바뀌던 것을 고쳤습니다
  > 칸을 누르면 포커스만 먼저 옮기고 다시 그리기는 **그 클릭이 끝난 뒤**로
  > 미룹니다 — 누르는 순간 다시 그리면 그 클릭이 어떤 핸들러에도 닿지 못하기
  > 때문입니다. 그런데 미룬 것을 실제로 하는 신호가 `click` 하나였고, **터미널
  > 위에서는 그 신호가 오지 않습니다**: xterm 은 출력을 갱신할 때 문자 조각을
  > 교체하고, 누름을 받은 그 조각이 사라지면 브라우저는 클릭을 발생시키지 않습니다
  >
  > 그래서 미룬 다시 그리기가 유실됐고, 무관한 git 목록 갱신이 사이드바 탭만
  > 우연히 정정할 때까지 기다렸습니다 — 회차마다 다른 딜레이(실측 195ms·593ms,
  > 놓치면 3초 폴링)의 정체가 그것입니다
  >
  > 이제 **누름이 끝나는 것**도 신호로 받습니다. 클릭이 정상으로 오면 그것이 먼저
  > 그리므로 하는 일은 그대로이고, 오지 않을 때만 이 두 번째 신호가 그립니다

- **다른 칸을 눌러도 원래 칸이 보던 분할 칸을 잃지 않습니다.** 칸을 옮겨 다니면
  이전 칸의 창이 첫 분할 칸으로 돌아가 있던 것을 고쳤습니다
  > 분할 칸을 누르는 손잡이가 **칸 포커스보다 먼저** 돌았습니다. 그 순간 활성 창은
  > 아직 이전 칸의 창이고, 분할 칸 포커스를 정하는 함수는 창을 인자로 받지 않으므로
  > **이전 칸의 창이 누른 칸의 자리를 자기 것으로 받았습니다**
  >
  > 그래서 그 창으로 돌아가면 보던 분할 칸이 아니라 첫 칸이 섰습니다. 서버에 남는
  > 값은 아니어서 새로 고치면 풀리는 종류였지만, 한 세션 안에서는 계속 겪습니다
  >
  > 이제 **칸이 먼저, 분할 칸이 그 다음**입니다. 같은 칸 안의 클릭과 칸이 하나일
  > 때는 달라지는 것이 없습니다

- **응답 요함·작업 종료 알람이 한 번만 웁니다.** 기다리는 동안 같은 알람이
  되풀이 울리지 않습니다
  > 에이전트의 `Notification` 훅은 사용자를 기다리는 동안 **되풀이 발화합니다.**
  > 서버는 그 되풀이 하나하나를 새 사건으로 읽었고, 그래서 한 번의 권한 요청이
  > 대기가 이어지는 내내 소리와 데스크톱 알림을 냈습니다. 알람을 보고 거둔 뒤에도
  > 다음 훅이 그것을 되살렸습니다
  >
  > 이제 대기 하나가 낳는 알람은 한 번입니다. 다시 우는 것은 **사용자가 새 일을
  > 시켰을 때** 입니다 — 그 도구에 키를 눌렀거나, 에이전트가 다시 일하기 시작했다고
  > 보고했을 때. 종료 알람은 이전부터 프롬프트당 한 번이었고 그대로입니다

- **보고 있는 칸에서 뜬 알람도 테두리가 보입니다.** 알람이 포커스된 칸에서 울리면
  점멸 테두리가 보이지 않던 것을 고쳤습니다
  > 알람 표식을 붙이는 자리가 둘인데(알람 갱신·화면 다시 그리기) 한쪽에만 "포커스된
  > 칸은 강조하지 않는다" 는 옛 규약이 남아 있었습니다. 알람 갱신이 붙인 표식을
  > 다음 다시 그리기가 도로 뗐습니다
  >
  > 이제 두 자리가 같은 판정을 씁니다. 포커스 테두리와 알람 링은 서로 다른 픽셀에
  > 앉으므로 겹쳐도 서로를 가리지 않습니다

- **TUI 를 띄운 터미널이 스스로 맨 위로 올라가지 않습니다.** claude code · pi agent
  처럼 쉬지 않고 출력하는 화면이 보던 자리를 지킵니다
  > 종전에는 화면이 위로 올라가 멈춘 채 새 출력을 따라가지 않았고, 아무 키나 누르면
  > 맨 아래로 돌아왔습니다. 원인은 스크롤이 아니라 **다시 그리기**였습니다 — 그릴
  > 때마다 레이아웃을 통째로 새로 짓고 살아 있는 터미널을 그 사이에서 옮겼으며,
  > 옮긴 뒤 자리를 되돌리는 보정이 "그동안 출력이 없다" 를 전제하고 있었습니다.
  > TUI 는 그 전제를 늘 깹니다
  >
  > 이제 바뀌지 않은 자리는 다시 짓지 않습니다. 터미널이 움직이지 않으므로 되돌릴
  > 일도 없습니다. 창을 바꾸는 것처럼 옮김이 불가피한 자리에서는 **맨 아래를 보고
  > 있었는지**를 먼저 봅니다 — 그것이 참이면 값이 낡아도 결과가 옳습니다

- **서버를 내렸다 올려도 터미널마다 자기 명령 기록을 갖습니다.** 위 화살표에 남의
  터미널에서 친 명령이 섞여 나오지 않습니다
  > 종전에는 모든 도구 셸이 `~/.zsh_history` 하나를 함께 썼습니다. 셸은 끝날 때
  > 자기 기록을 그 파일에 붙이고 시작할 때 파일 전체를 읽으므로, 서버를 내리는
  > 순간 모든 터미널의 기록이 한 파일에서 합쳐지고 되살아난 모든 셸이 그 합본을
  > 읽었습니다
  >
  > 이제 도구마다 자기 파일을 씁니다. 도구 식별자는 재기동을 넘어 유지되므로 **그
  > 터미널이 지난번에 하던 기록으로 돌아옵니다.** 처음 만들어질 때 한 번만 기존
  > 기록을 옮겨 심으므로 새 탭에서도 위 화살표가 비어 있지 않습니다 (bash 도 같습니다)

- **편집기에서 `Cmd/Ctrl+S` 가 지금 보고 있는 파일을 저장합니다.**
  > 종전에는 편집기를 둘 이상 열면 **마지막에 연 편집기**가 그 키를 가져갔습니다.
  > 그쪽에 고친 것이 없으면 아무 일도 일어나지 않아 "저장이 안 된다" 로 보였고,
  > 창을 닫을 때는 정상적으로 저장됐습니다
  >
  > 저장도 다른 편집기 단축키들과 같은 자리로 옮겼습니다. 그래서 **설정에서 조합을
  > 바꿀 수 있습니다** — 종전에는 코드에 박혀 있어 바꿀 수 없었습니다

- **Git 화면이 스스로 갱신됩니다.** 터미널에서 친 git 명령도, 에이전트가 고친
  파일도, dongminal 밖에서 만든 변경도 새로고침 없이 들어옵니다
  > 종전에는 한 번 멎으면 새로고침을 누를 때까지 영영 낡은 채였습니다. 자동 갱신은
  > 두 갈래로 보였지만 실은 하나였습니다 — 주기 관측이 멎으면 그 요청이 곧 서버에
  > 대한 "보고 있습니다" 였으므로 서버의 알림까지 함께 멎었습니다. 그리고 멎은
  > 것을 아무도 알아채지 못했습니다
  >
  > 이제 화면이 **관측이 얼마나 낡았는지**를 스스로 보고, 멎어 있으면 되살립니다.
  > 정상일 때는 요청이 한 건도 늘지 않습니다. `?diag=1` 을 붙이면 지금 이 화면이
  > 언제의 것인지 눈으로 볼 수 있습니다

- **한글을 치는 중에 `Cmd+←/→` 를 눌러도 글자가 제자리에 들어갑니다.**
  `Alt+←/→` 도 같습니다
  > 종전에는 커서가 먼저 옮겨지고 조합 중이던 글자가 **옮겨진 자리**에 찍혔습니다.
  > 조합을 붙잡아 두는 장치가 있었는데 이 키들이 그 장치의 바깥 길로 빠져나가고
  > 있었습니다

## [1.0.12] — 2026-09-08

### 추가

- **창 슬롯을 키로 곧바로 오갑니다.** `이전 슬롯`·`다음 슬롯` (`Ctrl+Alt+[` · `]`)
  > 종전에는 칸 전용 키가 없어, 옆 칸으로 가려면 `Pane ←/→` 로 지금 칸의 **끝까지**
  > 밀어야 했습니다. 칸 안의 분할이 깊을수록 가려는 자리 앞을 다 지나야 했습니다
  >
  > 끝에서는 반대편으로 감깁니다. `Pane ←/→` 의 경계 넘침은 그대로 남습니다 — 둘은
  > 다른 손짓입니다

- **백그라운드에 명령 하나를 바로 띄웁니다.** `detach --run '<명령>'` 은 셸을 띄우고
  그 안에 타이핑하는 대신, **그 명령 자체를 도구의 프로세스로** 세웁니다
  > 그래서 명령이 끝나면 도구가 죽고 목록에서 스스로 사라집니다. 끝난 뒤에도 남아
  > 기다리는 셸이 없습니다 — 백그라운드에 있는 것은 언제나 **도는 것**입니다
  >
  > 일할 자리를 정하려면 `--cwd <경로>` 를 함께 줍니다. 명령은 그 호스트의 셸에
  > 넘어가므로 파이프·리디렉션이 그대로 동작하고, 끝난 뒤에도 자리를 남기고
  > 싶으면 그것까지 명령에 적습니다 (`npm test; exec $SHELL`)

### 수정

- **모바일에서 연 파일·diff·이미지·미리보기가 화면에 뜹니다.** 여는 손짓이 자기가
  연 칸을 가리킵니다
  > 종전에는 **첫 파일만** 열렸습니다. 그 뒤로는 무엇을 눌러도 Explorer·Changes
  > 화면이 그대로 남아 아무 일도 일어나지 않는 것으로 보였습니다 — 탭은 만들어져
  > 있었고 그것을 볼 방법이 없었습니다
  >
  > 원인은 하나였습니다. 사이드를 떠나는 판정이 "포커스가 **바뀌었는가**" 에
  > 걸려 있었는데, 본문에 칸이 이미 있고 그것이 이미 포커스면 바뀔 것이 없습니다.
  > 에디터·diff·이미지·미리보기 넷이 그 한 자리였습니다

- **모바일에서 사이드바를 여닫아도 오류가 나지 않습니다.** 드로어 토글이 없는
  함수를 부르고 있었습니다 — 상단 줄이 그때마다 갱신되지 않았습니다

- **설치 제안 띠가 `미리보기` 손잡이를 덮지 않습니다.** 띠가 밀어 내리는 값이
  `38px` 로 박혀 있었습니다 — 띠가 한 줄이라는 가정입니다
  > 좁은 화면에서는 안내가 두세 줄로 접혀 띠가 그보다 커지고, 그러면 손잡이가 띠
  > **안**에 놓입니다. 눌러도 띠가 먹으므로 "미리보기가 동작하지 않는다" 로
  > 보였습니다. 이제 띠가 실제로 먹은 높이만큼 내려 앉습니다 (찾기 줄·알림 줄도
  > 같은 값입니다)

- **접은 사이드바에서 목록 이름이 읽힙니다.** 첫 글자 하나 대신, 글자를 줄여 두
  줄까지 싣고 넘치면 마지막 줄 끝에 `…` 를 찍습니다
  > `창`·`메` 로는 어느 항목인지 알 수 없어 결국 툴팁을 띄워야 했습니다. 크기는
  > 두 목록(창·Repo)이 한 벌을 씁니다 — 목록마다 다르면 레일이 계단처럼 보입니다.
  > 전체 이름은 여전히 마우스를 얹으면 나옵니다

- **설정창이 가로로 스크롤하지 않습니다.** 탭 열 열 개가 한 줄로 서도록 폭을
  넓히고, 긴 라벨은 잘리는 대신 줄을 바꿉니다
  > 종전에는 `Backup` 이 화면 밖에 있어 가로로 밀어야 찾을 수 있었습니다.
  > 좁은 화면에서는 상자 자신이 뷰포트를 넘었습니다 — 이제 화면 안에 들고, 탭 열은
  > 밀리는 대신 접힙니다
  >
  > 테마 이름도 더 이상 말줄임으로 잘리지 않습니다 (`Tokyo Night Storm` 과
  > `Tokyo Night Light` 가 같은 글자로 보였습니다)

- **한글을 치다 엔터·스페이스를 눌러도 순서가 뒤집히지 않습니다.** 조합이 정리되기
  전에 들어온 키를 붙잡았다가, 조합 문자열이 나간 뒤에 흘려보냅니다
  > 종전에는 "안녕하세요" 를 치고 엔터를 누르면 **마지막 글자 앞에 엔터가**
  > 들어갔습니다. 모바일에서는 기호·스페이스가 같은 자리에서 앞섰습니다
  >
  > 원인은 둘이었습니다. 조합 중에 다른 키가 오면 터미널이 그 자리에서 **한 글자
  > 낡은** 조합 조각을 내보내고 그 키를 이어 보냈고, 조합이 먼저 닫힌 경우에는
  > 조합 문자열의 전송이 한 박자 늦어 확정 문자가 그 앞을 질렀습니다

- **diff 를 열어 둔 채 파일을 고치면 화면이 따라옵니다.** 열려 있는 diff 를 관측
  회차마다 다시 받되, 내용이 같으면 다시 그리지 않아 커서와 스크롤이 남습니다
  > 파일 **내용**의 변화는 `git status` 로 알 수 없습니다 — 이미 수정된 파일이 또
  > 수정돼도 같은 줄이 나옵니다. 그래서 알 수 없는 것을 기다리는 대신 열려 있는
  > 하나를 다시 묻습니다. 서버가 거부한 대상(바이너리·상한 초과)은 되풀이하지
  > 않습니다

- **칸을 늘렸다 줄여도 Git 이 계속 갱신됩니다.** 사라지는 칸의 패널이 그 저장소의
  관측을 통째로 끄고 있었습니다 — 타이머는 저장소마다 하나인데 끄는 쪽이 칸마다
  있었습니다. 이제 물러난 뒤 남은 칸으로 다시 세웁니다

- **변경 하나를 눌러도 목록의 자리가 남습니다.** 화면을 다시 그릴 때 Git 뷰가 문서
  에서 떨어졌다 붙으면서 스크롤 위치를 잃던 것을 갈무리해 되돌립니다

- **Run 카드를 누르면 그 세션으로 갑니다.** 칸이 여럿일 때 활성 창만 바꾸고 칸은
  그대로 두어 아무 일도 일어나지 않던 것을 고쳤습니다

- **컨텍스트 사용률을 실제 토큰으로 잽니다.** 대화 기록 **파일 크기**로 어림하던
  것을 그만두고, 마지막 요청이 실제로 보낸 토큰 수를 읽습니다
  > 기록 파일은 대화 전체의 누적이라 압축과 무관하게 자랍니다 — 실측에서 202,185
  > 토큰인 세션의 파일이 968KB 였고, 옛 공식으로는 **134%** 가 나왔습니다
  >
  > 한계값도 200k 로 못박혀 있던 것을 **모델이 말하게** 했습니다. 긴 컨텍스트 판은
  > 모델 이름에 표식이 붙습니다. 표식 없이 기록되는 경우를 위해, 관측이 지금 창을
  > 넘으면 아는 다음 창으로 넓힙니다 — 창을 넘는 사용률은 불가능한 관측이므로
  > 창이 더 크다는 뜻입니다
  >
  > 화면과 `dmctl run status` 가 **분자와 분모**를 함께 보입니다

- **백그라운드 개수가 다른 브라우저에서도 갱신됩니다.** 도구를 보내고 되돌리는 것과
  프로세스가 끝나 사라지는 것, 셋 모두를 알립니다

- **Run 을 접으면 팀원의 탭과 빈 터미널까지 정리됩니다.** 종전에는 정리 대상 목록만
  돌려주고 종료·탭 닫기는 조정자의 몫이었고, 그 절차를 건너뛴 조정자가 죽은
  에이전트의 탭과 아무도 앉지 않은 터미널을 남겼습니다

- **승계 문서가 버려지지 않습니다.** 인수인계를 기다리는 시한을 30초에서 180초로
  늘리고, 그러고도 늦게 온 요약은 **후임의 프리앰블을 만드는 자리**가 기다립니다 —
  어느 쪽이 빨랐든 문서가 사라지지 않습니다

- **없는 자리를 지목한 탭 닫기가 엉뚱한 탭을 닫지 않습니다.** 이미 닫힌 탭을 한 번
  더 닫으라는 요청이 **포커스한 탭**을 지우던 자리를 막았습니다

- **Repo 창의 기본 탭이 Changes 입니다.** 탭 순서를 Changes 로 옮긴 근거가 기본값
  에도 그대로 적용됩니다
  > 이미 열어 둔 창의 선택은 그대로입니다 — 기본값은 **정해진 적이 없을 때**의
  > 답입니다
  >
  > 그 자리가 저장소가 아니면(`~`·메모장) **더 이상 묻지 않습니다.** 종전에는
  > "저장소가 아니다" 를 확인하고도 3초마다 다시 물었습니다. 되살아나는 길은
  > 창에 포커스가 올 때·새로고침·`git init` 셋입니다
  >
  > 검색이나 `dmctl open` 으로 연 파일은 Changes 를 보고 있어도 **탐색기로 가면
  > 그 자리에 드러납니다**

- **태그가 최신부터 보입니다.** 이름순이라 맨 위가 가장 오래된 태그였고, `v1.10` 이
  `v1.9` 보다 위에 왔습니다

- **편집기와 diff 의 미리보기가 스크롤바와 맞습니다.** 파일이 길면 미리보기가 자신도
  함께 스크롤해 두 슬라이더가 다른 자리에 서던 것을, 파일 전체를 미리보기 높이에
  맞춰 같은 좌표계에 세웁니다

### 변경

- **샌드박스 창을 열 때 마운트와 복사를 고릅니다.** 종전에는 프로파일이 방식을
  고정했고, `sandbox.json` 에 `dev` 를 정의하지 않은 사용자에게는 마운트를 고를 길이
  화면 어디에도 없었습니다
  > 격리 등급 배지는 **프로파일의 성질**이므로 고른 방식에 따라 바뀌지 않습니다.
  > 대신 마운트에 폴더를 함께 고르면 "이 창 안의 코드가 그 폴더를 고칠 수 있습니다"
  > 를 그 자리에서 말합니다

- **대기 중인 에이전트가 알람을 되풀이하지 않습니다.** 아무것도 시키지 않았는데
  알람이 계속 서던 세 자리를 막았습니다
  > 관측한 것은 이렇습니다. 한 도구가 3분 동안 **사용자 입력을 한 번도 받지 않은
  > 채** 네 번 알람을 냈습니다. 백그라운드 작업 알림으로 깨어난 턴이 끝날 때마다
  > 에이전트의 종료 훅이 울렸고, 그것이 그대로 알람이 되었습니다
  >
  > 이제 **완료 알람은 사용자가 시킨 일이 끝났을 때만** 섭니다. 사용자 프롬프트로
  > 시작되지 않은 턴 — 배경 이벤트로 깨어나 스스로 끝난 턴 — 의 종료는 부를
  > 사건이 아닙니다
  >
  > **입력 대기 알림도 더 이상 알람이 아닙니다.** 에이전트는 권한을 물을 때와
  > 사람이 60초 넘게 자리를 비웠을 때 같은 신호를 보내는데, 앞의 것만 알람으로
  > 남깁니다. 뒤의 것은 새 소식이 아니라 "여전히 기다리는 중" 이라는 되풀이입니다
  >
  > **조용해졌다는 이유만으로 우는 알람도 좁혔습니다.** 에이전트가 스스로 끝났다고
  > 말한 뒤의 정적은 이미 알린 사실이고, 아직 아무 일도 시작하지 않은 세션의
  > 정적은 알릴 것이 없습니다. 이 알람이 남아 있는 자리는 하나입니다 — 일하는
  > 중이라고 말해 놓고 훅이 끊겨 소식이 멎은 도구

## [1.0.11] — 2026-09-06

### 추가

- **로딩 화면이 준비될 때까지 화면을 덮습니다.** 페이지를 열면 로고와 진행 바가
  선 뒤, 테마와 워크스페이스가 모두 준비된 다음에 걷힙니다
  > 지금까지는 서버에서 설정을 받아오는 동안 **빈 골격이 채워지는 과정**이 그대로
  > 보였습니다. 창·탭·터미널이 하나씩 들어서는 장면은 준비된 화면이 아니고, 그
  > 사이의 클릭은 어디로 가는지 알 수 없었습니다
  >
  > 걷히는 시점은 테마 결정과 워크스페이스 복원이 **둘 다** 끝난 때입니다. 둘 중
  > 하나가 실패로 끝나도 걷힙니다 — 실패는 화면에 갇힐 사유가 아닙니다. 서버가
  > 아무 답도 주지 않으면 6초 뒤에 걷힙니다
  >
  > 진행 바는 얼마나 남았는지 말하지 않고 흐릅니다. 부팅의 두 걸음은 각자 네트워크에
  > 달려 있어 남은 양을 셀 수 없습니다

- **포커스를 잃은 창을 화면 가장자리가 알립니다.** 이 브라우저 창이 키를 받지 않는
  동안 네 변에서 안쪽으로 옅은 띠가 그려집니다. 설정 ▸ Display 의
  `포커스 잃은 창 표시` 에서 0~10 으로 세기를 정하고, **0 이면 그리지 않습니다**
  (기본 5)
  > 창을 여러 개 띄우면 어느 쪽이 키를 받는지 화면이 말해 주지 않았습니다 — 눌러
  > 봐야 알았고, 그 사이에 친 키는 다른 창의 터미널로 갔습니다
  >
  > 색은 팔레트에서 고르지 않고 **그 자리에 있는 색의 반대값**을 씁니다. 테마가
  > 43개이고 화면 위에 무엇이 있을지 알 수 없으므로, 어떤 색을 고르든 어딘가에서는
  > 묻힙니다. 반대값은 무엇 위에 얹히든 반드시 갈립니다
  >
  > 흐림(blur)은 쓰지 않습니다. 터미널은 1px 획의 텍스트라 약한 흐림에도 글자가
  > 뭉개집니다 — 알리려다 읽지 못하게 만드는 셈입니다. 화면 안쪽은 손대지 않으므로
  > 글을 읽는 데는 지장이 없습니다
  >
  > 세기를 옮기는 동안에는 창에 포커스가 있어도 띠가 보입니다. 그러지 않으면 값을
  > 눈 감고 고르게 됩니다

### 변경

- **설정의 손잡이가 한 벌의 생김새로 모였습니다.** 슬롯 방향이 토글 스위치가 되고,
  스위치 양쪽에 `Horizontal`·`Vertical` 이 늘 보입니다
  > 지금까지 그 자리는 **지금 값이 적힌 버튼 하나**였고 누르면 뒤집혔습니다. 그
  > 버튼은 "지금 무엇인가" 만 말하고 "무엇을 고를 수 있는가" 는 눌러 봐야 알았습니다
  >
  > 새로 들어온 세기 조절(레인지)과 이 스위치는 이미 쓰이던 규약 — 바탕·테두리·반경,
  > 손이 닿으면 강조색 — 을 그대로 씁니다. 같은 패널 안에서 어느 것이 누를 수 있는
  > 것인지 매번 다시 배우지 않아도 됩니다

### 고침

- **저장한 테마가 아닌 색이 먼저 보이던 것을 고쳤습니다.** 페이지의 첫 화면이
  곧바로 자기 테마 색으로 그려집니다
  > 테마는 서버에서 설정을 받아온 **뒤에야** 화면에 닿았습니다. 그 전의 첫 화면은
  > 언제나 기본 테마(Tokyo Night)였고, 밝은 테마를 쓰는 사람은 어두운 화면을 먼저
  > 본 뒤 색이 갈리는 것을 봤습니다
  >
  > 이제 마지막에 쓴 테마를 기기에 남겨 두고 **첫 그림 전에** 세웁니다. 다른 기기에서
  > 테마를 바꿔 이 값이 낡았더라도, 교정은 로딩 화면 아래에서 일어나 보이지 않습니다

## [1.0.10] — 2026-09-06

### 추가

- **좌측 사이드바를 접습니다.** 상단바 맨 왼쪽의 `☰` 로 사이드바를 접고 펼칩니다.
  접힌 사이드바는 사라지지 않고 **아이콘 레일**(40px)이 되어 탭 아이콘과
  `⟳`·`⚙` 가 남습니다
  > 지금까지 사이드바를 좁히는 손잡이는 드래그뿐이었고 그것은 100px 아래로
  > 내려가지 않았습니다 — 화면을 온전히 콘텐츠에 쓰고 싶을 때 할 수 있는 일이
  > 없었습니다
  >
  > 버튼이 사이드바 **밖**에 있는 것은 접힌 상태에서도 같은 자리에 있어야 하기
  > 때문입니다. 안에 두면 접을 때 함께 줄어 여는 손잡이가 사라집니다
  >
  > 레일의 탭 아이콘을 누르면 **그 탭으로 가면서 펼쳐집니다** — 접힌 채 탭만
  > 바꾸는 것은 아무것도 보여 주지 않습니다. 펼침 폭은 보존되므로 접었다 펼치면
  > 접기 전 폭으로 돌아갑니다
  >
  > 접힘은 이 기기의 것입니다(`localStorage`) — 다른 기기의 화면 폭에 맞춘 선택이
  > 넘어오지 않습니다. 모바일은 종전대로 드로어이며 이 버튼이 뜨지 않습니다

- **탭 너비를 고정합니다.** 설정 ▸ Display 의 `탭 너비 고정` 을 켜면 탭이 이름
  길이와 무관하게 같은 너비를 갖습니다. 기본은 꺼짐이고, 끈 상태의 동작은 지금과
  같습니다
  > 이름이 길고 짧음에 따라 탭이 넓어졌다 좁아졌다 하면 **같은 탭이 이웃의 이름이
  > 바뀔 때마다 다른 자리로 옮겨 갑니다.** 손이 기억한 위치가 무효가 되고, 연속으로
  > 닫을 때 다음 탭이 커서 밑으로 오지 않습니다
  >
  > 너비는 40~480px 에서 정합니다(기본 160). 이름이 길면 말줄임으로 잘리고 **전체
  > 이름은 툴팁**에 뜹니다. 탭이 많아 줄이 넘치면 지금처럼 가로로 스크롤합니다 —
  > 폭은 줄지 않습니다
  >
  > VSCode 의 `workbench.editor.tabSizing` 에 해당하지만 그쪽의 `shrink` 는 두지
  > 않았습니다. 이 앱은 넘칠 때 스크롤하므로, 셋을 다 두면 "스크롤과 줄임 중
  > 무엇이 언제 이기는가" 를 사용자가 알아야 합니다

- **서브모듈을 따로 관리합니다.** Git 창에 `Submodules` 탭이 섭니다. 등록된
  서브모듈이 경로·상태·기록된 커밋과 함께 서고, 각 행에서 `Init`·`Update`·`Sync`
  를 실행하거나 그 서브모듈을 **자기 저장소 창으로 열 수 있습니다**
  > 상태는 `git submodule status` 가 답한 그대로입니다 — `ok`·`uninit`·
  > `modified`·`conflict`. 초기화되지 않은 것에는 여는 버튼이 붙지 않습니다:
  > 아직 열 저장소가 없기 때문입니다. 그 자리에는 `Init` 이 섭니다
  >
  > **`Update` 는 파괴적입니다.** 서브모듈 안의 체크아웃을 기록된 커밋으로
  > 옮기므로 커밋하지 않은 변경을 덮을 수 있습니다 — 확인창이 실행될 명령을
  > 그대로 보여 줍니다. `Sync` 는 URL 설정만 옮기므로 그렇지 않습니다
  >
  > 중첩된 서브모듈은 한 목록에 섞지 않습니다. 그 서브모듈을 저장소로 연 뒤
  > 그 창의 `Submodules` 탭에서 봅니다

- **폴더 단위로 버립니다.** 트리 보기의 폴더 행에 `↺` 가 섭니다 — 그 폴더 아래
  그 그룹의 변경을 통째로 되돌립니다
  > `Changes` 의 것은 **되돌리기**이고 `Untracked` 의 것은 **파일 삭제**입니다.
  > 그룹 머리의 `↺` 와 같은 구분이며, 툴팁과 확인창이 그 차이를 먼저 말합니다
  >
  > 접혀 있어도, 목록이 잘려 있어도 대상은 폴더 아래 전부입니다. 다른 그룹의
  > 같은 이름 폴더는 건드리지 않습니다

- **도커가 없거나 꺼져 있으면 그 둘을 갈라 말합니다.** 샌드박스 창 추가(`▣`)를
  누르면 먼저 런타임 상태를 확인합니다
  > **설치되어 있지 않으면** 설치 안내와 **그 운영체제의 명령 하나**를 복사
  > 버튼과 함께 보입니다. 설치한 뒤에는 dongminal 을 다시 시작해야 한다는 것도
  > 함께 알립니다
  >
  > **설치되어 있으나 꺼져 있으면** 실행할지 묻습니다. 실행을 고르면 데몬이 뜰
  > 때까지 기다렸다가 **하던 일(프로파일 선택)로 그대로 이어집니다** — 버튼을
  > 다시 누를 필요가 없습니다
  >
  > linux 에서는 실행 버튼 대신 명령을 줍니다. 데몬 기동에 권한이 필요해
  > 서버가 대신 실행하면 비밀번호를 받을 길이 없기 때문입니다

- **모든 버튼에 영어 툴팁이 붙습니다.** 오래 올려 두면 그 버튼이 무엇을 하는지
  영어로 알립니다 — 바로 보이는 화면뿐 아니라 **띄워야 보이는 자리**도 함께입니다:
  모바일 화면 · 확인창 · 다이얼로그 · 알림창 · 도구 닫기 확인 · 터미널 복사창 ·
  진단 오버레이 · 알림 센터 · 진행 중 조작 바 · diff 의 조각(hunk) 버튼
  > **되돌릴 수 없는 조작의 승인 버튼에 안내가 없었습니다.** 파괴적 확인창의
  > `Run`, `Revert hunk`, 백그라운드 종료와 Run 삭제의 `예` — 무엇이 실행되는지
  > 버튼 자신은 말하지 않았습니다. 이제 그 자리에서 알립니다
  >
  > 바뀐 것은 툴팁뿐입니다 — 버튼 라벨과 안내·오류 문구의 언어는 그대로입니다.
  > 커밋 버튼처럼 사유를 두 자리에 보이던 것은 버튼 옆 한 줄이 한국어로 남고
  > 툴팁만 영어가 됩니다

### 고침

- **Submodules 탭이 열 때마다 최신 목록을 보입니다.** 탭을 활성화해도 목록을 다시
  받지 않아 낡은 상태가 남을 수 있었고, 탭이나 창을 닫아도 그 뷰가 정리되지
  않았습니다
  > Git 패널의 뷰 목록이 다섯 자리에 손으로 적혀 있었고 여덟 번째 탭인
  > Submodules 가 그중 넷에서 빠져 있었습니다 — 뷰 하나를 더하는 비용이 여섯
  > 자리였기 때문에 생긴 누락입니다. 이제 목록 하나에서 파생되므로 같은 일이
  > 되풀이될 수 없습니다
  >
  > 함께 고친 것: **Git 창을 닫을 때 열어 본 적도 없는 탭들이 그 순간 만들어지고
  > 있었습니다.** 열지 않은 탭은 만들지 않는다는 원칙이 창을 닫는 경로에서만
  > 깨져 있었습니다

- **편집기 탐색기가 돌아온 즉시 갱신됩니다.** 다른 탭에 다녀오면 한 주기(수 초)
  동안 낡은 git 표시가 남아 있었습니다 — 이제 화면이 돌아온 그 순간 다시
  읽습니다
  > 같은 규칙을 쓰는 다섯 자리의 정확도가 제각각이었습니다. 상태바와 Git 목록은
  > 복귀 시 갱신했지만 탐색기는 하지 않았고, **Agents 패널은 보이지 않는 탭에서도
  > 계속 요청을 보내고 있었습니다.** 이제 다섯이 같은 규약을 씁니다 — 숨은 화면에
  > 요청을 쓰지 않고, 돌아오면 즉시 갚습니다

- **서브모듈 행이 왜 사라지지 않는지 말합니다.** 서브모듈 **안**에만 커밋하지
  않은 변경이 있는 행은 여기서 스테이지·커밋해도 사라지지 않습니다 — 이제 그
  사실과 해야 할 일을 행 툴팁과 미리보기 안내가 말합니다
  > 부모 저장소가 커밋할 수 있는 것은 서브모듈의 커밋 해시 하나뿐입니다. 그
  > 해시가 그대로면 `git add` 가 올릴 변화가 없어 아무 일도 일어나지 않습니다 —
  > 오류도 아니어서 화면은 지금까지 아무 말도 하지 않았고, 눌러도 반응이 없는
  > 것처럼 보였습니다
  >
  > 행은 이제 세 가지로 갈려 말합니다: 기록된 커밋이 바뀐 행(스테이지하면
  > 담깁니다) · 안쪽만 더러운 행(사라지지 않습니다) · 둘 다인 행(커밋 몫은
  > 담기고 안쪽 몫은 남습니다)
  >
  > 길은 종전과 같은 자리에 있습니다 — 안내 옆의 `저장소로 이동`(또는
  > `저장소로 추가`)으로 서브모듈을 자기 저장소로 열어 그 안에서 먼저 커밋하면
  > 됩니다. `Stage` 버튼은 그대로 둡니다: 둘 다인 행에서는 실제로 담기는 것이
  > 있기 때문입니다

- **폴더 행의 버튼이 파일 행과 열이 맞습니다.** 폴더 행에는 여는 버튼(`↗`)이
  없어 같은 아이콘이 파일 행과 다른 자리에 섰습니다 — 이제 그 자리를 비워 둡니다
- **Worktrees 탭의 `Open` 이 하는 일을 바르게 말합니다.** "활성 리포로 엽니다"
  는 저장소마다 창이 서기 전의 표현이었습니다 — 지금 하는 일은 그 워크트리를
  자기 저장소 창으로 여는 것입니다
- **사이드 진입점이 좁아져도 버튼이 작아지지 않습니다.** 진입점이 늘면 폭이
  균등하게 줄어 히트 영역 하한(30px)을 조용히 깼습니다 — 이제 줄을 바꿉니다

## [1.0.9] — 2026-09-05

### 추가

- **변경 목록에서 한 번에 버립니다.** `Changes` 와 `Untracked` 그룹 머리에 `↺` 가
  섭니다 — 그 그룹의 변경을 통째로 되돌립니다. 스테이지 버튼(`+`) 오른쪽에 있는
  것은 파괴적인 것이 손에서 가장 멀어야 하기 때문입니다
  > **두 그룹은 같은 일을 하지 않습니다.** `Changes` 의 것은 **되돌리기**이고
  > (스테이지해 둔 몫은 남습니다), `Untracked` 의 것은 **파일 삭제**입니다 —
  > 되살릴 값이 없습니다. 툴팁과 확인창이 그 차이를 눌러 보기 전에 말합니다
  >
  > 확인창은 여전히 `git stash push` 로 먼저 담아 두는 길을 안내합니다.
  > 자동으로 실행하지는 않습니다

- **탐색기에서 파일과 폴더를 복사합니다.** 우클릭 메뉴에 `복사`·`붙여넣기`·`복제`
  가 섭니다. 폴더도 통째로 복사되고, 붙여넣는 자리는 우클릭한 곳이 정합니다 —
  폴더면 그 안, 파일이면 그 형제입니다
  > **이름이 겹치면 서버가 비켜 줍니다** — `a.txt` → `a copy.txt` →
  > `a copy 2.txt`. 덮어쓰는 일은 없습니다. `복제` 는 제자리에 하나 더 만드는
  > 바로가기입니다
  >
  > **다른 Editor 창에도 붙여넣을 수 있습니다** — 홈에서 복사해 저장소 트리에
  > 놓는 식입니다. 복사와 붙여넣기는 **서버 안에서** 일어나므로 어느 기기에서
  > 접속했든 결과가 같고, 파일이 네트워크를 오가지 않습니다
  >
  > 복사해 둔 것은 새로고침하면 비워집니다

- **폴더 단위로 스테이지합니다.** `Changes` 를 트리 보기로 두면 폴더 행에도
  `+`(또는 `−`)가 섭니다. 그 폴더 **아래 전부**가 대상입니다 — 접혀 있어도,
  목록이 잘려 있어도 같습니다
  > 폴더 행은 그 그룹의 것만 건드립니다. `Changes` 의 `src` 와 `Untracked` 의
  > `src` 는 다른 행이고 서로를 건드리지 않습니다

- **Git 과 Editor 가 한 자리로 합쳐졌습니다.** 사이드바 탭이 `Windows`·`Repo`
  둘이 되고, 저장소 하나가 창 하나입니다. 창의 좌측은 `Explorer` 와 `Changes` 를
  **탭으로 갈아 끼우고**, diff·history·branches 는 오른쪽에 **편집기 탭과 똑같은
  자격으로** 뜹니다
  > `+ Add` 한 번이 저장소와 편집기를 함께 세웁니다 — 사실 서버는 처음부터 두
  > 목록을 함께 바꾸고 있었고, 화면만 둘로 그리고 있었습니다
  >
  > **저장소가 아닌 폴더에는 `git init` 버튼**이 섭니다. 누르면 확인을 거쳐
  > 저장소가 되고 곧바로 목록·색이 따라옵니다
  >
  > **변경 목록의 한 번 클릭이 곧바로 diff 를 엽니다** — VSCode 와 같습니다.
  > 추적되는 파일은 오른쪽의 `Diff` 탭에, 새 파일은 편집기 탭에 뜹니다. 그 탭은
  > 창에 하나뿐이라 목록을 훑어도 탭이 쌓이지 않습니다
  >
  > 탐색기에서 파일을 한 번 클릭하면 **미리보기 탭**이 열립니다(기울임) — 다음
  > 클릭이 그 자리를 갈아 끼우고, 더블클릭하거나 고치기 시작하면 고정됩니다
  >
  > Changes 는 목록 하나입니다 — 안에 있던 좁은 diff 칸과 그 손잡이를 걷었습니다.
  > 260px 사이드를 둘로 나누면 목록이 90px 이 되어 파일 이름이 사라졌습니다
  >
  > **git 뷰 탭도 편집기 탭과 같습니다** — 닫고, 끌어 옮기고, 끌어 나눌 수 있습니다
  >
  > **diff 의 오른쪽을 고칠 수 있습니다.** 워킹 트리 쪽이 디스크의 파일인 축
  > (Changes·Conflicts)에서 고치고 `Cmd+S` 로 저장하면 편집기 탭과 같은 경로로
  > 저장됩니다. 스테이지된 내용은 파일이 아니라 읽기 전용이고, 새 파일은 diff 가
  > 아니라 편집기로 열립니다 — VSCode 와 같은 규약입니다

- **코드 탐색.** Editor 창에서 `F12` 로 커서가 놓인 심볼의 **정의로 뛰고**,
  `Shift+F12` 로 **참조 목록**을 보고, `Ctrl+Alt+-` 로 돌아옵니다. 심볼에 마우스를
  얹으면 타입과 문서가 뜨고, 언어 서버가 찾은 **에러·경고가 밑줄**로 섭니다.
  정의가 다른 파일이면 그 파일이 탭으로 열리고 탐색기에서도 보입니다
  > **저장하지 않은 편집도 반영됩니다** — 방금 쓴 함수로도 뜁니다. 요청이 그 파일의
  > 현재 내용을 함께 싣기 때문이며, 그러지 않으면 "정의를 보려면 저장하라" 가 됩니다
  >
  > 언어 서버 실행 파일을 **우리 배포물에 넣지 않았습니다.** gopls·
  > typescript-language-server·pyright 는 모두 독립 실행 파일을 릴리스하지 않으므로,
  > 설정 ▸ Code 의 `받기` 는 **이 기계의 `go`·`npm` 을 부르는 일**입니다. 받은 것은
  > `~/.dongminal/lsp/` 안에만 살고 그 폴더를 지우면 원상복구됩니다 — 시스템은
  > 건드리지 않습니다. 그 도구가 없으면 무엇이 없어서 못 받는지를 이름으로 알립니다
  >
  > 서버가 없는 언어의 파일을 열면 편집기 위에 **한 번** 제안이 뜹니다. `다시 보지
  > 않기` 는 그 언어에 대한 것이라 파일마다 다시 뜨지 않습니다
- **떠날 때 확인 (설정).** 설정 ▸ Display 의 `떠날 때 확인` (**기본 끔**). 켜면 탭을
  닫거나 새로고침할 때 되묻습니다. 종전에는 도구가 하나라도 있으면 **끌 수 없이**
  물었습니다
  > 켜도 잃는 것은 **연결뿐**입니다 — 세션은 서버에 남아 다시 접속하면 이어집니다.
  > 확인 문구는 브라우저가 정하므로 바꿀 수 없고, 새 판을 받는 자동 새로고침은 이
  > 설정과 무관하게 묻지 않습니다

### 수정

- **변경을 버리기 전에 안내하던 `stash` 명령이 듣지 않던 것.** 확인창은 "먼저
  이것을 실행하면 stash 로 남습니다" 라며 `git stash push -- <경로>` 를 보여
  주는데, 대상에 **새 파일(untracked)이 하나라도 섞이면 그 명령이 실패하고 아무
  것도 담기지 않았습니다.** 되살릴 길을 알려 준다면서 듣지 않는 명령을 주고
  있었습니다. 이제 `-u` 를 붙여 어느 경우에도 담깁니다

- **저장하는 중에 그 칸을 닫으면 그 뒤로 그 파일이 저장되지 않던 것.** 같은
  파일을 두 칸에서 열어 두고 한쪽에서 저장한 직후 그 칸을 없애면, 남은 칸에서
  `Cmd+S` 를 눌러도 **아무 일도 일어나지 않았습니다.** "저장 중" 표시가 문서에
  걸린 채 남아 그 뒤의 모든 저장을 건너뛰었기 때문입니다
  > 같은 자리에서 하나가 더 있었습니다 — **쓰기가 성공했는데도 문서가 "저장 안
  > 됨" 으로 남아** 탭에 표시가 남고, 목록에서 그 창을 지워도 "저장하지 않은
  > 편집이 있어 미뤘습니다" 가 떴습니다. 이미 디스크에 있는 내용을 두고서입니다
  >
  > 미리보기 탭은 탐색기에서 다른 파일을 누를 때마다 닫히므로 드문 길이
  > 아니었습니다

- **내부 새로고침이 탐색기를 갱신하지 않던 것.** `내부 새로고침` 은 서버 상태를
  다시 받아 오는 기능인데 **탐색기만 낡은 채 남았습니다.** 터미널에서 만든 파일이
  목록에 없을 때 눌러도 소용이 없었습니다. 이제 펼쳐 둔 폴더를 다시 읽습니다 —
  펼침은 그대로 보존됩니다

- **서브모듈·중첩 저장소가 `?` 한 줄로만 보이고 색이 없던 것.** 저장소 안에 다른
  저장소가 있으면(서브모듈이든, 그냥 `git init` 한 폴더든) Git 패널에는 **`?` 한
  줄**만 뜨고 눌러도 내용이 나오지 않았으며, 탐색기에서는 그 폴더도 그 안의 파일도
  **색이 없었습니다**
  > 원인은 넷이었습니다. ① `git status` 는 다른 저장소 안을 들여다보지 않아
  > `--untracked-files=all` 로도 폴더 한 줄로 접힙니다 ② 그 경로만 끝에 `/` 가 붙어
  > 탐색기의 어떤 항목과도 짝이 맞지 않았습니다 ③ 서브모듈은 폴더인데 파일로
  > 취급됐습니다 ④ `/tmp` 처럼 심볼릭 링크가 낀 경로에서는 루트 비교가 어긋나 그
  > 창의 색이 **영영 꺼졌습니다**
  >
  > 이제 서버가 그 항목이 디렉터리임을 확정해 알리고, 폴더에 색이 서며 **그 안의
  > 파일들이 색을 물려받습니다.** Git 패널에서 그 행을 고르면 빈 diff 대신 사유와
  > **`저장소로 추가`** 버튼이 나옵니다 — 누르면 그 저장소가 목록에 서고 그 창으로
  > 갑니다
- **저장소 하위 폴더를 Editor 로 열면 색이 하나도 없던 것.** `~/repo/app/src` 처럼
  저장소의 **안**을 루트로 잡으면 변경 표시가 통째로 없었습니다. 이제 그 저장소를
  기준으로 색이 섭니다
- **`git init` 뒤에도 색이 돌아오지 않던 것.** "저장소가 아니다" 라는 답을 한 번
  받으면 그 창은 새로고침 전까지 다시 묻지 않았습니다. 이제 그 답은 굳지 않고,
  창을 다시 보는 순간 곧바로 다시 확인합니다
- **샌드박스 창의 프로파일·작업 폴더 설정창이 뜨지 않던 것.** `▣ Box` 를 누르면
  아무것도 묻지 않고 창이 열려, 작업 폴더를 고를 길도 프로파일을 늘릴 길도 화면에
  없었습니다
  > 프로파일이 `scratch` 하나뿐이고 그것이 작업 폴더를 받지 않았기 때문에 "물을
  > 것이 없다" 로 판정됐습니다. 이제 **언제나 묻고**, `scratch` 도 작업 폴더를
  > 받습니다 — 다만 **마운트가 아니라 복사**입니다
  >
  > 복사는 컨테이너를 만들 때 한 번 일어나고, **컨테이너 안의 변경은 호스트로
  > 돌아오지 않습니다.** 그래서 `scratch` 는 여전히 격리 경계이며, 그 사실을
  > 선택창·입력란·사이드바 배지 세 자리가 함께 알립니다. 되돌아와야 하는 작업은
  > 마운트를 쓰는 `dev` 프로파일의 자리이고, 그것을 정의하는 길을 선택창이 안내합니다
  >
  > 폴더가 2 GiB 또는 5만 항목을 넘으면 **창을 열지 않고 거부합니다** — 조용히
  > 일부만 넣지 않습니다
- **찾기 키가 파일을 편집하던 것.** Editor 창에서 `Cmd+F` 를 누른 뒤 타이핑하면 그
  글자가 **문서에 삽입됐습니다.** 찾기 상자는 떠 있는데 글자를 하나도 받지 못했고,
  검색하려고 누른 키가 파일을 고쳤습니다
  > 원인은 두 겹이었습니다. 편집기 안의 키 판정이 늦어 Monaco 가 자기 찾기 상자를
  > 먼저 열어 포커스를 가져갔고, 곧이어 우리 코드가 그 포커스를 **본문으로 되돌렸습니다.**
  > 이제 우리가 키를 먼저 잡고 **편집기 우상단에 찾기 줄**을 띄웁니다 — 일치 하이라이트,
  > `현재/전체`, 대소문자·정규식·단어 단위 토글이 있고 질의는 탭마다 따로 남습니다
- **언어 서버와의 연결이 끊길 때 서버가 죽을 수 있던 것.** 드물게 `close of closed
  channel` 로 프로세스가 통째로 내려갔습니다. 편집기를 닫거나 언어 서버가 죽는
  순간이 그 자리였습니다
  > 연결을 정리하는 자리를 **읽기 루프의 끝과 닫기가 동시에** 지나갈 수 있는데,
  > "이미 정리됐는가" 를 잠금 밖에서 보고 잠금 밖에서 정리했습니다. 둘이 같은
  > 순간에 "아직" 이라고 읽으면 둘 다 정리해 죽었습니다. 이제 그 판정과 정리가
  > 같은 잠금 안에 있습니다

- **Windows 에서 언어 서버를 받아 두고도 못 찾던 것.** Windows 에서는 `설정 ▸ Code`
  의 `받기` 가 끝나도 상태가 계속 "없음" 이었고, 설정에 적은 절대경로도 무시됐습니다.
  PATH 에 이미 있는 서버 말고는 아무것도 쓰지 못했습니다
  > "실행할 수 있는 파일인가" 를 **실행 권한 비트**로 판정했기 때문입니다. Windows
  > 에는 그 비트가 없어 그 판정이 언제나 거짓이었습니다. 이제 그 판정은 OS 마다
  > 다릅니다 — Windows 는 `PATHEXT` 의 확장자로 봅니다. 전용 디렉터리에서 찾는
  > 파일 이름도 도구가 실제로 놓는 이름(`gopls.exe`, npm 의 `.cmd`)으로 맞췄습니다

- **핀을 걸면 화면이 따라오지 않던 것.** 리포를 고정하거나 `Editor` 목록을 고쳐도
  그 행과 창이 **새로고침 전에는 서지 않았습니다.** 반대로 지운 것이 남아 있기도
  했습니다
  > 원인은 저장과 알림이 겹치는 순간이었습니다. 화면이 저장하는 동안 도착한 변경
  > 알림을 "어차피 내가 방금 보낸 것의 메아리" 로 보고 **버렸는데**, 그 알림이 남이
  > 바꾼 것이면 다시 올 것이 없어 화면과 서버가 그대로 갈렸습니다. 이제 알림에
  > 실린 판 번호로 가려, 내 것보다 새로우면 버리지 않습니다

- **핀 직후에 연 Git 창이 사라지던 것.** 리포를 고정한 바로 다음에 그 리포의 Git
  창을 열면, 창이 통째로 사라지고 브랜치도 커밋 상자도 보이지 않았습니다
  > 바로 위의 "먼저 저장된 쪽이 이긴다" 가 **아직 저장되지 않은 내 창까지** 삼켰기
  > 때문입니다. 물러날 때 지워도 되는 것은 서버가 **알았다가 없앤** 창뿐입니다 —
  > 서버가 한 번도 본 적 없는 창은 남깁니다. 남의 창을 되살리지는 않습니다

- **다른 화면이 만든 창이 사라지던 것.** 같은 서버를 여러 화면에서 열어 두었을 때,
  한쪽이 만든 창이 다른 쪽의 저장에 **지워졌습니다.** 콘솔의 409 는 그 손실의
  신호였습니다
  > 이제 **먼저 저장된 쪽이 이깁니다.** 충돌한 화면은 그 자리에서 물러나 서버의
  > 상태를 받아들입니다 — 남의 창은 사라지지 않고, 대신 같은 순간에 겹친 내 조작
  > (사이드바 폭 같은)이 물러날 수 있습니다. 다시 하면 되는 일과 되돌릴 수 없는 일
  > 사이의 선택입니다. 충돌이 이어지면 콘솔에 그 사실이 적힙니다
- **보고 있는 탭의 알람에 데스크톱 배너가 뜨지 않던 것.** 지금 보고 있는 도구의
  알람이면 배너와 소리를 억제했습니다 — "눈앞의 것을 다시 부르는 것은 방해" 라는
  판단이었습니다. 그런데 이 앱에서 알람이 서는 **가장 흔한 순간이 에이전트가 도는
  것을 지켜보다 끝나는 순간**이고, 그때 사용자는 그 탭을 보고 있습니다. 방해를
  막으려던 조건이 기능 자체를 막고 있었습니다
  > 이제 **언제나** 뜹니다. 시끄러우면 설정 ▸ Notifications 의 두 토글로 끕니다 —
  > 그 판단을 우리가 대신 짐작하지 않습니다
- **검색·정의 이동이 이미 열어 둔 파일에서 그 줄로 가지 않던 것.** 전체 검색으로
  **이미 열려 있는** 파일의 결과를 고르면 파일만 열리고 커서는 첫 줄에 남았습니다.
  탭을 활성화한 직후 도착하는 내용 갱신이 방금 옮긴 커서를 되돌렸기 때문입니다 —
  이제 내용이 같으면 다시 넣지 않습니다

- **메모장.** `Editor` 탭 맨 아래, `~` 위에 **메모장** 행이 고정으로 섭니다. 누르면
  메모 전용 창이 서고 그 안에서는 여느 편집기와 똑같이 씁니다 — 탐색기의 `새 파일` 로
  메모를 늘리고, 분할해서 나란히 놓고, `Cmd+S` 로 저장합니다. 새로 익힐 조작이 없는
  이유는 그 창이 **루트가 메모 폴더인 Editor 창**이기 때문입니다
  > 메모는 서버의 `~/.dongminal/notes/` 에 **그냥 파일로** 삽니다. 브라우저를 닫아도
  > 다른 기기에서 접속해도 그대로이고, 터미널의 `grep` 이나 git 이 그대로 닿습니다.
  > 브라우저 저장소에 두지 않은 이유는 이 앱의 브라우저 저장소가 설정과 "지금 어디를
  > 보고 있는가" 만 담기 때문입니다 — 거기 두면 브라우저 탭을 닫는 순간 사라집니다

### 수정

- **탐색기가 바깥의 변경을 따라가지 않던 것.** 터미널에서 파일을 만들거나
  `git checkout` 이 파일을 더하고 지워도, 탐색기는 **새로고침 버튼을 누르기 전까지
  영영** 옛 목록을 보였습니다. 3초마다 도는 갱신이 바꾸는 것은 git 상태 **색뿐**이라
  목록 자체는 한 번 읽은 그대로였습니다 — 느린 것이 아니라 반영하지 않던 것입니다.
  이제 같은 주기에 겹이 바뀌었는지 함께 묻고 **바뀐 겹만** 다시 읽습니다. 근거가
  디렉터리의 수정 시각이므로 **git 저장소가 아닌 경로에서도** 따라오고, 아무것도
  바뀌지 않은 주기에는 목록 요청이 나가지 않습니다
- **편집기 커서가 미끄러지듯 움직이던 것.** 커서가 이전 자리에서 새 자리로 애니메이션
  되어 이동과 타이핑에 지연으로 느껴졌습니다. 이제 VS Code 와 같이 곧바로 옮겨갑니다.
  깜빡임은 그대로입니다
- **부분 쓰기와 저장 경쟁이 상태를 잃던 것.** `workspace.json`·`tools.json`·
  `settings.json` 과 편집기가 저장하는 파일이 도중에 죽으면 **잘린 파일이 살아 있는
  파일**이 됐습니다. 이제 임시 파일에 쓰고 디스크에 닿은 뒤 바꿔 겁니다 — 실패해도
  기존 내용은 그대로입니다. 저장 두 개가 순서를 뒤집어 디스크에 닿던 것과, HTTP
  핸들러의 패닉이 응답 없이 연결만 끊던 것도 함께 고쳤습니다

- **`+ Add` 가 채워 주는 경로가 엉뚱한 자리를 가리키던 것.** Git 과 Editor 의
  `+ Add` 는 지금 터미널의 경로를 미리 채웁니다. 그런데 "지금 터미널" 의 기억이
  **칸을 클릭할 때만** 갱신되어, 창을 옮기거나 탭을 바꾸거나 탭을 새로 열어
  자리를 옮기면 그 기억이 낡은 채로 남았습니다 — 창마다 다른 저장소를 열어 두고
  쓰면, 방금까지 있던 자리가 아니라 **한참 전에 마지막으로 클릭한 자리**가
  채워졌습니다. 이제 여는 순간에 지금 자리를 다시 읽습니다
- **서버가 도구의 경로를 모를 때 자기 경로를 대신 내주던 것.** 그 값은 "이 도구의
  경로" 라는 표시를 달고 나갔기 때문에 화면은 그것을 사용자의 자리로 믿고
  채웠습니다 — 그럴듯한 남의 경로라 빈 칸보다 알아채기 어렵습니다. 이제 모르면
  모른다고 답하고, 화면은 빈 칸으로 열어 그 사실을 보입니다

## [1.0.8] — 2026-09-02

### 변경

- **새 빌드가 확실히 화면에 닿습니다.** 지금까지 브라우저 캐시를 비우는 판 번호를
  `index.html` 이 70 곳에 손으로 적고 있었고, 자산을 고치면서 그 번호를 올리지 않으면
  새 버전을 띄워도 **캐시에 있는 옛 화면이 계속 돌았습니다** — 실제로 그렇게 지나간
  커밋이 있었습니다. 이제 그 값을 서버가 **자산의 내용에서 계산해** 넣습니다. 사람이
  올릴 것이 없으므로 잊을 것도 없습니다

### 추가

- **설정을 파일로 내보내고 가져옵니다.** 설정 ⚙ → **Backup**. 지금까지 새 기계에
  dongminal 을 세우면 테마·단축키·상태바 항목·레이아웃 프리셋을 손으로 다시 짜야
  했습니다 — 값이 서버와 브라우저 저장소에 흩어져 있어 직접 복제할 수도 없었습니다.
  이제 **그 창에서 설정할 수 있는 값 전부**가 JSON 파일 하나로 나가고 그 파일로
  돌아옵니다. 알림 토글·에이전트 패널 주기·슬롯 방향처럼 브라우저에 사는 값도 함께
  실립니다
  > 가져오기는 **전체 교체**입니다 — 파일에 없는 설정은 기본값으로 돌아가고,
  > 되돌릴 수 없습니다. 무엇이 덮이는지 보이고 한 번 확인을 받은 뒤 페이지를 다시
  > 엽니다. 사이드바 너비 같은 화면 치수와 리포별 Git 보기 취향은 옮겨도 뜻을
  > 잃으므로 담지 않습니다

## [1.0.7] — 2026-09-01

### 변경

- **파괴적인 동작을 한 번만 확인합니다.** 지금까지 브랜치 삭제처럼 되돌릴 수 없는
  동작은 확인 버튼을 두 번 눌러야 했습니다 — 첫 화면이 사라질 목록을, 다음 화면이
  되살릴 명령을 보였습니다. 이제 **한 화면이 둘을 함께** 보이고 한 번으로 끝납니다.
  실수로 누르는 것을 막는 것은 걸음 수가 아니라 기본 선택이 `취소` 이고 `Enter` 가
  실행이 아닌 것이며, 그 둘은 그대로입니다. Git 의 모든 파괴적 동작 — worktree 제거,
  `reset --hard`, stash 버리기, force push, 태그·원격 ref 삭제 — 이 함께 한 걸음이
  됩니다
- **새 버전이 뜨면 화면이 스스로 다시 뜹니다.** 지금까지는 아래쪽에 배너가 뜨고
  사용자가 눌러야 했습니다. 누르지 않으면 옛 화면이 계속 돌아, 고친 것이 화면에
  도달하지 않은 채로 쓰게 됩니다. 이제 감지하는 즉시 새로고침하며, **"이 페이지를
  나가시겠습니까?" 를 묻지 않습니다** — 물으면 자동이 아니게 되고, 화면을 보고 있지
  않으면 대화만 떠 있고 갱신은 오지 않습니다. 사용자가 탭을 닫거나 주소를 옮길 때는
  그 확인이 그대로 걸립니다
  > 편집기 탭의 **미저장 내용은 사라집니다.** 자동이 되면 그 순간을 고를 수 없으므로,
  > 이 선택에 딸린 대가로 적어 둡니다
- **새 버전을 서버가 알려 줍니다.** 종전에는 화면이 주기적으로 물어봤습니다(60초).
  이제 서버가 연결이 열릴 때 자기 판을 실어 보내므로, **서버가 뜨는 순간** 화면이
  압니다 — 확인 요청은 한 번도 나가지 않습니다. 자산은 프로그램에 담겨 있어 그것이
  바뀌는 길은 서버 교체뿐이고, 서버가 바뀌면 연결이 반드시 끊기기 때문입니다
- **잠자기에서 깨어나도 소식이 옵니다.** 잠든 기기의 연결은 **끊긴 줄 모른 채 살아
  있는 것처럼 남습니다.** 그동안 다른 컴퓨터에서 서버를 올려도 화면은 그것을 모르고,
  새 버전뿐 아니라 명령·창 구성·주의 알림까지 함께 멎었습니다. 이제 서버의 소식이
  일정 시간 끊기면 연결을 다시 엽니다 — 화면으로 돌아오는 순간에는 곧바로 확인합니다

### 수정

- **터미널이 스스로 끊었다 다시 붙기를 되풀이하던 것.** 도구를 관리하는 안쪽
  프로세스에 다시 붙는 짧은 동안, 서버는 도구 목록을 **모르면서도 "하나도 없다"고**
  답했습니다. 화면은 그것을 사실로 받아 살아 있는 터미널을 전부 닫고, 다음 응답에서
  전부 다시 열었습니다 — 도구 10개가 3초 간격으로 통째로 재연결되는 것이 실측으로
  남아 있습니다. 심할 때는 그 pane 과 창까지 함께 지워졌습니다. 이제 서버가 **모른다는
  사실을 함께** 보내고, 모르는 동안에는 화면이 아무것도 지우지 않습니다
- **화면을 두 칸으로 나눠 쓸 때 터미널이 매번 다시 붙던 것.** 창 구성이 바뀔 때마다
  둘째 칸의 터미널이 죽은 것으로 잘못 판정돼 파괴되고 다시 만들어졌습니다
- **새로고침하면 사이드바가 첫 창으로 돌아가던 것.** 세 번째 창을 보다가 다른 탭에
  갔다 새로고침하고 `Windows` 로 돌아오면 첫 창이 열렸습니다 — 탭 자체는 기억되는데
  **그 탭이 돌아갈 자리**는 기억되지 않았습니다. 이제 보던 창으로 돌아갑니다.
  `Editor` 탭도 같습니다
- **History 의 브랜치 이름표에서 연 메뉴가 반쪽이던 것.** 커밋 옆 이름표를 우클릭하면
  `Delete (local + remote)` 가 언제나 "upstream 이 설정돼 있지 않습니다" 로 죽어
  있었고, `Delete` 의 복구 명령에는 되살릴 커밋이 빠져 있었습니다. 같은 브랜치라도
  Branches 탭에서 열면 멀쩡했습니다 — 이름표가 넘기는 정보에 그 값들이 없었기
  때문입니다. 이제 세 자리(Branches 탭·refs 목록·History 이름표)가 같은 판정을 합니다
- **옆 칸의 Git·편집기가 멎어 있던 것.** 화면을 둘로 나눠 한쪽에서 터미널을 쓰면,
  **눈앞에 보이는** 옆 칸의 History·Changes·Branches 가 갱신을 멈췄습니다. 그 칸을
  한 번 눌러 포커스를 줘야 되살아났습니다 — 관측 조건이 "보이는가" 가 아니라
  "포커스인가" 였기 때문입니다. 편집기의 탐색기도 같은 이유로 서 있지 않은 쪽이
  멎었습니다. 이제 **보이면 갱신됩니다.** 칸을 넷으로 늘려도 서버로 나가는 요청 수는
  그대로입니다
- **History 의 커밋 목록이 한 번 멎으면 영영 돌아오지 않던 것.** 목록을 받는 도중에
  저장소가 바뀌면(브랜치 전환, 커밋 등) 그 응답을 버리면서 **다음 요청까지 함께
  막아** 커밋 목록이 굳었습니다. 왼쪽의 브랜치 목록은 다른 경로라 계속 갱신돼서,
  한쪽만 살아 있는 화면으로 보였습니다
- **브랜치를 만들거나 지워도 History 그래프의 이름표가 그대로던 것.** 왼쪽 브랜치
  목록은 바로 바뀌는데 커밋 옆에 붙는 이름표는 낡은 채로 남아, 새로고침을 눌러야
  했습니다. 그 이름표는 커밋 목록과 함께 오는 값이라 **목록을 다시 받아야** 갱신되는데
  사이드바만 다시 받고 있었습니다
- **터미널에서 만든 브랜치를 화면이 알아채지 못하던 것.** 변화 감지가 `HEAD`·index·
  **현재** 브랜치만 보고 있어서, 다른 브랜치가 생기거나 사라지는 것은 어느 값도
  움직이지 않았습니다. 이제 `refs` 아래 디렉터리의 변경 시각까지 봅니다 — git 을
  실행하지 않으므로 감지 비용은 그대로입니다
  > 다만 **다른 브랜치가 제자리에서 움직이는 것**(터미널에서 그 브랜치에 커밋)은
  > 여전히 감지되지 않습니다. 그때는 새로고침이 필요합니다

## [1.0.6] — 2026-09-01

### 추가

- **History 에서 브랜치를 버튼으로 만듭니다.** 지금까지 새 브랜치를 만들려면 커밋이나
  브랜치를 우클릭해야 했습니다 — 만들 수는 있었지만 눈에 보이는 자리가 없었습니다.
  이제 History 위쪽 줄에 `+ Branch` 가 서고, 누르면 현재 HEAD 에서 갈라지는 브랜치를
  만듭니다. 특정 커밋에서 갈라지고 싶으면 종전대로 그 커밋을 우클릭합니다
- **브랜치의 로컬과 원격을 한 번에 지웁니다.** 지금까지는 `Delete`(로컬)와
  `Delete remote branch`(원격)를 따로 눌러야 했습니다. 이제 `Delete (local + remote)`
  하나로 끝납니다. 확인 화면에 사라질 로컬·원격 이름이 함께 보이고, 로컬을 먼저 지운
  뒤 성공했을 때만 원격을 잇습니다 — 되돌리기 어려운 쪽을 먼저 잃지 않기 위해서입니다.
  한쪽만 지우던 두 항목은 그대로 있습니다

### 수정

- **칸의 이름표가 무슨 창인지 말합니다.** 지금까지 칸 머리글은 창 이름만 적었고, Git
  창은 저장된 이름 그대로 `Git` 이라고만 나와 어느 리포를 보고 있는지 알 수 없었습니다
  — 위쪽 이름줄은 리포 이름을 말하는데 머리글은 같은 창을 다른 이름으로 부르고
  있었습니다. 이제 두 자리가 같은 이름을 쓰고, 앞에 종류(`Windows`·`Git`·`Editor`)가
  붙습니다. 칸이 여럿일 때는 위쪽 이름줄을 비우고 각 머리글이 그 자리를 잇습니다
- **칸 경계가 창 안 분할과 구분됩니다.** 지금까지 칸 사이 경계와 창 안 분할 경계가 같은
  색에 굵기만 1px 달라 어디까지가 한 칸인지 읽히지 않았습니다. 이제 칸 경계는 쓰고 있는
  테마에서 뽑은 색으로 굵게 섭니다 — 테마를 바꾸면 경계색도 따라갑니다
- **Run 대시보드를 두 칸에서 함께 봅니다.** 지금까지 같은 Run 탭을 두 칸에 두면 뒤에
  그린 칸이 앞 칸에서 화면을 떼어 가 한쪽이 비었습니다. 이제 양쪽 모두 보이고 Run 이
  바뀌면 함께 갱신됩니다
- **로컬 브랜치에서도 합치기가 열립니다.** 지금까지 `Merge into current` 와 `Pull/Merge`
  가 나란히 있으면서 각각 반대쪽 브랜치에서 막혀 있었습니다 — 이름은 둘이었지만 사실
  같은 일을 했습니다. 이제 `Merge into current` 하나가 로컬·원격 모두에서 열립니다

## [1.0.5] — 2026-09-01

### 추가

- **칸마다 다른 탭을 봅니다.** 같은 창을 두 칸에 놓으면 지금까지는 양쪽이 늘 같은
  탭을 보였습니다 — 활성 탭이 창에 붙어 있었기 때문입니다. 이제 왼쪽 칸에 Changes,
  오른쪽 칸에 History 를 나란히 둘 수 있습니다. 각 칸이 보던 탭은 새로고침 뒤에도
  돌아옵니다. 칸이 하나일 때의 동작은 종전과 완전히 같습니다
- **칸마다 다른 폴더를 펼칩니다.** Editor 창을 두 칸에 놓으면 지금까지는 뒤에 그린
  칸이 앞 칸에서 탐색기를 떼어 가 한쪽이 비었습니다. 이제 양쪽 모두 자기 탐색기를
  갖고, 칸마다 다른 폴더를 펼쳐 볼 수 있습니다. 폴더 목록과 git 색은 여전히 한 번만
  조회하므로 칸을 늘려도 요청이 늘지 않습니다
- **같은 탭을 두 칸에서, 서로 다른 파일로 봅니다.** Git 창의 같은 탭을 두 칸에 두면
  지금까지는 한쪽만 보였습니다. 이제 양쪽 모두 보이고, 왼쪽에서 파일 A 의 diff·
  오른쪽에서 파일 B 의 diff 를 나란히 볼 수 있습니다. 파일 선택·그룹 접힘·미리보기
  대상이 칸마다 따로 남습니다. `git status` 폴링은 칸 수와 무관하게 한 벌입니다
- **같은 파일을 두 칸에서 함께 편집합니다.** 한쪽에서 입력한 것이 다른 쪽에 즉시
  보이고, 커서·스크롤·접힘은 칸마다 남습니다

### 수정

- **다른 칸을 누른 첫 클릭이 사라지지 않습니다.** 지금까지 다른 칸의 탭이나 탐색기
  폴더를 누르면 첫 번째 클릭은 칸 포커스만 옮기고 아무 일도 하지 않아, 두 번 눌러야
  했습니다. 포커스가 옮겨지는 순간 화면을 통째로 다시 그려 눌린 자리가 사라졌기
  때문입니다 — 이제 포커스 이동은 강조만 바꾸고, 그리기는 그 클릭이 끝난 뒤에 합니다
- **같은 파일을 두 칸에서 편집할 때 한쪽 저장이 다른 쪽 저장을 덮지 않습니다.**
  지금까지 두 칸의 편집기는 각자의 내용을 들고 있어, 나중에 저장한 쪽이 먼저 저장한
  내용을 되돌렸습니다. `● ` 표시도 서로 밟았습니다
- **칸이 셋·넷일 때 그 칸의 편집기가 사라지지 않습니다.** 칸 3·4에 편집기 탭이 있으면
  화면을 다시 그릴 때마다 그 편집기가 파괴되어 편집 중이던 상태를 잃었습니다

## [1.0.4] — 2026-08-31

### 추가

- **Editor 의 검색 셋에 단축키가 생겼습니다.** `파일 내에서 검색`(`Cmd/Ctrl+F`) ·
  `파일 검색`(`Cmd/Ctrl+P`) · `파일 전체에서 검색`(`Cmd/Ctrl+Shift+F`) 셋 다 설정 →
  Shortcuts → `Editor 검색` 에서 바꿀 수 있습니다. 지금까지 뒤의 둘은 조합이 코드에
  박혀 있어 바꿀 수 없었고, 파일 내 검색은 아예 앱이 관여하지 않았습니다. 기본값이
  `Ctrl` 과 `Cmd` 를 모두 받는 이유는 두 OS 의 관용이 다르기 때문입니다 — 직접
  녹음하면 누른 조합 하나로 굳습니다. 세 키는 Editor 창에서만 듣고, 터미널 창의
  `Cmd/Ctrl+F` 는 종전대로 터미널 검색입니다
- **검색으로 연 파일이 탐색기에 드러납니다.** 파일 검색·전체 검색으로 파일을 열면
  탐색기가 그 파일까지 폴더를 모두 펼치고 해당 줄을 선택으로 표시합니다. 지금까지는
  파일만 열리고 트리는 접힌 채여서, 검색이 방금 알려 준 경로를 손으로 다시 펼쳐야
  했습니다. git 변경파일·`dmctl open` 으로 열어도 같습니다

### 변경

- **설정 → Display 의 `슬롯 방향` 이 토글이 되었습니다.** 값이 가로·세로 둘뿐이라
  버튼 하나를 누를 때마다 뒤집힙니다. 버튼에는 지금 값이 적히고, 눌러서 바뀔 방향은
  툴팁이 말합니다

- **창 슬롯 — 여러 창을 한 화면에.** 터미널 창과 Git·Editor 는 서로 다른 창이라 한 번에
  하나만 볼 수 있었고, 그래서 브라우저 창을 여럿 띄우거나 브라우저 화면 분할을 쓰게
  됐습니다. 이제 툴바의 `− Slot +`(`Ctrl+Shift+S` / `Ctrl+Shift+X`)로 화면을 **넷까지**
  나눠 창을 하나씩 놓습니다. `+` 로 만든 칸은 보고 있던 칸 바로 뒤에 서서 포커스를
  가져가므로, 사이드바에서 다른 창을 고르면 곧바로 나란히 놓입니다. 칸 사이 이동에는
  **새 키가 없습니다** — `Ctrl+Shift+←/→` 가 창 안에서 더 갈 분할 칸이 없을 때 옆 칸으로
  넘어갑니다. 칸마다 창 이름이 적히고, 경계를 끌면 이웃한 두 칸의 넓이가 바뀝니다.
  설정 → Display → `슬롯 방향` 에서 가로·세로를 고릅니다(기본 가로). 칸 배치는 브라우저
  탭마다 따로 기억되므로 데스크톱은 여러 칸, 휴대폰은 한 칸으로 볼 수 있습니다.
  (`docs/internal/WINDOW_SLOTS_SRS.md`)

### 고침

- **Git 창의 상단 이름이 늘 `Git` 이던 것.** Window·Editor 는 상단만 봐도 어느
  것인지 아는데 Git 만 그러지 못했습니다 — 창은 하나이고 리포를 갈아타므로 그
  이름은 아무것도 말하지 않았습니다. 이제 **지금 보고 있는 리포의 이름**이 뜹니다.
  사이드바 목록이 부르는 그 이름이며, 리포를 고르지 않았으면 종전대로 `Git` 입니다
- **복사창이 엉뚱한 브라우저 창에 뜨던 것.** 터미널의 클립보드 쓰기(OSC 52)가 막혀
  마지막 수단인 복사창이 설 때, 그 창이 **붙어 있는 모든 브라우저에** 떴습니다. 한
  도구의 출력은 모든 브라우저로 가는데 각자 독립적으로 창을 세웠기 때문입니다 —
  사용자는 자기가 보던 창이 아닌 곳에서 그것을 만났고, 한쪽을 닫아도 다른 쪽에
  남았습니다. 이제 **그 터미널을 지금 보고 있는 창에서만** 섭니다
- **폐지된 런타임 헬퍼가 `bin/` 에 남던 것.** 설치는 지금까지 헬퍼를 **놓기만 하고
  거두지 않았습니다.** 목록에서 빠진 이름은 파일이 그대로 남아, 코드 어디에도 없는
  `mdview` 가 계속 있었습니다. 이제 자기가 놓았던 것 중 지금 목록에 없는 것을
  거둡니다 — 무엇을 놓았는지는 `bin/.helpers` 에 기록으로 남기고, **기록에 없는
  파일은 건드리지 않습니다**(그 디렉터리에는 셸 훅·에이전트 플러그인도 함께 삽니다)

## [1.0.3] — 2026-08-31

### 추가

- **`BG` 와 `Runs` 에 단축키가 생겼습니다.** `Ctrl+Shift+B` 로 백그라운드 도구
  목록을, `Ctrl+Shift+O` 로 Run 오케스트레이션을 엽니다(다시 누르면 닫힙니다).
  상단 툴바의 다섯 진입점 중 `Agents` 만 키가 있었습니다. `Runs` 가 `R` 이 아닌
  이유는 그 조합을 브라우저가 가져가기 때문입니다 — 내부 새로고침이 `K` 를 쓰는
  것과 같은 이유입니다. 설정 → Shortcuts 에서 바꿀 수 있습니다

### 변경

- **백그라운드 버튼 이름이 `Background` 가 됐습니다.** 1.0.2 에서 `BG` 로 나갔는데,
  줄임말은 자리를 아끼지만 처음 보는 사람에게 아무것도 말하지 않습니다 — 이웃이
  전부 낱말(`Split H`·`Runs`·`Agents`)인 줄에서 혼자 줄임말인 것도 어색했습니다

### 고침

- **설치된 헬퍼가 죽어 에이전트 훅이 실패하던 것.** `bin/dmctl` 이 **사라진 파일을
  가리키는 심볼릭 링크**가 되는 경우가 있었습니다 — `go run ./cmd/dongminal doctor`
  처럼 **`go run` 으로 실행하면** 설치가 그 임시 빌드 산출물을 가리키고, 프로세스가
  끝나면서 지워집니다. 설치는 성공한 채로 헬퍼 전부가 한꺼번에 죽고, 한참 뒤 훅이
  `No such file or directory` 로 실패할 때에야 드러났습니다. 이제 사라질 경로는
  가리키지 않고 `bin` 안에 실체를 붙들어 둡니다

### 변경

- **`doctor` 가 진단 대상을 바꾸지 않습니다.** 종전에는 검사를 위해 **운영 홈의
  `bin` 에 실제로 설치**했고, 위 사고가 퍼진 경로가 바로 그것이었습니다. 이제
  설치 계층은 임시 자리에서 시험하고 끝나면 지웁니다. 대신 운영 홈에 이미 설치된
  헬퍼가 **쓸 수 있는 상태인지 검사해서 보고**합니다 — 고치지는 않습니다
- **`health` 가 헬퍼 상태를 함께 봅니다.** 헬퍼가 죽어도 서버와 데몬은 멀쩡해서
  아무도 알려 주지 않았습니다. 기동이 이것을 스스로 고치지만, **다시 띄우기 전에**
  아는 수단이 없었습니다

## [1.0.2] — 2026-08-30

### 추가

- **페이지 제목을 설정에서 정합니다.** Settings ▸ Display 의 `페이지 제목` 에
  적으면 브라우저 탭 이름이 그것이 됩니다. 여러 대에 띄웠을 때 탭 줄에서 어느
  것이 어느 기계인지 갈립니다. 값은 서버가 가지므로 기기를 옮겨도 같은 이름이
  섭니다. 비우면 `Dongminal` 로 돌아갑니다
- **`dongminal window` 가 생겼습니다.** 돌고 있는 서버에 주소창 없는 창을 하나
  엽니다 — **서버를 띄우지도, 죽이지도 않습니다.** 종전에는 창을 여는 수단이
  `start --open` 뿐이라, 이미 돌고 있는 서버에 창만 붙일 길이 없었습니다
  (`start` 를 다시 부르면 돌던 서버를 죽이고 새로 띄웁니다). 서버가 떠 있지
  않으면 창을 열지 않고 그 사실을 알립니다

### 변경

- **하단 상태바가 창 크기에 맞춰 줄을 늘립니다.** 좁아지면 지표를 잘라내고
  있었습니다 — 실측으로 560px 에서 3개, 420px 에서 5개가 아무 표시 없이
  사라졌습니다. 이제 폭이 모자라면 다음 줄로 넘깁니다. 줄 수는 내용과 폭이
  정합니다. 한 줄로 들어가는 폭에서는 높이가 종전과 같습니다
- **백그라운드 버튼이 상단바로 옮겨 항상 보입니다.** 자리는 `Split H · Split V ·
  Runs · BG · Agents` 입니다. 백그라운드 도구가 0개여도 숨지 않고, 있을 때
  하이라이트와 개수가 켜집니다 — 나타났다 사라지는 버튼이 이웃의 위치를
  흔드는 편이 더 해롭기 때문입니다
- **주의 알림 표식이 숨쉽니다.** 알람이 걸린 창·분할 칸·탭·배지의 강조색이 2초
  주기로 사라졌다 돌아옵니다(1초씩). 정지한 색은 잠시 눈을 뗐다 돌아온 사람에게
  배경과 같습니다. 글자는 어느 시점에도 읽을 수 있고, 움직임을 원하지 않는
  설정(`prefers-reduced-motion`)에서는 종전처럼 정지합니다
- **`start --open` 을 없앴습니다.** 같은 일을 `dongminal window` 가 합니다.
  설치 직후에는 `start` 다음에 `window` 를 부르면 종전과 같습니다

### 고침

- **알람 표식의 테두리 두께가 면마다 달랐습니다.** 분할 칸의 링이 테두리와 안쪽
  그림자 **두 곳**에서 나오는데 안쪽 그림자를 자식이 면마다 다르게 덮어, 실측으로
  위 2px · 왼쪽 3px · 아래 4px 이었습니다 — 모서리가 우그러져 보이던 이유입니다.
  이제 네 변이 같습니다

## [1.0.1] — 2026-08-30

### 추가

- **내부 새로고침이 생겼습니다.** 사이드바 최하단의 `⟳` 또는 `Ctrl+Shift+K`
  (설정 → Shortcuts 에서 바꿀 수 있습니다). 페이지를 다시 열지 않고 **서버 상태만 다시 받아** 화면을
  맞춥니다 — 편집기의 미저장 내용, 탐색기의 펼침·스크롤, Git 패널의 열린 탭이
  그대로 남습니다. 터미널은 전부 다시 붙어 서버의 스크롤백을 받아옵니다. 연결이
  한동안 끊겼다 붙은 뒤 화면이 서버를 따라가지 못할 때 쓰는 수단입니다
- **탐색기가 `.gitignore` 대상을 흐리게 보입니다.** VS Code 와 같은 판정입니다 —
  패턴에 맞아도 **추적 중인 파일은 흐려지지 않습니다.** 근거는 보고 있는 겹에만
  묻는 `git check-ignore` 라, 큰 저장소에서도 비용이 화면에 비례합니다.
  `node_modules` 처럼 무시된 폴더는 그 안을 다시 묻지 않습니다
- **폴더를 통째로 내려받습니다.** 탐색기의 폴더 행에서 다운로드를 고르면 zip 으로
  옵니다. 압축 없이 폴더 그대로 받는 길은 브라우저가 HTTPS 를 요구해, 지금
  구성(평문 HTTP + Tailscale)에서는 쓸 수 없습니다
- **폴더를 통째로 올립니다.** 탐색기에 폴더를 끌어다 놓거나 메뉴의 `폴더 업로드`
  로 고르면 하위 구조가 그대로 올라갑니다
- **올리다 실패하면 묻습니다.** 재시도 · 건너뛰기 · 이후 모두 건너뛰기 · 중단 중
  고릅니다. 종전에는 첫 실패에서 통째로 멈춰, 수백 개 중 하나가 부딪히면 처음부터
  다시 해야 했습니다. 덮어쓰지 않는 규칙은 그대로입니다
- **Changes 탭의 두 칸을 끌어 나눕니다.** 파일 목록과 미리보기 사이에 손잡이가
  섰습니다. 가로·세로 값을 따로 기억하므로 데스크톱에서 정한 폭이 모바일의
  높이가 되지 않습니다
- **Diff 에 개요 눈금이 섭니다.** VSCode 처럼 스크롤바에 변경 위치가 색으로
  찍힙니다. 변경 없는 구간의 접기는 **기본이 꺼짐**입니다 — 접으면 눈금이 접힌
  좌표계 위에 서서 실제 파일의 줄 위치와 어긋나기 때문입니다. 필요하면 Diff
  머리의 `Fold Unchanged` 로 켭니다
- **Editor 에서 파일을 찾습니다.** `cmd+p` 파일 이름, `cmd+shift+f` 전체 내용.
  `cmd+f`(현재 파일)는 Monaco 의 것을 그대로 씁니다. 전체 검색은 ripgrep 이
  있으면 그것을, 없으면 자체 구현으로 훑고 어느 쪽을 썼는지 함께 보입니다
- **이미지를 봅니다.** 편집기 탭에서 PNG·JPEG 등이 그대로 보입니다
- **열 수 없는 형식을 알립니다.** 이진 파일은 열지 않고 사유를 보입니다.
  종전에는 깨진 글자로 열렸고, 그것을 저장하면 원본이 파괴됐습니다. 판정은
  확장자가 아니라 내용으로 합니다

### 변경

- **탐색기 머리의 아이콘이 그림이 됐습니다.** 새 파일·새 폴더가 `⊕`·`⊞` 였는데
  원과 사각은 파일도 폴더도 뜻하지 않아, 어느 쪽이 무엇인지 알 수 없었습니다.
  이제 문서와 폴더를 그대로 그립니다. 새로고침도 같은 형식으로 맞췄습니다 —
  한 줄에 나란히 서는데 하나만 글자면 그 하나가 도리어 눈에 띕니다

### 고침

- **터미널에서 복사가 안 되던 것.** 브라우저 터미널이 OSC 52(클립보드 쓰기)를
  받지 않아, 셸이 보낸 복사가 **받는 사람 없이 버려지고 있었습니다.** 서버가 열린
  자리에서만 되던 것은 그쪽에서 iTerm2·Terminal.app 이 그 일을 대신하고 있었기
  때문입니다. 이제 받습니다 — 브라우저가 자동 복사를 막으면(원격 HTTP 접속이 늘
  그렇습니다) 내용을 담은 창을 띄워 한 번 누르면 복사되게 합니다
- **`+ Add` 의 경로 채우기가 엉뚱하던 것.** 서버가 지금 터미널을 찾지 못하면
  **자기 프로세스의 cwd** 로 답했고, 그것이 마침 저장소라 그럴듯하게 채워졌습니다.
  Git 창·Editor 창에서 누르면 언제나 그 길을 탔습니다. 이제 그 값이 누구의
  것인지를 함께 보고, 사용자의 것이 아니면 **빈 칸으로 열고 사유를 보입니다**
- **재연결 폭주가 이제 멎습니다.** 종전의 2초 지연은 규모를 30배 낮췄을 뿐
  멎히지는 못했습니다 — `onclose` 가 브라우저에서 재연결의 유일한 계기라,
  **소켓을 닫는 한 재연결은 반드시 다시 오기** 때문입니다. 이제 같은 도구를
  되풀이해 부르는 연결은 통보를 보낸 뒤 **닫지 않고 붙잡습니다.** 닫히지 않으면
  재연결의 계기가 없어 옛 탭이 스스로 조용해집니다. 상한(동시 64개·10분)이 있고,
  브라우저가 탭을 닫으면 그 즉시 풀립니다
- **서버가 1분마다 자기 상태를 기록합니다.** 마지막 요청 이후 흐른 시간, 붙어
  있는 연결 수, 도구 수, 부하가 한 줄로 남습니다. 종전에는 **"끊겼다"는 사실
  자체가 아무 데도 남지 않아** 다음에 겪어도 원인을 좁힐 수 없었습니다. 이제
  그 구간의 공백이 "경로가 없었는가(연결 거부)"와 "서버가 느렸는가(로딩 멈춤)"를
  가릅니다
- **원격 접속이 한 번씩 끊기던 것.** 사라진 도구를 가리키는 터미널 패널이 지연 0
  으로 무한히 재접속했습니다 — 실측 초당 95연결, TIME_WAIT 2,881개. macOS 의 임시
  포트(16,384개)가 마르면 Tailscale 경로가 먼저 죽고, 루프백을 쓰는 `health` 는
  통과해 "서버는 정상인데 붙지 않는" 모양이 됐습니다. 세 겹으로 막았습니다:
  도구 종료 통보를 **종단으로 처리**하고, 재연결 백오프를 연결이 3초 유지된 뒤에만
  되돌리고, 커맨드 SSE 가 **더 이상 포기하지 않게** 했습니다. SSE 가 20회 실패로
  영구히 포기하면 죽은 도구를 거두는 자가 치유가 영영 돌지 않는다는 것이
  마지막 고리였습니다
- **서버가 되풀이되는 미스 요청을 늦춥니다.** 배포된 클라이언트 수정은 이미 열려
  있는 탭에 닿지 않으므로, 없는 도구를 창 안에 다시 부르는 연결만 2초 늦춥니다.
  첫 요청은 면제되어 정상 경로는 그대로입니다
- **로그가 무한히 자라던 것.** 서버가 자기 로그를 1분마다 확인해 64 MB 를 넘으면
  끝 8 MB 를 `dongminal.log.1` 로 남기고 본체를 비웁니다. 폭주는 막았지만 상한이
  없다는 사실은 그대로였습니다 — 정상 운영에서도 하루 ~124 MB 가 쌓입니다
- **`dmctl: No such file or directory` 훅 오류.** 헬퍼 설치가 `지우기 → 다시 걸기`
  순서라 그 사이에 경로가 비었습니다(실측 300회 재설치 중 소실 5,037회). 임시
  이름에 만든 뒤 `rename` 으로 덮도록 바꿔 목적 경로가 한 순간도 비지 않습니다.
  Windows 에서는 덮어쓰기가 일시적으로 거부되는 일이 있어(바이러스 검사가 갓 쓴
  파일을 잠깐 엽니다) 그때 물러섰다 다시 시도합니다 — 거부에 목적 경로를 지워서
  답하면 같은 창이 다시 열립니다

## [1.0.0] — 2026-08-30

첫 정식 배포입니다. 이제 [Releases](https://github.com/Biisairo/dongminal/releases)
에서 자기 OS 바이너리 하나를 받아 바로 실행할 수 있습니다 — Go 도 다른 의존도
필요 없습니다.

### 추가

- **바이너리 배포.** 태그 `v*` 를 밀면 다섯 대상(macOS arm64·amd64, Linux
  amd64·arm64, Windows amd64)이 빌드되어 릴리스에 첨부됩니다. `SHA256SUMS` 동봉
- **`dongminal version`** (`--version`) — 판·대상·go 런타임을 찍습니다. 릴리스
  산출물이 아니면 `dev` 로 나옵니다
- **Windows 10 1809+ 네이티브 지원.** ConPTY 로 PTY 의미론을 얻습니다. WSL 은
  `linux-amd64` 를 그대로 씁니다
- **`dongminal doctor`** — 플랫폼 계층을 계층별로 실제 실행하는 진단. 터미널이
  뜨지 않을 때 어느 계층에서 막혔는지 그대로 보여 줍니다
- **`dongminal verify`** — 격리 인스턴스를 띄워 종단간 22항목을 훑는 검증.
  세 OS 가 같은 목록을 돕니다 (개발·CI 용)
- 파일 전송

### 변경

- **OS 이음매를 인터페이스 뒤로.** PTY, 프로세스 제어·그룹·조회, 셸 선택과 훅,
  로컬 IPC, 경로 규약, 브라우저 실행이 `internal/shared/platform` 안으로 들어갔고
  그 패키지 밖에는 OS 분기가 한 줄도 없습니다
- git 기록의 자격증명 마스킹이 `https://user:***@h` 에서 `https://***@h` 로
  바뀌었습니다. `user` 자리가 토큰인 형태가 흔하고 구분할 수 없기 때문입니다
- `a/b` 같은 원격 이름을 `fetch`·`tag` 에서도 실행 전에 거부합니다. 종전에는
  엄격한 검사가 `push` 에만 걸려 있었습니다
- `stop` 이 대상을 죽이지 못하면 실패로 보고하고 pidfile 을 보존합니다. 살아 있는
  데몬의 pidfile 을 지우면 고아가 되기 때문입니다

### 수정

- **Windows 에서 터미널이 비어 있던 결함.** `CreateProcess` 에
  `STARTF_USESTDHANDLES` 를 세우지 않아 자식이 부모 콘솔을 물려받았고, 의사
  콘솔이 아니라 그쪽에 글자를 그리고 있었습니다
- Windows 에서 파일 전송이 통째로 막혀 있던 결함
- 경로 구분자·줄 끝 전제에서 비롯된 결함 다수
- 셸이 스스로 끝나도 탭이 닫히지 않던 결함
- `migrate` 가 대상이 없는데도 `settings.json` 을 재작성하던 결함
- **붙여넣기 감싸기를 셸에게 물어보지 않던 결함.** API·에이전트 입력이 언제나
  bracketed paste 로 감싸여 나가, 그 모드를 모르는 셸(macOS 가 싣는 bash 3.2)에서는
  마커가 명령줄에 글자로 들어가 `bash: 200~echo: command not found` 가 됐다.
  에이전트 간 메시지와 API 입력 주입이 그 셸에서 통째로 깨져 있었다. 이제 셸이
  그 모드를 켰을 때만 감싼다

[1.0.12]: https://github.com/Biisairo/dongminal/releases/tag/v1.0.12
[1.0.11]: https://github.com/Biisairo/dongminal/releases/tag/v1.0.11
[1.0.10]: https://github.com/Biisairo/dongminal/releases/tag/v1.0.10
[1.0.9]: https://github.com/Biisairo/dongminal/releases/tag/v1.0.9
[1.0.8]: https://github.com/Biisairo/dongminal/releases/tag/v1.0.8
[1.0.7]: https://github.com/Biisairo/dongminal/releases/tag/v1.0.7
[1.0.6]: https://github.com/Biisairo/dongminal/releases/tag/v1.0.6
[1.0.5]: https://github.com/Biisairo/dongminal/releases/tag/v1.0.5
[1.0.4]: https://github.com/Biisairo/dongminal/releases/tag/v1.0.4
[1.0.3]: https://github.com/Biisairo/dongminal/releases/tag/v1.0.3
[1.0.2]: https://github.com/Biisairo/dongminal/releases/tag/v1.0.2
[1.0.1]: https://github.com/Biisairo/dongminal/releases/tag/v1.0.1
[1.0.0]: https://github.com/Biisairo/dongminal/releases/tag/v1.0.0

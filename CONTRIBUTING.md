# 기여 안내

이 문서는 **이 저장소가 지키는 규약**을 적습니다. 취향이 아니라 규약만 적습니다 —
취향을 적으면 아무도 읽지 않는 문서가 됩니다.

---

## 1. 시작하기

```bash
git clone <repo> && cd dongminal
go build ./...
make gates          # 커밋 전에 도는 것 전부 (CI 와 같은 목록)
```

**격리 인스턴스로 확인하세요.** 이 저장소의 바이너리로 `start`/`stop` 을 부르면
여러분이 쓰고 있는 실제 인스턴스를 건드립니다 — 도구 셸의 `DONGMINAL_PORT` 가
그것을 가리키기 때문입니다.

```bash
./dongminal start --isolated   # 임시 홈 + 빈 포트. 운영 인스턴스를 건드리지 않습니다
./dongminal verify             # 격리 인스턴스를 스스로 띄워 종단간 표면을 훑습니다
```

---

## 2. 브랜치와 커밋

### 브랜치

`main` 에서 갈라 `main` 으로 돌아옵니다. 이름은 자유이나 무엇을 하는지 알아볼 수
있어야 합니다 (`fix/editor-save-race`).

### 커밋 메시지

```
<type>(<scope>): <한 줄 요약> (<FR 번호>)

<왜 이렇게 했는가. 무엇을 했는가가 아니라.>
```

- `type`: `feat` · `fix` · `refactor` · `test` · `docs` · `chore`
- **FR 번호를 답니다.** 이 저장소는 요구를 SRS 에 적고 번호로 가리킵니다.
  그 번호가 커밋에 있으면 "왜 이 줄이 있는가" 에 답할 수 있습니다.
- **AI 서명을 넣지 않습니다** (`Co-Authored-By` 등).

본문에 적을 것은 **왜**입니다. 무엇을 했는지는 diff 가 말합니다.
동작을 바꿨으면 **이전 동작 / 새 동작 / 이유** 셋을 적습니다.

---

## 3. 이 저장소가 지키는 것

### 3-1. 스펙 → 테스트 → 코드

중·대 규모 변경은 순서가 고정입니다.

1. **SRS 를 씁니다** (`docs/internal/*_SRS.md`). IEEE 29148 형태이고,
   제목 바로 아래에 상태 한 줄이 있어야 합니다:
   `> **문서 상태**: 초안`
   값은 enum 입니다 — `초안`·`승인대기`·`승인·구현중`·`승인·구현완료`·`폐기`·`대체`.
   `scripts/check-srs-status.sh` 가 강제합니다.
2. **테스트를 먼저 씁니다.** RED 를 실제로 보세요 — 이미 구현했다면 `git stash`
   로 코드만 되돌려 확인합니다. **RED 를 보기 전에 원인을 적지 마세요.**
3. **구현합니다.** GREEN.

### 3-2. 동작을 바꾸면 그 근거 문서를 같은 변경에서 고칩니다

문서가 결정을 못박고 있으면, 그 결정을 뒤집는 코드는 문서를 함께 고쳐야 합니다.
고치지 않으면 다음 사람이 문서를 믿고 되돌립니다.

### 3-3. 게이트를 느슨하게 만들어 통과시키지 않습니다

`make gates` 가 막으면 코드를 고칩니다. 게이트를 고치는 것은 **그 게이트가 재는
것이 바뀌었을 때**뿐이고, 그때는 근거를 커밋에 적습니다.

**새 게이트를 세우면 탐침으로 검출을 확인하고 지웁니다.** 잡지 못하는 게이트는
없는 것보다 나쁩니다 — 있다고 믿게 만듭니다.

### 3-4. 문서는 생성물이거나 대조됩니다

손으로 적은 문서는 조용히 낡습니다. 그래서 이 저장소는 둘 중 하나로 만듭니다.

| 문서 | 방식 | 원천 |
|---|---|---|
| `docs/external/errors.md` | **생성** | `apierr/codes_doc.go` |
| `docs/internal/decisions.md` | **생성** | SRS 안의 결정 항목 |
| `docs/external/commands.md` | 대조 | `dmctl.go` |
| `docs/external/api.md` | 대조 | 라우트 표 |
| `docs/external/shortcuts.md` | 대조 | `helpers.js` |
| `docs/external/getting-started.md` 환경변수 표 | 대조 | 코드의 `DONGMINAL_*` |

생성물은 직접 고치지 마세요. 원천을 고치고 다시 만듭니다:

```bash
go run ./scripts/gen-errors
go run ./scripts/gen-decisions
```

---

## 4. 검사

```bash
make gates      # 포맷·정적분석·이음매·문서 대조 (빠름)
go test ./...   # Go 단위·통합
npm run unit    # 프론트 순수 모듈
npm run lint
npm run typecheck
make e2e        # 브라우저 종단간 — CI 와 같은 8샤드를 병렬로 (~8분)
```

### e2e 에 대해 아셔야 할 것

- **`make e2e` 를 쓰세요.** `npx playwright test` 로 한 프로세스에 돌리지 마세요 —
  느릴 뿐 아니라 CI 와 **상태 누적의 범위**가 달라 답이 갈립니다.
- **출력을 자르지 마세요.** `| tail -N` 은 종료 코드를 `tail` 의 것으로 만들어
  `exit 0` 이 초록을 뜻하지 않게 합니다. 샤드 로그에는 이진 바이트가 섞이므로
  읽을 때는 `grep -a` 를 씁니다.
- **다른 세션과 겹쳐 돌리지 마세요.** 같은 기계에서 함께 돌리면 PTY 가 소진되고
  (`kern.tty.ptmx_max` 기본 511) 실패 목록이 오염됩니다. 샤드끼리는 안전합니다.
- **흔들리는 검사를 만나면 단독으로 한 번 더 돌려 가르세요.** 이 저장소는 전량을
  다섯 번 재서 다섯 번 다 다른 목록을 얻었습니다 — 고정 목록으로 쫓지 마세요.
  고칠 자리는 개별 검사가 아니라 **시간으로 전제를 세우는 곳**입니다.

### 상한은 `var` 로 둡니다

검사가 낮춰 쓸 수 있어야 하기 때문입니다 — 실제 값으로 픽스처를 만들면 검사가
디스크를 수백 MiB 씁니다. 그리고 그 묶음마다 **기본값을 지키는 검사**를 따로 둡니다.

---

## 5. 리뷰

- **작게 보내세요.** 한 변경은 한 가지를 합니다.
- 리뷰어가 보는 것은 셋입니다: **근거 문서가 함께 고쳐졌는가** · **RED 를 봤는가** ·
  **게이트를 우회하지 않았는가.**
- 무관한 리팩터를 섞지 마세요. 변경된 모든 줄이 요구사항에 대응해야 합니다.

---

## 6. 릴리스 절차

### 누가 언제 태그를 미는가

**저장소 소유자가 밉니다.** 태그 하나가 워크플로우 전부를 돌리므로 되돌리기가
비쌉니다.

### 순서

1. **CHANGELOG 를 닫습니다.** `## [Unreleased]` 를 `## [x.y.z] - YYYY-MM-DD` 로
   바꾸고 새 `[Unreleased]` 를 엽니다. **태그를 밀기 전입니다** — 릴리스 노트가
   이 절을 그대로 가져가므로, 닫지 않으면 노트가 빕니다.
2. `make gates && go test ./... && make e2e` 가 전부 초록인지 확인합니다.
3. 태그를 밉니다: `git tag v1.2.3 && git push origin v1.2.3`
4. 워크플로우가 나머지를 합니다 — 다섯 대상 빌드 · e2e 게이트 · 체크섬 ·
   **cosign keyless 서명** · provenance 증명 · 릴리스 생성.
5. 릴리스 페이지에서 **다섯 대상과 `SHA256SUMS`·`.sig`·`.pem` 이 다 올라왔는지**
   확인합니다. 절반만 올라간 릴리스가 가장 나쁩니다.

### 판 번호

SemVer 입니다.

- **patch** — 동작 변경 없는 수정
- **minor** — 기능 추가, 하위 호환
- **major** — 기동을 거부하게 만드는 파괴적 변경. **지금 그럴 사유가 없습니다** —
  인증 도입이 유일한 후보였고 그것은 확정된 비목표입니다.

**동작이 바뀌면 patch 가 아닙니다.** CHANGELOG 에 이전/새 동작을 적습니다.

# 사용자 확인 후 오류 및 수정, 확인 리스트

> **진행 상태** (2026-08-24) — **8개 항목 전부 닫힘.** 스펙 `docs/internal/USER_CHECKLIST_FIXES_SRS.md`,
> 인계 `docs/internal/USER_CHECKLIST_FIXES_HANDOFF.md`
>
> | 항목 | 상태 |
> |---|---|
> | 오류 1 (확인창 버튼 UI) | ✅ 완료 `eec0ddd` |
> | 오류 2 (모바일 하단 버튼 터치·슬라이드) | ✅ 완료 `18c7f14` |
> | 오류 3 (외부 컴퓨터 off-focus) | ✅ 완료 `854ada6` |
> | 수정 1 (백그라운드 버튼을 우측 끝으로) | ✅ 완료 `eec0ddd` |
> | 수정 2 (백그라운드 목록 중앙 모달) | ✅ 완료 `eec0ddd` |
> | 수정 3 (dongminal 명령어 없음 · 마이그레이션 스크립트) | ✅ 완료 `e6463e1` |
> | 수정 4 (모바일 키보드 표출 시 스크롤) | ✅ 완료 `cb4cc8c` — **iOS 실기기 수동 확인 남음** (`test-checklist.md` C11.8~C11.10) |
> | 확인 1 (백그라운드 복귀 위치) | ✅ 정상 동작으로 판정 → 대상 지정 기능으로 확장 `a906418` |


## 오류

1. 바쁜 상태인 탭 종료 시 팝업의 백그라운드로 전환 버튼이 다른 종료, 취소 버튼과 ui 가 다름. 통일 시킬 필요 있음.
2. 모바일 화면에서 하단의 버튼들(esc, ctrl, tab 방향키 등)이 터치되지않고, 옆으로 슬라이드도 잘 안됨.
3. 여러 창에서 포커스되어 한 창을 제외한 다른 창에서는 off focus 되는 로직이 같은 컴퓨터 내에서만 동작하고 외부 컴퓨터에서 접속하는 경우에는 동작하지 않음.

## 수정사항

1. 하단의 백그라운드 확인 버튼이 다른 하단 status 정보들에 묻혀 잘 보이지 않음. 하단 바의 우측 끝에 눈에띄는 다른 ui (현재 ui 에서 사용하는 버튼과 같은 ui 를 사용)로 생성. 색상은 테마의 색상 팔레트를 이용해야함.
2. 백그라운드 창이 버튼위에 작은 메뉴로 뜨는데 이렇게 말고 중앙 모달로 보이도록 수정.
3. stop 명령어 시 dongminal 명령어 없음. 또한 dongminal 명령어가 아닌 dmctl 명령어만 있는데 확인 필요. 마이그레이션은 script 내부에 별도의 스크립트로 만드는게 좋겠다는 판단.
4. 모바일 페이지에서 이용 시 화면을 터치하여 키볻가 올라왔을 때 탭이 줄어드는걸 감지하지 못하는 것 같음. 터미널 창만 줄어들면 좋겠는데 화면 아래 키보드가 생기면서 화면을 스크롤해야하게됨.

## 확인 필요 사항

1. 현재 백그라운드 터미널을 살리면 현재 선택 된 pane 에 새 탭으로 열리는거같은데 맞는동작인지 확인 필요